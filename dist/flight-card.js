function hn(d, r) {
  for (var h = 0; h < r.length; h++) {
    const m = r[h];
    if (typeof m != "string" && !Array.isArray(m)) {
      for (const f in m)
        if (f !== "default" && !(f in d)) {
          const y = Object.getOwnPropertyDescriptor(m, f);
          y && Object.defineProperty(d, f, y.get ? y : {
            enumerable: !0,
            get: () => m[f]
          });
        }
    }
  }
  return Object.freeze(Object.defineProperty(d, Symbol.toStringTag, { value: "Module" }));
}
const ln = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.408'%20height='14.408'/%3e%3c/defs%3e%3cg%20id='Thin-L'%20transform='matrix(0,%20-1.526083,%201.526083,%200,%20154.192352,%20224.515808)'%3e%3cpath%20d='M%20147.119%20-35.2539%20C%20147.119%20-41.8457%20138.086%20-46.6797%20127.344%20-46.6797%20L%20103.125%20-46.6797%20C%20100.049%20-46.6797%2098.877%20-47.168%2096.9238%20-49.2676%20L%2059.2773%20-90.4785%20C%2058.0566%20-91.7969%2056.5918%20-92.5293%2055.0293%20-92.5293%20L%2048.4375%20-92.5293%20C%2046.9727%20-92.5293%2046.1426%20-91.2109%2046.875%20-89.6484%20L%2066.2598%20-46.6797%20L%2034.9121%20-43.5547%20L%2024.6582%20-62.1582%20C%2023.8281%20-63.4766%2022.9004%20-64.0625%2021.0938%20-64.0625%20L%2018.3594%20-64.0625%20C%2016.8945%20-64.0625%2016.0645%20-63.2812%2016.0645%20-61.7676%20L%2016.0645%20-8.78906%20C%2016.0645%20-7.22656%2016.8945%20-6.49414%2018.3594%20-6.49414%20L%2021.0938%20-6.49414%20C%2022.9004%20-6.49414%2023.8281%20-7.08008%2024.6582%20-8.39844%20L%2034.9121%20-26.9531%20L%2066.2598%20-23.877%20L%2046.875%2019.1406%20C%2046.1426%2020.6543%2046.9727%2021.9727%2048.4375%2021.9727%20L%2055.0293%2021.9727%20C%2056.5918%2021.9727%2058.0566%2021.2402%2059.2773%2019.9219%20L%2096.9238%20-21.2402%20C%2098.877%20-23.3887%20100.049%20-23.877%20103.125%20-23.877%20L%20127.344%20-23.877%20C%20138.086%20-23.877%20147.119%20-28.6621%20147.119%20-35.2539%20Z'/%3e%3c/g%3e%3c/svg%3e", cn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.408'%20height='14.408'/%3e%3c/defs%3e%3cg%20id='Thin-L'%20transform='matrix(0,%20-1.526083,%201.526083,%200,%20154.192352,%20224.515808)'%3e%3cpath%20d='M%20137.678%20-39.456%20C%20137.678%20-41.883%20134.94%20-44.391%20133.645%20-44.391%20C%20130.326%20-44.391%20135.025%20-44.391%20128.237%20-44.391%20L%20109.354%20-44.391%20C%20106.278%20-44.391%20101.866%20-46.186%2099.913%20-48.286%20L%2099.913%20-91.597%20C%2098.693%20-92.916%2094.12%20-96.731%2090.472%20-96.731%20C%2086.824%20-96.731%2080.299%20-93.16%2081.031%20-91.597%20L%2071.59%20-44.391%20L%2062.149%20-44.391%20L%2033.822%20-39.639%20C%2033.825%20-40.147%2033.825%20-57.191%2033.825%20-63.273%20C%2033.825%20-67.493%2021.167%20-63.273%2021.167%20-63.273%20C%2019.703%20-63.273%2020.894%20-55.345%2020.979%20-53.858%20C%2020.894%20-53.832%2016.125%20-41.44%2016.143%20-34.95%20C%2016.161%20-28.451%2021.006%20-16.068%2021.006%20-16.068%20C%2021.006%20-14.506%2019.141%20-6.626%2021.176%20-6.6129999999999995%20C%2020.605%20-6.626%2033.825%20-2.219%2033.825%20-6.626%20C%2033.825%20-13.832%2033.825%20-30.235%2033.825%20-30.235%20L%2062.149%20-25.509%20L%2071.59%20-25.509%20L%2081.031%2021.697%20C%2080.299%2023.21%2086.998%2025.911%2090.472%2025.911%20C%2093.946%2025.911%2098.693%2023.015%2099.913%2021.697%20L%2099.913%20-20.794%20C%20101.866%20-22.943%20106.278%20-25.509%20109.354%20-25.509%20L%20128.237%20-25.509%20C%20135.418%20-25.509%20130.239%20-25.509%20133.62%20-25.485%20C%20135.593%20-25.509%20137.678%20-28.025%20137.678%20-30.396%20C%20137.678%20-30.396%20146.133%20-33.618%20146.135%20-34.95%20C%20146.137%20-36.315%20137.678%20-39.456%20137.678%20-39.456%20Z'/%3e%3c/g%3e%3c/svg%3e", un = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.408'%20height='14.408'/%3e%3c/defs%3e%3cg%20id='Thin-L'%20transform='matrix(0,%20-1.526083,%201.526083,%200,%20154.192352,%20224.515808)'%3e%3cpath%20d='M%20147.119%20-34.95%20C%20147.119%20-41.542%20138.979%20-44.391%20128.237%20-44.391%20L%20109.354%20-44.391%20C%20106.278%20-44.391%20101.866%20-46.186%2099.913%20-48.286%20L%2090.472%20-91.597%20C%2089.252%20-92.916%2084.679%20-97.135%2081.031%20-97.135%20C%2077.383%20-97.135%2070.858%20-93.16%2071.59%20-91.597%20L%2073.978%20-44.391%20L%2062.149%20-44.391%20C%2062.149%20-44.391%2064.374%20-51.607%2062.149%20-53.832%20C%2057.698%20-58.283%2047.717%20-58.283%2043.266%20-53.832%20C%2041.041%20-51.607%2043.266%20-44.391%2043.266%20-44.391%20L%2034.912%20-43.555%20L%2024.658%20-62.158%20C%2023.828%20-63.477%2022.9%20-64.062%2021.094%20-64.062%20L%2018.359%20-64.062%20C%2016.895%20-64.062%2016.064%20-63.281%2016.064%20-61.768%20L%2019.737%20-34.95%20L%2016.064%20-8.789%20C%2016.064%20-7.227%2016.895%20-6.494%2018.359%20-6.494%20L%2021.094%20-6.494%20C%2022.9%20-6.494%2023.828%20-7.08%2024.658%20-8.398%20L%2034.912%20-26.953%20L%2043.451%20-26.66%20C%2043.451%20-26.66%2040.791%20-18.587%2043.266%20-16.068%20C%2047.678%20-11.579%2057.698%20-11.617%2062.149%20-16.068%20C%2064.374%20-18.293%2062.149%20-25.509%2062.149%20-25.509%20L%2073.978%20-25.509%20L%2071.59%2021.697%20C%2070.858%2023.21%2077.557%2026.113%2081.031%2026.113%20C%2084.505%2026.113%2089.252%2023.015%2090.472%2021.697%20L%2099.913%20-20.794%20C%20101.866%20-22.943%20106.278%20-25.509%20109.354%20-25.509%20L%20128.237%20-25.509%20C%20138.979%20-25.509%20147.119%20-28.358%20147.119%20-35.511%20L%20147.119%20-34.95%20Z'/%3e%3c/g%3e%3c/svg%3e", fn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.408'%20height='14.408'/%3e%3c/defs%3e%3cg%20id='Thin-L'%20transform='matrix(0,%20-1.526083,%201.526083,%200,%20154.192352,%20224.515808)'%3e%3cpath%20d='M%20147.119%20-35.254%20C%20147.119%20-41.846000000000004%20138.086%20-46.68%20127.344%20-46.68%20L%20103.125%20-46.68%20C%20100.049%20-46.68%2098.877%20-47.168%2096.924%20-49.268%20L%2090.503%20-56.217%20L%2090.503%20-66.571%20L%2081.002%20-66.571%20L%2059.277%20-90.478%20C%2058.057%20-91.797%2056.592%20-92.529%2055.029%20-92.529%20L%2048.438%20-92.529%20C%2046.973%20-92.529%2046.143%20-91.211%2046.875%20-89.648%20L%2066.26%20-46.68%20L%2034.912%20-43.555%20L%2024.658%20-62.158%20C%2023.828%20-63.477%2022.9%20-64.062%2021.094%20-64.062%20L%2018.359%20-64.062%20C%2016.895%20-64.062%2016.064%20-63.281%2016.064%20-61.768%20L%2016.064%20-8.789%20C%2016.064%20-7.227%2016.895%20-6.494%2018.359%20-6.494%20L%2021.094%20-6.494%20C%2022.9%20-6.494%2023.828%20-7.08%2024.658%20-8.398%20L%2034.912%20-26.953%20L%2066.26%20-23.877%20L%2046.875%2019.141%20C%2046.143%2020.654%2046.973%2021.973%2048.438%2021.973%20L%2055.029%2021.973%20C%2056.592%2021.973%2058.057%2021.24%2059.277%2019.922%20L%2081.069%20-3.904%20L%2090.503%20-3.927%20L%2090.503%20-14.345%20L%2096.924%20-21.24%20C%2098.877%20-23.389%20100.049%20-23.877%20103.125%20-23.877%20L%20127.344%20-23.877%20C%20138.086%20-23.877%20147.119%20-28.662%20147.119%20-35.254%20Z'/%3e%3c/g%3e%3c/svg%3e", dn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20version='1.0'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%20xmlns='http://www.w3.org/2000/svg'%3e%3cpath%20d='M%20251.239%204.012%20C%20242.63%2011.82%20234.623%2025.533%20230.719%2039.347%20C%20229.117%2045.252%20228.817%2051.959%20228.316%20111.718%20L%20227.816%20177.582%20L%20211.3%20186.391%20L%20194.784%20195.299%20L%20194.183%20180.085%20C%20193.783%20169.674%20193.082%20163.769%20191.981%20161.166%20L%20190.379%20157.362%20L%20176.566%20157.663%20L%20162.752%20157.963%20L%20161.251%20160.966%20C%20160.25%20163.168%20159.749%20168.773%20159.449%20181.686%20C%20158.948%20202.006%20159.249%20204.208%20162.752%20205.109%20C%20164.654%20205.609%20165.255%20206.41%20165.255%20208.412%20C%20165.255%20211.015%20161.451%20213.117%2097.989%20247.15%20C%2060.953%20266.869%2029.622%20284.186%2028.421%20285.488%20C%2025.218%20288.891%2023.416%20296.098%2022.315%20309.611%20C%2021.814%20315.918%2021.514%20321.323%2021.714%20321.423%20C%2021.814%20321.623%2023.216%20319.221%2024.817%20316.018%20L%2027.62%20310.212%20L%2099.09%20285.388%20L%20170.46%20260.563%20L%20199.388%20260.563%20L%20228.316%20260.563%20L%20228.316%20315.817%20C%20228.316%20382.282%20229.618%20399.7%20236.725%20427.427%20L%20239.027%20436.536%20L%20233.922%20441.541%20C%20228.116%20447.246%20213.602%20457.856%20190.279%20473.272%20C%20181.471%20479.077%20173.463%20484.983%20172.562%20486.485%20C%20171.361%20488.186%20170.76%20491.79%20170.46%20497.896%20C%20170.059%20505.703%20170.26%20506.804%20171.661%20506.804%20C%20172.562%20506.804%20189.478%20502.7%20209.298%20497.796%20C%20229.117%20492.791%20245.633%20488.787%20246.134%20488.787%20C%20246.634%20488.787%20247.435%20492.29%20248.036%20496.695%20C%20248.636%20500.999%20249.938%20506.304%20250.939%20508.406%20C%20252.64%20511.809%20253.241%20512.31%20256.344%20512.31%20C%20259.447%20512.31%20260.047%20511.809%20261.749%20508.406%20C%20262.75%20506.304%20264.051%20500.999%20264.652%20496.695%20C%20265.253%20492.29%20266.053%20488.787%20266.554%20488.787%20C%20267.054%20488.787%20283.571%20492.891%20303.39%20497.796%20C%20323.209%20502.801%20340.126%20506.804%20341.027%20506.804%20C%20342.428%20506.804%20342.628%20505.703%20342.228%20497.896%20C%20341.928%20491.79%20341.327%20488.186%20340.126%20486.384%20C%20339.125%20484.983%20331.317%20479.077%20322.609%20473.372%20C%20299.987%20458.457%20284.672%20447.346%20278.766%20441.541%20L%20273.661%20436.536%20L%20275.963%20427.427%20C%20283.07%20399.7%20284.371%20382.282%20284.371%20315.817%20L%20284.371%20260.563%20L%20313.3%20260.563%20L%20342.228%20260.563%20L%20413.598%20285.388%20L%20485.068%20310.212%20L%20487.871%20316.018%20C%20489.472%20319.221%20490.873%20321.623%20490.974%20321.423%20C%20491.174%20321.323%20490.873%20315.918%20490.373%20309.611%20C%20489.272%20296.098%20487.47%20288.891%20484.267%20285.488%20C%20483.066%20284.186%20451.735%20266.869%20414.699%20247.15%20C%20351.237%20213.117%20347.433%20211.015%20347.433%20208.412%20C%20347.433%20206.41%20348.034%20205.609%20349.935%20205.109%20C%20353.439%20204.208%20353.739%20202.006%20353.239%20181.686%20C%20352.938%20168.773%20352.438%20163.168%20351.437%20160.966%20L%20349.935%20157.963%20L%20336.122%20157.663%20L%20322.308%20157.362%20L%20320.707%20161.166%20C%20319.606%20163.769%20318.905%20169.674%20318.505%20180.085%20L%20317.904%20195.299%20L%20301.188%20186.291%20L%20284.371%20177.282%20L%20284.371%20115.121%20C%20284.271%2048.356%20284.071%2044.652%20279.066%2031.139%20C%20277.765%2027.435%20275.062%2021.83%20273.26%2018.727%20C%20269.757%2012.921%20258.246%200.308%20256.344%200.308%20C%20255.843%200.308%20253.541%202.01%20251.239%204.012%20Z'/%3e%3c/svg%3e", _n = "data:image/svg+xml,%3c?xml%20version='1.0'%20standalone='no'?%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%2020010904//EN'%20'http://www.w3.org/TR/2001/REC-SVG-20010904/DTD/svg10.dtd'%3e%3csvg%20version='1.0'%20xmlns='http://www.w3.org/2000/svg'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cg%20transform='translate(0.000000,512.000000)%20scale(0.100000,-0.100000)'%20fill='%23000000'%20stroke='none'%3e%3cpath%20d='M2507%205080%20c-68%20-80%20-132%20-246%20-161%20-420%20-28%20-167%20-36%20-354%20-36%20-820%20l0%20-456%20-47%20-36%20c-43%20-32%20-309%20-198%20-318%20-198%20-2%200%20-5%2056%20-7%20123%20-2%2086%20-7%20129%20-17%20140%20-11%2014%20-33%2017%20-112%2017%20-134%200%20-134%201%20-134%20-209%200%20-152%201%20-156%2023%20-166%2059%20-26%2050%20-32%20-754%20-534%20-514%20-321%20-783%20-495%20-798%20-515%20-12%20-17%20-48%20-66%20-79%20-108%20-44%20-60%20-57%20-85%20-57%20-113%200%20-19%202%20-35%205%20-35%203%200%20427%20165%20942%20366%20l938%20366%20185%2014%20c102%207%20190%2013%20196%2014%209%200%2013%20-138%2017%20-512%205%20-512%2013%20-672%2053%20-983%2014%20-114%2048%20-297%2060%20-328%204%20-11%20-62%20-59%20-233%20-172%20-131%20-86%20-269%20-178%20-308%20-203%20-86%20-58%20-105%20-91%20-105%20-184%200%20-48%204%20-68%2013%20-68%207%200%20159%2040%20337%2090%20179%2049%20332%2090%20340%2090%2011%200%2021%20-28%2038%20-98%2024%20-106%2043%20-142%2072%20-142%2029%200%2048%2036%2072%20142%2017%2070%2027%2098%2038%2098%208%200%20161%20-41%20340%20-90%20178%20-50%20330%20-90%20337%20-90%2020%200%2017%20127%20-5%20170%20-18%2038%20-9%2031%20-395%20285%20-173%20114%20-237%20161%20-233%20172%2013%2032%2046%20214%2060%20328%2042%20336%2056%20603%2056%201088%200%20319%203%20407%2013%20407%206%20-1%2095%20-7%20197%20-14%20l185%20-14%20938%20-366%20c515%20-201%20939%20-366%20942%20-366%203%200%205%2016%205%2035%200%2028%20-13%2053%20-57%20113%20-31%2042%20-67%2091%20-79%20108%20-15%2020%20-285%20195%20-798%20515%20-804%20502%20-813%20508%20-753%20534%2021%2010%2022%2014%2022%20166%200%20210%200%20209%20-134%20209%20-79%200%20-101%20-3%20-112%20-17%20-10%20-11%20-15%20-54%20-17%20-140%20-2%20-67%20-5%20-123%20-7%20-123%20-9%200%20-275%20166%20-317%20198%20l-48%2036%200%20451%20c0%20600%20-14%20777%20-75%20988%20-44%20153%20-129%20297%20-175%20297%20-10%200%20-34%20-18%20-53%20-40z'/%3e%3c/g%3e%3c/svg%3e", mn = "data:image/svg+xml,%3c?xml%20version='1.0'%20standalone='no'?%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%2020010904//EN'%20'http://www.w3.org/TR/2001/REC-SVG-20010904/DTD/svg10.dtd'%3e%3csvg%20version='1.0'%20xmlns='http://www.w3.org/2000/svg'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cg%20transform='translate(0.000000,512.000000)%20scale(0.100000,-0.100000)'%20fill='%23000000'%20stroke='none'%3e%3cpath%20d='M2507%205053%20c-58%20-88%20-78%20-129%20-116%20-236%20-73%20-207%20-74%20-216%20-80%20-894%20-6%20-591%20-7%20-612%20-26%20-638%20-11%20-15%20-92%20-73%20-180%20-129%20l-160%20-103%20-3%2088%20c-3%2081%20-5%2090%20-29%20109%20-42%2033%20-149%2025%20-175%20-14%20-4%20-6%20-8%20-81%20-8%20-166%20l0%20-155%20-306%20-195%20c-168%20-107%20-310%20-196%20-315%20-198%20-5%20-2%20-9%2045%20-9%20111%200%20125%20-6%20141%20-56%20153%20-39%2010%20-114%20-2%20-133%20-20%20-13%20-14%20-17%20-48%20-21%20-203%20l-5%20-187%20-325%20-207%20-325%20-207%20-67%20-104%20c-55%20-85%20-67%20-110%20-67%20-146%20l-1%20-42%2060%2030%20c62%2031%20785%20320%20801%20320%205%200%209%20-6%209%20-14%200%20-7%206%20-19%2014%20-25%2012%20-10%2017%20-8%2025%208%206%2011%2011%2028%2011%2039%200%2014%2014%2025%2053%2040%2028%2011%2059%2023%2068%2027%2013%205%2018%20-2%2023%20-29%208%20-43%2023%20-47%2032%20-8%2012%2060%2021%2068%20105%20101%2046%2018%2089%2035%2095%2037%206%203%2015%20-9%2020%20-26%2013%20-45%2026%20-42%2039%2010%20l11%2045%20104%2041%20105%2041%2010%20-34%20c14%20-47%2028%20-43%2039%2011%20l9%2045%2089%2036%20c48%2019%2094%2035%20101%2035%207%200%2017%20-19%2023%20-42%2012%20-58%2036%20-57%2050%202%2013%2053%2016%2054%20194%2065%20l118%207%205%20-468%20c4%20-479%2013%20-632%2057%20-989%2011%20-88%2028%20-198%2037%20-243%2011%20-56%2013%20-86%206%20-90%20-118%20-75%20-569%20-392%20-579%20-407%20-17%20-27%20-44%20-131%20-44%20-173%200%20-27%203%20-32%2018%20-28%209%203%2046%2012%2082%2021%2036%209%20181%2052%20322%2096%20142%2043%20261%2079%20266%2079%204%200%2013%20-28%2020%20-63%2017%20-89%2030%20-130%2047%20-152%2015%20-20%2015%20-20%2030%200%2017%2022%2030%2063%2047%20153%207%2034%2016%2062%2021%2062%204%200%20103%20-30%20220%20-66%20175%20-54%20451%20-134%20464%20-134%2017%200%20-16%20167%20-41%20205%20-10%2015%20-461%20332%20-579%20407%20-7%204%20-5%2034%206%2090%209%2045%2026%20155%2037%20243%2044%20357%2053%20510%2057%20989%20l5%20469%20106%20-7%20c186%20-12%20189%20-12%20196%20-54%203%20-21%2011%20-44%2016%20-52%209%20-13%2012%20-12%2025%204%208%2011%2015%2029%2015%2040%200%2045%2012%2046%20110%208%20l95%20-38%2012%20-48%20c7%20-27%2015%20-51%2018%20-54%2010%20-10%2025%2021%2025%2052%200%2023%203%2027%2018%2021%209%20-4%2058%20-23%20109%20-42%2085%20-33%2092%20-38%2098%20-67%204%20-17%209%20-40%2012%20-50%205%20-19%206%20-19%2019%20-1%208%2010%2014%2027%2014%2037%200%2024%202%2024%20111%20-20%20l94%20-38%207%20-44%20c4%20-23%2010%20-45%2013%20-49%209%20-8%2025%2021%2025%2047%200%2028%203%2027%2075%20-2%2052%20-21%2060%20-28%2070%20-62%2013%20-47%2032%20-54%2046%20-16%20l10%2029%20382%20-152%20c210%20-83%20406%20-164%20435%20-178%20l52%20-27%20-1%2042%20c0%2036%20-12%2061%20-67%20146%20l-67%20104%20-325%20207%20-325%20207%20-6%20189%20c-3%20105%20-6%20191%20-7%20192%20-1%201%20-16%2010%20-34%2019%20-40%2021%20-131%2015%20-156%20-10%20-13%20-13%20-18%20-41%20-22%20-130%20l-5%20-113%20-307%20196%20-308%20196%200%20157%200%20157%20-26%2020%20c-43%2034%20-150%2026%20-176%20-13%20-4%20-6%20-8%20-49%20-8%20-95%200%20-71%20-2%20-82%20-16%20-77%20-43%2016%20-317%20202%20-332%20224%20-15%2024%20-18%2085%20-23%20637%20-5%20577%20-7%20615%20-28%20707%20-23%20104%20-70%20247%20-103%20313%20-29%2058%20-108%20175%20-118%20175%20-5%200%20-29%20-30%20-53%20-67z'/%3e%3c/g%3e%3c/svg%3e", pn = "data:image/svg+xml,%3c?xml%20version='1.0'%20standalone='no'?%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%2020010904//EN'%20'http://www.w3.org/TR/2001/REC-SVG-20010904/DTD/svg10.dtd'%3e%3csvg%20version='1.0'%20xmlns='http://www.w3.org/2000/svg'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cg%20transform='translate(0.000000,512.000000)%20scale(0.100000,-0.100000)'%20fill='%23000000'%20stroke='none'%3e%3cpath%20d='M2504%205083%20c-69%20-68%20-128%20-238%20-159%20-453%20-15%20-107%20-34%20-457%20-38%20-720%20l-2%20-144%20-265%20-223%20-265%20-223%20-5%20130%20c-4%20101%20-8%20133%20-20%20140%20-22%2014%20-184%2012%20-198%20-2%20-9%20-9%20-12%20-76%20-12%20-239%20l0%20-226%20-232%20-195%20-233%20-196%20-3%20129%20c-3%20129%20-3%20130%20-28%20139%20-33%2013%20-145%2013%20-178%200%20l-26%20-10%200%20-227%200%20-228%20-358%20-301%20c-196%20-165%20-367%20-315%20-380%20-334%20-40%20-59%20-54%20-127%20-59%20-270%20l-4%20-135%2029%2032%20c22%2026%20113%2070%20432%20212%20223%2099%20411%20181%20419%20183%208%202%2017%20-10%2023%20-29%2011%20-40%2035%20-43%2044%20-5%2016%2073%2017%2075%20115%20119%2052%2023%2097%2042%20102%2042%204%201%207%20-8%207%20-18%200%20-62%2035%20-68%2050%20-8%206%2023%2012%2047%2014%2052%203%208%20118%2063%20191%2091%207%203%2017%20-12%2024%20-36%2017%20-58%2038%20-51%2051%2018%20l12%2059%20366%20162%20c202%2090%20372%20165%20379%20168%208%203%2012%20-143%2016%20-584%205%20-619%2011%20-714%2056%20-876%20l19%20-68%20-317%20-262%20c-174%20-143%20-322%20-268%20-329%20-277%20-76%20-89%20-134%20-226%20-148%20-348%20-11%20-98%20-18%20-99%20454%2081%20389%20148%20407%20154%20414%20133%203%20-11%2018%20-77%2033%20-146%2054%20-252%2076%20-252%20130%200%2015%2069%2030%20135%2033%20146%207%2021%2025%2015%20414%20-133%20472%20-180%20465%20-179%20454%20-81%20-14%20122%20-72%20259%20-148%20348%20-7%209%20-155%20134%20-329%20277%20l-317%20262%2019%2068%20c45%20162%2051%20257%2056%20876%204%20441%208%20587%2016%20584%207%20-3%20177%20-78%20379%20-168%20l366%20-162%2012%20-59%20c13%20-69%2034%20-76%2051%20-18%207%2024%2017%2039%2024%2036%2073%20-28%20188%20-83%20191%20-91%202%20-5%208%20-29%2014%20-52%2015%20-60%2050%20-54%2050%208%200%2010%203%2019%208%2018%204%200%2049%20-19%20101%20-42%2098%20-44%2099%20-46%20115%20-119%209%20-38%2033%20-35%2044%205%206%2019%2015%2031%2023%2029%208%20-2%20196%20-84%20419%20-183%20319%20-142%20410%20-186%20432%20-212%20l29%20-32%20-4%20135%20c-5%20139%20-18%20205%20-56%20267%20-11%2017%20-182%20168%20-380%20335%20l-361%20303%200%20227%20c0%20214%20-1%20228%20-19%20238%20-25%2013%20-151%2013%20-185%200%20-25%20-9%20-25%20-10%20-28%20-139%20l-3%20-129%20-232%20196%20-233%20195%200%20226%20c0%20163%20-3%20230%20-12%20239%20-14%2014%20-176%2016%20-198%202%20-12%20-7%20-16%20-39%20-20%20-140%20l-5%20-130%20-265%20223%20-265%20223%20-2%20144%20c-4%20263%20-23%20613%20-38%20720%20-20%20138%20-58%20280%20-96%20358%20-35%2071%20-90%20132%20-119%20132%20-11%200%20-36%20-16%20-56%20-37z'/%3e%3c/g%3e%3c/svg%3e", gn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.408'%20height='14.408'/%3e%3c/defs%3e%3cg%20id='Thin-L'%20transform='matrix(0,%20-1.526083,%201.526083,%200,%20154.192352,%20224.515808)'%3e%3cpath%20d='M%20147.119%20-35.254%20C%20147.119%20-41.846000000000004%20138.086%20-46.68%20127.344%20-46.68%20L%20103.125%20-46.68%20C%20100.049%20-46.68%2098.877%20-47.168%2096.924%20-49.268%20L%2090.503%20-56.217%20L%2090.503%20-66.571%20L%2081.002%20-66.571%20L%2059.277%20-90.478%20C%2058.057%20-91.797%2056.592%20-92.529%2055.029%20-92.529%20L%2048.438%20-92.529%20C%2046.973%20-92.529%2046.143%20-91.211%2046.875%20-89.648%20L%2066.26%20-46.68%20L%2034.912%20-43.555%20L%2024.658%20-62.158%20C%2023.828%20-63.477%2022.9%20-64.062%2021.094%20-64.062%20L%2018.359%20-64.062%20C%2016.895%20-64.062%2016.064%20-63.281%2016.064%20-61.768%20L%2024.384%20-34.95%20L%2016.064%20-8.789%20C%2016.064%20-7.227%2016.895%20-6.494%2018.359%20-6.494%20L%2021.094%20-6.494%20C%2022.9%20-6.494%2023.828%20-7.08%2024.658%20-8.398%20L%2034.912%20-26.953%20L%2066.26%20-23.877%20L%2046.875%2019.141%20C%2046.143%2020.654%2046.973%2021.973%2048.438%2021.973%20L%2055.029%2021.973%20C%2056.592%2021.973%2058.057%2021.24%2059.277%2019.922%20L%2080.805%20-3.927%20L%2090.503%20-3.927%20L%2090.503%20-14.345%20L%2096.924%20-21.24%20C%2098.877%20-23.389%20100.049%20-23.877%20103.125%20-23.877%20L%20127.344%20-23.877%20C%20138.086%20-23.877%20147.119%20-28.662%20147.119%20-35.254%20Z'/%3e%3c/g%3e%3c/svg%3e", vn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.408'%20height='14.408'/%3e%3c/defs%3e%3cg%20id='Thin-L'%20transform='matrix(0,%20-1.526083,%201.526083,%200,%20154.192352,%20224.515808)'%3e%3cpath%20d='M%20147.119%20-35.254%20C%20147.119%20-41.846000000000004%20138.086%20-46.68%20127.344%20-46.68%20L%20103.125%20-46.68%20C%20100.049%20-46.68%2098.877%20-47.168%2096.924%20-49.268%20L%2090.503%20-56.217%20L%2090.503%20-66.571%20L%2081.002%20-66.571%20L%2081.031%20-82.156%20L%2066.863%20-82.156%20L%2059.277%20-90.478%20C%2058.057%20-91.797%2056.592%20-92.529%2055.029%20-92.529%20L%2048.438%20-92.529%20C%2046.973%20-92.529%2046.143%20-91.211%2046.875%20-89.648%20L%2066.26%20-46.68%20L%2034.912%20-43.555%20L%2024.658%20-62.158%20C%2023.828%20-63.477%2022.9%20-64.062%2021.094%20-64.062%20L%2018.359%20-64.062%20C%2016.895%20-64.062%2016.064%20-63.281%2016.064%20-61.768%20L%2016.064%20-8.789%20C%2016.064%20-7.227%2016.895%20-6.494%2018.359%20-6.494%20L%2021.094%20-6.494%20C%2022.9%20-6.494%2023.828%20-7.08%2024.658%20-8.398%20L%2034.912%20-26.953%20L%2066.26%20-23.877%20L%2046.875%2019.141%20C%2046.143%2020.654%2046.973%2021.973%2048.438%2021.973%20L%2055.029%2021.973%20C%2056.592%2021.973%2058.057%2021.24%2059.277%2019.922%20L%2066.848%2012.193%20L%2081.031%2012.256%20L%2081.002%20-3.927%20L%2090.503%20-3.927%20L%2090.503%20-14.345%20L%2096.924%20-21.24%20C%2098.877%20-23.389%20100.049%20-23.877%20103.125%20-23.877%20L%20127.344%20-23.877%20C%20138.086%20-23.877%20147.119%20-28.662%20147.119%20-35.254%20Z'/%3e%3c/g%3e%3c/svg%3e", yn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.408'%20height='14.408'/%3e%3c/defs%3e%3cg%20id='Thin-L'%20transform='matrix(0,%20-1.526083,%201.526083,%200,%20154.192352,%20224.515808)'%3e%3cpath%20d='M%2017.718%20-10.644%20L%2024.384%20-34.95%20L%2017.718%20-59.193%20L%2022.376%20-63.188%20L%2042.042%20-45.053%20L%2046.445%20-45.053%20L%2042.042%20-78.667%20L%2053.954%20-79.462%20L%2099.913%20-44.391%20C%2099.913%20-44.391%20119.05%20-46.566%20128.237%20-44.391%20C%20135.089%20-42.769%20147.119%20-34.918%20147.119%20-34.918%20C%20147.119%20-34.918%20135.081%20-27.123%20128.237%20-25.509%20C%20119.056%20-23.344%2099.939%20-25.485%2099.939%20-25.485%20L%2053.954%209.636%20L%2042.042%208.831%20L%2046.445%20-24.784%20L%2042.042%20-24.784%20L%2022.376%20-6.638%20L%2017.718%20-10.644%20Z'%20stroke='none'%20fill='%23000'/%3e%3c/g%3e%3c/svg%3e", xn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.408'%20height='14.408'/%3e%3c/defs%3e%3cg%20id='Thin-L'%20transform='matrix(0,%20-1.526083,%201.526083,%200,%20154.192352,%20224.515808)'%3e%3cpath%20d='M%20142.944%20-44.391%20C%20139.538%20-52.615%20135.405%20-50.329%20128.237%20-53.832%20C%20116.927%20-59.359%20102.102%20-58.649%2090.472%20-53.832%20C%2082.248%20-50.426%2071.59%20-40.893%2071.59%20-40.893%20L%2033.825%20-40.893%20L%2024.658%20-62.158%20C%2023.828%20-63.477%2022.9%20-64.062%2021.094%20-64.062%20L%2018.359%20-64.062%20C%2016.895%20-64.062%2016.064%20-63.281%2016.064%20-61.768%20L%2024.384%20-44.391%20L%2024.384%20-25.509%20L%2016.064%20-8.789%20C%2016.064%20-7.227%2016.895%20-6.494%2018.359%20-6.494%20L%2021.094%20-6.494%20C%2022.9%20-6.494%2023.828%20-7.08%2024.658%20-8.398%20L%2033.822%20-30.006%20L%2071.59%20-29.975%20C%2071.59%20-29.975%2082.248%20-19.474%2090.472%20-16.068%20C%20102.102%20-11.25%20116.937%20-10.521%20128.237%20-16.068%20C%20135.379%20-19.574%20139.429%20-17.285%20142.835%20-25.509%20C%20145.244%20-31.324%20145.353%20-38.576%20142.86%20-44.422%20L%20142.944%20-44.391%20Z'/%3e%3c/g%3e%3cpath%20style='stroke:%20rgb(0,%200,%200);%20stroke-width:%200px;'%20d='M%2096.385%20-25.442%20C%2096.705%2055.448%2096.705%20113.74%2096.607%20193.603%20C%2096.599%20200.4%20105.318%20200.399%20105.292%20193.603%20C%20104.981%20113.74%20104.981%2055.448%20104.846%20-25.442%20C%20104.835%20-32.064%2096.359%20-32.064%2096.385%20-25.442%20Z'%20transform='matrix(0.642788,%20-0.766044,%200.766044,%200.642788,%20-28.43878,%20107.304662)'/%3e%3cpath%20style='stroke:%20rgb(0,%200,%200);%20stroke-width:%200px;'%20d='M%2096.348%20-25.479%20C%2096.668%2055.411%2096.668%20113.703%2096.57%20193.566%20C%2096.562%20200.363%20105.281%20200.362%20105.255%20193.566%20C%20104.944%20113.703%20104.944%2055.411%20104.809%20-25.479%20C%20104.798%20-32.101%2096.322%20-32.101%2096.348%20-25.479%20Z'%20transform='matrix(0.642788,%200.766044,%20-0.766044,%200.642788,%20100.438642,%20-47.173674)'/%3e%3c/svg%3e", wn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.286'%20height='14.3'/%3e%3c/defs%3e%3cpath%20d='M%20565%20355%20m%20-35.428%200%20a%2035.428%2035.428%200%201%200%2070.856%200%20a%2035.428%2035.428%200%201%200%20-70.856%200%20Z%20M%20565%20355%20m%20-21.257%200%20a%2021.257%2021.257%200%200%201%2042.514%200%20a%2021.257%2021.257%200%200%201%20-42.514%200%20Z'%20transform='matrix(-0.724416,%200.689363,%20-0.689363,%20-0.724416,%20703.93356,%20-82.23506)'%20bx:shape='ring%20565%20355%2021.257%2021.257%2035.428%2035.428%201@ee613e3f'/%3e%3cpath%20d='M%20565%20355%20m%20-35.428%200%20a%2035.428%2035.428%200%201%200%2070.856%200%20a%2035.428%2035.428%200%201%200%20-70.856%200%20Z%20M%20565%20355%20m%20-21.257%200%20a%2021.257%2021.257%200%200%201%2042.514%200%20a%2021.257%2021.257%200%200%201%20-42.514%200%20Z'%20transform='matrix(-0.724416,%200.689363,%20-0.689363,%20-0.724416,%20804.108365,%20-82.23506)'%20bx:shape='ring%20565%20355%2021.257%2021.257%2035.428%2035.428%201@ee613e3f'/%3e%3cpath%20d='M%20565%20355%20m%20-35.428%200%20a%2035.428%2035.428%200%201%200%2070.856%200%20a%2035.428%2035.428%200%201%200%20-70.856%200%20Z%20M%20565%20355%20m%20-21.257%200%20a%2021.257%2021.257%200%200%201%2042.514%200%20a%2021.257%2021.257%200%200%201%20-42.514%200%20Z'%20transform='matrix(-0.724416,%200.689363,%20-0.689363,%20-0.724416,%20704.106351,%2017.864939)'%20bx:shape='ring%20565%20355%2021.257%2021.257%2035.428%2035.428%201@ee613e3f'/%3e%3cpath%20d='M%20565%20355%20m%20-35.428%200%20a%2035.428%2035.428%200%201%200%2070.856%200%20a%2035.428%2035.428%200%201%200%20-70.856%200%20Z%20M%20565%20355%20m%20-21.257%200%20a%2021.257%2021.257%200%200%201%2042.514%200%20a%2021.257%2021.257%200%200%201%20-42.514%200%20Z'%20transform='matrix(-0.724416,%200.689363,%20-0.689363,%20-0.724416,%20804.108365,%2017.7901)'%20bx:shape='ring%20565%20355%2021.257%2021.257%2035.428%2035.428%201@ee613e3f'/%3e%3cpath%20style='stroke:%20rgb(0,%200,%200);%20stroke-width:%200px;'%20d='M%2057.144%2042.9%20C%2074.235%2041.207%2079.789%2071.5%20100.002%2071.5%20C%20120.215%2071.5%20125.769%2041.207%20142.86%2042.9%20C%20149.565%2043.564%20156.482%2050.495%20157.146%2057.2%20C%20158.839%2074.298%20128.574%2079.887%20128.574%20100.1%20C%20128.574%20120.313%20158.839%20125.902%20157.146%20143%20C%20156.482%20149.705%20149.565%20156.636%20142.86%20157.3%20C%20125.769%20158.993%20120.215%20128.7%20100.002%20128.7%20C%2079.789%20128.7%2074.235%20158.993%2057.144%20157.3%20C%2050.439%20156.636%2043.522%20149.705%2042.858%20143%20C%2041.165%20125.902%2071.43%20120.313%2071.43%20100.1%20C%2071.43%2079.887%2041.165%2074.298%2042.858%2057.2%20C%2043.522%2050.495%2050.439%2043.564%2057.144%2042.9%20Z'/%3e%3c/svg%3e", Cn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.286'%20height='14.3'/%3e%3c/defs%3e%3cg%20id='Thin-L'%20transform='matrix(0,%20-1.526083,%201.526083,%200,%20154.192352,%20224.515808)'%3e%3cpath%20d='M%20147.119%20-35.509%20C%20147.119%20-42.101%20128.247%20-40.196%20128.247%20-40.196%20C%20128.247%20-40.196%20121.869%20-39.494%20118.811%20-40.229%20C%20115.479%20-41.03%20109.637%20-44.871%20109.637%20-44.871%20C%20109.637%20-44.871%20109.637%20-82.316%20109.637%20-97.762%20C%20109.637%20-101.182%2099.913%20-101.038%2099.913%20-101.038%20C%2098.849%20-102.188%2099.913%20-59.556%2097.974%20-44.881%20C%2096.725%20-42.05%2090.503%20-40.229%2090.503%20-40.229%20L%2024.386%20-38.787%20L%2024.658%20-62.158%20C%2023.828%20-63.477%2022.9%20-64.062%2021.094%20-64.062%20L%2018.359%20-64.062%20C%2016.895%20-64.062%2016.064%20-63.281%2016.064%20-61.768%20L%2014.943%20-34.95%20L%2016.064%20-8.789%20C%2016.064%20-7.227%2016.895%20-6.494%2018.359%20-6.494%20L%2021.094%20-6.494%20C%2022.9%20-6.494%2023.828%20-7.08%2024.658%20-8.398%20L%2024.386%20-32.234%20L%2090.503%20-30.858%20C%2090.503%20-30.858%2096.893%20-28.325%2097.974%20-26.14%20C%20100.715%20-9.365%2099.913%2028.489%2099.939%2030.016%20C%2099.181%2030.002%20109.637%2030.048%20109.637%2026.756%20C%20109.637%2011.297%20109.637%20-26.148%20109.637%20-26.148%20C%20111.59%20-28.297%20115.471%20-30.44%20119.008%20-30.858%20C%20122.368%20-30.977%20128.247%20-30.826%20128.247%20-30.826%20C%20128.247%20-30.826%20147.119%20-28.917%20147.119%20-35.509%20Z'/%3e%3c/g%3e%3c/svg%3e", bn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.286'%20height='14.3'/%3e%3c/defs%3e%3cellipse%20cx='100'%20cy='100.1'%20rx='46.209'%20ry='85.8'/%3e%3c/svg%3e", Ln = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.286'%20height='14.3'/%3e%3c/defs%3e%3cpath%20style='stroke:%20rgb(0,%200,%200);%20stroke-width:%200px;'%20d='M%20142.86%2028.6%20L%20171.432%2057.2%20L%20185.718%20100.1%20L%20171.432%20143%20L%20142.86%20171.6%20L%20100.002%20185.9%20L%2057.144%20171.6%20L%2028.572%20143%20L%2014.286%20100.1%20L%2028.572%2057.2%20L%2057.144%2028.6%20L%20100.002%2014.3%20L%20142.86%2028.6%20Z'/%3e%3ccircle%20cx='100'%20cy='100'%20r='12.48'%20style='fill:%20rgb(255,%20255,%20255);'/%3e%3c/svg%3e", Pn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.286'%20height='14.3'/%3e%3c/defs%3e%3cg%20id='Thin-L'%20transform='matrix(0,%20-1.526083,%201.526083,%200,%20154.197083,%20253.115128)'%3e%3cpath%20d='M%20147.64%20-35.505%20C%20147.64%20-42.097%2091.064%20-101.03%2091.064%20-101.03%20C%2090.511%20-101.627%2091.418%20-78.099%2091.418%20-78.099%20L%2082.048%20-63.585%20C%2082.048%20-63.585%2098.395%20-44.866%2091.418%20-44.866%20C%2072.677%20-44.866%2072.677%20-44.866%2063.306%20-44.866%20C%2060.254%20-44.866%2072.744%20-40.081%2072.677%20-35.505%20C%2072.606%20-30.724%2060.117%20-26.143%2063.306%20-26.143%20C%2072.677%20-26.143%2078.924%20-26.143%2091.418%20-26.143%20C%20100.788%20-26.143%2082.048%20-7.418%2082.048%20-7.418%20L%2091.418%207.087%20L%2091.09%2030.024%20C%2091.09%2030.024%20147.64%20-28.913%20147.64%20-35.505%20Z'/%3e%3c/g%3e%3c/svg%3e", Tn = "data:image/svg+xml,%3c?xml%20version='1.0'%20standalone='no'?%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%2020010904//EN'%20'http://www.w3.org/TR/2001/REC-SVG-20010904/DTD/svg10.dtd'%3e%3csvg%20version='1.0'%20xmlns='http://www.w3.org/2000/svg'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cg%20transform='translate(0.000000,512.000000)%20scale(0.100000,-0.100000)'%20fill='%23000000'%20stroke='none'%3e%3cpath%20d='M2517%205043%20c-135%20-290%20-233%20-673%20-267%20-1043%20-5%20-63%20-10%20-246%20-10%20-406%20l0%20-292%20-64%20-59%20c-94%20-88%20-302%20-243%20-327%20-243%20-5%200%20-9%2029%20-9%2065%20l0%2066%20-107%20-3%20-108%20-3%20-5%20-128%20-5%20-129%20-690%20-339%20c-379%20-187%20-701%20-350%20-714%20-362%20-48%20-44%20-71%20-113%20-71%20-214%200%20-63%204%20-93%2011%20-93%2011%200%201377%20381%201433%20400%2026%209%2033%206%2065%20-28%2028%20-29%2039%20-52%2049%20-101%2016%20-78%2036%20-77%2040%202%203%2053%205%2057%2028%2057%2044%200%2064%2017%2064%2055%20l0%2035%2098%200%20c53%200%20140%20-3%20193%20-6%20l96%20-7%206%20-326%20c7%20-429%2039%20-804%2089%20-1046%2011%20-55%2021%20-106%2022%20-114%200%20-8%20-60%20-57%20-135%20-110%20-340%20-240%20-561%20-405%20-568%20-425%20-9%20-23%20-22%20-246%20-15%20-246%205%200%20840%20208%20865%20216%2012%203%2018%20-7%2025%20-43%20l9%20-48%2045%200%2045%200%209%2048%20c7%2036%2013%2046%2025%2043%2059%20-18%20864%20-216%20867%20-214%202%202%200%2059%20-5%20126%20-6%20102%20-11%20126%20-27%20141%20-25%2025%20-484%20356%20-602%20434%20-75%2050%20-91%2066%20-87%2082%2063%20249%20104%20669%20112%201156%20l6%20326%2096%207%20c53%203%20140%206%20194%206%20l97%200%200%20-35%20c0%20-38%2020%20-55%2064%20-55%2023%200%2025%20-4%2028%20-57%204%20-79%2024%20-80%2040%20-2%2010%2049%2021%2072%2049%20101%2032%2034%2039%2037%2065%2028%2056%20-19%201422%20-400%201433%20-400%207%200%2011%2030%2011%2093%200%20101%20-23%20170%20-71%20214%20-13%2012%20-335%20175%20-714%20362%20l-690%20339%20-5%20129%20-5%20128%20-107%203%20-108%203%200%20-66%20c0%20-36%20-4%20-65%20-9%20-65%20-25%200%20-233%20155%20-327%20243%20l-64%2059%200%20292%20c0%20160%20-5%20343%20-10%20406%20-28%20299%20-99%20616%20-197%20875%20-42%20111%20-104%20245%20-113%20245%20-4%200%20-23%20-35%20-43%20-77z'/%3e%3c/g%3e%3c/svg%3e", Mn = "data:image/svg+xml,%3c?xml%20version='1.0'%20standalone='no'?%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%2020010904//EN'%20'http://www.w3.org/TR/2001/REC-SVG-20010904/DTD/svg10.dtd'%3e%3csvg%20version='1.0'%20xmlns='http://www.w3.org/2000/svg'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cg%20transform='translate(0.000000,512.000000)%20scale(0.100000,-0.100000)'%20fill='%23000000'%20stroke='none'%3e%3cpath%20d='M2512%205104%20c-83%20-58%20-152%20-303%20-182%20-639%20-6%20-66%20-14%20-281%20-17%20-478%20l-6%20-358%20-226%20-199%20-226%20-200%20-5%2066%20c-6%2082%20-20%2094%20-105%2094%20-98%200%20-105%20-9%20-105%20-138%200%20-59%204%20-113%209%20-120%204%20-7%2020%20-16%2036%20-20%2027%20-8%2016%20-18%20-256%20-259%20l-284%20-252%20-3%2077%20c-2%2042%20-8%2082%20-14%2089%20-14%2017%20-159%2018%20-176%201%20-8%20-8%20-12%20-52%20-12%20-130%200%20-96%203%20-120%2015%20-124%209%20-4%2015%20-19%2015%20-38%20-1%20-28%20-27%20-54%20-285%20-281%20l-284%20-250%200%20-235%20c0%20-129%203%20-228%206%20-220%204%208%2013%2048%2022%2089%20l16%2074%20590%20335%20590%20335%20325%20104%20c179%2057%20333%20106%20343%20109%2016%205%2017%20-27%2017%20-579%200%20-450%204%20-619%2015%20-738%2016%20-163%2040%20-314%2055%20-353%2010%20-27%2016%20-20%20-426%20-447%20l-202%20-194%20-7%20-108%20c-4%20-60%20-5%20-111%20-2%20-113%202%20-3%20168%2044%20368%20104%20200%2060%20365%20108%20367%20107%202%20-2%2012%20-42%2023%20-89%2012%20-47%2029%20-96%2040%20-108%20l19%20-23%2019%2023%20c11%2012%2028%2061%2040%20108%2011%2047%2021%2087%2023%2089%202%201%20167%20-47%20367%20-107%20200%20-60%20366%20-107%20368%20-104%203%202%202%2053%20-2%20113%20l-7%20108%20-202%20194%20c-442%20427%20-436%20420%20-426%20447%2015%2039%2039%20190%2055%20353%2011%20119%2015%20288%2015%20738%200%20552%201%20584%2018%20579%209%20-3%20163%20-52%20342%20-109%20l326%20-104%20589%20-335%20590%20-335%2016%20-74%20c9%20-41%2018%20-81%2022%20-89%203%20-8%206%2091%206%20220%20l0%20235%20-284%20250%20c-258%20227%20-284%20253%20-285%20281%200%2019%206%2034%2015%2038%2012%204%2015%2028%2015%20124%200%2078%20-4%20122%20-12%20130%20-17%2017%20-162%2016%20-176%20-1%20-6%20-7%20-12%20-47%20-14%20-89%20l-3%20-77%20-284%20252%20c-272%20241%20-283%20251%20-256%20259%2016%204%2032%2013%2036%2020%205%207%209%2061%209%20120%200%20129%20-7%20138%20-105%20138%20-85%200%20-99%20-12%20-105%20-94%20l-5%20-66%20-226%20200%20-226%20199%20-6%20358%20c-4%20197%20-11%20412%20-17%20478%20-36%20406%20-123%20655%20-230%20655%20-14%200%20-36%20-7%20-48%20-16z'/%3e%3c/g%3e%3c/svg%3e", kn = "data:image/svg+xml,%3c?xml%20version='1.0'%20standalone='no'?%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%2020010904//EN'%20'http://www.w3.org/TR/2001/REC-SVG-20010904/DTD/svg10.dtd'%3e%3csvg%20version='1.0'%20xmlns='http://www.w3.org/2000/svg'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cg%20transform='translate(0.000000,512.000000)%20scale(0.100000,-0.100000)'%20fill='%23000000'%20stroke='none'%3e%3cpath%20d='M2504%205088%20c-123%20-108%20-199%20-352%20-224%20-720%20-6%20-84%20-10%20-331%20-9%20-548%20l0%20-395%20-34%20-40%20c-29%20-35%20-365%20-285%20-381%20-285%20-4%200%20-3%2062%200%20138%206%20131%206%20138%20-16%20165%20l-22%2028%20-121%20-3%20-122%20-3%20-19%20-38%20c-17%20-36%20-18%20-49%20-7%20-180%2010%20-123%2014%20-144%2032%20-159%2017%20-14%2020%20-27%2017%20-75%20l-3%20-58%20-230%20-159%20c-126%20-87%20-477%20-328%20-779%20-534%20-456%20-312%20-551%20-381%20-562%20-408%20-17%20-41%20-19%20-175%20-2%20-192%207%20-7%2021%20-12%2031%20-12%2018%200%20542%20194%201077%20399%20222%2085%20236%2089%20244%2070%2013%20-28%2032%20-23%2042%2012%208%2026%2019%2035%2084%2060%2058%2023%20312%2079%20359%2079%205%200%2012%20-11%2015%20-25%209%20-34%2033%20-31%2041%205%208%2035%201%2032%20195%2065%2085%2015%20163%2029%20173%2031%2016%205%2017%20-22%2017%20-478%200%20-367%204%20-514%2015%20-613%2017%20-142%2040%20-282%2054%20-333%209%20-31%207%20-35%20-37%20-76%20-55%20-51%20-327%20-282%20-564%20-479%20-132%20-110%20-167%20-144%20-172%20-169%20-7%20-37%20-8%20-158%20-1%20-158%203%200%20203%2050%20445%20110%20242%2061%20444%20110%20449%20110%205%200%2012%20-15%2016%20-32%2033%20-168%2077%20-168%20110%200%204%2017%2011%2032%2016%2032%205%200%20207%20-50%20449%20-110%20242%20-61%20442%20-110%20445%20-110%207%200%206%20121%20-1%20158%20-5%2025%20-40%2059%20-172%20169%20-237%20197%20-509%20428%20-564%20479%20-44%2041%20-46%2045%20-37%2076%2014%2051%2037%20191%2054%20333%2011%2099%2015%20246%2015%20613%200%20456%201%20483%2018%20478%209%20-2%2087%20-16%20172%20-31%20194%20-33%20187%20-30%20195%20-65%208%20-36%2032%20-39%2041%20-5%203%2014%2010%2025%2015%2025%2047%200%20301%20-56%20359%20-79%2065%20-25%2076%20-34%2084%20-60%2010%20-35%2029%20-40%2042%20-12%208%2019%2022%2015%20244%20-70%20535%20-205%201059%20-399%201077%20-399%2010%200%2024%205%2031%2012%2017%2017%2015%20151%20-2%20192%20-11%2027%20-106%2096%20-562%20408%20-302%20206%20-652%20447%20-779%20534%20l-230%20159%20-3%2058%20c-3%2048%200%2061%2017%2075%2018%2015%2022%2037%2032%20154%2012%20148%205%20199%20-31%20218%20-10%206%20-68%2010%20-129%2010%20-108%200%20-110%200%20-131%20-27%20-21%20-27%20-22%20-34%20-16%20-165%204%20-76%204%20-138%201%20-138%20-17%200%20-352%20250%20-382%20285%20l-34%2040%200%20395%20c2%20575%20-19%20812%20-90%201026%20-47%20142%20-143%20274%20-199%20274%20-11%200%20-36%20-15%20-56%20-32z'/%3e%3c/g%3e%3c/svg%3e", zn = "data:image/svg+xml,%3c?xml%20version='1.0'%20standalone='no'?%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%2020010904//EN'%20'http://www.w3.org/TR/2001/REC-SVG-20010904/DTD/svg10.dtd'%3e%3csvg%20version='1.0'%20xmlns='http://www.w3.org/2000/svg'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cg%20transform='translate(0.000000,512.000000)%20scale(0.100000,-0.100000)'%20fill='%23000000'%20stroke='none'%3e%3cpath%20d='M2498%205101%20c-61%20-48%20-124%20-222%20-158%20-436%20-33%20-206%20-41%20-346%20-47%20-816%20l-6%20-456%20-150%20-104%20c-83%20-57%20-155%20-105%20-159%20-107%20-4%20-2%20-8%2043%20-8%20101%200%20122%20-13%20149%20-83%20168%20-51%2014%20-155%205%20-197%20-16%20l-30%20-16%200%20-192%20c0%20-161%203%20-196%2016%20-216%2014%20-19%2014%20-25%203%20-36%20-8%20-7%20-348%20-243%20-756%20-525%20-843%20-581%20-784%20-531%20-791%20-683%20-6%20-108%201%20-114%2086%20-75%20100%2044%20522%20218%201008%20414%20l431%20173%20311%207%20311%207%204%20-589%204%20-589%2031%20-175%2032%20-174%20-20%20-21%20c-11%20-11%20-161%20-135%20-333%20-276%20l-312%20-255%20-3%20-98%20c-2%20-88%20-1%20-98%2014%20-92%2089%2034%20758%20256%20761%20252%203%20-3%2019%20-62%2037%20-132%2021%20-83%2038%20-131%2049%20-138%2034%20-21%2050%205%2083%20138%2018%2070%2034%20129%2037%20132%203%204%20672%20-218%20761%20-252%2015%20-6%2016%204%2014%2092%20l-3%2098%20-312%20255%20c-172%20141%20-322%20265%20-333%20276%20l-20%2021%2032%20174%2031%20175%204%20589%204%20589%20311%20-7%20311%20-7%20431%20-173%20c486%20-196%20908%20-370%201008%20-414%2085%20-39%2092%20-33%2086%2075%20-7%20152%2052%20102%20-791%20683%20-408%20282%20-746%20516%20-751%20521%20-6%205%20-2%2024%208%2046%2014%2032%2016%2066%2014%20217%20-3%20178%20-3%20179%20-28%20198%20-33%2024%20-140%2034%20-198%2019%20-68%20-19%20-76%20-35%20-82%20-166%20l-5%20-114%20-161%20111%20-161%20112%20-6%20455%20c-3%20251%20-11%20510%20-17%20576%20-24%20268%20-72%20485%20-132%20598%20-39%2072%20-69%2097%20-118%2097%20-22%200%20-48%20-8%20-62%20-19z'/%3e%3c/g%3e%3c/svg%3e", Sn = "data:image/svg+xml,%3c?xml%20version='1.0'%20standalone='no'?%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%2020010904//EN'%20'http://www.w3.org/TR/2001/REC-SVG-20010904/DTD/svg10.dtd'%3e%3csvg%20version='1.0'%20xmlns='http://www.w3.org/2000/svg'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cg%20transform='translate(0.000000,512.000000)%20scale(0.100000,-0.100000)'%20fill='%23000000'%20stroke='none'%3e%3cpath%20d='M2496%205099%20c-57%20-45%20-109%20-172%20-145%20-351%20-47%20-237%20-61%20-444%20-61%20-913%20l-1%20-370%20-187%20-149%20-187%20-148%20-5%20151%20c-7%20201%20-2%20196%20-181%20196%20l-131%200%20-24%20-28%20c-24%20-28%20-24%20-31%20-24%20-236%200%20-196%201%20-210%2021%20-235%2011%20-14%2028%20-26%2037%20-26%2010%200%2022%20-8%2029%20-19%2010%20-16%20-52%20-69%20-633%20-536%20-635%20-511%20-750%20-612%20-819%20-720%20-62%20-96%20-104%20-242%20-76%20-260%207%20-4%2037%2017%2073%2050%20169%20156%20230%20188%201146%20589%20l403%20176%20274%200%20274%200%203%20-487%20c4%20-439%206%20-504%2027%20-653%2012%20-91%2031%20-204%2041%20-253%20l18%20-88%20-308%20-294%20-307%20-295%20-7%20-76%20c-9%20-107%20-8%20-124%207%20-124%206%200%20147%2059%20311%20130%20224%2097%20310%20130%20341%20130%20l41%200%2012%20-58%20c6%20-31%2022%20-78%2034%20-102%2022%20-43%2025%20-45%2068%20-45%2043%200%2046%202%2068%2045%2012%2024%2028%2071%2034%20102%20l12%2058%2041%200%20c31%200%20117%20-33%20341%20-130%20164%20-71%20305%20-130%20313%20-130%2011%200%2013%2015%207%2077%20-3%2042%20-6%2086%20-6%2098%200%2016%20-85%20104%20-309%20318%20l-309%20296%2018%2088%20c10%2049%2029%20162%2041%20253%2021%20149%2023%20214%2027%20652%20l3%20488%20274%200%20274%200%20403%20-176%20c916%20-401%20977%20-433%201146%20-589%2036%20-33%2066%20-54%2073%20-50%2028%2018%20-14%20164%20-76%20260%20-69%20108%20-184%20209%20-819%20720%20-581%20467%20-643%20520%20-633%20536%207%2011%2019%2019%2029%2019%209%200%2026%2012%2037%2026%2020%2025%2021%2039%2021%20235%200%20205%200%20208%20-24%20236%20l-24%2028%20-131%200%20c-179%200%20-174%205%20-181%20-197%20l-5%20-151%20-185%20149%20-185%20149%20-6%20455%20c-7%20495%20-15%20594%20-60%20828%20-49%20247%20-119%20372%20-209%20372%20-23%200%20-48%20-8%20-64%20-21z'/%3e%3c/g%3e%3c/svg%3e", En = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.286'%20height='14.3'/%3e%3c/defs%3e%3cg%20id='Thin-L'%20transform='matrix(0,%20-1.526083,%201.526083,%200,%20154.197083,%20247.503403)'%3e%3cpath%20d='M%20143.535%20-82.319%20C%20143.535%20-82.819%20134.164%20-82.319%20134.164%20-82.319%20C%20134.164%20-82.319%20136.37%20-89.474%20134.164%20-91.68%20C%20130.987%20-94.857%20109.502%20-95.129%20106.053%20-91.68%20C%20103.847%20-89.474%20106.053%20-82.319%20106.053%20-82.319%20L%2087.312%20-82.319%20C%2087.312%20-82.319%2089.518%20-89.474%2087.312%20-91.68%20C%2083.747%20-95.245%2062.262%20-94.741%2059.201%20-91.68%20C%2056.995%20-89.474%2059.201%20-82.319%2059.201%20-82.319%20L%2040.46%20-82.319%20C%2040.367%20-82.419%2040.46%20-54.235%2040.46%20-54.235%20L%2096.683%20-44.874%20L%2096.683%20-26.151%20L%2040.46%20-16.79%20L%2040.46%2011.294%20L%2059.201%2011.294%20C%2059.201%2011.294%2056.995%2018.449%2059.201%2020.655%20C%2062.344%2023.798%2083.829%2024.138%2087.312%2020.655%20C%2089.518%2018.449%2087.312%2011.294%2087.312%2011.294%20C%20107.852%2011.294%20106.053%2011.294%20106.053%2011.294%20C%20106.053%2011.294%20103.847%2018.449%20106.053%2020.655%20C%20109.208%2023.81%20130.693%2024.126%20134.164%2020.655%20C%20136.37%2018.449%20134.164%2011.294%20134.164%2011.294%20L%20143.535%2011.294%20L%20143.535%20-7.429%20L%20152.905%20-7.429%20L%20152.905%20-63.596%20L%20143.535%20-63.596%20C%20143.535%20-63.596%20143.535%20-81.196%20143.535%20-82.319%20Z'/%3e%3c/g%3e%3c/svg%3e", An = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20version='1.0'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%20xmlns='http://www.w3.org/2000/svg'%3e%3cpath%20d='M%20250.857%2065.688%20C%20249.764%2066.371%20247.17%2068.555%20245.122%2070.558%20C%20241.618%2073.971%20241.163%2074.608%20237.886%2081.253%20C%20231.879%2093.495%20226.599%20110.425%20224.369%20124.488%20C%20221.593%20142.465%20221.593%20142.42%20221.684%20163.628%20C%20221.821%20189.387%20221.457%20196.532%20219.864%20200.446%20C%20218.726%20203.313%20217.452%20204.679%20215.404%20205.27%20C%20214.675%20205.452%20214.038%20205.179%20213.037%20204.178%20C%20211.626%20202.813%20211.581%20202.585%20210.761%20195.713%20C%20210.488%20193.437%20210.079%20190.206%20209.806%20188.477%20C%20209.26%20184.426%20209.533%20181.514%20210.716%20179.921%20L%20211.626%20178.646%20L%20208.804%20177.873%20C%20206.756%20177.281%20205.846%20176.735%20205.346%20175.87%20C%20204.526%20174.505%20198.701%20170.454%20197.518%20170.454%20C%20196.334%20170.454%20190.509%20174.505%20189.69%20175.87%20C%20189.189%20176.735%20188.279%20177.281%20186.186%20177.873%20C%20183.455%20178.646%20183.364%20178.737%20184.046%20179.511%20C%20184.957%20180.512%20184.957%20180.922%20184.092%20180.922%20C%20183.682%20180.922%20183.546%20181.195%20183.682%20181.605%20C%20183.864%20181.969%20184.32%20182.287%20184.82%20182.287%20C%20185.548%20182.287%20185.639%20182.651%20185.412%20186.383%20C%20185.321%20188.613%20185.002%20190.98%20184.775%20191.572%20C%20184.547%20192.163%20184.229%20194.484%20184.092%20196.714%20C%20183.728%20201.629%20183.182%20203.541%20181.907%20204.224%20C%20181.179%20204.633%20176.674%20204.861%20166.434%20204.952%20L%20152.007%20205.134%20L%20151.37%20203.632%20C%20151.051%20202.767%20150.641%20200.173%20150.459%20197.852%20C%20150.277%20195.531%20149.959%20193.073%20149.686%20192.391%20C%20149.413%20191.708%20149.185%20189.205%20149.14%20186.747%20C%20149.049%20182.833%20149.14%20182.196%20150.095%20180.967%20L%20151.097%20179.557%20L%20148.275%20178.783%20C%20146.227%20178.191%20145.317%20177.645%20144.77%20176.78%20C%20144.406%20176.098%20142.586%20174.596%20140.766%20173.367%20C%20138.945%20172.138%20137.443%20171.001%20137.443%20170.819%20C%20137.443%20169.226%20129.206%20173.959%20127.84%20176.325%20C%20127.294%20177.19%20126.384%20177.736%20124.291%20178.328%20C%20121.651%20179.056%20121.469%20179.193%20122.061%20179.921%20C%20123.38%20181.514%20123.699%20183.106%20123.517%20186.975%20C%20123.426%20189.159%20123.107%20191.435%20122.88%20192.027%20C%20122.652%20192.664%20122.334%20195.076%20122.152%20197.397%20C%20121.969%20199.718%20121.605%20202.267%20121.287%20203.086%20C%20120.604%20204.724%20120.604%20204.724%20107.497%20205.498%20C%20103.401%20205.725%2099.897%20206.044%2099.669%20206.181%20C%2099.442%20206.317%2089.384%20206.636%2077.278%20206.863%20C%2065.172%20207.091%2052.52%20207.5%2049.152%20207.773%20C%2045.784%20208.047%2037.046%20208.456%2029.764%20208.684%20C%2015.61%20209.139%2012.607%20209.548%208.875%20211.551%20C%208.374%20211.824%207.327%20212.37%206.554%20212.734%20C%204.961%20213.553%202.731%20215.965%201.684%20218.059%20C%200.546%20220.289%200%20224.749%200%20232.122%20C%200.046%20239.677%200.774%20244.501%202.139%20246.367%20C%202.64%20247.049%203.504%20248.415%204.005%20249.416%20C%204.915%20251.191%206.781%20252.966%209.102%20254.195%20C%2010.604%20255.014%2013.062%20255.378%2027.762%20256.88%20C%2030.128%20257.153%2033.724%20257.562%2035.726%20257.79%20C%2037.729%20258.063%2041.415%20258.473%2043.918%20258.746%20C%2056.479%20260.111%2059.665%20260.521%2060.393%20260.748%20C%2062.168%20261.34%2076.868%20263.297%2081.419%20263.57%20C%2084.059%20263.752%2087.063%20264.162%2088.018%20264.526%20C%2089.793%20265.117%2096.119%20265.982%20107.725%20267.165%20C%20111.047%20267.484%20113.914%20267.848%20114.096%20267.984%20C%20114.551%20268.303%20129.524%20270.306%20140.174%20271.489%20C%20144.816%20271.989%20150.459%20272.718%20152.735%20273.082%20C%20155.01%20273.446%20164.204%20274.447%20173.215%20275.357%20C%20182.181%20276.267%20190.281%20277.178%20191.146%20277.405%20C%20192.011%20277.678%20197.063%20277.86%20202.296%20277.906%20C%20207.576%20277.906%20213.629%20278.088%20215.813%20278.27%20C%20223.277%20278.907%20223.049%20277.997%20223.505%20309.946%20C%20223.732%20327.968%20224.051%20336.615%20224.551%20340.893%20C%20224.961%20344.124%20225.279%20348.22%20225.279%20349.995%20C%20225.279%20351.907%20225.553%20353.682%20225.962%20354.501%20C%20226.736%20355.957%20226.872%20357.186%20226.144%20356.139%20C%20225.78%20355.639%20225.78%20356.503%20226.008%20359.098%20C%20226.645%20365.469%20226.554%20366.834%20225.279%20368.2%20C%20223.823%20369.747%20220.819%20371.294%20217.315%20372.25%20C%20213.31%20373.342%20208.577%20374.753%20205.482%20375.755%20C%20203.98%20376.21%20200.612%20377.12%20197.973%20377.712%20C%20195.333%20378.349%20192.375%20379.122%20191.374%20379.486%20C%20190.373%20379.851%20188.552%20380.26%20187.278%20380.488%20C%20186.049%20380.67%20184.456%20381.079%20183.773%20381.443%20C%20183.091%20381.807%20181.998%20382.081%20181.361%20382.081%20C%20180.724%20382.081%20179.45%20382.399%20178.494%20382.809%20C%20176.537%20383.628%20171.303%20385.175%20165.432%20386.632%20C%20157.923%20388.498%20152.598%20392.457%20151.643%20396.917%20C%20150.641%20401.605%20150.505%20405.519%20151.233%20408.067%20C%20152.28%20411.617%20154.601%20415.668%20156.012%20416.396%20C%20156.694%20416.76%20157.65%20417.443%20158.105%20417.989%20C%20158.606%20418.535%20159.197%20418.944%20159.425%20418.944%20C%20159.698%20418.944%20160.244%20419.218%20160.608%20419.582%20C%20161.61%20420.492%20166.343%20422.13%20169.301%20422.585%20C%20170.666%20422.813%20175.172%20423.405%20179.313%20423.951%20C%20183.455%20424.451%20188.37%20425.088%20190.236%20425.316%20C%20192.102%20425.589%20196.926%20425.953%20200.931%20426.226%20C%20219.09%20427.318%20232.379%20428.729%20235.519%20429.913%20C%20238.068%20430.868%20240.708%20433.326%20242.938%20436.921%20C%20243.711%20438.196%20245.077%20439.88%20245.987%20440.653%20C%20246.852%20441.427%20247.58%20442.474%20247.58%20442.929%20C%20247.625%20443.93%20254.498%20448.527%20255.999%20448.527%20C%20257.501%20448.527%20264.373%20443.93%20264.419%20442.929%20C%20264.419%20442.474%20265.147%20441.427%20266.012%20440.653%20C%20266.922%20439.88%20268.287%20438.196%20269.061%20436.921%20C%20271.291%20433.326%20273.931%20430.868%20276.479%20429.913%20C%20279.62%20428.729%20292.909%20427.318%20311.068%20426.226%20C%20315.073%20425.953%20319.897%20425.589%20321.763%20425.316%20C%20323.629%20425.088%20328.544%20424.451%20332.685%20423.951%20C%20336.827%20423.405%20341.332%20422.813%20342.698%20422.585%20C%20345.656%20422.13%20350.389%20420.492%20351.39%20419.582%20C%20351.755%20419.218%20352.301%20418.944%20352.574%20418.944%20C%20352.801%20418.944%20353.393%20418.535%20353.894%20417.989%20C%20354.349%20417.443%20355.304%20416.76%20355.987%20416.396%20C%20357.398%20415.668%20359.719%20411.617%20360.766%20408.067%20C%20361.494%20405.519%20361.357%20401.605%20360.356%20396.917%20C%20359.4%20392.457%20354.076%20388.498%20346.566%20386.632%20C%20340.695%20385.175%20335.462%20383.628%20333.505%20382.809%20C%20332.549%20382.399%20331.275%20382.081%20330.637%20382.081%20C%20330%20382.081%20328.908%20381.807%20328.225%20381.443%20C%20327.543%20381.079%20325.95%20380.67%20324.721%20380.488%20C%20323.447%20380.26%20321.626%20379.851%20320.625%20379.486%20C%20319.624%20379.122%20316.666%20378.349%20314.026%20377.712%20C%20311.386%20377.12%20308.018%20376.21%20306.517%20375.755%20C%20303.422%20374.753%20298.689%20373.342%20294.684%20372.25%20C%20291.179%20371.294%20288.176%20369.747%20286.719%20368.2%20C%20285.445%20366.834%20285.354%20365.469%20285.991%20359.098%20C%20286.219%20356.503%20286.219%20355.639%20285.855%20356.139%20C%20285.126%20357.186%20285.263%20355.957%20286.037%20354.501%20C%20286.446%20353.682%20286.719%20351.907%20286.719%20349.995%20C%20286.719%20348.22%20287.038%20344.124%20287.447%20340.893%20C%20287.948%20336.615%20288.267%20327.968%20288.494%20309.946%20C%20288.949%20277.997%20288.722%20278.907%20296.186%20278.27%20C%20298.37%20278.088%20304.469%20277.906%20309.702%20277.906%20C%20314.982%20277.86%20319.988%20277.678%20320.853%20277.405%20C%20321.717%20277.178%20329.818%20276.267%20338.784%20275.357%20C%20347.795%20274.447%20356.988%20273.446%20359.264%20273.082%20C%20361.539%20272.718%20367.183%20271.989%20371.825%20271.489%20C%20382.474%20270.306%20397.448%20268.303%20397.903%20267.984%20C%20398.085%20267.848%20400.952%20267.484%20404.274%20267.165%20C%20415.88%20265.982%20422.206%20265.117%20423.98%20264.526%20C%20424.936%20264.162%20427.94%20263.752%20430.58%20263.57%20C%20435.131%20263.297%20449.831%20261.34%20451.606%20260.748%20C%20452.334%20260.521%20455.52%20260.111%20468.081%20258.746%20C%20470.584%20258.473%20474.27%20258.063%20476.273%20257.79%20C%20478.275%20257.562%20481.825%20257.153%20484.146%20256.925%20C%20498.892%20255.378%20501.395%20255.014%20502.897%20254.195%20C%20505.218%20252.966%20507.084%20251.191%20507.994%20249.416%20C%20508.494%20248.415%20509.359%20247.049%20509.86%20246.367%20C%20511.225%20244.501%20511.953%20239.677%20511.999%20232.122%20C%20511.999%20224.749%20511.453%20220.289%20510.315%20218.059%20C%20509.268%20215.965%20507.038%20213.553%20505.445%20212.734%20C%20504.671%20212.37%20503.625%20211.824%20503.124%20211.551%20C%20499.392%20209.548%20496.388%20209.139%20482.235%20208.684%20C%20474.953%20208.456%20466.215%20208.047%20462.847%20207.773%20C%20459.479%20207.5%20446.827%20207.091%20434.721%20206.863%20C%20422.615%20206.636%20412.557%20206.317%20412.33%20206.181%20C%20412.102%20206.044%20408.598%20205.725%20404.502%20205.498%20C%20391.395%20204.724%20391.395%20204.724%20390.712%20203.086%20C%20390.393%20202.267%20390.029%20199.718%20389.847%20197.397%20C%20389.665%20195.076%20389.347%20192.664%20389.119%20192.027%20C%20388.891%20191.435%20388.573%20189.159%20388.482%20186.975%20C%20388.3%20183.106%20388.618%20181.514%20389.938%20179.921%20C%20390.53%20179.193%20390.348%20179.056%20387.708%20178.328%20C%20385.615%20177.736%20384.704%20177.19%20384.158%20176.325%20C%20382.793%20173.959%20374.556%20169.226%20374.556%20170.819%20C%20374.556%20171.001%20373.054%20172.138%20371.233%20173.367%20C%20369.413%20174.596%20367.592%20176.098%20367.228%20176.78%20C%20366.682%20177.645%20365.772%20178.191%20363.724%20178.783%20L%20360.902%20179.557%20L%20361.903%20180.967%20C%20362.859%20182.196%20362.95%20182.833%20362.859%20186.747%20C%20362.814%20189.205%20362.586%20191.708%20362.313%20192.391%20C%20362.04%20193.073%20361.721%20195.531%20361.539%20197.852%20C%20361.357%20200.173%20360.948%20202.767%20360.629%20203.632%20L%20359.992%20205.134%20L%20345.565%20204.952%20C%20335.189%20204.861%20330.819%20204.633%20330.091%20204.224%20C%20328.862%20203.541%20328.362%20201.766%20327.725%20195.941%20C%20327.452%20193.574%20327.042%20190.206%20326.769%20188.477%20C%20326.223%20184.426%20326.496%20181.514%20327.679%20179.921%20L%20328.589%20178.646%20L%20325.768%20177.873%20C%20323.72%20177.281%20322.81%20176.735%20322.309%20175.87%20C%20321.49%20174.505%20315.664%20170.454%20314.481%20170.454%20C%20313.298%20170.454%20307.472%20174.505%20306.653%20175.87%20C%20306.152%20176.735%20305.242%20177.281%20303.194%20177.873%20L%20300.373%20178.646%20L%20301.283%20179.921%20C%20302.466%20181.514%20302.739%20184.426%20302.193%20188.477%20C%20301.92%20190.206%20301.51%20193.437%20301.237%20195.713%20C%20300.418%20202.585%20300.373%20202.813%20298.962%20204.178%20C%20297.961%20205.179%20297.323%20205.452%20296.595%20205.27%20C%20294.547%20204.679%20293.273%20203.313%20292.135%20200.446%20C%20290.542%20196.532%20290.178%20189.387%20290.315%20163.628%20C%20290.406%20142.42%20290.406%20142.465%20287.63%20124.488%20C%20285.399%20110.425%20280.12%2093.495%20274.113%2081.253%20C%20270.836%2074.654%20270.381%2073.971%20266.877%2070.558%20C%20261.552%2065.37%20259.822%2064.414%20255.954%2064.414%20C%20253.314%2064.414%20252.495%2064.641%20250.857%2065.688%20Z'/%3e%3c/svg%3e", On = "data:image/svg+xml,%3csvg%20version='1.0'%20width='682.667'%20height='682.667'%20viewBox='0%200%20512%20512'%20xmlns='http://www.w3.org/2000/svg'%3e%3cpath%20d='m253.8%2078.5-1.1%202.5h-22.4c-19.2%200-22.3.2-22.3%201.5s3.1%201.5%2021.5%201.5c11.8%200%2021.5.3%2021.5.7%200%20.5-.9%202.2-1.9%203.9-1.4%202.3-2.9%203.3-5.8%203.7-4.8.8-8.2%204.2-8.9%208.8-.4%203.3-3.4%2050-3.4%2054%200%201.9-1.3%201.9-49.7%201.9-56.1.1-172.4%203.9-176.1%205.9-5%202.7-5.2%203.9-5.2%2030.2v24.6l5.8.6c3.1.3%2036.9%205.1%2075%2010.7l69.3%2010h83.5l6.6%2057.2c3.6%2031.5%206.5%2057.4%206.4%2057.5-.1.1-13.4%202.5-29.6%205.2-37.5%206.4-36.4%206.1-38.4%2011.1-2.9%207.3-1.6%2025.4%202.2%2030%201.4%201.7%205.9%202.6%2029.2%206%2015.1%202.2%2028.3%204%2029.2%204%201.2%200%203.2-2.8%206.2-8.8%202.6-5.2%204.6-8.1%204.9-7.2.3.8%201.2%2010.5%202.1%2021.4.9%2011%202%2020.4%202.6%2021%202.2%202.2%203.1-1.7%204.6-21%20.9-10.9%201.8-20.6%202.1-21.4.3-.9%202.3%202%204.9%207.2%203%206%205%208.8%206.2%208.8.9%200%2014.1-1.8%2029.2-4%2023.3-3.4%2027.8-4.3%2029.2-6%203.8-4.6%205.1-22.7%202.2-30-2-5-.9-4.7-38.4-11.1-16.2-2.7-29.5-5.1-29.6-5.2-.1-.1%202.8-26%206.4-57.5l6.6-57.2h83.5l69.3-10c38.1-5.6%2071.9-10.4%2075.1-10.7l5.7-.6v-24.6c0-26.3-.2-27.5-5.2-30.2-3.7-2-120-5.8-176-5.9-48.5%200-49.8%200-49.8-1.9%200-4-3-50.7-3.4-54-.7-4.6-4.1-8-8.9-8.8-2.9-.4-4.4-1.4-5.8-3.7-1-1.7-1.9-3.4-1.9-3.9%200-.4%209.7-.7%2021.5-.7%2018.4%200%2021.5-.2%2021.5-1.5s-3.1-1.5-22.3-1.5h-22.4l-1.1-2.5c-.6-1.4-1.6-2.5-2.2-2.5-.6%200-1.6%201.1-2.2%202.5Z'/%3e%3c/svg%3e", Bn = "data:image/svg+xml,%3c?xml%20version='1.0'%20standalone='no'?%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%2020010904//EN'%20'http://www.w3.org/TR/2001/REC-SVG-20010904/DTD/svg10.dtd'%3e%3csvg%20version='1.0'%20xmlns='http://www.w3.org/2000/svg'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cg%20transform='translate(0.000000,512.000000)%20scale(0.100000,-0.100000)'%20fill='%23000000'%20stroke='none'%3e%3cpath%20d='M2508%205083%20c-64%20-68%20-104%20-159%20-145%20-328%20-15%20-62%20-17%20-171%20-22%20-959%20l-6%20-888%20-636%20-387%20c-350%20-212%20-646%20-393%20-657%20-401%20-19%20-13%20-25%20-36%20-42%20-145%20-11%20-71%20-20%20-133%20-20%20-137%200%20-20%2028%20-4%2053%2031%2029%2038%2030%2038%20470%20180%20l442%20141%20198%200%20197%200%200%20-351%20c0%20-331%20-1%20-351%20-17%20-343%20-31%2013%20-120%2016%20-143%204%20-31%20-17%20-42%20-54%20-42%20-150%200%20-68%208%20-114%2041%20-235%2022%20-82%2045%20-170%2052%20-195%20l11%20-45%2027%2031%2026%2030%2071%20-34%2071%20-35%2027%20-150%20c25%20-142%2026%20-150%209%20-163%20-10%20-7%20-142%20-100%20-294%20-206%20l-277%20-193%20-6%20-74%20c-4%20-40%20-5%20-76%20-1%20-79%203%20-3%20141%2038%20308%2090%20298%2095%20302%2096%20323%2077%2026%20-24%2042%20-24%2068%200%2021%2019%2025%2018%20323%20-77%20167%20-52%20305%20-93%20308%20-90%204%203%203%2039%20-1%2079%20l-6%2074%20-277%20193%20c-152%20106%20-284%20199%20-294%20206%20-17%2013%20-16%2021%209%20163%20l27%20150%2071%2035%2071%2034%2026%20-30%2027%20-31%2011%2045%20c7%2025%2030%20113%2052%20195%2057%20210%2057%20354%20-1%20385%20-23%2012%20-112%209%20-142%20-4%20-17%20-8%20-18%2012%20-18%20343%20l0%20351%20198%200%20197%200%20442%20-141%20c440%20-142%20441%20-142%20470%20-180%2025%20-35%2053%20-51%2053%20-31%200%204%20-9%2066%20-20%20137%20-17%20110%20-23%20132%20-42%20145%20-12%208%20-308%20189%20-657%20401%20l-636%20387%20-6%20888%20c-5%20788%20-7%20897%20-22%20959%20-26%20105%20-56%20195%20-84%20245%20-32%2056%20-92%20120%20-113%20120%20-9%200%20-32%20-17%20-52%20-37z'/%3e%3c/g%3e%3c/svg%3e", Zn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20version='1.0'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%20xmlns='http://www.w3.org/2000/svg'%3e%3cpath%20d='M%208.407%20176.994%20C%2010.999%20173.366%2022.401%20171.293%2072.415%20165.851%20C%20105.845%20162.482%20139.533%20158.336%20146.789%20156.781%20L%20160.524%20154.449%20L%20160.783%20142.269%20C%20160.783%20135.532%20162.337%20125.424%20164.152%20119.723%20C%20166.484%20111.431%20168.298%20109.358%20173.481%20109.358%20C%20178.664%20109.358%20180.478%20111.431%20182.81%20119.723%20C%20184.624%20125.424%20186.179%20135.532%20186.179%20142.269%20L%20186.438%20154.708%20L%20204.578%20154.708%20L%20222.717%20154.708%20L%20222.717%20102.88%20C%20222.717%2053.383%20222.977%2050.274%20229.196%2034.725%20C%20237.748%2013.994%20247.077%200%20252.519%200%20C%20257.961%200%20267.291%2013.994%20275.842%2034.725%20C%20282.062%2050.274%20282.321%2053.383%20282.321%20102.88%20L%20282.321%20154.708%20L%20299.165%20154.708%20L%20316.009%20154.708%20L%20316.268%20142.269%20C%20316.268%20135.532%20317.823%20125.424%20319.637%20119.723%20C%20321.97%20111.431%20323.783%20109.358%20328.967%20109.358%20C%20334.149%20109.358%20335.963%20111.431%20338.295%20119.723%20C%20340.109%20125.424%20341.665%20136.05%20341.665%20143.306%20L%20341.924%20156.522%20L%20363.173%20158.336%20C%20375.094%20159.372%20409.042%20163.001%20439.102%20166.369%20C%20497.927%20173.107%20500%20173.884%20500%20188.915%20C%20500%20194.097%20498.186%20196.948%20494.299%20198.762%20C%20487.821%20201.353%20431.846%20206.018%20376.39%20208.35%20L%20341.924%20209.905%20L%20341.924%20216.643%20C%20341.924%20226.749%20337.258%20241.78%20332.853%20245.408%20C%20326.893%20250.332%20319.119%20239.965%20317.045%20224.677%20L%20315.232%20211.72%20L%20299.165%20211.72%20L%20283.098%20211.72%20L%20281.544%20264.066%20C%20279.729%20313.303%20275.324%20355.025%20269.622%20374.201%20C%20266.772%20384.308%20266.772%20384.308%20298.646%20388.195%20C%20326.116%20391.824%20328.707%20393.896%20328.967%20412.037%20C%20328.967%20422.92%20328.189%20424.735%20322.229%20426.808%20C%20313.677%20430.176%20191.362%20430.176%20182.81%20426.808%20C%20176.849%20424.735%20176.072%20422.92%20176.072%20411.259%20C%20176.072%20394.674%20180.219%20392.083%20210.797%20387.936%20C%20223.754%20386.382%20234.898%20384.567%20235.934%20383.79%20C%20236.97%20383.272%20236.711%20378.866%20235.416%20374.201%20C%20229.714%20355.025%20225.309%20313.303%20223.495%20264.066%20L%20221.94%20211.72%20L%20204.578%20211.72%20L%20187.216%20211.72%20L%20185.401%20224.418%20C%20184.365%20231.414%20181.514%20239.706%20178.923%20243.076%20L%20174.259%20248.777%20L%20168.816%20243.335%20C%20165.447%20239.965%20162.596%20232.969%20161.56%20224.936%20C%20159.487%20209.905%20160.783%20210.424%20120.357%20208.091%20C%2061.272%20204.723%2016.181%20200.835%2010.739%20198.762%20C%204.261%20195.912%202.965%20184.51%208.407%20176.994%20Z'/%3e%3c/svg%3e", In = "data:image/svg+xml,%3c?xml%20version='1.0'%20standalone='no'?%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%2020010904//EN'%20'http://www.w3.org/TR/2001/REC-SVG-20010904/DTD/svg10.dtd'%3e%3csvg%20version='1.0'%20xmlns='http://www.w3.org/2000/svg'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cg%20transform='translate(0.000000,512.000000)%20scale(0.100000,-0.100000)'%20fill='%23000000'%20stroke='none'%3e%3cpath%20d='M2518%205102%20c-61%20-40%20-106%20-129%20-139%20-279%20-22%20-95%20-23%20-120%20-28%20-917%20l-6%20-820%20-125%20-48%20c-69%20-26%20-141%20-54%20-161%20-62%20l-36%20-15%2014%2052%20c18%2064%2013%20220%20-8%20262%20-21%2043%20-52%2055%20-154%2055%20-102%200%20-130%20-11%20-158%20-57%20-45%20-77%20-27%20-281%2029%20-331%2014%20-12%2026%20-34%2027%20-48%202%20-26%20-10%20-31%20-692%20-295%20-764%20-295%20-743%20-284%20-781%20-387%20-21%20-56%20-42%20-210%20-29%20-217%203%20-3%2018%2018%2033%2045%2039%2072%2072%2096%20145%20103%2069%207%20493%2091%20992%20196%20l337%2071%20270%200%20270%200%2011%20-31%20c8%20-22%2011%20-231%2011%20-663%200%20-619%201%20-634%2025%20-801%2013%20-94%2031%20-193%2039%20-219%208%20-27%2013%20-53%2010%20-58%20-6%20-10%20-130%20-101%20-401%20-296%20l-211%20-152%20-16%20-67%20c-8%20-37%20-14%20-68%20-13%20-69%201%20-2%20158%2036%20349%2084%20191%2048%20349%2084%20352%2082%202%20-3%2013%20-52%2023%20-110%20l19%20-105%2044%200%2044%200%2019%20105%20c10%2058%2021%20107%2023%20110%203%202%20161%20-34%20352%20-82%20191%20-48%20348%20-86%20349%20-84%201%201%20-5%2032%20-13%2069%20l-16%2067%20-211%20152%20c-271%20195%20-395%20286%20-401%20296%20-3%205%202%2031%2010%2058%208%2026%2026%20125%2039%20219%2024%20167%2025%20182%2025%20801%200%20432%203%20641%2011%20663%20l11%2031%20269%200%20270%200%20457%20-94%20c251%20-52%20556%20-113%20677%20-136%20121%20-23%20230%20-47%20243%20-53%2012%20-6%2046%20-39%2074%20-73%2029%20-34%2055%20-61%2058%20-59%2012%207%20-10%20163%20-30%20217%20-38%20103%20-17%2092%20-781%20387%20-682%20264%20-694%20269%20-692%20295%201%2014%2013%2036%2027%2048%2056%2050%2074%20254%2029%20331%20-28%2046%20-56%2057%20-158%2057%20-102%200%20-133%20-12%20-154%20-55%20-21%20-42%20-26%20-198%20-8%20-262%20l14%20-52%20-36%2015%20c-20%208%20-92%2036%20-161%2062%20l-125%2048%20-6%20820%20c-5%20797%20-6%20822%20-28%20917%20-23%20104%20-57%20191%20-91%20235%20-25%2032%20-69%2062%20-90%2062%20-8%200%20-27%20-8%20-42%20-18z'/%3e%3c/g%3e%3c/svg%3e", Rn = "data:image/svg+xml,%3c?xml%20version='1.0'%20standalone='no'?%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%2020010904//EN'%20'http://www.w3.org/TR/2001/REC-SVG-20010904/DTD/svg10.dtd'%3e%3csvg%20version='1.0'%20xmlns='http://www.w3.org/2000/svg'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cg%20transform='translate(0.000000,512.000000)%20scale(0.100000,-0.100000)'%20fill='%23000000'%20stroke='none'%3e%3cpath%20d='M2509%205083%20c-90%20-104%20-148%20-333%20-169%20-673%20-5%20-91%20-10%20-475%20-10%20-855%20l0%20-690%20-778%20-364%20c-600%20-280%20-784%20-371%20-810%20-397%20-31%20-33%20-32%20-36%20-32%20-124%200%20-53%204%20-90%2010%20-90%205%200%20126%2027%20267%2060%20142%2033%20393%2083%20558%20111%20l300%2051%20175%20-11%20c96%20-6%20203%20-14%20238%20-17%20l62%20-6%200%20-254%20c0%20-140%20-3%20-254%20-7%20-254%20-5%201%20-17%2015%20-28%2032%20-23%2037%20-47%2046%20-133%2047%20-54%201%20-66%20-2%20-87%20-24%20l-25%20-24%200%20-272%20c0%20-278%207%20-346%2046%20-421%2014%20-26%2019%20-28%2079%20-28%20l65%200%2028%2049%20c27%2048%2028%2049%2054%2035%2022%20-12%2028%20-23%2033%20-71%2013%20-109%2046%20-266%2070%20-329%20l24%20-63%20-52%20-19%20c-29%20-11%20-142%20-50%20-252%20-87%20-132%20-45%20-209%20-77%20-227%20-93%20-31%20-29%20-53%20-91%20-63%20-175%20l-6%20-57%2033%200%20c18%200%20157%2020%20308%2045%20151%2025%20279%2044%20286%2041%206%20-2%2017%20-24%2023%20-49%2018%20-64%2047%20-107%2071%20-107%2024%200%2053%2043%2071%20107%206%2025%2017%2047%2023%2049%207%203%20135%20-16%20286%20-41%20151%20-25%20290%20-45%20308%20-45%20l33%200%20-6%2057%20c-10%2084%20-32%20146%20-63%20175%20-18%2017%20-103%2050%20-257%20103%20-126%2043%20-240%2082%20-252%2086%20l-22%208%2024%2064%20c24%2064%2057%20220%2070%20330%205%2048%2011%2059%2033%2071%2026%2014%2027%2013%2054%20-35%20l28%20-49%2065%200%20c61%200%2065%201%2080%2030%2036%2070%2045%20148%2045%20423%20l0%20268%20-25%2024%20c-21%2022%20-33%2025%20-87%2024%20-86%20-1%20-110%20-10%20-133%20-47%20-11%20-17%20-23%20-31%20-27%20-32%20-5%200%20-8%20114%20-8%20254%20l0%20254%2063%206%20c34%203%20141%2011%20237%2017%20l175%2011%20300%20-51%20c165%20-28%20416%20-78%20558%20-111%20141%20-33%20262%20-60%20267%20-60%206%200%2010%2037%2010%2090%200%2088%20-1%2091%20-32%20124%20-26%2026%20-210%20117%20-810%20397%20l-778%20364%200%20690%20c0%20861%20-11%201056%20-70%201288%20-37%20144%20-114%20277%20-160%20277%20-10%200%20-33%20-17%20-51%20-37z'/%3e%3c/g%3e%3c/svg%3e", Dn = "data:image/svg+xml,%3c?xml%20version='1.0'%20standalone='no'?%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%2020010904//EN'%20'http://www.w3.org/TR/2001/REC-SVG-20010904/DTD/svg10.dtd'%3e%3csvg%20version='1.0'%20xmlns='http://www.w3.org/2000/svg'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cg%20transform='translate(0.000000,512.000000)%20scale(0.100000,-0.100000)'%20fill='%23000000'%20stroke='none'%3e%3cpath%20d='M2495%205079%20c-59%20-54%20-99%20-120%20-135%20-218%20-62%20-171%20-63%20-183%20-69%20-1005%20l-6%20-749%20-190%20-84%20c-377%20-168%20-772%20-317%20-1200%20-454%20-233%20-74%20-328%20-156%20-337%20-290%20l-3%20-44%2090%20-3%20c92%20-3%20465%2012%201173%2049%20221%2012%20409%2018%20417%2015%208%20-3%2015%20-16%2015%20-29%200%20-12%209%20-40%2020%20-62%2018%20-35%2020%20-58%2019%20-200%20-1%20-139%20-2%20-155%20-12%20-118%20-21%2074%20-37%2083%20-143%2083%20-130%200%20-124%2016%20-124%20-338%200%20-296%207%20-350%2051%20-406%2017%20-22%2028%20-26%2069%20-26%2038%200%2051%20-4%2063%20-23%2013%20-20%2017%20-21%2038%20-8%2019%2011%2028%2010%2069%20-9%20l47%20-22%2046%20-271%20c26%20-149%2045%20-271%2044%20-272%20-1%20-1%20-136%20-82%20-301%20-181%20-164%20-98%20-305%20-189%20-314%20-202%20-20%20-31%20-39%20-189%20-24%20-204%209%20-9%2091%205%20324%2052%20171%2034%20319%2065%20328%2067%2012%203%2029%20-12%2054%20-46%2022%20-29%2046%20-51%2056%20-51%2010%200%2034%2022%2056%2051%2029%2039%2041%2049%2058%2045%20136%20-32%20631%20-126%20643%20-122%2013%205%2014%2018%208%2084%20-11%20131%20-2%20122%20-335%20323%20-162%2098%20-298%20180%20-302%20181%20-4%202%2013%20125%2039%20275%20l46%20271%2047%2022%20c41%2019%2050%2020%2069%209%2021%20-13%2025%20-12%2038%208%2012%2019%2025%2023%2063%2023%2041%200%2052%204%2069%2026%2044%2056%2051%20110%2051%20406%200%20354%206%20338%20-124%20338%20-106%200%20-117%20-6%20-140%20-81%20-13%20-42%20-14%20-35%20-15%20116%20-1%20142%201%20165%2019%20200%2011%2022%2020%2050%2020%2062%200%2013%207%2026%2015%2029%208%203%20196%20-3%20417%20-15%20708%20-37%201081%20-52%201173%20-49%20l90%203%20-3%2045%20c-7%2096%20-62%20169%20-166%20222%20-33%2016%20-199%2076%20-370%20133%20-324%20107%20-678%20244%20-1001%20388%20l-190%2084%20-6%20749%20c-4%20647%20-7%20761%20-22%20836%20-21%20108%20-67%20235%20-107%20298%20-39%2061%20-113%20130%20-140%20130%20-11%200%20-40%20-18%20-65%20-41z'/%3e%3c/g%3e%3c/svg%3e", Nn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.286'%20height='14.3'/%3e%3c/defs%3e%3cellipse%20cx='100'%20cy='65.49'%20rx='50'%20ry='51.19'/%3e%3crect%20x='71.43'%20y='143'%20width='57.144'%20height='42.9'/%3e%3cpath%20style='fill:%20rgb(216,%20216,%20216);%20stroke:%20rgb(0,%200,%200);%20stroke-width:%208px;'%20d='M%20123.755%20148.558%20L%20144.86%2064.556'/%3e%3cpath%20style='fill:%20rgb(216,%20216,%20216);%20stroke:%20rgb(0,%200,%200);%20stroke-width:%208px;'%20d='M%2055.144%2064.298%20L%2076.249%20148.3'%20transform='matrix(-1,%200,%200,%20-1,%20131.393002,%20212.598)'/%3e%3c/svg%3e", Fn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.286'%20height='14.3'/%3e%3c/defs%3e%3cg%20transform='matrix(1.068976,%200,%200,%201.185981,%2014.763699,%2014.145811)'%3e%3cpath%20style='stroke:%20rgb(0,%200,%200);%20stroke-width:%200px;'%20d='M%2078.309%2090.777%20C%2078.309%20117.993%2038.222%2077.892%2065.438%2077.892%20C%2092.653%2077.892%2093.993%2019.209%20117.005%2021.49%20C%20126.034%2022.383%20135.347%2031.716%20136.242%2040.744%20C%20138.521%2063.766%2078.309%2063.561%2078.309%2090.777%20Z'%20transform='matrix(0.707107,%20-0.707107,%200.707107,%200.707107,%20-14.860118,%2085.796529)'/%3e%3cpath%20style='stroke:%20rgb(0,%200,%200);%20stroke-width:%200px;'%20d='M%20112.354%20144.442%20C%20112.354%20171.658%2072.267%20131.557%2099.483%20131.557%20C%20126.698%20131.557%20128.038%2072.874%20151.05%2075.155%20C%20160.079%2076.048%20169.392%2085.381%20170.287%2094.409%20C%20172.566%20117.431%20112.354%20117.226%20112.354%20144.442%20Z'%20transform='matrix(0.258819,%200.965926,%20-0.965926,%200.258819,%20207.086794,%20-40.878752)'/%3e%3cpath%20style='stroke:%20rgb(0,%200,%200);%20stroke-width:%200px;'%20d='M%2041.461%20143.906%20C%2041.461%20171.122%201.374%20131.021%2028.59%20131.021%20C%2055.805%20131.021%2057.145%2072.338%2080.157%2074.619%20C%2089.186%2075.512%2098.499%2084.845%2099.394%2093.873%20C%20101.673%20116.895%2041.461%20116.69%2041.461%20143.906%20Z'%20transform='matrix(-0.965926,%20-0.258819,%200.258819,%20-0.965926,%2087.058543,%20239.391501)'/%3e%3c/g%3e%3c/svg%3e", Wn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20viewBox='0%200%20200%20200'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cdefs%3e%3cbx:grid%20x='0'%20y='0'%20width='14.408'%20height='14.408'/%3e%3c/defs%3e%3cpath%20style='stroke:%20rgb(0,%200,%200);%20stroke-width:%200px;'%20d='M%20158.488%2043.224%20L%20187.304%2072.04%20L%20201.712%20115.264%20L%20187.304%20158.488%20L%20158.488%20187.304%20L%20115.264%20201.712%20L%2072.04%20187.304%20L%2043.224%20158.488%20L%2028.816%20115.264%20L%2043.224%2072.04%20L%2072.04%2043.224%20L%20115.264%2028.816%20Z%20M%20115.724%2055.012%20C%20115.057%2055.012%20114.664%2055.481%20113.324%2059.258%20C%20113.164%2059.702%20112.848%2061.595%20112.848%2061.595%20C%20112.848%2061.595%20111.781%2061.651%20111.5%2061.643%20C%20110.969%2061.643%20110.243%2062.143%20109.88%2062.737%20C%20108.76%2064.599%20107.004%2074.825%20107.004%2079.547%20C%20107.006%2082.473%20106.761%2084.224%20106.322%2084.583%20C%20105.945%2084.881%20103.984%2085.796%20101.964%2086.616%20C%2098.39%2088.071%2097.506%2088.156%2071.705%2089.572%20C%2051.279%2090.706%2044.845%2091.237%2043.892%2091.863%20C%2041.227%2093.614%2041.085%2097.656%2043.421%20105.295%20L%2044.632%20109.236%20L%20107.518%20119.988%20L%20112.801%20159.925%20L%20111.402%20160.325%20C%20110.63%20160.543%20105.476%20161.13%2099.935%20161.631%20C%2094.395%20162.131%2089.552%20162.803%2089.165%20163.124%20C%2087.629%20164.445%2088.841%20174.844%2090.536%20174.844%20C%2093.199%20174.845%20103.084%20176.221%20103.599%20176.658%20C%20103.969%20176.98%20112.454%20178.161%20115.723%20178.16%20C%20119%20178.151%20127.485%20176.972%20127.855%20176.665%20C%20128.376%20176.221%20138.254%20174.853%20140.926%20174.845%20C%20142.614%20174.853%20143.825%20164.454%20142.281%20163.125%20C%20141.903%20162.795%20137.059%20162.124%20131.526%20161.631%20C%20125.978%20161.138%20120.825%20160.551%20120.052%20160.333%20L%20118.659%20159.925%20L%20123.942%20119.988%20L%20186.814%20109.236%20L%20188.025%20105.296%20C%20190.371%2097.649%20190.234%2093.615%20187.571%2091.863%20C%20186.601%2091.237%20180.175%2090.697%20159.748%2089.581%20C%20133.955%2088.155%20133.055%2088.07%20129.49%2086.625%20C%20127.47%2085.803%20125.501%2084.882%20125.131%2084.576%20C%20124.684%2084.223%20124.451%2082.465%20124.45%2079.54%20C%20124.45%2074.832%20122.7%2064.598%20121.567%2062.737%20C%20121.211%2062.136%20120.483%2061.65%20119.954%2061.649%20C%20119.674%2061.642%20118.63%2061.596%20118.63%2061.596%20C%20118.63%2061.596%20118.304%2059.695%20118.145%2059.251%20C%20116.806%2055.489%20116.388%2055.013%20115.724%2055.012%20Z'/%3e%3c/svg%3e", Hn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20version='1.0'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%20xmlns='http://www.w3.org/2000/svg'%3e%3cpath%20d='M%20251.774%200.568%20C%20247.737%201.991%20246.223%204.881%20240.901%2020.937%20C%20236.726%2033.599%20234.203%2043.371%20231.909%2056.078%20L%20230.258%2065.254%20L%20229.891%2088.65%20C%20229.432%20115.809%20228.652%20149.023%20228.285%20155.629%20L%20228.01%20160.217%20L%20225.854%20162.419%20C%20224.661%20163.658%20222.275%20165.722%20220.486%20167.052%20C%20217%20169.667%20191.034%20187.834%20171.628%20201.276%20C%20164.93%20205.909%20156.168%20212.011%20152.131%20214.809%20C%20144.607%20220.085%20105.062%20247.427%2093.18%20255.547%20C%2089.51%20258.07%2081.253%20263.759%2074.83%20268.209%20C%2068.407%20272.705%2055.195%20281.834%2045.469%20288.532%20L%2027.807%20300.735%20L%2022.394%20306.745%20C%2016.43%20313.305%2015.054%20315.737%2011.062%20326.517%20C%209.503%20330.738%209.09%20332.435%209.044%20335.004%20C%208.998%20337.757%209.09%20338.216%209.87%20338.353%20C%2010.328%20338.445%2011.567%20337.94%2012.622%20337.206%20C%2013.631%20336.472%2015.833%20335.096%2017.485%20334.225%20C%2023.724%20330.784%2043.818%20320.875%2047.442%20319.452%20C%2053.039%20317.205%2081.069%20305.552%2097.768%20298.533%20C%20102.677%20296.469%20111.072%20292.982%20116.348%20290.734%20C%20134.515%20283.119%20143.415%20279.357%20152.131%20275.595%20C%20162.086%20271.283%20162.683%20271.099%20171.399%20269.172%20C%20199.246%20262.979%20216.22%20259.86%20221.037%20260.089%20L%20224.156%20260.227%20L%20224.982%20263.897%20C%20225.533%20266.374%20225.854%20269.815%20225.9%20274.448%20L%20225.991%20281.33%20L%20219.706%20281.559%20C%20213.513%20281.788%20213.421%20281.788%20211.953%20283.165%20C%20211.173%20283.899%20209.889%20285.458%20209.201%20286.559%20L%20207.87%20288.624%20L%20207.87%20298.028%20C%20207.87%20321.609%20210.99%20339.821%20215.761%20344.455%20C%20217.642%20346.244%20220.716%20347.987%20224.294%20349.272%20C%20226.45%20350.052%20229.111%20351.474%20231.038%20352.942%20C%20235.717%20356.383%20235.809%20356.52%20234.02%20358.034%20C%20231.175%20360.466%20211.219%20375.651%20190.346%20391.294%20L%20169.38%20407.03%20L%20169.335%20419.049%20C%20169.335%20425.793%20169.518%20431.573%20169.793%20432.216%20C%20170.16%20433.271%20170.481%20433.408%20172.317%20433.408%20C%20173.693%20433.408%20174.84%20433.087%20175.665%20432.491%20C%20178.464%20430.427%20224.065%20411.893%20237.506%20407.305%20C%20243.654%20405.241%20243.378%20405.149%20244.938%20410.149%20C%20247.003%20416.664%20247.37%20417.214%20249.939%20417.673%20C%20252.829%20418.132%20260.261%20418.132%20263.059%20417.673%20C%20265.628%20417.214%20265.995%20416.618%20268.06%20410.149%20C%20269.62%20405.149%20269.344%20405.241%20275.492%20407.305%20C%20288.933%20411.893%20334.534%20430.427%20337.332%20432.491%20C%20338.158%20433.087%20339.305%20433.408%20340.681%20433.408%20C%20342.516%20433.408%20342.838%20433.271%20343.205%20432.216%20C%20343.48%20431.573%20343.663%20425.793%20343.663%20419.049%20L%20343.618%20407.03%20L%20322.652%20391.294%20C%20301.779%20375.651%20281.823%20360.466%20278.978%20358.034%20C%20277.189%20356.52%20277.281%20356.383%20281.96%20352.942%20C%20283.887%20351.474%20286.548%20350.052%20288.704%20349.272%20C%20292.282%20347.987%20295.356%20346.244%20297.237%20344.455%20C%20302.008%20339.821%20305.128%20321.609%20305.128%20298.028%20L%20305.128%20288.624%20L%20303.797%20286.559%20C%20303.109%20285.458%20301.824%20283.899%20301.045%20283.165%20C%20299.577%20281.788%20299.485%20281.788%20293.292%20281.559%20L%20287.007%20281.33%20L%20287.098%20274.448%20C%20287.144%20269.815%20287.465%20266.374%20288.016%20263.897%20L%20288.842%20260.227%20L%20291.961%20260.089%20C%20296.778%20259.86%20313.752%20262.979%20341.599%20269.172%20C%20350.315%20271.099%20350.912%20271.283%20360.867%20275.595%20C%20369.583%20279.357%20378.483%20283.119%20396.65%20290.734%20C%20401.972%20292.982%20410.321%20296.469%20415.23%20298.533%20C%20431.929%20305.552%20459.959%20317.205%20465.556%20319.452%20C%20469.18%20320.875%20489.274%20330.784%20495.513%20334.225%20C%20497.164%20335.096%20499.367%20336.472%20500.376%20337.206%20C%20501.431%20337.94%20502.67%20338.445%20503.128%20338.353%20C%20503.908%20338.216%20504%20337.757%20503.954%20335.004%20C%20503.908%20332.435%20503.495%20330.738%20501.936%20326.517%20C%20497.944%20315.737%20496.568%20313.305%20490.604%20306.745%20L%20485.191%20300.735%20L%20467.529%20288.532%20C%20457.803%20281.834%20444.591%20272.705%20438.168%20268.209%20C%20431.745%20263.759%20423.488%20258.07%20419.818%20255.547%20C%20407.936%20247.427%20368.391%20220.085%20360.867%20214.809%20C%20356.83%20212.011%20348.067%20205.909%20341.37%20201.276%20C%20321.964%20187.834%20295.998%20169.667%20292.512%20167.052%20C%20290.768%20165.722%20288.337%20163.658%20287.144%20162.419%20L%20284.988%20160.217%20L%20284.713%20155.629%20C%20284.346%20149.023%20283.566%20115.809%20283.107%2088.65%20L%20282.74%2065.254%20L%20281.089%2056.078%20C%20278.795%2043.371%20276.272%2033.599%20272.097%2020.937%20C%20266.683%204.514%20265.032%201.532%20260.72%200.339%20C%20258.38%20-0.303%20253.884%20-0.212%20251.774%200.568%20Z'/%3e%3c/svg%3e", Gn = "data:image/svg+xml,%3c?xml%20version='1.0'%20encoding='utf-8'?%3e%3csvg%20version='1.0'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%20xmlns='http://www.w3.org/2000/svg'%20xmlns:bx='https://boxy-svg.com'%3e%3cpath%20d='M%20248.804%205.22%20C%20247.991%206.278%20241.897%2019.632%20241.897%2019.632%20C%20241.897%2019.632%20238.755%2029.274%20238.485%2029.853%20C%20237.782%2031.345%20237.886%2032.073%20237.387%2034.375%20C%20237.136%2035.534%20236.805%2037.565%20236.333%2039.864%20C%20235.806%2042.435%20235.635%2044.694%20235.016%2049.48%20C%20234.385%2054.362%20234.433%2055.656%20233.875%2057.427%20C%20233.396%2058.945%20232.996%2060.457%20232.777%2070.205%20C%20232.469%2083.422%20232.469%2080.128%20232.821%20110.996%20C%20232.952%20124.652%20232.952%20137.474%20232.777%20139.449%20C%20232.469%20143.05%20232.426%20143.138%20231.064%20143.709%20C%20230.318%20144.06%2076.197%20242.065%2073.826%20243.777%20C%2071.236%20245.709%2068.338%20248.519%2067.723%20249.749%20C%2067.372%20250.364%2067.02%20252.34%2066.845%20254.14%20C%2066.449%20258.662%2060.697%20275.48%2060.61%20280.046%20C%2060.566%20285.491%2061.005%20287.203%2062.629%20287.73%20C%2063.815%20288.081%2064.605%20287.555%2069.304%20283.251%20C%2073.738%20279.212%2076.329%20277.719%2079.973%20277.016%20C%2081.422%20276.709%20204.763%20244.304%20207.397%20244.129%20L%20211.042%20243.909%20L%20211.086%20250.012%20C%20211.174%20268.366%20211.437%20266.841%20212.096%20277.104%20C%20212.206%20278.83%20213.106%20282.768%20213.501%20296.951%20C%20213.676%20302.264%20213.984%20306.962%20214.159%20307.357%20C%20214.95%20308.85%20216.091%20309.07%20219.033%20308.323%20C%20222.59%20307.401%20227.288%20307.665%20229.747%20308.938%20C%20232.118%20310.124%20234.753%20312.934%20236.904%20316.534%20C%20238.88%20319.828%20241.602%20336.777%20241.822%20341.036%20C%20241.998%20343.846%20245.554%20361.717%20245.423%20362.156%20C%20245.159%20363.122%20241.602%20366.547%20240.856%20366.547%20C%20240.197%20366.547%20197.21%20396.537%20196.376%20397.02%20C%20193.434%20398.82%20187.419%20404.879%20187.111%20407.163%20C%20186.936%20408.348%20186.804%20409.665%20186.848%20410.017%20C%20186.848%20410.368%20186.892%20426.087%20187.331%20427.141%20C%20188.473%20429.864%20190.844%20429.995%20195.893%20427.756%20C%20197.825%20426.878%20244.105%20410.368%20246.169%20409.622%20C%20248.233%20408.875%20250.56%20408.26%20251.35%20408.26%20C%20252.711%20408.26%20252.755%20408.304%20252.755%20410.236%20C%20252.755%20412.739%20253.941%20414.1%20255.917%20413.881%20C%20257.366%20413.749%20257.673%20413.266%20258.244%20409.841%20L%20258.507%20408.129%20L%20260.966%20408.392%20C%20262.328%20408.524%20263.645%20408.875%20263.864%20409.095%20C%20264.128%20409.358%20313.306%20427.141%20313.657%20427.141%20C%20313.965%20427.141%20321.166%20429.644%20322.615%20428.502%20C%20323.625%20427.712%20323.625%20414.583%20323.8%20412.871%20C%20324.239%20408.831%20323.581%20405.187%20322%20403.211%20C%20321.297%20402.333%20317.477%20398.908%20314.404%20397.02%20C%20313.569%20396.537%20270.89%20366.766%20269.616%20366.371%20C%20269.133%20366.239%20265.313%20362.595%20265.313%20362.595%20C%20265.313%20362.595%20268.475%20348.456%20269.046%20342.221%20C%20269.265%20339.279%20272.471%20320.179%20273.7%20317.764%20C%20275.412%20314.339%20279.364%20310.08%20281.955%20308.85%20C%20284.458%20307.665%20289.463%20307.401%20292.273%20308.323%20C%20294.952%20309.202%20295.962%20308.982%20296.62%20307.357%20C%20296.884%20306.699%20297.411%20300.025%20297.762%20292.516%20C%20298.157%20285.008%20298.64%20278.202%20298.904%20277.368%20C%20299.167%20276.533%20299.475%20268.849%20299.606%20259.892%20L%20299.87%20243.953%20L%20303.646%20244.172%20C%20306.324%20244.304%20428.347%20276.402%20430.192%20276.709%20C%20435.241%20277.631%20437.92%20279.124%20441.871%20283.164%20C%20446.965%20288.389%20448.633%20289.047%20449.775%20286.325%20C%20450.785%20283.866%20450.39%20275.655%20449.16%20273.24%20C%20447.316%20269.596%20445.516%20263.361%20444.638%20257.433%20C%20444.111%20254.052%20443.628%20250.934%20443.452%20250.451%20C%20443.101%20249.266%20440.335%20246.324%20437.612%20244.304%20C%20434.758%20242.197%20280.55%20144.06%20279.935%20143.84%20C%20278.881%20143.489%20278.75%20143.138%20278.486%20140.635%20C%20278.354%20139.098%20278.223%20122.149%20278.267%20103.005%20C%20278.31%2067.131%20278.135%2060.501%20277.125%2057.867%20C%20276.818%2056.988%20276.335%2053.52%20276.027%2050.182%20C%20275.764%2046.801%20275.193%2042.454%20274.71%2040.522%20C%20274.271%2038.59%20273.744%2036%20273.612%2034.814%20C%20273.437%2033.585%20272.822%2031.126%20272.251%2029.326%20C%20271.68%2027.525%20270.407%2023.53%20269.441%2020.5%20C%20267.158%2013.343%20265.313%209.611%20262.02%205.307%20L%20259.342%201.795%20L%20255.29%200%20L%20251.438%201.795%20L%20248.804%205.22%20Z'%20bx:origin='0.479804%200.5'/%3e%3c/svg%3e", Un = "data:image/svg+xml,%3c?xml%20version='1.0'%20standalone='no'?%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%2020010904//EN'%20'http://www.w3.org/TR/2001/REC-SVG-20010904/DTD/svg10.dtd'%3e%3csvg%20version='1.0'%20xmlns='http://www.w3.org/2000/svg'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cg%20transform='translate(0.000000,512.000000)%20scale(0.100000,-0.100000)'%20fill='%23000000'%20stroke='none'%3e%3cpath%20d='M2522%205078%20c-74%20-107%20-161%20-318%20-206%20-493%20-59%20-236%20-59%20-236%20-63%20-950%20-2%20-363%20-7%20-663%20-11%20-667%20-4%20-4%20-403%20-131%20-887%20-283%20-484%20-152%20-902%20-283%20-929%20-292%20-43%20-14%20-50%20-20%20-71%20-67%20-12%20-28%20-37%20-114%20-54%20-191%20-17%20-77%20-42%20-182%20-55%20-232%20-13%20-51%20-20%20-93%20-16%20-93%205%200%2037%2034%2072%2075%2052%2062%2068%2075%2093%2075%2017%200%20374%2016%20795%2035%20421%2019%20783%2035%20806%2035%20l41%200%206%20-172%20c7%20-179%2020%20-286%2039%20-321%2010%20-19%2017%20-19%20144%20-13%20l133%207%205%20-23%20c3%20-13%2026%20-202%2052%20-422%2038%20-331%2044%20-400%2033%20-407%20-8%20-5%20-148%20-86%20-312%20-181%20-164%20-95%20-309%20-184%20-323%20-197%20-13%20-13%20-28%20-40%20-34%20-60%20-11%20-39%20-14%20-241%20-4%20-241%204%200%20169%2034%20367%2076%20198%2041%20365%2073%20372%2070%207%20-2%2016%20-22%2019%20-43%204%20-21%2011%20-45%2016%20-53%209%20-13%2011%20-13%2020%200%205%208%2012%2032%2016%2053%203%2021%2012%2041%2019%2043%207%203%20174%20-29%20372%20-70%20198%20-42%20363%20-76%20367%20-76%2011%200%206%20201%20-6%20242%20-6%2021%20-21%2049%20-34%2061%20-12%2012%20-157%20100%20-321%20195%20-164%2095%20-304%20176%20-312%20181%20-11%207%20-5%2076%2033%20407%2026%20220%2049%20409%2052%20422%20l5%2023%20133%20-7%20c127%20-6%20134%20-6%20144%2013%2019%2035%2032%20142%2039%20321%20l6%20172%2041%200%20c23%200%20385%20-16%20806%20-35%20421%20-19%20778%20-35%20795%20-35%2025%200%2041%20-13%2093%20-75%2035%20-41%2067%20-75%2072%20-75%204%200%20-3%2042%20-16%2093%20-13%2050%20-38%20155%20-55%20232%20-17%2077%20-42%20163%20-54%20191%20-21%2047%20-28%2053%20-71%2067%20-27%209%20-445%20140%20-929%20292%20-484%20152%20-883%20279%20-887%20283%20-4%204%20-9%20304%20-11%20667%20-3%20571%20-6%20673%20-21%20755%20-39%20216%20-99%20407%20-179%20565%20-45%2090%20-93%20165%20-107%20165%20-5%200%20-22%20-19%20-38%20-42z'/%3e%3c/g%3e%3c/svg%3e", Vn = "data:image/svg+xml,%3c?xml%20version='1.0'%20standalone='no'?%3e%3c!DOCTYPE%20svg%20PUBLIC%20'-//W3C//DTD%20SVG%2020010904//EN'%20'http://www.w3.org/TR/2001/REC-SVG-20010904/DTD/svg10.dtd'%3e%3csvg%20version='1.0'%20xmlns='http://www.w3.org/2000/svg'%20width='512.000000pt'%20height='512.000000pt'%20viewBox='0%200%20512.000000%20512.000000'%20preserveAspectRatio='xMidYMid%20meet'%3e%3cg%20transform='translate(0.000000,512.000000)%20scale(0.100000,-0.100000)'%20fill='%23000000'%20stroke='none'%3e%3cpath%20d='M2482%205059%20c-68%20-68%20-120%20-159%20-154%20-269%20-22%20-75%20-22%20-78%20-28%20-848%20l-5%20-773%20-125%20-99%20-125%20-99%20-2%2087%20c-5%20143%207%20132%20-136%20132%20-82%200%20-127%20-4%20-135%20-12%20-8%20-8%20-12%20-58%20-12%20-160%200%20-147%200%20-149%2026%20-173%2015%20-14%2024%20-32%2022%20-43%20-2%20-12%20-256%20-219%20-653%20-531%20l-650%20-512%20-29%20-170%20c-16%20-93%20-26%20-173%20-22%20-176%203%20-3%20299%20138%20658%20314%20l653%20320%20240%2037%20c132%2021%20252%2040%20268%2043%20l27%206%200%20-364%20c0%20-369%209%20-508%2045%20-694%208%20-44%2015%20-85%2015%20-91%200%20-6%20-75%20-74%20-167%20-151%20-93%20-77%20-207%20-175%20-255%20-217%20l-88%20-77%200%20-118%20c0%20-65%203%20-120%206%20-123%203%20-3%20131%2036%20285%2087%20l279%2093%200%20-87%20c0%20-85%202%20-91%2064%20-236%2036%20-83%2070%20-150%2077%20-150%207%200%2041%2068%2076%20150%2061%20145%2063%20153%2063%20237%20l0%2086%20279%20-93%20c154%20-51%20282%20-90%20285%20-87%203%203%206%2058%206%20123%20l0%20118%20-87%2077%20c-49%2042%20-163%20140%20-255%20217%20-93%2077%20-168%20145%20-168%20151%200%206%207%2047%2015%2091%2036%20186%2045%20325%2045%20694%20l0%20364%2028%20-6%20c15%20-3%20135%20-22%20267%20-43%20l240%20-37%20653%20-320%20c359%20-176%20655%20-317%20658%20-314%204%203%20-6%2082%20-21%20175%20l-27%20168%20-659%20519%20c-362%20286%20-659%20526%20-659%20534%200%207%2011%2024%2025%2037%2025%2023%2025%2026%2025%20172%200%20102%20-4%20152%20-12%20160%20-8%208%20-53%2012%20-135%2012%20-142%200%20-131%2010%20-135%20-128%20l-3%20-83%20-120%2095%20-120%2095%20-5%20773%20c-6%20770%20-6%20773%20-28%20848%20-13%2041%20-39%20105%20-59%20142%20-38%2073%20-144%20188%20-173%20188%20-10%200%20-45%20-27%20-78%20-61z'/%3e%3c/g%3e%3c/svg%3e", qn = '.leaflet-pane,.leaflet-tile,.leaflet-marker-icon,.leaflet-marker-shadow,.leaflet-tile-container,.leaflet-pane>svg,.leaflet-pane>canvas,.leaflet-zoom-box,.leaflet-image-layer,.leaflet-layer{position:absolute;left:0;top:0}.leaflet-container{overflow:hidden}.leaflet-tile,.leaflet-marker-icon,.leaflet-marker-shadow{-webkit-user-select:none;-moz-user-select:none;user-select:none;-webkit-user-drag:none}.leaflet-tile::selection{background:transparent}.leaflet-safari .leaflet-tile{image-rendering:-webkit-optimize-contrast}.leaflet-safari .leaflet-tile-container{width:1600px;height:1600px;-webkit-transform-origin:0 0}.leaflet-marker-icon,.leaflet-marker-shadow{display:block}.leaflet-container .leaflet-overlay-pane svg{max-width:none!important;max-height:none!important}.leaflet-container .leaflet-marker-pane img,.leaflet-container .leaflet-shadow-pane img,.leaflet-container .leaflet-tile-pane img,.leaflet-container img.leaflet-image-layer,.leaflet-container .leaflet-tile{max-width:none!important;max-height:none!important;width:auto;padding:0}.leaflet-container img.leaflet-tile{mix-blend-mode:plus-lighter}.leaflet-container.leaflet-touch-zoom{-ms-touch-action:pan-x pan-y;touch-action:pan-x pan-y}.leaflet-container.leaflet-touch-drag{-ms-touch-action:pinch-zoom;touch-action:none;touch-action:pinch-zoom}.leaflet-container.leaflet-touch-drag.leaflet-touch-zoom{-ms-touch-action:none;touch-action:none}.leaflet-container{-webkit-tap-highlight-color:transparent}.leaflet-container a{-webkit-tap-highlight-color:rgba(51,181,229,.4)}.leaflet-tile{filter:inherit;visibility:hidden}.leaflet-tile-loaded{visibility:inherit}.leaflet-zoom-box{width:0;height:0;-moz-box-sizing:border-box;box-sizing:border-box;z-index:800}.leaflet-overlay-pane svg{-moz-user-select:none}.leaflet-pane{z-index:400}.leaflet-tile-pane{z-index:200}.leaflet-overlay-pane{z-index:400}.leaflet-shadow-pane{z-index:500}.leaflet-marker-pane{z-index:600}.leaflet-tooltip-pane{z-index:650}.leaflet-popup-pane{z-index:700}.leaflet-map-pane canvas{z-index:100}.leaflet-map-pane svg{z-index:200}.leaflet-vml-shape{width:1px;height:1px}.lvml{behavior:url(#default#VML);display:inline-block;position:absolute}.leaflet-control{position:relative;z-index:800;pointer-events:visiblePainted;pointer-events:auto}.leaflet-top,.leaflet-bottom{position:absolute;z-index:1000;pointer-events:none}.leaflet-top{top:0}.leaflet-right{right:0}.leaflet-bottom{bottom:0}.leaflet-left{left:0}.leaflet-control{float:left;clear:both}.leaflet-right .leaflet-control{float:right}.leaflet-top .leaflet-control{margin-top:10px}.leaflet-bottom .leaflet-control{margin-bottom:10px}.leaflet-left .leaflet-control{margin-left:10px}.leaflet-right .leaflet-control{margin-right:10px}.leaflet-fade-anim .leaflet-popup{opacity:0;-webkit-transition:opacity .2s linear;-moz-transition:opacity .2s linear;transition:opacity .2s linear}.leaflet-fade-anim .leaflet-map-pane .leaflet-popup{opacity:1}.leaflet-zoom-animated{-webkit-transform-origin:0 0;-ms-transform-origin:0 0;transform-origin:0 0}svg.leaflet-zoom-animated{will-change:transform}.leaflet-zoom-anim .leaflet-zoom-animated{-webkit-transition:-webkit-transform .25s cubic-bezier(0,0,.25,1);-moz-transition:-moz-transform .25s cubic-bezier(0,0,.25,1);transition:transform .25s cubic-bezier(0,0,.25,1)}.leaflet-zoom-anim .leaflet-tile,.leaflet-pan-anim .leaflet-tile{-webkit-transition:none;-moz-transition:none;transition:none}.leaflet-zoom-anim .leaflet-zoom-hide{visibility:hidden}.leaflet-interactive{cursor:pointer}.leaflet-grab{cursor:-webkit-grab;cursor:-moz-grab;cursor:grab}.leaflet-crosshair,.leaflet-crosshair .leaflet-interactive{cursor:crosshair}.leaflet-popup-pane,.leaflet-control{cursor:auto}.leaflet-dragging .leaflet-grab,.leaflet-dragging .leaflet-grab .leaflet-interactive,.leaflet-dragging .leaflet-marker-draggable{cursor:move;cursor:-webkit-grabbing;cursor:-moz-grabbing;cursor:grabbing}.leaflet-marker-icon,.leaflet-marker-shadow,.leaflet-image-layer,.leaflet-pane>svg path,.leaflet-tile-container{pointer-events:none}.leaflet-marker-icon.leaflet-interactive,.leaflet-image-layer.leaflet-interactive,.leaflet-pane>svg path.leaflet-interactive,svg.leaflet-image-layer.leaflet-interactive path{pointer-events:visiblePainted;pointer-events:auto}.leaflet-container{background:#ddd;outline-offset:1px}.leaflet-container a{color:#0078a8}.leaflet-zoom-box{border:2px dotted #38f;background:#ffffff80}.leaflet-container{font-family:Helvetica Neue,Arial,Helvetica,sans-serif;font-size:12px;font-size:.75rem;line-height:1.5}.leaflet-bar{box-shadow:0 1px 5px #000000a6;border-radius:4px}.leaflet-bar a{background-color:#fff;border-bottom:1px solid #ccc;width:26px;height:26px;line-height:26px;display:block;text-align:center;text-decoration:none;color:#000}.leaflet-bar a,.leaflet-control-layers-toggle{background-position:50% 50%;background-repeat:no-repeat;display:block}.leaflet-bar a:hover,.leaflet-bar a:focus{background-color:#f4f4f4}.leaflet-bar a:first-child{border-top-left-radius:4px;border-top-right-radius:4px}.leaflet-bar a:last-child{border-bottom-left-radius:4px;border-bottom-right-radius:4px;border-bottom:none}.leaflet-bar a.leaflet-disabled{cursor:default;background-color:#f4f4f4;color:#bbb}.leaflet-touch .leaflet-bar a{width:30px;height:30px;line-height:30px}.leaflet-touch .leaflet-bar a:first-child{border-top-left-radius:2px;border-top-right-radius:2px}.leaflet-touch .leaflet-bar a:last-child{border-bottom-left-radius:2px;border-bottom-right-radius:2px}.leaflet-control-zoom-in,.leaflet-control-zoom-out{font:700 18px Lucida Console,Monaco,monospace;text-indent:1px}.leaflet-touch .leaflet-control-zoom-in,.leaflet-touch .leaflet-control-zoom-out{font-size:22px}.leaflet-control-layers{box-shadow:0 1px 5px #0006;background:#fff;border-radius:5px}.leaflet-control-layers-toggle{background-image:url(data:image/png;base64,);width:36px;height:36px}.leaflet-retina .leaflet-control-layers-toggle{background-image:url(data:image/png;base64,);background-size:26px 26px}.leaflet-touch .leaflet-control-layers-toggle{width:44px;height:44px}.leaflet-control-layers .leaflet-control-layers-list,.leaflet-control-layers-expanded .leaflet-control-layers-toggle{display:none}.leaflet-control-layers-expanded .leaflet-control-layers-list{display:block;position:relative}.leaflet-control-layers-expanded{padding:6px 10px 6px 6px;color:#333;background:#fff}.leaflet-control-layers-scrollbar{overflow-y:scroll;overflow-x:hidden;padding-right:5px}.leaflet-control-layers-selector{margin-top:2px;position:relative;top:1px}.leaflet-control-layers label{display:block;font-size:13px;font-size:1.08333em}.leaflet-control-layers-separator{height:0;border-top:1px solid #ddd;margin:5px -10px 5px -6px}.leaflet-default-icon-path{background-image:url(data:image/png;base64,)}.leaflet-container .leaflet-control-attribution{background:#fff;background:#fffc;margin:0}.leaflet-control-attribution,.leaflet-control-scale-line{padding:0 5px;color:#333;line-height:1.4}.leaflet-control-attribution a{text-decoration:none}.leaflet-control-attribution a:hover,.leaflet-control-attribution a:focus{text-decoration:underline}.leaflet-attribution-flag{display:inline!important;vertical-align:baseline!important;width:1em;height:.6669em}.leaflet-left .leaflet-control-scale{margin-left:5px}.leaflet-bottom .leaflet-control-scale{margin-bottom:5px}.leaflet-control-scale-line{border:2px solid #777;border-top:none;line-height:1.1;padding:2px 5px 1px;white-space:nowrap;-moz-box-sizing:border-box;box-sizing:border-box;background:#fffc;text-shadow:1px 1px #fff}.leaflet-control-scale-line:not(:first-child){border-top:2px solid #777;border-bottom:none;margin-top:-2px}.leaflet-control-scale-line:not(:first-child):not(:last-child){border-bottom:2px solid #777}.leaflet-touch .leaflet-control-attribution,.leaflet-touch .leaflet-control-layers,.leaflet-touch .leaflet-bar{box-shadow:none}.leaflet-touch .leaflet-control-layers,.leaflet-touch .leaflet-bar{border:2px solid rgba(0,0,0,.2);background-clip:padding-box}.leaflet-popup{position:absolute;text-align:center;margin-bottom:20px}.leaflet-popup-content-wrapper{padding:1px;text-align:left;border-radius:12px}.leaflet-popup-content{margin:13px 24px 13px 20px;line-height:1.3;font-size:13px;font-size:1.08333em;min-height:1px}.leaflet-popup-content p{margin:1.3em 0}.leaflet-popup-tip-container{width:40px;height:20px;position:absolute;left:50%;margin-top:-1px;margin-left:-20px;overflow:hidden;pointer-events:none}.leaflet-popup-tip{width:17px;height:17px;padding:1px;margin:-10px auto 0;pointer-events:auto;-webkit-transform:rotate(45deg);-moz-transform:rotate(45deg);-ms-transform:rotate(45deg);transform:rotate(45deg)}.leaflet-popup-content-wrapper,.leaflet-popup-tip{background:#fff;color:#333;box-shadow:0 3px 14px #0006}.leaflet-container a.leaflet-popup-close-button{position:absolute;top:0;right:0;border:none;text-align:center;width:24px;height:24px;font:16px/24px Tahoma,Verdana,sans-serif;color:#757575;text-decoration:none;background:transparent}.leaflet-container a.leaflet-popup-close-button:hover,.leaflet-container a.leaflet-popup-close-button:focus{color:#585858}.leaflet-popup-scrolled{overflow:auto}.leaflet-oldie .leaflet-popup-content-wrapper{-ms-zoom:1}.leaflet-oldie .leaflet-popup-tip{width:24px;margin:0 auto;-ms-filter:"progid:DXImageTransform.Microsoft.Matrix(M11=0.70710678, M12=0.70710678, M21=-0.70710678, M22=0.70710678)";filter:progid:DXImageTransform.Microsoft.Matrix(M11=.70710678,M12=.70710678,M21=-.70710678,M22=.70710678)}.leaflet-oldie .leaflet-control-zoom,.leaflet-oldie .leaflet-control-layers,.leaflet-oldie .leaflet-popup-content-wrapper,.leaflet-oldie .leaflet-popup-tip{border:1px solid #999}.leaflet-div-icon{background:#fff;border:1px solid #666}.leaflet-tooltip{position:absolute;padding:6px;background-color:#fff;border:1px solid #fff;border-radius:3px;color:#222;white-space:nowrap;-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none;pointer-events:none;box-shadow:0 1px 3px #0006}.leaflet-tooltip.leaflet-interactive{cursor:pointer;pointer-events:auto}.leaflet-tooltip-top:before,.leaflet-tooltip-bottom:before,.leaflet-tooltip-left:before,.leaflet-tooltip-right:before{position:absolute;pointer-events:none;border:6px solid transparent;background:transparent;content:""}.leaflet-tooltip-bottom{margin-top:6px}.leaflet-tooltip-top{margin-top:-6px}.leaflet-tooltip-bottom:before,.leaflet-tooltip-top:before{left:50%;margin-left:-6px}.leaflet-tooltip-top:before{bottom:0;margin-bottom:-12px;border-top-color:#fff}.leaflet-tooltip-bottom:before{top:0;margin-top:-12px;margin-left:-6px;border-bottom-color:#fff}.leaflet-tooltip-left{margin-left:-6px}.leaflet-tooltip-right{margin-left:6px}.leaflet-tooltip-left:before,.leaflet-tooltip-right:before{top:50%;margin-top:-6px}.leaflet-tooltip-left:before{right:0;margin-right:-12px;border-left-color:#fff}.leaflet-tooltip-right:before{left:0;margin-left:-12px;border-right-color:#fff}@media print{.leaflet-control{-webkit-print-color-adjust:exact;print-color-adjust:exact}}', Yn = "0.3.1", b0 = "flight-card", jn = /* @__PURE__ */ Object.assign({
  "./assets/adsb-icons/a0.svg": ln,
  "./assets/adsb-icons/a1.svg": cn,
  "./assets/adsb-icons/a2.svg": un,
  "./assets/adsb-icons/a3.svg": fn,
  "./assets/adsb-icons/a320.svg": dn,
  "./assets/adsb-icons/a330.svg": _n,
  "./assets/adsb-icons/a340.svg": mn,
  "./assets/adsb-icons/a380.svg": pn,
  "./assets/adsb-icons/a4.svg": gn,
  "./assets/adsb-icons/a5.svg": vn,
  "./assets/adsb-icons/a6.svg": yn,
  "./assets/adsb-icons/a7.svg": xn,
  "./assets/adsb-icons/b0.svg": wn,
  "./assets/adsb-icons/b1.svg": Cn,
  "./assets/adsb-icons/b2.svg": bn,
  "./assets/adsb-icons/b3.svg": Ln,
  "./assets/adsb-icons/b4.svg": Pn,
  "./assets/adsb-icons/b737.svg": Tn,
  "./assets/adsb-icons/b747.svg": Mn,
  "./assets/adsb-icons/b767.svg": kn,
  "./assets/adsb-icons/b777.svg": zn,
  "./assets/adsb-icons/b787.svg": Sn,
  "./assets/adsb-icons/c0.svg": En,
  "./assets/adsb-icons/c130.svg": An,
  "./assets/adsb-icons/cessna.svg": On,
  "./assets/adsb-icons/crjx.svg": Bn,
  "./assets/adsb-icons/dh8a.svg": Zn,
  "./assets/adsb-icons/e195.svg": In,
  "./assets/adsb-icons/erj.svg": Rn,
  "./assets/adsb-icons/f100.svg": Dn,
  "./assets/adsb-icons/f11.svg": Nn,
  "./assets/adsb-icons/f15.svg": Fn,
  "./assets/adsb-icons/f5.svg": Wn,
  "./assets/adsb-icons/fa7x.svg": Hn,
  "./assets/adsb-icons/glf5.svg": Gn,
  "./assets/adsb-icons/learjet.svg": Un,
  "./assets/adsb-icons/md11.svg": Vn
}), t0 = Object.fromEntries(
  Object.entries(jn).map(([d, r]) => {
    const h = d.match(/\/([^/]+)\.svg$/i);
    return h ? [h[1].toLowerCase(), r] : null;
  }).filter((d) => d !== null)
), ft = "a320", $2 = [
  { altitudeFt: 0, color: [198, 44, 22] },
  { altitudeFt: 500, color: [233, 79, 23] },
  { altitudeFt: 1e3, color: [242, 121, 28] },
  { altitudeFt: 2e3, color: [245, 164, 30] },
  { altitudeFt: 4e3, color: [239, 204, 38] },
  { altitudeFt: 6e3, color: [204, 220, 48] },
  { altitudeFt: 8e3, color: [138, 221, 63] },
  { altitudeFt: 1e4, color: [48, 216, 85] },
  { altitudeFt: 2e4, color: [94, 212, 206] },
  { altitudeFt: 3e4, color: [86, 140, 242] },
  { altitudeFt: 4e4, color: [224, 82, 248] }
], l2 = {
  title: "Nearby Aircraft",
  entity: "sensor.flight_card_aircraft",
  map_height: 420,
  default_zoom: 8,
  fit_bounds: !0,
  center_lat: null,
  center_lon: null,
  tile_url: "https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",
  attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap contributors</a>'
}, dt = {
  type: "FeatureCollection",
  features: []
};
class Jn extends HTMLElement {
  constructor() {
    super(...arguments), this._config = { ...l2 }, this._resizeFixTimeouts = [], this._hasAutofit = !1, this._latestGeoJson = dt, this._lastRenderedFingerprint = "", this._resolvedEntityId = "", this._els = {};
  }
  static getStubConfig() {
    return {
      title: l2.title,
      entity: l2.entity
    };
  }
  static getConfigForm() {
    return {
      schema: [
        { name: "title", selector: { text: {} } },
        { name: "entity", required: !0, selector: { entity: { domain: "sensor" } } },
        {
          type: "grid",
          name: "",
          flatten: !0,
          schema: [
            { name: "default_zoom", selector: { number: { min: 1, max: 18, mode: "box" } } },
            { name: "map_height", selector: { number: { min: 200, max: 1200, mode: "box" } } }
          ]
        },
        { name: "fit_bounds", selector: { boolean: {} } },
        {
          type: "expandable",
          name: "advanced",
          title: "Advanced",
          flatten: !1,
          schema: [
            { name: "center_lat", selector: { number: { min: -90, max: 90, step: 1e-6, mode: "box" } } },
            { name: "center_lon", selector: { number: { min: -180, max: 180, step: 1e-6, mode: "box" } } },
            { name: "tile_url", selector: { text: {} } },
            { name: "attribution", selector: { text: {} } }
          ]
        }
      ],
      computeLabel(r) {
        if (r.name === "entity") return "Aircraft entity";
        if (r.name === "default_zoom") return "Default zoom";
        if (r.name === "map_height") return "Map height (px)";
        if (r.name === "fit_bounds") return "Auto-fit map to aircraft";
        if (r.name === "center_lat") return "Center latitude";
        if (r.name === "center_lon") return "Center longitude";
        if (r.name === "tile_url") return "Tile URL";
        if (r.name === "attribution") return "Tile attribution";
      }
    };
  }
  set hass(r) {
    if (this._hass = r, this._map && !this._hasConfiguredCenter() && this._latestGeoJson.features.length === 0) {
      const h = this._resolveInitialCenter();
      this._map.setView(h, this._config.default_zoom, { animate: !1 });
    }
    this._syncFromHass();
  }
  get hass() {
    return this._hass;
  }
  setConfig(r) {
    if (this._config = Kn(r), this._hasAutofit = !1, this._render(), this._applyVisualConfig(), this._map) {
      const h = this._resolveInitialCenter();
      this._map.setView(h, this._config.default_zoom, { animate: !1 }), this._scheduleResizeFixes();
    }
    this.isConnected && (this._ensureMap(), this._syncFromHass());
  }
  connectedCallback() {
    this._render(), this._applyVisualConfig(), this._ensureMap(), this._syncFromHass();
  }
  disconnectedCallback() {
    this._map && (this._map.remove(), this._map = void 0, this._aircraftLayer = void 0), this._mapResizeObserver && (this._mapResizeObserver.disconnect(), this._mapResizeObserver = void 0), this._resizeFixTimeouts.forEach((r) => window.clearTimeout(r)), this._resizeFixTimeouts = [], this._mapInitPromise = void 0;
  }
  getCardSize() {
    const r = Math.ceil((this._config.map_height + 110) / 50);
    return Math.max(4, r);
  }
  getGridOptions() {
    const r = Math.ceil((this._config.map_height + 70) / 56);
    return {
      columns: 12,
      rows: Math.max(4, r),
      min_rows: 4
    };
  }
  _render() {
    this._root || (this._root = this.shadowRoot ?? this.attachShadow({ mode: "open" })), !this._els.card && (this._root.innerHTML = `
      <style>
        ${qn}

        :host {
          display: block;
        }

        .flight-card {
          display: block;
        }

        .flight-card__header {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          margin-bottom: 8px;
        }

        .flight-card__title {
          font-size: 1.1rem;
          font-weight: 600;
          color: var(--primary-text-color);
          margin: 0;
        }

        .flight-card__meta {
          display: flex;
          flex-wrap: wrap;
          gap: 8px 16px;
          margin-bottom: 12px;
          font-size: 0.875rem;
          color: var(--secondary-text-color);
        }

        .flight-card__status {
          font-size: 0.75rem;
          font-weight: 600;
          text-transform: uppercase;
          letter-spacing: 0.05em;
          border-radius: 999px;
          padding: 4px 10px;
          line-height: 1;
        }

        .flight-card__status--idle {
          color: var(--secondary-text-color);
          background: var(--secondary-background-color);
        }

        .flight-card__status--ok {
          color: #14632f;
          background: #d8f7e2;
        }

        .flight-card__status--error {
          color: #7a2020;
          background: #ffe2e2;
        }

        .flight-card__map {
          position: relative;
          width: 100%;
          border-radius: 12px;
          overflow: hidden;
          border: 1px solid var(--divider-color);
          background: var(--secondary-background-color);
        }

        .flight-card__map .leaflet-container {
          width: 100%;
          height: 100%;
        }

        .flight-card__map .leaflet-tile,
        .flight-card__map .leaflet-marker-icon,
        .flight-card__map .leaflet-marker-shadow,
        .flight-card__map .leaflet-container img {
          max-width: none !important;
          max-height: none !important;
        }

        .flight-card__map .flight-card__aircraft-marker {
          background: transparent;
          border: 0;
        }

        .flight-card__aircraft-icon {
          width: 24px;
          height: 24px;
          display: block;
          transform: rotate(var(--aircraft-rotation, 0deg));
          transform-origin: 50% 50%;
          filter: drop-shadow(0 1px 1px rgba(0, 0, 0, 0.35));
        }

        .flight-card__aircraft-icon-mask {
          width: 100%;
          height: 100%;
          display: block;
          -webkit-mask-repeat: no-repeat;
          mask-repeat: no-repeat;
          -webkit-mask-size: contain;
          mask-size: contain;
          -webkit-mask-position: center;
          mask-position: center;
          pointer-events: none;
        }

        .flight-card__aircraft-fallback {
          width: 12px;
          height: 12px;
          border-radius: 50%;
          display: block;
          background: var(--aircraft-color, #ef4444);
          border: 2px solid rgba(15, 23, 42, 0.65);
        }

        .flight-card__popup {
          margin: 0;
          line-height: 1.35;
        }

        .flight-card__popup-image {
          margin-top: 6px;
          width: 160px;
          max-width: 100%;
          height: auto;
          border-radius: 6px;
          border: 1px solid #cbd5e1;
          background: #f8fafc;
        }
      </style>
      <ha-card>
        <div class="card-content flight-card">
          <div class="flight-card__header">
            <h2 class="flight-card__title"></h2>
            <span class="flight-card__status flight-card__status--idle">Idle</span>
          </div>
          <div class="flight-card__meta">
            <span class="flight-card__count">Aircraft: 0</span>
            <span class="flight-card__updated">Updated: never</span>
          </div>
          <div class="flight-card__map"></div>
        </div>
      </ha-card>
    `, this._els = {
      card: this._root.querySelector("ha-card") ?? void 0,
      title: this._root.querySelector(".flight-card__title") ?? void 0,
      status: this._root.querySelector(".flight-card__status") ?? void 0,
      count: this._root.querySelector(".flight-card__count") ?? void 0,
      updated: this._root.querySelector(".flight-card__updated") ?? void 0,
      map: this._root.querySelector(".flight-card__map") ?? void 0
    });
  }
  _applyVisualConfig() {
    !this._els.title || !this._els.map || (this._els.title.textContent = this._config.title, this._els.map.style.height = `${this._config.map_height}px`, this._invalidateMapSize(!1));
  }
  async _ensureMap() {
    if (!(this._map || !this._els.map))
      return this._mapInitPromise ? this._mapInitPromise : (this._mapInitPromise = (async () => {
        try {
          this._setStatus("idle", "Loading map"), this._leaflet = await ao(), await this._waitForMapContainerReady();
          const r = this._leaflet, h = this._resolveInitialCenter();
          this._map = r.map(this._els.map, {
            zoomControl: !0,
            scrollWheelZoom: !0,
            zoomAnimation: !1,
            fadeAnimation: !1,
            markerZoomAnimation: !1
          }), r.tileLayer(this._config.tile_url, {
            attribution: this._config.attribution,
            maxZoom: 19,
            updateWhenIdle: !0,
            keepBuffer: 4
          }).addTo(this._map), this._map.setView(h, this._config.default_zoom), this._aircraftLayer = r.geoJSON([], {
            pointToLayer: (m, f) => {
              const y = m?.properties;
              return r.marker(f, {
                icon: r.divIcon({
                  className: "flight-card__aircraft-marker",
                  html: no(y),
                  iconSize: [24, 24],
                  iconAnchor: [12, 12],
                  popupAnchor: [0, -10]
                }),
                keyboard: !1
              });
            },
            onEachFeature: (m, f) => {
              f.bindPopup(this._popupHtml(m.properties));
            }
          }), this._aircraftLayer.addTo(this._map), this._startResizeObserver(), this._map.whenReady(() => this._scheduleResizeFixes()), this._renderGeoJson(this._latestGeoJson), this._syncFromHass();
        } catch (r) {
          const h = r instanceof Error ? r.message : "Unknown map error";
          this._setStatus("error", `Map error: ${h}`);
        } finally {
          this._mapInitPromise = void 0;
        }
      })(), this._mapInitPromise);
  }
  _syncFromHass() {
    if (!this._hass)
      return;
    const r = Qn(this._hass, this._config.entity);
    this._resolvedEntityId = r;
    const h = r ? this._hass.states?.[r] : void 0;
    if (!h) {
      this._els.count && (this._els.count.textContent = "Aircraft: 0"), this._els.updated && (this._els.updated.textContent = "Updated: never"), this._setStatus("error", `Entity not found: ${this._config.entity}`), this._latestGeoJson = dt, this._renderGeoJson(this._latestGeoJson);
      return;
    }
    const m = h.attributes?.geojson, f = I2(m) && m.type === "FeatureCollection" && Array.isArray(m.features), y = Xn(m), T = to(h.attributes?.updated ?? h.last_updated), j = `${h.state}|${String(h.attributes?.updated ?? "")}|${y.features.length}`;
    if (j !== this._lastRenderedFingerprint && (this._lastRenderedFingerprint = j, this._latestGeoJson = y, this._renderGeoJson(y)), this._els.count) {
      const C = Number(h.state), p2 = Number.isFinite(C) ? C : y.features.length;
      this._els.count.textContent = `Aircraft: ${Math.max(0, Math.round(p2))}`;
    }
    if (this._els.updated && (this._els.updated.textContent = `Updated: ${T}`), h.state === "unavailable") {
      this._setStatus("error", "Entity unavailable");
      return;
    }
    if (h.state === "unknown" || !f) {
      this._setStatus("idle", `Waiting for backend data (${h.entity_id})`);
      return;
    }
    this._setStatus("ok", "Live");
  }
  _renderGeoJson(r) {
    if (!(!this._aircraftLayer || !this._map)) {
      if (this._aircraftLayer.clearLayers(), this._aircraftLayer.addData(r), r.features.length === 0) {
        this._hasAutofit = !1;
        return;
      }
      if (this._config.fit_bounds) {
        const h = this._aircraftLayer.getBounds();
        h.isValid() && !this._hasAutofit && (this._map.fitBounds(h, {
          padding: [24, 24],
          maxZoom: this._config.default_zoom + 3
        }), this._hasAutofit = !0);
      }
    }
  }
  _setStatus(r, h) {
    this._els.status && (this._els.status.className = `flight-card__status flight-card__status--${r}`, this._els.status.textContent = h);
  }
  _resolveInitialCenter() {
    if (this._hasConfiguredCenter())
      return [this._config.center_lat, this._config.center_lon];
    const r = this._hass?.states?.["zone.home"], h = Number(r?.attributes?.latitude), m = Number(r?.attributes?.longitude);
    if (Number.isFinite(h) && Number.isFinite(m))
      return [h, m];
    if (this._hass?.config && Number.isFinite(this._hass.config.latitude) && Number.isFinite(this._hass.config.longitude))
      return [this._hass.config.latitude, this._hass.config.longitude];
    const f = this._latestGeoJson.features[0], y = Number(f?.geometry?.coordinates?.[0]), T = Number(f?.geometry?.coordinates?.[1]);
    return Number.isFinite(T) && Number.isFinite(y) ? [T, y] : [0, 0];
  }
  _hasConfiguredCenter() {
    return Number.isFinite(this._config.center_lat) && Number.isFinite(this._config.center_lon);
  }
  _popupHtml(r) {
    const h = [], m = ro([r.flight, r.registration, r.aircraft_type, "Aircraft"]);
    h.push(`<strong>${T2(m)}</strong>`), r.aircraft_type && h.push(`Type: ${T2(r.aircraft_type)}`), r.registration && h.push(`Registration: ${T2(r.registration)}`), r.manufacturer && h.push(`Manufacturer: ${T2(r.manufacturer)}`), r.registered_owners && h.push(`Owner: ${T2(r.registered_owners)}`), Number.isFinite(r.altitude_ft) && h.push(`Altitude: ${Math.round(r.altitude_ft).toLocaleString()} ft`), Number.isFinite(r.speed_kt) && h.push(`Speed: ${Math.round(r.speed_kt)} kt`), Number.isFinite(r.track_deg) && h.push(`Track: ${Math.round(r.track_deg)}°`), Number.isFinite(r.seen_s) && h.push(`Seen: ${r.seen_s.toFixed(1)} s ago`);
    const f = r.airframe_image_url ? `<img class="flight-card__popup-image" src="${T2(r.airframe_image_url)}" alt="Airframe image" loading="lazy" referrerpolicy="no-referrer" />` : "";
    return `<p class="flight-card__popup">${h.join("<br>")}${f ? `<br>${f}` : ""}</p>`;
  }
  _startResizeObserver() {
    this._mapResizeObserver || !this._els.map || !this._map || (this._mapResizeObserver = new ResizeObserver(() => {
      this._invalidateMapSize(!1);
    }), this._mapResizeObserver.observe(this._els.map));
  }
  _scheduleResizeFixes() {
    this._resizeFixTimeouts.forEach((r) => window.clearTimeout(r)), this._resizeFixTimeouts = [], [0, 150, 600, 1200, 2500].forEach((r) => {
      const h = window.setTimeout(() => this._invalidateMapSize(!1), r);
      this._resizeFixTimeouts.push(h);
    });
  }
  _invalidateMapSize(r) {
    this._map && requestAnimationFrame(() => {
      this._map && this._map.invalidateSize({ pan: r });
    });
  }
  async _waitForMapContainerReady() {
    const r = this._els.map;
    if (!r)
      return;
    const h = () => r.clientWidth >= 280 && r.clientHeight >= 200;
    h() || await new Promise((m) => {
      const f = window.setTimeout(() => {
        y.disconnect(), m();
      }, 5e3);
      this._resizeFixTimeouts.push(f);
      const y = new ResizeObserver(() => {
        h() && (y.disconnect(), window.clearTimeout(f), m());
      });
      y.observe(r);
    });
  }
}
function Kn(d) {
  if (!d || typeof d != "object")
    throw new Error("Invalid configuration");
  const r = {
    ...l2,
    ...d
  };
  r.title = String(r.title || l2.title), r.entity = String(r.entity || l2.entity).trim(), r.map_height = Ee(r.map_height, 200, 1200, l2.map_height), r.default_zoom = Ee(r.default_zoom, 1, 18, l2.default_zoom), r.fit_bounds = r.fit_bounds !== !1, r.center_lat = w0(r.center_lat), r.center_lon = w0(r.center_lon);
  const h = d.advanced;
  if (h && typeof h == "object" && (r.center_lat = w0(h.center_lat ?? r.center_lat), r.center_lon = w0(h.center_lon ?? r.center_lon), typeof h.tile_url == "string" && h.tile_url.length > 0 && (r.tile_url = h.tile_url), typeof h.attribution == "string" && h.attribution.length > 0 && (r.attribution = h.attribution)), r.tile_url = String(r.tile_url || l2.tile_url), r.attribution = String(r.attribution || l2.attribution), !r.entity)
    throw new Error("Please set entity in card configuration");
  return r;
}
function w0(d) {
  if (d == null || d === "")
    return null;
  const r = Number(d);
  return Number.isFinite(r) ? r : null;
}
function Ee(d, r, h, m) {
  const f = Number(d);
  return Number.isFinite(f) ? Math.min(h, Math.max(r, f)) : m;
}
function Xn(d) {
  if (!I2(d) || d.type !== "FeatureCollection" || !Array.isArray(d.features))
    return dt;
  const r = [];
  for (const h of d.features) {
    const m = $n(h);
    m && r.push(m);
  }
  return {
    type: "FeatureCollection",
    features: r
  };
}
function Qn(d, r) {
  const h = d.states ?? {}, m = r.trim();
  if (m && h[m])
    return m;
  const f = Object.values(h), y = f.find((j) => {
    const C = j.attributes?.source_domain;
    return typeof C == "string" && C === "flight_card";
  });
  return y ? y.entity_id : f.find((j) => {
    const C = j.attributes, p2 = C?.geojson;
    return I2(p2) && p2.type === "FeatureCollection" && Array.isArray(p2.features) && typeof C?.config_entry_id == "string";
  })?.entity_id ?? "";
}
function $n(d) {
  if (!I2(d))
    return null;
  const r = d.geometry;
  if (!I2(r) || r.type !== "Point" || !Array.isArray(r.coordinates))
    return null;
  const h = Number(r.coordinates[0]), m = Number(r.coordinates[1]);
  if (!Number.isFinite(m) || !Number.isFinite(h))
    return null;
  const f = I2(d.properties) ? d.properties : {};
  return {
    type: "Feature",
    geometry: {
      type: "Point",
      coordinates: [h, m]
    },
    properties: {
      hex: h2(f.hex, "unknown"),
      flight: h2(f.flight),
      category: h2(f.category),
      aircraft_type: h2(f.aircraft_type),
      registration: h2(f.registration),
      manufacturer: h2(f.manufacturer),
      icao_type_code: h2(f.icao_type_code),
      operator_flag_code: h2(f.operator_flag_code),
      registered_owners: h2(f.registered_owners),
      airframe_image_url: h2(f.airframe_image_url),
      altitude_ft: C0(f.altitude_ft),
      speed_kt: C0(f.speed_kt),
      track_deg: C0(f.track_deg),
      seen_s: C0(f.seen_s)
    }
  };
}
function I2(d) {
  return !!d && typeof d == "object";
}
function h2(d, r = "") {
  return typeof d == "string" ? d : r;
}
function C0(d) {
  const r = Number(d);
  return Number.isFinite(r) ? r : null;
}
function to(d) {
  if (typeof d == "string" && d.length > 0) {
    const r = new Date(d);
    if (!Number.isNaN(r.getTime()))
      return r.toLocaleTimeString();
  }
  return "unknown";
}
function eo(d) {
  if (!Number.isFinite(d))
    return "#64748b";
  const r = Math.max(0, d), h = $2[$2.length - 1];
  if (r >= h.altitudeFt)
    return lt(h.color);
  for (let m = 1; m < $2.length; m += 1) {
    const f = $2[m - 1], y = $2[m];
    if (r > y.altitudeFt)
      continue;
    const T = y.altitudeFt - f.altitudeFt, j = T > 0 ? (r - f.altitudeFt) / T : 0;
    return lt([
      ht(f.color[0], y.color[0], j),
      ht(f.color[1], y.color[1], j),
      ht(f.color[2], y.color[2], j)
    ]);
  }
  return lt(h.color);
}
function ht(d, r, h) {
  return d + (r - d) * h;
}
function lt(d) {
  const r = (h) => Math.max(0, Math.min(255, Math.round(h))).toString(16).padStart(2, "0");
  return `#${r(d[0])}${r(d[1])}${r(d[2])}`;
}
function io(d) {
  return Number.isFinite(d) ? (d % 360 + 360) % 360 : 0;
}
function no(d) {
  const r = io(d?.track_deg), h = eo(d?.altitude_ft), m = oo(d), f = t0[m] ?? t0[ft];
  if (!f)
    return `<span class="flight-card__aircraft-fallback" style="--aircraft-color: ${h};"></span>`;
  const y = T2(f), T = `background: ${h}; -webkit-mask-image: url(&quot;${y}&quot;); mask-image: url(&quot;${y}&quot;);`;
  return `
    <span class="flight-card__aircraft-icon" style="--aircraft-rotation: ${r}deg;">
      <span class="flight-card__aircraft-icon-mask" style="${T}"></span>
    </span>
  `;
}
function oo(d) {
  if (!d)
    return ft;
  const r = /* @__PURE__ */ new Set(), h = [
    ...ct(d.icao_type_code),
    ...ct(d.aircraft_type),
    ...ct(d.category)
  ];
  for (const f of h) {
    if (r.has(f))
      continue;
    r.add(f);
    const y = so(f);
    if (y && t0[y])
      return y;
  }
  const m = Ae(d.category);
  return m && t0[m] ? m : ft;
}
function ct(d) {
  if (!d)
    return [];
  const r = d.toUpperCase().split(/[^A-Z0-9]+/).map((m) => m.trim()).filter((m) => m.length > 0), h = [];
  for (const m of r) {
    h.push(m);
    const f = m.replace(/[^A-Z0-9]/g, "");
    f && f !== m && h.push(f), f.length >= 5 && h.push(f.slice(0, 4)), f.length >= 4 && h.push(f.slice(0, 3));
    const y = f.match(/^([A-Z]{1,3}\d{1,3}[A-Z]?)/);
    y && y[1] !== f && h.push(y[1]);
  }
  return h;
}
function Ae(d) {
  const r = d.trim().toUpperCase();
  return /^[A-F]\d{1,2}$/.test(r) ? `${r[0].toLowerCase()}${Number(r.slice(1))}` : "";
}
function so(d) {
  const r = d.toUpperCase();
  if (!r)
    return "";
  const h = Ae(r);
  return h && t0[h] ? h : E(r, ["A7", "F3", "F03", "EC35", "EC45", "H145"]) ? "a7" : E(r, ["B0", "B5", "B6", "B7", "F13"]) ? "b0" : E(r, ["B1", "F1", "F01", "ULM", "ULTRALIGHT"]) ? "b1" : E(r, ["B2", "F12"]) ? "b2" : E(r, ["B3", "F4", "F04"]) ? "b3" : E(r, ["B4", "F6", "F7", "F06", "F07"]) ? "b4" : E(r, ["C0", "C1", "C2", "C3"]) ? "c0" : E(r, ["F5", "F05"]) ? "f5" : E(r, ["F11"]) ? "f11" : E(r, ["F15"]) ? "f15" : r.startsWith("A32") || E(r, ["A318", "A319", "A320", "A321", "A20N", "A21N"]) ? "a320" : r.startsWith("A33") || E(r, ["A300", "A306", "A310", "A332", "A333", "A339"]) ? "a330" : r.startsWith("A34") || r.startsWith("A35") || E(r, ["A340", "A350", "A359", "A35K"]) ? "a340" : r.startsWith("A38") || E(r, ["A380", "A388"]) ? "a380" : r.startsWith("B73") || r.startsWith("B38") || r.startsWith("B39") || E(r, ["B727", "B737", "B738", "B739", "B37M", "B38M", "B39M"]) ? "b737" : r.startsWith("B74") || E(r, ["B741", "B742", "B743", "B744", "B748"]) ? "b747" : r.startsWith("B76") || E(r, ["B761", "B762", "B763", "B764", "B767"]) ? "b767" : r.startsWith("B77") || E(r, ["B772", "B773", "B77L", "B77W"]) ? "b777" : r.startsWith("B78") || E(r, ["B788", "B789", "B78X"]) ? "b787" : r.startsWith("C13") || r.startsWith("C30") || E(r, ["C130", "C135", "C17"]) ? "c130" : r.startsWith("CRJ") || E(r, ["CRJ1", "CRJ2", "CRJ7", "CRJ9", "CRJX"]) ? "crjx" : r.startsWith("DH8") || r.startsWith("AT7") || r.startsWith("AT4") || E(r, ["DHC8"]) ? "dh8a" : r.startsWith("E17") || r.startsWith("E19") || E(r, ["E170", "E175", "E190", "E195"]) ? "e195" : r.startsWith("E13") || r.startsWith("E14") || E(r, ["ERJ", "E135", "E145"]) ? "erj" : r.startsWith("F10") || r.startsWith("MD8") || E(r, ["F100", "MD80", "MD81", "MD82", "MD83", "MD87", "MD88"]) ? "f100" : r.startsWith("FA7") || r.startsWith("FA8") || E(r, ["E35L"]) ? "fa7x" : r.startsWith("GLF") || r.startsWith("G5") || r.startsWith("G6") || E(r, ["GLEX"]) ? "glf5" : r.startsWith("C25") || r.startsWith("LJ") || r.startsWith("LEAR") ? "learjet" : r.startsWith("C15") || r.startsWith("C17") || r.startsWith("C18") || r.startsWith("C20") || r.startsWith("CESS") ? "cessna" : r.startsWith("MD11") || E(r, ["MD11"]) ? "md11" : "";
}
function E(d, r) {
  return r.includes(d);
}
function ro(d) {
  for (const r of d) {
    if (typeof r != "string")
      continue;
    const h = r.trim();
    if (h.length > 0)
      return h;
  }
  return "";
}
function T2(d) {
  return d.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/\"/g, "&quot;").replace(/'/g, "&#39;");
}
let ut;
function ao() {
  return ut || (ut = Promise.resolve().then(() => fo)), ut;
}
function ho() {
  window.customCards = window.customCards || [], window.customCards.find((d) => d.type === b0) || window.customCards.push({
    type: b0,
    name: "Flight Card",
    description: "Display aircraft from the Flight Card integration sensor on a live map.",
    documentationURL: "https://developers.home-assistant.io/docs/frontend/custom-ui/custom-card/"
  });
}
customElements.get(b0) || customElements.define(b0, Jn);
ho();
console.info(
  `%c FLIGHT-CARD %c ${Yn} `,
  "color: white; background: #3b82f6; font-weight: 700;",
  "color: #3b82f6; background: white; font-weight: 700;"
);
var lo = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function co(d) {
  return d && d.__esModule && Object.prototype.hasOwnProperty.call(d, "default") ? d.default : d;
}
var _t = { exports: {} };
/* @preserve
 * Leaflet 1.9.4, a JS library for interactive maps. https://leafletjs.com
 * (c) 2010-2023 Vladimir Agafonkin, (c) 2010-2011 CloudMade
 */
(function(d, r) {
  (function(h, m) {
    m(r);
  })(lo, function(h) {
    var m = "1.9.4";
    function f(t) {
      var e, i, n, o;
      for (i = 1, n = arguments.length; i < n; i++) {
        o = arguments[i];
        for (e in o)
          t[e] = o[e];
      }
      return t;
    }
    var y = Object.create || /* @__PURE__ */ function() {
      function t() {
      }
      return function(e) {
        return t.prototype = e, new t();
      };
    }();
    function T(t, e) {
      var i = Array.prototype.slice;
      if (t.bind)
        return t.bind.apply(t, i.call(arguments, 1));
      var n = i.call(arguments, 2);
      return function() {
        return t.apply(e, n.length ? n.concat(i.call(arguments)) : arguments);
      };
    }
    var j = 0;
    function C(t) {
      return "_leaflet_id" in t || (t._leaflet_id = ++j), t._leaflet_id;
    }
    function p2(t, e, i) {
      var n, o, s, a;
      return a = function() {
        n = !1, o && (s.apply(i, o), o = !1);
      }, s = function() {
        n ? o = arguments : (t.apply(i, arguments), setTimeout(a, e), n = !0);
      }, s;
    }
    function R2(t, e, i) {
      var n = e[1], o = e[0], s = n - o;
      return t === n && i ? t : ((t - o) % s + s) % s + o;
    }
    function I() {
      return !1;
    }
    function n2(t, e) {
      if (e === !1)
        return t;
      var i = Math.pow(10, e === void 0 ? 6 : e);
      return Math.round(t * i) / i;
    }
    function L0(t) {
      return t.trim ? t.trim() : t.replace(/^\s+|\s+$/g, "");
    }
    function x2(t) {
      return L0(t).split(/\s+/);
    }
    function O(t, e) {
      Object.prototype.hasOwnProperty.call(t, "options") || (t.options = t.options ? y(t.options) : {});
      for (var i in e)
        t.options[i] = e[i];
      return t.options;
    }
    function mt(t, e, i) {
      var n = [];
      for (var o in t)
        n.push(encodeURIComponent(i ? o.toUpperCase() : o) + "=" + encodeURIComponent(t[o]));
      return (!e || e.indexOf("?") === -1 ? "?" : "&") + n.join("&");
    }
    var Be = /\{ *([\w_ -]+) *\}/g;
    function pt(t, e) {
      return t.replace(Be, function(i, n) {
        var o = e[n];
        if (o === void 0)
          throw new Error("No value provided for variable " + i);
        return typeof o == "function" && (o = o(e)), o;
      });
    }
    var t2 = Array.isArray || function(t) {
      return Object.prototype.toString.call(t) === "[object Array]";
    };
    function P0(t, e) {
      for (var i = 0; i < t.length; i++)
        if (t[i] === e)
          return i;
      return -1;
    }
    var e0 = "data:image/gif;base64,R0lGODlhAQABAAD/ACwAAAAAAQABAAACADs=";
    function T0(t) {
      return window["webkit" + t] || window["moz" + t] || window["ms" + t];
    }
    var gt = 0;
    function vt(t) {
      var e = +/* @__PURE__ */ new Date(), i = Math.max(0, 16 - (e - gt));
      return gt = e + i, window.setTimeout(t, i);
    }
    var M0 = window.requestAnimationFrame || T0("RequestAnimationFrame") || vt, yt = window.cancelAnimationFrame || T0("CancelAnimationFrame") || T0("CancelRequestAnimationFrame") || function(t) {
      window.clearTimeout(t);
    };
    function U(t, e, i) {
      if (i && M0 === vt)
        t.call(e);
      else
        return M0.call(window, T(t, e));
    }
    function K(t) {
      t && yt.call(window, t);
    }
    var Ze = {
      __proto__: null,
      extend: f,
      create: y,
      bind: T,
      get lastId() {
        return j;
      },
      stamp: C,
      throttle: p2,
      wrapNum: R2,
      falseFn: I,
      formatNum: n2,
      trim: L0,
      splitWords: x2,
      setOptions: O,
      getParamString: mt,
      template: pt,
      isArray: t2,
      indexOf: P0,
      emptyImageUrl: e0,
      requestFn: M0,
      cancelFn: yt,
      requestAnimFrame: U,
      cancelAnimFrame: K
    };
    function c2() {
    }
    c2.extend = function(t) {
      var e = function() {
        O(this), this.initialize && this.initialize.apply(this, arguments), this.callInitHooks();
      }, i = e.__super__ = this.prototype, n = y(i);
      n.constructor = e, e.prototype = n;
      for (var o in this)
        Object.prototype.hasOwnProperty.call(this, o) && o !== "prototype" && o !== "__super__" && (e[o] = this[o]);
      return t.statics && f(e, t.statics), t.includes && (Ie(t.includes), f.apply(null, [n].concat(t.includes))), f(n, t), delete n.statics, delete n.includes, n.options && (n.options = i.options ? y(i.options) : {}, f(n.options, t.options)), n._initHooks = [], n.callInitHooks = function() {
        if (!this._initHooksCalled) {
          i.callInitHooks && i.callInitHooks.call(this), this._initHooksCalled = !0;
          for (var s = 0, a = n._initHooks.length; s < a; s++)
            n._initHooks[s].call(this);
        }
      }, e;
    }, c2.include = function(t) {
      var e = this.prototype.options;
      return f(this.prototype, t), t.options && (this.prototype.options = e, this.mergeOptions(t.options)), this;
    }, c2.mergeOptions = function(t) {
      return f(this.prototype.options, t), this;
    }, c2.addInitHook = function(t) {
      var e = Array.prototype.slice.call(arguments, 1), i = typeof t == "function" ? t : function() {
        this[t].apply(this, e);
      };
      return this.prototype._initHooks = this.prototype._initHooks || [], this.prototype._initHooks.push(i), this;
    };
    function Ie(t) {
      if (!(typeof L > "u" || !L || !L.Mixin)) {
        t = t2(t) ? t : [t];
        for (var e = 0; e < t.length; e++)
          t[e] === L.Mixin.Events && console.warn("Deprecated include of L.Mixin.Events: this property will be removed in future releases, please inherit from L.Evented instead.", new Error().stack);
      }
    }
    var J = {
      /* @method on(type: String, fn: Function, context?: Object): this
       * Adds a listener function (`fn`) to a particular event type of the object. You can optionally specify the context of the listener (object the this keyword will point to). You can also pass several space-separated types (e.g. `'click dblclick'`).
       *
       * @alternative
       * @method on(eventMap: Object): this
       * Adds a set of type/listener pairs, e.g. `{click: onClick, mousemove: onMouseMove}`
       */
      on: function(t, e, i) {
        if (typeof t == "object")
          for (var n in t)
            this._on(n, t[n], e);
        else {
          t = x2(t);
          for (var o = 0, s = t.length; o < s; o++)
            this._on(t[o], e, i);
        }
        return this;
      },
      /* @method off(type: String, fn?: Function, context?: Object): this
       * Removes a previously added listener function. If no function is specified, it will remove all the listeners of that particular event from the object. Note that if you passed a custom context to `on`, you must pass the same context to `off` in order to remove the listener.
       *
       * @alternative
       * @method off(eventMap: Object): this
       * Removes a set of type/listener pairs.
       *
       * @alternative
       * @method off: this
       * Removes all listeners to all events on the object. This includes implicitly attached events.
       */
      off: function(t, e, i) {
        if (!arguments.length)
          delete this._events;
        else if (typeof t == "object")
          for (var n in t)
            this._off(n, t[n], e);
        else {
          t = x2(t);
          for (var o = arguments.length === 1, s = 0, a = t.length; s < a; s++)
            o ? this._off(t[s]) : this._off(t[s], e, i);
        }
        return this;
      },
      // attach listener (without syntactic sugar now)
      _on: function(t, e, i, n) {
        if (typeof e != "function") {
          console.warn("wrong listener type: " + typeof e);
          return;
        }
        if (this._listens(t, e, i) === !1) {
          i === this && (i = void 0);
          var o = { fn: e, ctx: i };
          n && (o.once = !0), this._events = this._events || {}, this._events[t] = this._events[t] || [], this._events[t].push(o);
        }
      },
      _off: function(t, e, i) {
        var n, o, s;
        if (this._events && (n = this._events[t], !!n)) {
          if (arguments.length === 1) {
            if (this._firingCount)
              for (o = 0, s = n.length; o < s; o++)
                n[o].fn = I;
            delete this._events[t];
            return;
          }
          if (typeof e != "function") {
            console.warn("wrong listener type: " + typeof e);
            return;
          }
          var a = this._listens(t, e, i);
          if (a !== !1) {
            var l = n[a];
            this._firingCount && (l.fn = I, this._events[t] = n = n.slice()), n.splice(a, 1);
          }
        }
      },
      // @method fire(type: String, data?: Object, propagate?: Boolean): this
      // Fires an event of the specified type. You can optionally provide a data
      // object — the first argument of the listener function will contain its
      // properties. The event can optionally be propagated to event parents.
      fire: function(t, e, i) {
        if (!this.listens(t, i))
          return this;
        var n = f({}, e, {
          type: t,
          target: this,
          sourceTarget: e && e.sourceTarget || this
        });
        if (this._events) {
          var o = this._events[t];
          if (o) {
            this._firingCount = this._firingCount + 1 || 1;
            for (var s = 0, a = o.length; s < a; s++) {
              var l = o[s], c = l.fn;
              l.once && this.off(t, c, l.ctx), c.call(l.ctx || this, n);
            }
            this._firingCount--;
          }
        }
        return i && this._propagateEvent(n), this;
      },
      // @method listens(type: String, propagate?: Boolean): Boolean
      // @method listens(type: String, fn: Function, context?: Object, propagate?: Boolean): Boolean
      // Returns `true` if a particular event type has any listeners attached to it.
      // The verification can optionally be propagated, it will return `true` if parents have the listener attached to it.
      listens: function(t, e, i, n) {
        typeof t != "string" && console.warn('"string" type argument expected');
        var o = e;
        typeof e != "function" && (n = !!e, o = void 0, i = void 0);
        var s = this._events && this._events[t];
        if (s && s.length && this._listens(t, o, i) !== !1)
          return !0;
        if (n) {
          for (var a in this._eventParents)
            if (this._eventParents[a].listens(t, e, i, n))
              return !0;
        }
        return !1;
      },
      // returns the index (number) or false
      _listens: function(t, e, i) {
        if (!this._events)
          return !1;
        var n = this._events[t] || [];
        if (!e)
          return !!n.length;
        i === this && (i = void 0);
        for (var o = 0, s = n.length; o < s; o++)
          if (n[o].fn === e && n[o].ctx === i)
            return o;
        return !1;
      },
      // @method once(…): this
      // Behaves as [`on(…)`](#evented-on), except the listener will only get fired once and then removed.
      once: function(t, e, i) {
        if (typeof t == "object")
          for (var n in t)
            this._on(n, t[n], e, !0);
        else {
          t = x2(t);
          for (var o = 0, s = t.length; o < s; o++)
            this._on(t[o], e, i, !0);
        }
        return this;
      },
      // @method addEventParent(obj: Evented): this
      // Adds an event parent - an `Evented` that will receive propagated events
      addEventParent: function(t) {
        return this._eventParents = this._eventParents || {}, this._eventParents[C(t)] = t, this;
      },
      // @method removeEventParent(obj: Evented): this
      // Removes an event parent, so it will stop receiving propagated events
      removeEventParent: function(t) {
        return this._eventParents && delete this._eventParents[C(t)], this;
      },
      _propagateEvent: function(t) {
        for (var e in this._eventParents)
          this._eventParents[e].fire(t.type, f({
            layer: t.target,
            propagatedFrom: t.target
          }, t), !0);
      }
    };
    J.addEventListener = J.on, J.removeEventListener = J.clearAllEventListeners = J.off, J.addOneTimeEventListener = J.once, J.fireEvent = J.fire, J.hasEventListeners = J.listens;
    var D2 = c2.extend(J);
    function x(t, e, i) {
      this.x = i ? Math.round(t) : t, this.y = i ? Math.round(e) : e;
    }
    var xt = Math.trunc || function(t) {
      return t > 0 ? Math.floor(t) : Math.ceil(t);
    };
    x.prototype = {
      // @method clone(): Point
      // Returns a copy of the current point.
      clone: function() {
        return new x(this.x, this.y);
      },
      // @method add(otherPoint: Point): Point
      // Returns the result of addition of the current and the given points.
      add: function(t) {
        return this.clone()._add(v(t));
      },
      _add: function(t) {
        return this.x += t.x, this.y += t.y, this;
      },
      // @method subtract(otherPoint: Point): Point
      // Returns the result of subtraction of the given point from the current.
      subtract: function(t) {
        return this.clone()._subtract(v(t));
      },
      _subtract: function(t) {
        return this.x -= t.x, this.y -= t.y, this;
      },
      // @method divideBy(num: Number): Point
      // Returns the result of division of the current point by the given number.
      divideBy: function(t) {
        return this.clone()._divideBy(t);
      },
      _divideBy: function(t) {
        return this.x /= t, this.y /= t, this;
      },
      // @method multiplyBy(num: Number): Point
      // Returns the result of multiplication of the current point by the given number.
      multiplyBy: function(t) {
        return this.clone()._multiplyBy(t);
      },
      _multiplyBy: function(t) {
        return this.x *= t, this.y *= t, this;
      },
      // @method scaleBy(scale: Point): Point
      // Multiply each coordinate of the current point by each coordinate of
      // `scale`. In linear algebra terms, multiply the point by the
      // [scaling matrix](https://en.wikipedia.org/wiki/Scaling_%28geometry%29#Matrix_representation)
      // defined by `scale`.
      scaleBy: function(t) {
        return new x(this.x * t.x, this.y * t.y);
      },
      // @method unscaleBy(scale: Point): Point
      // Inverse of `scaleBy`. Divide each coordinate of the current point by
      // each coordinate of `scale`.
      unscaleBy: function(t) {
        return new x(this.x / t.x, this.y / t.y);
      },
      // @method round(): Point
      // Returns a copy of the current point with rounded coordinates.
      round: function() {
        return this.clone()._round();
      },
      _round: function() {
        return this.x = Math.round(this.x), this.y = Math.round(this.y), this;
      },
      // @method floor(): Point
      // Returns a copy of the current point with floored coordinates (rounded down).
      floor: function() {
        return this.clone()._floor();
      },
      _floor: function() {
        return this.x = Math.floor(this.x), this.y = Math.floor(this.y), this;
      },
      // @method ceil(): Point
      // Returns a copy of the current point with ceiled coordinates (rounded up).
      ceil: function() {
        return this.clone()._ceil();
      },
      _ceil: function() {
        return this.x = Math.ceil(this.x), this.y = Math.ceil(this.y), this;
      },
      // @method trunc(): Point
      // Returns a copy of the current point with truncated coordinates (rounded towards zero).
      trunc: function() {
        return this.clone()._trunc();
      },
      _trunc: function() {
        return this.x = xt(this.x), this.y = xt(this.y), this;
      },
      // @method distanceTo(otherPoint: Point): Number
      // Returns the cartesian distance between the current and the given points.
      distanceTo: function(t) {
        t = v(t);
        var e = t.x - this.x, i = t.y - this.y;
        return Math.sqrt(e * e + i * i);
      },
      // @method equals(otherPoint: Point): Boolean
      // Returns `true` if the given point has the same coordinates.
      equals: function(t) {
        return t = v(t), t.x === this.x && t.y === this.y;
      },
      // @method contains(otherPoint: Point): Boolean
      // Returns `true` if both coordinates of the given point are less than the corresponding current point coordinates (in absolute values).
      contains: function(t) {
        return t = v(t), Math.abs(t.x) <= Math.abs(this.x) && Math.abs(t.y) <= Math.abs(this.y);
      },
      // @method toString(): String
      // Returns a string representation of the point for debugging purposes.
      toString: function() {
        return "Point(" + n2(this.x) + ", " + n2(this.y) + ")";
      }
    };
    function v(t, e, i) {
      return t instanceof x ? t : t2(t) ? new x(t[0], t[1]) : t == null ? t : typeof t == "object" && "x" in t && "y" in t ? new x(t.x, t.y) : new x(t, e, i);
    }
    function B(t, e) {
      if (t)
        for (var i = e ? [t, e] : t, n = 0, o = i.length; n < o; n++)
          this.extend(i[n]);
    }
    B.prototype = {
      // @method extend(point: Point): this
      // Extends the bounds to contain the given point.
      // @alternative
      // @method extend(otherBounds: Bounds): this
      // Extend the bounds to contain the given bounds
      extend: function(t) {
        var e, i;
        if (!t)
          return this;
        if (t instanceof x || typeof t[0] == "number" || "x" in t)
          e = i = v(t);
        else if (t = V(t), e = t.min, i = t.max, !e || !i)
          return this;
        return !this.min && !this.max ? (this.min = e.clone(), this.max = i.clone()) : (this.min.x = Math.min(e.x, this.min.x), this.max.x = Math.max(i.x, this.max.x), this.min.y = Math.min(e.y, this.min.y), this.max.y = Math.max(i.y, this.max.y)), this;
      },
      // @method getCenter(round?: Boolean): Point
      // Returns the center point of the bounds.
      getCenter: function(t) {
        return v(
          (this.min.x + this.max.x) / 2,
          (this.min.y + this.max.y) / 2,
          t
        );
      },
      // @method getBottomLeft(): Point
      // Returns the bottom-left point of the bounds.
      getBottomLeft: function() {
        return v(this.min.x, this.max.y);
      },
      // @method getTopRight(): Point
      // Returns the top-right point of the bounds.
      getTopRight: function() {
        return v(this.max.x, this.min.y);
      },
      // @method getTopLeft(): Point
      // Returns the top-left point of the bounds (i.e. [`this.min`](#bounds-min)).
      getTopLeft: function() {
        return this.min;
      },
      // @method getBottomRight(): Point
      // Returns the bottom-right point of the bounds (i.e. [`this.max`](#bounds-max)).
      getBottomRight: function() {
        return this.max;
      },
      // @method getSize(): Point
      // Returns the size of the given bounds
      getSize: function() {
        return this.max.subtract(this.min);
      },
      // @method contains(otherBounds: Bounds): Boolean
      // Returns `true` if the rectangle contains the given one.
      // @alternative
      // @method contains(point: Point): Boolean
      // Returns `true` if the rectangle contains the given point.
      contains: function(t) {
        var e, i;
        return typeof t[0] == "number" || t instanceof x ? t = v(t) : t = V(t), t instanceof B ? (e = t.min, i = t.max) : e = i = t, e.x >= this.min.x && i.x <= this.max.x && e.y >= this.min.y && i.y <= this.max.y;
      },
      // @method intersects(otherBounds: Bounds): Boolean
      // Returns `true` if the rectangle intersects the given bounds. Two bounds
      // intersect if they have at least one point in common.
      intersects: function(t) {
        t = V(t);
        var e = this.min, i = this.max, n = t.min, o = t.max, s = o.x >= e.x && n.x <= i.x, a = o.y >= e.y && n.y <= i.y;
        return s && a;
      },
      // @method overlaps(otherBounds: Bounds): Boolean
      // Returns `true` if the rectangle overlaps the given bounds. Two bounds
      // overlap if their intersection is an area.
      overlaps: function(t) {
        t = V(t);
        var e = this.min, i = this.max, n = t.min, o = t.max, s = o.x > e.x && n.x < i.x, a = o.y > e.y && n.y < i.y;
        return s && a;
      },
      // @method isValid(): Boolean
      // Returns `true` if the bounds are properly initialized.
      isValid: function() {
        return !!(this.min && this.max);
      },
      // @method pad(bufferRatio: Number): Bounds
      // Returns bounds created by extending or retracting the current bounds by a given ratio in each direction.
      // For example, a ratio of 0.5 extends the bounds by 50% in each direction.
      // Negative values will retract the bounds.
      pad: function(t) {
        var e = this.min, i = this.max, n = Math.abs(e.x - i.x) * t, o = Math.abs(e.y - i.y) * t;
        return V(
          v(e.x - n, e.y - o),
          v(i.x + n, i.y + o)
        );
      },
      // @method equals(otherBounds: Bounds): Boolean
      // Returns `true` if the rectangle is equivalent to the given bounds.
      equals: function(t) {
        return t ? (t = V(t), this.min.equals(t.getTopLeft()) && this.max.equals(t.getBottomRight())) : !1;
      }
    };
    function V(t, e) {
      return !t || t instanceof B ? t : new B(t, e);
    }
    function q(t, e) {
      if (t)
        for (var i = e ? [t, e] : t, n = 0, o = i.length; n < o; n++)
          this.extend(i[n]);
    }
    q.prototype = {
      // @method extend(latlng: LatLng): this
      // Extend the bounds to contain the given point
      // @alternative
      // @method extend(otherBounds: LatLngBounds): this
      // Extend the bounds to contain the given bounds
      extend: function(t) {
        var e = this._southWest, i = this._northEast, n, o;
        if (t instanceof S)
          n = t, o = t;
        else if (t instanceof q) {
          if (n = t._southWest, o = t._northEast, !n || !o)
            return this;
        } else
          return t ? this.extend(M(t) || D(t)) : this;
        return !e && !i ? (this._southWest = new S(n.lat, n.lng), this._northEast = new S(o.lat, o.lng)) : (e.lat = Math.min(n.lat, e.lat), e.lng = Math.min(n.lng, e.lng), i.lat = Math.max(o.lat, i.lat), i.lng = Math.max(o.lng, i.lng)), this;
      },
      // @method pad(bufferRatio: Number): LatLngBounds
      // Returns bounds created by extending or retracting the current bounds by a given ratio in each direction.
      // For example, a ratio of 0.5 extends the bounds by 50% in each direction.
      // Negative values will retract the bounds.
      pad: function(t) {
        var e = this._southWest, i = this._northEast, n = Math.abs(e.lat - i.lat) * t, o = Math.abs(e.lng - i.lng) * t;
        return new q(
          new S(e.lat - n, e.lng - o),
          new S(i.lat + n, i.lng + o)
        );
      },
      // @method getCenter(): LatLng
      // Returns the center point of the bounds.
      getCenter: function() {
        return new S(
          (this._southWest.lat + this._northEast.lat) / 2,
          (this._southWest.lng + this._northEast.lng) / 2
        );
      },
      // @method getSouthWest(): LatLng
      // Returns the south-west point of the bounds.
      getSouthWest: function() {
        return this._southWest;
      },
      // @method getNorthEast(): LatLng
      // Returns the north-east point of the bounds.
      getNorthEast: function() {
        return this._northEast;
      },
      // @method getNorthWest(): LatLng
      // Returns the north-west point of the bounds.
      getNorthWest: function() {
        return new S(this.getNorth(), this.getWest());
      },
      // @method getSouthEast(): LatLng
      // Returns the south-east point of the bounds.
      getSouthEast: function() {
        return new S(this.getSouth(), this.getEast());
      },
      // @method getWest(): Number
      // Returns the west longitude of the bounds
      getWest: function() {
        return this._southWest.lng;
      },
      // @method getSouth(): Number
      // Returns the south latitude of the bounds
      getSouth: function() {
        return this._southWest.lat;
      },
      // @method getEast(): Number
      // Returns the east longitude of the bounds
      getEast: function() {
        return this._northEast.lng;
      },
      // @method getNorth(): Number
      // Returns the north latitude of the bounds
      getNorth: function() {
        return this._northEast.lat;
      },
      // @method contains(otherBounds: LatLngBounds): Boolean
      // Returns `true` if the rectangle contains the given one.
      // @alternative
      // @method contains (latlng: LatLng): Boolean
      // Returns `true` if the rectangle contains the given point.
      contains: function(t) {
        typeof t[0] == "number" || t instanceof S || "lat" in t ? t = M(t) : t = D(t);
        var e = this._southWest, i = this._northEast, n, o;
        return t instanceof q ? (n = t.getSouthWest(), o = t.getNorthEast()) : n = o = t, n.lat >= e.lat && o.lat <= i.lat && n.lng >= e.lng && o.lng <= i.lng;
      },
      // @method intersects(otherBounds: LatLngBounds): Boolean
      // Returns `true` if the rectangle intersects the given bounds. Two bounds intersect if they have at least one point in common.
      intersects: function(t) {
        t = D(t);
        var e = this._southWest, i = this._northEast, n = t.getSouthWest(), o = t.getNorthEast(), s = o.lat >= e.lat && n.lat <= i.lat, a = o.lng >= e.lng && n.lng <= i.lng;
        return s && a;
      },
      // @method overlaps(otherBounds: LatLngBounds): Boolean
      // Returns `true` if the rectangle overlaps the given bounds. Two bounds overlap if their intersection is an area.
      overlaps: function(t) {
        t = D(t);
        var e = this._southWest, i = this._northEast, n = t.getSouthWest(), o = t.getNorthEast(), s = o.lat > e.lat && n.lat < i.lat, a = o.lng > e.lng && n.lng < i.lng;
        return s && a;
      },
      // @method toBBoxString(): String
      // Returns a string with bounding box coordinates in a 'southwest_lng,southwest_lat,northeast_lng,northeast_lat' format. Useful for sending requests to web services that return geo data.
      toBBoxString: function() {
        return [this.getWest(), this.getSouth(), this.getEast(), this.getNorth()].join(",");
      },
      // @method equals(otherBounds: LatLngBounds, maxMargin?: Number): Boolean
      // Returns `true` if the rectangle is equivalent (within a small margin of error) to the given bounds. The margin of error can be overridden by setting `maxMargin` to a small number.
      equals: function(t, e) {
        return t ? (t = D(t), this._southWest.equals(t.getSouthWest(), e) && this._northEast.equals(t.getNorthEast(), e)) : !1;
      },
      // @method isValid(): Boolean
      // Returns `true` if the bounds are properly initialized.
      isValid: function() {
        return !!(this._southWest && this._northEast);
      }
    };
    function D(t, e) {
      return t instanceof q ? t : new q(t, e);
    }
    function S(t, e, i) {
      if (isNaN(t) || isNaN(e))
        throw new Error("Invalid LatLng object: (" + t + ", " + e + ")");
      this.lat = +t, this.lng = +e, i !== void 0 && (this.alt = +i);
    }
    S.prototype = {
      // @method equals(otherLatLng: LatLng, maxMargin?: Number): Boolean
      // Returns `true` if the given `LatLng` point is at the same position (within a small margin of error). The margin of error can be overridden by setting `maxMargin` to a small number.
      equals: function(t, e) {
        if (!t)
          return !1;
        t = M(t);
        var i = Math.max(
          Math.abs(this.lat - t.lat),
          Math.abs(this.lng - t.lng)
        );
        return i <= (e === void 0 ? 1e-9 : e);
      },
      // @method toString(): String
      // Returns a string representation of the point (for debugging purposes).
      toString: function(t) {
        return "LatLng(" + n2(this.lat, t) + ", " + n2(this.lng, t) + ")";
      },
      // @method distanceTo(otherLatLng: LatLng): Number
      // Returns the distance (in meters) to the given `LatLng` calculated using the [Spherical Law of Cosines](https://en.wikipedia.org/wiki/Spherical_law_of_cosines).
      distanceTo: function(t) {
        return g2.distance(this, M(t));
      },
      // @method wrap(): LatLng
      // Returns a new `LatLng` object with the longitude wrapped so it's always between -180 and +180 degrees.
      wrap: function() {
        return g2.wrapLatLng(this);
      },
      // @method toBounds(sizeInMeters: Number): LatLngBounds
      // Returns a new `LatLngBounds` object in which each boundary is `sizeInMeters/2` meters apart from the `LatLng`.
      toBounds: function(t) {
        var e = 180 * t / 40075017, i = e / Math.cos(Math.PI / 180 * this.lat);
        return D(
          [this.lat - e, this.lng - i],
          [this.lat + e, this.lng + i]
        );
      },
      clone: function() {
        return new S(this.lat, this.lng, this.alt);
      }
    };
    function M(t, e, i) {
      return t instanceof S ? t : t2(t) && typeof t[0] != "object" ? t.length === 3 ? new S(t[0], t[1], t[2]) : t.length === 2 ? new S(t[0], t[1]) : null : t == null ? t : typeof t == "object" && "lat" in t ? new S(t.lat, "lng" in t ? t.lng : t.lon, t.alt) : e === void 0 ? null : new S(t, e, i);
    }
    var u2 = {
      // @method latLngToPoint(latlng: LatLng, zoom: Number): Point
      // Projects geographical coordinates into pixel coordinates for a given zoom.
      latLngToPoint: function(t, e) {
        var i = this.projection.project(t), n = this.scale(e);
        return this.transformation._transform(i, n);
      },
      // @method pointToLatLng(point: Point, zoom: Number): LatLng
      // The inverse of `latLngToPoint`. Projects pixel coordinates on a given
      // zoom into geographical coordinates.
      pointToLatLng: function(t, e) {
        var i = this.scale(e), n = this.transformation.untransform(t, i);
        return this.projection.unproject(n);
      },
      // @method project(latlng: LatLng): Point
      // Projects geographical coordinates into coordinates in units accepted for
      // this CRS (e.g. meters for EPSG:3857, for passing it to WMS services).
      project: function(t) {
        return this.projection.project(t);
      },
      // @method unproject(point: Point): LatLng
      // Given a projected coordinate returns the corresponding LatLng.
      // The inverse of `project`.
      unproject: function(t) {
        return this.projection.unproject(t);
      },
      // @method scale(zoom: Number): Number
      // Returns the scale used when transforming projected coordinates into
      // pixel coordinates for a particular zoom. For example, it returns
      // `256 * 2^zoom` for Mercator-based CRS.
      scale: function(t) {
        return 256 * Math.pow(2, t);
      },
      // @method zoom(scale: Number): Number
      // Inverse of `scale()`, returns the zoom level corresponding to a scale
      // factor of `scale`.
      zoom: function(t) {
        return Math.log(t / 256) / Math.LN2;
      },
      // @method getProjectedBounds(zoom: Number): Bounds
      // Returns the projection's bounds scaled and transformed for the provided `zoom`.
      getProjectedBounds: function(t) {
        if (this.infinite)
          return null;
        var e = this.projection.bounds, i = this.scale(t), n = this.transformation.transform(e.min, i), o = this.transformation.transform(e.max, i);
        return new B(n, o);
      },
      // @method distance(latlng1: LatLng, latlng2: LatLng): Number
      // Returns the distance between two geographical coordinates.
      // @property code: String
      // Standard code name of the CRS passed into WMS services (e.g. `'EPSG:3857'`)
      //
      // @property wrapLng: Number[]
      // An array of two numbers defining whether the longitude (horizontal) coordinate
      // axis wraps around a given range and how. Defaults to `[-180, 180]` in most
      // geographical CRSs. If `undefined`, the longitude axis does not wrap around.
      //
      // @property wrapLat: Number[]
      // Like `wrapLng`, but for the latitude (vertical) axis.
      // wrapLng: [min, max],
      // wrapLat: [min, max],
      // @property infinite: Boolean
      // If true, the coordinate space will be unbounded (infinite in both axes)
      infinite: !1,
      // @method wrapLatLng(latlng: LatLng): LatLng
      // Returns a `LatLng` where lat and lng has been wrapped according to the
      // CRS's `wrapLat` and `wrapLng` properties, if they are outside the CRS's bounds.
      wrapLatLng: function(t) {
        var e = this.wrapLng ? R2(t.lng, this.wrapLng, !0) : t.lng, i = this.wrapLat ? R2(t.lat, this.wrapLat, !0) : t.lat, n = t.alt;
        return new S(i, e, n);
      },
      // @method wrapLatLngBounds(bounds: LatLngBounds): LatLngBounds
      // Returns a `LatLngBounds` with the same size as the given one, ensuring
      // that its center is within the CRS's bounds.
      // Only accepts actual `L.LatLngBounds` instances, not arrays.
      wrapLatLngBounds: function(t) {
        var e = t.getCenter(), i = this.wrapLatLng(e), n = e.lat - i.lat, o = e.lng - i.lng;
        if (n === 0 && o === 0)
          return t;
        var s = t.getSouthWest(), a = t.getNorthEast(), l = new S(s.lat - n, s.lng - o), c = new S(a.lat - n, a.lng - o);
        return new q(l, c);
      }
    }, g2 = f({}, u2, {
      wrapLng: [-180, 180],
      // Mean Earth Radius, as recommended for use by
      // the International Union of Geodesy and Geophysics,
      // see https://rosettacode.org/wiki/Haversine_formula
      R: 6371e3,
      // distance between two geographical points using spherical law of cosines approximation
      distance: function(t, e) {
        var i = Math.PI / 180, n = t.lat * i, o = e.lat * i, s = Math.sin((e.lat - t.lat) * i / 2), a = Math.sin((e.lng - t.lng) * i / 2), l = s * s + Math.cos(n) * Math.cos(o) * a * a, c = 2 * Math.atan2(Math.sqrt(l), Math.sqrt(1 - l));
        return this.R * c;
      }
    }), wt = 6378137, k0 = {
      R: wt,
      MAX_LATITUDE: 85.0511287798,
      project: function(t) {
        var e = Math.PI / 180, i = this.MAX_LATITUDE, n = Math.max(Math.min(i, t.lat), -i), o = Math.sin(n * e);
        return new x(
          this.R * t.lng * e,
          this.R * Math.log((1 + o) / (1 - o)) / 2
        );
      },
      unproject: function(t) {
        var e = 180 / Math.PI;
        return new S(
          (2 * Math.atan(Math.exp(t.y / this.R)) - Math.PI / 2) * e,
          t.x * e / this.R
        );
      },
      bounds: function() {
        var t = wt * Math.PI;
        return new B([-t, -t], [t, t]);
      }()
    };
    function z0(t, e, i, n) {
      if (t2(t)) {
        this._a = t[0], this._b = t[1], this._c = t[2], this._d = t[3];
        return;
      }
      this._a = t, this._b = e, this._c = i, this._d = n;
    }
    z0.prototype = {
      // @method transform(point: Point, scale?: Number): Point
      // Returns a transformed point, optionally multiplied by the given scale.
      // Only accepts actual `L.Point` instances, not arrays.
      transform: function(t, e) {
        return this._transform(t.clone(), e);
      },
      // destructive transform (faster)
      _transform: function(t, e) {
        return e = e || 1, t.x = e * (this._a * t.x + this._b), t.y = e * (this._c * t.y + this._d), t;
      },
      // @method untransform(point: Point, scale?: Number): Point
      // Returns the reverse transformation of the given point, optionally divided
      // by the given scale. Only accepts actual `L.Point` instances, not arrays.
      untransform: function(t, e) {
        return e = e || 1, new x(
          (t.x / e - this._b) / this._a,
          (t.y / e - this._d) / this._c
        );
      }
    };
    function N2(t, e, i, n) {
      return new z0(t, e, i, n);
    }
    var S0 = f({}, g2, {
      code: "EPSG:3857",
      projection: k0,
      transformation: function() {
        var t = 0.5 / (Math.PI * k0.R);
        return N2(t, 0.5, -t, 0.5);
      }()
    }), Re = f({}, S0, {
      code: "EPSG:900913"
    });
    function Ct(t) {
      return document.createElementNS("http://www.w3.org/2000/svg", t);
    }
    function bt(t, e) {
      var i = "", n, o, s, a, l, c;
      for (n = 0, s = t.length; n < s; n++) {
        for (l = t[n], o = 0, a = l.length; o < a; o++)
          c = l[o], i += (o ? "L" : "M") + c.x + " " + c.y;
        i += e ? p.svg ? "z" : "x" : "";
      }
      return i || "M0 0";
    }
    var E0 = document.documentElement.style, i0 = "ActiveXObject" in window, De = i0 && !document.addEventListener, Lt = "msLaunchUri" in navigator && !("documentMode" in document), A0 = o2("webkit"), Pt = o2("android"), Tt = o2("android 2") || o2("android 3"), Ne = parseInt(/WebKit\/([0-9]+)|$/.exec(navigator.userAgent)[1], 10), Fe = Pt && o2("Google") && Ne < 537 && !("AudioNode" in window), O0 = !!window.opera, Mt = !Lt && o2("chrome"), kt = o2("gecko") && !A0 && !O0 && !i0, We = !Mt && o2("safari"), zt = o2("phantom"), St = "OTransition" in E0, He = navigator.platform.indexOf("Win") === 0, Et = i0 && "transition" in E0, B0 = "WebKitCSSMatrix" in window && "m11" in new window.WebKitCSSMatrix() && !Tt, At = "MozPerspective" in E0, Ge = !window.L_DISABLE_3D && (Et || B0 || At) && !St && !zt, F2 = typeof orientation < "u" || o2("mobile"), Ue = F2 && A0, Ve = F2 && B0, Ot = !window.PointerEvent && window.MSPointerEvent, Bt = !!(window.PointerEvent || Ot), Zt = "ontouchstart" in window || !!window.TouchEvent, qe = !window.L_NO_TOUCH && (Zt || Bt), Ye = F2 && O0, je = F2 && kt, Je = (window.devicePixelRatio || window.screen.deviceXDPI / window.screen.logicalXDPI) > 1, Ke = function() {
      var t = !1;
      try {
        var e = Object.defineProperty({}, "passive", {
          get: function() {
            t = !0;
          }
        });
        window.addEventListener("testPassiveEventSupport", I, e), window.removeEventListener("testPassiveEventSupport", I, e);
      } catch {
      }
      return t;
    }(), Xe = function() {
      return !!document.createElement("canvas").getContext;
    }(), Z0 = !!(document.createElementNS && Ct("svg").createSVGRect), Qe = !!Z0 && function() {
      var t = document.createElement("div");
      return t.innerHTML = "<svg/>", (t.firstChild && t.firstChild.namespaceURI) === "http://www.w3.org/2000/svg";
    }(), $e = !Z0 && function() {
      try {
        var t = document.createElement("div");
        t.innerHTML = '<v:shape adj="1"/>';
        var e = t.firstChild;
        return e.style.behavior = "url(#default#VML)", e && typeof e.adj == "object";
      } catch {
        return !1;
      }
    }(), ti = navigator.platform.indexOf("Mac") === 0, ei = navigator.platform.indexOf("Linux") === 0;
    function o2(t) {
      return navigator.userAgent.toLowerCase().indexOf(t) >= 0;
    }
    var p = {
      ie: i0,
      ielt9: De,
      edge: Lt,
      webkit: A0,
      android: Pt,
      android23: Tt,
      androidStock: Fe,
      opera: O0,
      chrome: Mt,
      gecko: kt,
      safari: We,
      phantom: zt,
      opera12: St,
      win: He,
      ie3d: Et,
      webkit3d: B0,
      gecko3d: At,
      any3d: Ge,
      mobile: F2,
      mobileWebkit: Ue,
      mobileWebkit3d: Ve,
      msPointer: Ot,
      pointer: Bt,
      touch: qe,
      touchNative: Zt,
      mobileOpera: Ye,
      mobileGecko: je,
      retina: Je,
      passiveEvents: Ke,
      canvas: Xe,
      svg: Z0,
      vml: $e,
      inlineSvg: Qe,
      mac: ti,
      linux: ei
    }, It = p.msPointer ? "MSPointerDown" : "pointerdown", Rt = p.msPointer ? "MSPointerMove" : "pointermove", Dt = p.msPointer ? "MSPointerUp" : "pointerup", Nt = p.msPointer ? "MSPointerCancel" : "pointercancel", I0 = {
      touchstart: It,
      touchmove: Rt,
      touchend: Dt,
      touchcancel: Nt
    }, Ft = {
      touchstart: ai,
      touchmove: n0,
      touchend: n0,
      touchcancel: n0
    }, M2 = {}, Wt = !1;
    function ii(t, e, i) {
      return e === "touchstart" && ri(), Ft[e] ? (i = Ft[e].bind(this, i), t.addEventListener(I0[e], i, !1), i) : (console.warn("wrong event specified:", e), I);
    }
    function ni(t, e, i) {
      if (!I0[e]) {
        console.warn("wrong event specified:", e);
        return;
      }
      t.removeEventListener(I0[e], i, !1);
    }
    function oi(t) {
      M2[t.pointerId] = t;
    }
    function si(t) {
      M2[t.pointerId] && (M2[t.pointerId] = t);
    }
    function Ht(t) {
      delete M2[t.pointerId];
    }
    function ri() {
      Wt || (document.addEventListener(It, oi, !0), document.addEventListener(Rt, si, !0), document.addEventListener(Dt, Ht, !0), document.addEventListener(Nt, Ht, !0), Wt = !0);
    }
    function n0(t, e) {
      if (e.pointerType !== (e.MSPOINTER_TYPE_MOUSE || "mouse")) {
        e.touches = [];
        for (var i in M2)
          e.touches.push(M2[i]);
        e.changedTouches = [e], t(e);
      }
    }
    function ai(t, e) {
      e.MSPOINTER_TYPE_TOUCH && e.pointerType === e.MSPOINTER_TYPE_TOUCH && H(e), n0(t, e);
    }
    function hi(t) {
      var e = {}, i, n;
      for (n in t)
        i = t[n], e[n] = i && i.bind ? i.bind(t) : i;
      return t = e, e.type = "dblclick", e.detail = 2, e.isTrusted = !1, e._simulated = !0, e;
    }
    var li = 200;
    function ci(t, e) {
      t.addEventListener("dblclick", e);
      var i = 0, n;
      function o(s) {
        if (s.detail !== 1) {
          n = s.detail;
          return;
        }
        if (!(s.pointerType === "mouse" || s.sourceCapabilities && !s.sourceCapabilities.firesTouchEvents)) {
          var a = Yt(s);
          if (!(a.some(function(c) {
            return c instanceof HTMLLabelElement && c.attributes.for;
          }) && !a.some(function(c) {
            return c instanceof HTMLInputElement || c instanceof HTMLSelectElement;
          }))) {
            var l = Date.now();
            l - i <= li ? (n++, n === 2 && e(hi(s))) : n = 1, i = l;
          }
        }
      }
      return t.addEventListener("click", o), {
        dblclick: e,
        simDblclick: o
      };
    }
    function ui(t, e) {
      t.removeEventListener("dblclick", e.dblclick), t.removeEventListener("click", e.simDblclick);
    }
    var R0 = r0(
      ["transform", "webkitTransform", "OTransform", "MozTransform", "msTransform"]
    ), W2 = r0(
      ["webkitTransition", "transition", "OTransition", "MozTransition", "msTransition"]
    ), Gt = W2 === "webkitTransition" || W2 === "OTransition" ? W2 + "End" : "transitionend";
    function Ut(t) {
      return typeof t == "string" ? document.getElementById(t) : t;
    }
    function H2(t, e) {
      var i = t.style[e] || t.currentStyle && t.currentStyle[e];
      if ((!i || i === "auto") && document.defaultView) {
        var n = document.defaultView.getComputedStyle(t, null);
        i = n ? n[e] : null;
      }
      return i === "auto" ? null : i;
    }
    function z(t, e, i) {
      var n = document.createElement(t);
      return n.className = e || "", i && i.appendChild(n), n;
    }
    function Z(t) {
      var e = t.parentNode;
      e && e.removeChild(t);
    }
    function o0(t) {
      for (; t.firstChild; )
        t.removeChild(t.firstChild);
    }
    function k2(t) {
      var e = t.parentNode;
      e && e.lastChild !== t && e.appendChild(t);
    }
    function z2(t) {
      var e = t.parentNode;
      e && e.firstChild !== t && e.insertBefore(t, e.firstChild);
    }
    function D0(t, e) {
      if (t.classList !== void 0)
        return t.classList.contains(e);
      var i = s0(t);
      return i.length > 0 && new RegExp("(^|\\s)" + e + "(\\s|$)").test(i);
    }
    function b(t, e) {
      if (t.classList !== void 0)
        for (var i = x2(e), n = 0, o = i.length; n < o; n++)
          t.classList.add(i[n]);
      else if (!D0(t, e)) {
        var s = s0(t);
        N0(t, (s ? s + " " : "") + e);
      }
    }
    function R(t, e) {
      t.classList !== void 0 ? t.classList.remove(e) : N0(t, L0((" " + s0(t) + " ").replace(" " + e + " ", " ")));
    }
    function N0(t, e) {
      t.className.baseVal === void 0 ? t.className = e : t.className.baseVal = e;
    }
    function s0(t) {
      return t.correspondingElement && (t = t.correspondingElement), t.className.baseVal === void 0 ? t.className : t.className.baseVal;
    }
    function X(t, e) {
      "opacity" in t.style ? t.style.opacity = e : "filter" in t.style && fi(t, e);
    }
    function fi(t, e) {
      var i = !1, n = "DXImageTransform.Microsoft.Alpha";
      try {
        i = t.filters.item(n);
      } catch {
        if (e === 1)
          return;
      }
      e = Math.round(e * 100), i ? (i.Enabled = e !== 100, i.Opacity = e) : t.style.filter += " progid:" + n + "(opacity=" + e + ")";
    }
    function r0(t) {
      for (var e = document.documentElement.style, i = 0; i < t.length; i++)
        if (t[i] in e)
          return t[i];
      return !1;
    }
    function w2(t, e, i) {
      var n = e || new x(0, 0);
      t.style[R0] = (p.ie3d ? "translate(" + n.x + "px," + n.y + "px)" : "translate3d(" + n.x + "px," + n.y + "px,0)") + (i ? " scale(" + i + ")" : "");
    }
    function N(t, e) {
      t._leaflet_pos = e, p.any3d ? w2(t, e) : (t.style.left = e.x + "px", t.style.top = e.y + "px");
    }
    function C2(t) {
      return t._leaflet_pos || new x(0, 0);
    }
    var G2, U2, F0;
    if ("onselectstart" in document)
      G2 = function() {
        w(window, "selectstart", H);
      }, U2 = function() {
        A(window, "selectstart", H);
      };
    else {
      var V2 = r0(
        ["userSelect", "WebkitUserSelect", "OUserSelect", "MozUserSelect", "msUserSelect"]
      );
      G2 = function() {
        if (V2) {
          var t = document.documentElement.style;
          F0 = t[V2], t[V2] = "none";
        }
      }, U2 = function() {
        V2 && (document.documentElement.style[V2] = F0, F0 = void 0);
      };
    }
    function W0() {
      w(window, "dragstart", H);
    }
    function H0() {
      A(window, "dragstart", H);
    }
    var a0, G0;
    function U0(t) {
      for (; t.tabIndex === -1; )
        t = t.parentNode;
      t.style && (h0(), a0 = t, G0 = t.style.outlineStyle, t.style.outlineStyle = "none", w(window, "keydown", h0));
    }
    function h0() {
      a0 && (a0.style.outlineStyle = G0, a0 = void 0, G0 = void 0, A(window, "keydown", h0));
    }
    function Vt(t) {
      do
        t = t.parentNode;
      while ((!t.offsetWidth || !t.offsetHeight) && t !== document.body);
      return t;
    }
    function V0(t) {
      var e = t.getBoundingClientRect();
      return {
        x: e.width / t.offsetWidth || 1,
        y: e.height / t.offsetHeight || 1,
        boundingClientRect: e
      };
    }
    var di = {
      __proto__: null,
      TRANSFORM: R0,
      TRANSITION: W2,
      TRANSITION_END: Gt,
      get: Ut,
      getStyle: H2,
      create: z,
      remove: Z,
      empty: o0,
      toFront: k2,
      toBack: z2,
      hasClass: D0,
      addClass: b,
      removeClass: R,
      setClass: N0,
      getClass: s0,
      setOpacity: X,
      testProp: r0,
      setTransform: w2,
      setPosition: N,
      getPosition: C2,
      get disableTextSelection() {
        return G2;
      },
      get enableTextSelection() {
        return U2;
      },
      disableImageDrag: W0,
      enableImageDrag: H0,
      preventOutline: U0,
      restoreOutline: h0,
      getSizedParentNode: Vt,
      getScale: V0
    };
    function w(t, e, i, n) {
      if (e && typeof e == "object")
        for (var o in e)
          Y0(t, o, e[o], i);
      else {
        e = x2(e);
        for (var s = 0, a = e.length; s < a; s++)
          Y0(t, e[s], i, n);
      }
      return this;
    }
    var s2 = "_leaflet_events";
    function A(t, e, i, n) {
      if (arguments.length === 1)
        qt(t), delete t[s2];
      else if (e && typeof e == "object")
        for (var o in e)
          j0(t, o, e[o], i);
      else if (e = x2(e), arguments.length === 2)
        qt(t, function(l) {
          return P0(e, l) !== -1;
        });
      else
        for (var s = 0, a = e.length; s < a; s++)
          j0(t, e[s], i, n);
      return this;
    }
    function qt(t, e) {
      for (var i in t[s2]) {
        var n = i.split(/\d/)[0];
        (!e || e(n)) && j0(t, n, null, null, i);
      }
    }
    var q0 = {
      mouseenter: "mouseover",
      mouseleave: "mouseout",
      wheel: !("onwheel" in window) && "mousewheel"
    };
    function Y0(t, e, i, n) {
      var o = e + C(i) + (n ? "_" + C(n) : "");
      if (t[s2] && t[s2][o])
        return this;
      var s = function(l) {
        return i.call(n || t, l || window.event);
      }, a = s;
      !p.touchNative && p.pointer && e.indexOf("touch") === 0 ? s = ii(t, e, s) : p.touch && e === "dblclick" ? s = ci(t, s) : "addEventListener" in t ? e === "touchstart" || e === "touchmove" || e === "wheel" || e === "mousewheel" ? t.addEventListener(q0[e] || e, s, p.passiveEvents ? { passive: !1 } : !1) : e === "mouseenter" || e === "mouseleave" ? (s = function(l) {
        l = l || window.event, K0(t, l) && a(l);
      }, t.addEventListener(q0[e], s, !1)) : t.addEventListener(e, a, !1) : t.attachEvent("on" + e, s), t[s2] = t[s2] || {}, t[s2][o] = s;
    }
    function j0(t, e, i, n, o) {
      o = o || e + C(i) + (n ? "_" + C(n) : "");
      var s = t[s2] && t[s2][o];
      if (!s)
        return this;
      !p.touchNative && p.pointer && e.indexOf("touch") === 0 ? ni(t, e, s) : p.touch && e === "dblclick" ? ui(t, s) : "removeEventListener" in t ? t.removeEventListener(q0[e] || e, s, !1) : t.detachEvent("on" + e, s), t[s2][o] = null;
    }
    function b2(t) {
      return t.stopPropagation ? t.stopPropagation() : t.originalEvent ? t.originalEvent._stopped = !0 : t.cancelBubble = !0, this;
    }
    function J0(t) {
      return Y0(t, "wheel", b2), this;
    }
    function q2(t) {
      return w(t, "mousedown touchstart dblclick contextmenu", b2), t._leaflet_disable_click = !0, this;
    }
    function H(t) {
      return t.preventDefault ? t.preventDefault() : t.returnValue = !1, this;
    }
    function L2(t) {
      return H(t), b2(t), this;
    }
    function Yt(t) {
      if (t.composedPath)
        return t.composedPath();
      for (var e = [], i = t.target; i; )
        e.push(i), i = i.parentNode;
      return e;
    }
    function jt(t, e) {
      if (!e)
        return new x(t.clientX, t.clientY);
      var i = V0(e), n = i.boundingClientRect;
      return new x(
        // offset.left/top values are in page scale (like clientX/Y),
        // whereas clientLeft/Top (border width) values are the original values (before CSS scale applies).
        (t.clientX - n.left) / i.x - e.clientLeft,
        (t.clientY - n.top) / i.y - e.clientTop
      );
    }
    var _i = p.linux && p.chrome ? window.devicePixelRatio : p.mac ? window.devicePixelRatio * 3 : window.devicePixelRatio > 0 ? 2 * window.devicePixelRatio : 1;
    function Jt(t) {
      return p.edge ? t.wheelDeltaY / 2 : (
        // Don't trust window-geometry-based delta
        t.deltaY && t.deltaMode === 0 ? -t.deltaY / _i : (
          // Pixels
          t.deltaY && t.deltaMode === 1 ? -t.deltaY * 20 : (
            // Lines
            t.deltaY && t.deltaMode === 2 ? -t.deltaY * 60 : (
              // Pages
              t.deltaX || t.deltaZ ? 0 : (
                // Skip horizontal/depth wheel events
                t.wheelDelta ? (t.wheelDeltaY || t.wheelDelta) / 2 : (
                  // Legacy IE pixels
                  t.detail && Math.abs(t.detail) < 32765 ? -t.detail * 20 : (
                    // Legacy Moz lines
                    t.detail ? t.detail / -32765 * 60 : (
                      // Legacy Moz pages
                      0
                    )
                  )
                )
              )
            )
          )
        )
      );
    }
    function K0(t, e) {
      var i = e.relatedTarget;
      if (!i)
        return !0;
      try {
        for (; i && i !== t; )
          i = i.parentNode;
      } catch {
        return !1;
      }
      return i !== t;
    }
    var mi = {
      __proto__: null,
      on: w,
      off: A,
      stopPropagation: b2,
      disableScrollPropagation: J0,
      disableClickPropagation: q2,
      preventDefault: H,
      stop: L2,
      getPropagationPath: Yt,
      getMousePosition: jt,
      getWheelDelta: Jt,
      isExternalTarget: K0,
      addListener: w,
      removeListener: A
    }, Kt = D2.extend({
      // @method run(el: HTMLElement, newPos: Point, duration?: Number, easeLinearity?: Number)
      // Run an animation of a given element to a new position, optionally setting
      // duration in seconds (`0.25` by default) and easing linearity factor (3rd
      // argument of the [cubic bezier curve](https://cubic-bezier.com/#0,0,.5,1),
      // `0.5` by default).
      run: function(t, e, i, n) {
        this.stop(), this._el = t, this._inProgress = !0, this._duration = i || 0.25, this._easeOutPower = 1 / Math.max(n || 0.5, 0.2), this._startPos = C2(t), this._offset = e.subtract(this._startPos), this._startTime = +/* @__PURE__ */ new Date(), this.fire("start"), this._animate();
      },
      // @method stop()
      // Stops the animation (if currently running).
      stop: function() {
        this._inProgress && (this._step(!0), this._complete());
      },
      _animate: function() {
        this._animId = U(this._animate, this), this._step();
      },
      _step: function(t) {
        var e = +/* @__PURE__ */ new Date() - this._startTime, i = this._duration * 1e3;
        e < i ? this._runFrame(this._easeOut(e / i), t) : (this._runFrame(1), this._complete());
      },
      _runFrame: function(t, e) {
        var i = this._startPos.add(this._offset.multiplyBy(t));
        e && i._round(), N(this._el, i), this.fire("step");
      },
      _complete: function() {
        K(this._animId), this._inProgress = !1, this.fire("end");
      },
      _easeOut: function(t) {
        return 1 - Math.pow(1 - t, this._easeOutPower);
      }
    }), k = D2.extend({
      options: {
        // @section Map State Options
        // @option crs: CRS = L.CRS.EPSG3857
        // The [Coordinate Reference System](#crs) to use. Don't change this if you're not
        // sure what it means.
        crs: S0,
        // @option center: LatLng = undefined
        // Initial geographic center of the map
        center: void 0,
        // @option zoom: Number = undefined
        // Initial map zoom level
        zoom: void 0,
        // @option minZoom: Number = *
        // Minimum zoom level of the map.
        // If not specified and at least one `GridLayer` or `TileLayer` is in the map,
        // the lowest of their `minZoom` options will be used instead.
        minZoom: void 0,
        // @option maxZoom: Number = *
        // Maximum zoom level of the map.
        // If not specified and at least one `GridLayer` or `TileLayer` is in the map,
        // the highest of their `maxZoom` options will be used instead.
        maxZoom: void 0,
        // @option layers: Layer[] = []
        // Array of layers that will be added to the map initially
        layers: [],
        // @option maxBounds: LatLngBounds = null
        // When this option is set, the map restricts the view to the given
        // geographical bounds, bouncing the user back if the user tries to pan
        // outside the view. To set the restriction dynamically, use
        // [`setMaxBounds`](#map-setmaxbounds) method.
        maxBounds: void 0,
        // @option renderer: Renderer = *
        // The default method for drawing vector layers on the map. `L.SVG`
        // or `L.Canvas` by default depending on browser support.
        renderer: void 0,
        // @section Animation Options
        // @option zoomAnimation: Boolean = true
        // Whether the map zoom animation is enabled. By default it's enabled
        // in all browsers that support CSS3 Transitions except Android.
        zoomAnimation: !0,
        // @option zoomAnimationThreshold: Number = 4
        // Won't animate zoom if the zoom difference exceeds this value.
        zoomAnimationThreshold: 4,
        // @option fadeAnimation: Boolean = true
        // Whether the tile fade animation is enabled. By default it's enabled
        // in all browsers that support CSS3 Transitions except Android.
        fadeAnimation: !0,
        // @option markerZoomAnimation: Boolean = true
        // Whether markers animate their zoom with the zoom animation, if disabled
        // they will disappear for the length of the animation. By default it's
        // enabled in all browsers that support CSS3 Transitions except Android.
        markerZoomAnimation: !0,
        // @option transform3DLimit: Number = 2^23
        // Defines the maximum size of a CSS translation transform. The default
        // value should not be changed unless a web browser positions layers in
        // the wrong place after doing a large `panBy`.
        transform3DLimit: 8388608,
        // Precision limit of a 32-bit float
        // @section Interaction Options
        // @option zoomSnap: Number = 1
        // Forces the map's zoom level to always be a multiple of this, particularly
        // right after a [`fitBounds()`](#map-fitbounds) or a pinch-zoom.
        // By default, the zoom level snaps to the nearest integer; lower values
        // (e.g. `0.5` or `0.1`) allow for greater granularity. A value of `0`
        // means the zoom level will not be snapped after `fitBounds` or a pinch-zoom.
        zoomSnap: 1,
        // @option zoomDelta: Number = 1
        // Controls how much the map's zoom level will change after a
        // [`zoomIn()`](#map-zoomin), [`zoomOut()`](#map-zoomout), pressing `+`
        // or `-` on the keyboard, or using the [zoom controls](#control-zoom).
        // Values smaller than `1` (e.g. `0.5`) allow for greater granularity.
        zoomDelta: 1,
        // @option trackResize: Boolean = true
        // Whether the map automatically handles browser window resize to update itself.
        trackResize: !0
      },
      initialize: function(t, e) {
        e = O(this, e), this._handlers = [], this._layers = {}, this._zoomBoundLayers = {}, this._sizeChanged = !0, this._initContainer(t), this._initLayout(), this._onResize = T(this._onResize, this), this._initEvents(), e.maxBounds && this.setMaxBounds(e.maxBounds), e.zoom !== void 0 && (this._zoom = this._limitZoom(e.zoom)), e.center && e.zoom !== void 0 && this.setView(M(e.center), e.zoom, { reset: !0 }), this.callInitHooks(), this._zoomAnimated = W2 && p.any3d && !p.mobileOpera && this.options.zoomAnimation, this._zoomAnimated && (this._createAnimProxy(), w(this._proxy, Gt, this._catchTransitionEnd, this)), this._addLayers(this.options.layers);
      },
      // @section Methods for modifying map state
      // @method setView(center: LatLng, zoom: Number, options?: Zoom/pan options): this
      // Sets the view of the map (geographical center and zoom) with the given
      // animation options.
      setView: function(t, e, i) {
        if (e = e === void 0 ? this._zoom : this._limitZoom(e), t = this._limitCenter(M(t), e, this.options.maxBounds), i = i || {}, this._stop(), this._loaded && !i.reset && i !== !0) {
          i.animate !== void 0 && (i.zoom = f({ animate: i.animate }, i.zoom), i.pan = f({ animate: i.animate, duration: i.duration }, i.pan));
          var n = this._zoom !== e ? this._tryAnimatedZoom && this._tryAnimatedZoom(t, e, i.zoom) : this._tryAnimatedPan(t, i.pan);
          if (n)
            return clearTimeout(this._sizeTimer), this;
        }
        return this._resetView(t, e, i.pan && i.pan.noMoveStart), this;
      },
      // @method setZoom(zoom: Number, options?: Zoom/pan options): this
      // Sets the zoom of the map.
      setZoom: function(t, e) {
        return this._loaded ? this.setView(this.getCenter(), t, { zoom: e }) : (this._zoom = t, this);
      },
      // @method zoomIn(delta?: Number, options?: Zoom options): this
      // Increases the zoom of the map by `delta` ([`zoomDelta`](#map-zoomdelta) by default).
      zoomIn: function(t, e) {
        return t = t || (p.any3d ? this.options.zoomDelta : 1), this.setZoom(this._zoom + t, e);
      },
      // @method zoomOut(delta?: Number, options?: Zoom options): this
      // Decreases the zoom of the map by `delta` ([`zoomDelta`](#map-zoomdelta) by default).
      zoomOut: function(t, e) {
        return t = t || (p.any3d ? this.options.zoomDelta : 1), this.setZoom(this._zoom - t, e);
      },
      // @method setZoomAround(latlng: LatLng, zoom: Number, options: Zoom options): this
      // Zooms the map while keeping a specified geographical point on the map
      // stationary (e.g. used internally for scroll zoom and double-click zoom).
      // @alternative
      // @method setZoomAround(offset: Point, zoom: Number, options: Zoom options): this
      // Zooms the map while keeping a specified pixel on the map (relative to the top-left corner) stationary.
      setZoomAround: function(t, e, i) {
        var n = this.getZoomScale(e), o = this.getSize().divideBy(2), s = t instanceof x ? t : this.latLngToContainerPoint(t), a = s.subtract(o).multiplyBy(1 - 1 / n), l = this.containerPointToLatLng(o.add(a));
        return this.setView(l, e, { zoom: i });
      },
      _getBoundsCenterZoom: function(t, e) {
        e = e || {}, t = t.getBounds ? t.getBounds() : D(t);
        var i = v(e.paddingTopLeft || e.padding || [0, 0]), n = v(e.paddingBottomRight || e.padding || [0, 0]), o = this.getBoundsZoom(t, !1, i.add(n));
        if (o = typeof e.maxZoom == "number" ? Math.min(e.maxZoom, o) : o, o === 1 / 0)
          return {
            center: t.getCenter(),
            zoom: o
          };
        var s = n.subtract(i).divideBy(2), a = this.project(t.getSouthWest(), o), l = this.project(t.getNorthEast(), o), c = this.unproject(a.add(l).divideBy(2).add(s), o);
        return {
          center: c,
          zoom: o
        };
      },
      // @method fitBounds(bounds: LatLngBounds, options?: fitBounds options): this
      // Sets a map view that contains the given geographical bounds with the
      // maximum zoom level possible.
      fitBounds: function(t, e) {
        if (t = D(t), !t.isValid())
          throw new Error("Bounds are not valid.");
        var i = this._getBoundsCenterZoom(t, e);
        return this.setView(i.center, i.zoom, e);
      },
      // @method fitWorld(options?: fitBounds options): this
      // Sets a map view that mostly contains the whole world with the maximum
      // zoom level possible.
      fitWorld: function(t) {
        return this.fitBounds([[-90, -180], [90, 180]], t);
      },
      // @method panTo(latlng: LatLng, options?: Pan options): this
      // Pans the map to a given center.
      panTo: function(t, e) {
        return this.setView(t, this._zoom, { pan: e });
      },
      // @method panBy(offset: Point, options?: Pan options): this
      // Pans the map by a given number of pixels (animated).
      panBy: function(t, e) {
        if (t = v(t).round(), e = e || {}, !t.x && !t.y)
          return this.fire("moveend");
        if (e.animate !== !0 && !this.getSize().contains(t))
          return this._resetView(this.unproject(this.project(this.getCenter()).add(t)), this.getZoom()), this;
        if (this._panAnim || (this._panAnim = new Kt(), this._panAnim.on({
          step: this._onPanTransitionStep,
          end: this._onPanTransitionEnd
        }, this)), e.noMoveStart || this.fire("movestart"), e.animate !== !1) {
          b(this._mapPane, "leaflet-pan-anim");
          var i = this._getMapPanePos().subtract(t).round();
          this._panAnim.run(this._mapPane, i, e.duration || 0.25, e.easeLinearity);
        } else
          this._rawPanBy(t), this.fire("move").fire("moveend");
        return this;
      },
      // @method flyTo(latlng: LatLng, zoom?: Number, options?: Zoom/pan options): this
      // Sets the view of the map (geographical center and zoom) performing a smooth
      // pan-zoom animation.
      flyTo: function(t, e, i) {
        if (i = i || {}, i.animate === !1 || !p.any3d)
          return this.setView(t, e, i);
        this._stop();
        var n = this.project(this.getCenter()), o = this.project(t), s = this.getSize(), a = this._zoom;
        t = M(t), e = e === void 0 ? a : e;
        var l = Math.max(s.x, s.y), c = l * this.getZoomScale(a, e), u = o.distanceTo(n) || 1, _ = 1.42, g = _ * _;
        function P(F) {
          var x0 = F ? -1 : 1, on = F ? c : l, sn = c * c - l * l + x0 * g * g * u * u, rn = 2 * on * g * u, at = sn / rn, Se = Math.sqrt(at * at + 1) - at, an = Se < 1e-9 ? -18 : Math.log(Se);
          return an;
        }
        function G(F) {
          return (Math.exp(F) - Math.exp(-F)) / 2;
        }
        function W(F) {
          return (Math.exp(F) + Math.exp(-F)) / 2;
        }
        function $(F) {
          return G(F) / W(F);
        }
        var Y = P(0);
        function Z2(F) {
          return l * (W(Y) / W(Y + _ * F));
        }
        function $i(F) {
          return l * (W(Y) * $(Y + _ * F) - G(Y)) / g;
        }
        function tn(F) {
          return 1 - Math.pow(1 - F, 1.5);
        }
        var en = Date.now(), ke = (P(1) - Y) / _, nn = i.duration ? 1e3 * i.duration : 1e3 * ke * 0.8;
        function ze() {
          var F = (Date.now() - en) / nn, x0 = tn(F) * ke;
          F <= 1 ? (this._flyToFrame = U(ze, this), this._move(
            this.unproject(n.add(o.subtract(n).multiplyBy($i(x0) / u)), a),
            this.getScaleZoom(l / Z2(x0), a),
            { flyTo: !0 }
          )) : this._move(t, e)._moveEnd(!0);
        }
        return this._moveStart(!0, i.noMoveStart), ze.call(this), this;
      },
      // @method flyToBounds(bounds: LatLngBounds, options?: fitBounds options): this
      // Sets the view of the map with a smooth animation like [`flyTo`](#map-flyto),
      // but takes a bounds parameter like [`fitBounds`](#map-fitbounds).
      flyToBounds: function(t, e) {
        var i = this._getBoundsCenterZoom(t, e);
        return this.flyTo(i.center, i.zoom, e);
      },
      // @method setMaxBounds(bounds: LatLngBounds): this
      // Restricts the map view to the given bounds (see the [maxBounds](#map-maxbounds) option).
      setMaxBounds: function(t) {
        return t = D(t), this.listens("moveend", this._panInsideMaxBounds) && this.off("moveend", this._panInsideMaxBounds), t.isValid() ? (this.options.maxBounds = t, this._loaded && this._panInsideMaxBounds(), this.on("moveend", this._panInsideMaxBounds)) : (this.options.maxBounds = null, this);
      },
      // @method setMinZoom(zoom: Number): this
      // Sets the lower limit for the available zoom levels (see the [minZoom](#map-minzoom) option).
      setMinZoom: function(t) {
        var e = this.options.minZoom;
        return this.options.minZoom = t, this._loaded && e !== t && (this.fire("zoomlevelschange"), this.getZoom() < this.options.minZoom) ? this.setZoom(t) : this;
      },
      // @method setMaxZoom(zoom: Number): this
      // Sets the upper limit for the available zoom levels (see the [maxZoom](#map-maxzoom) option).
      setMaxZoom: function(t) {
        var e = this.options.maxZoom;
        return this.options.maxZoom = t, this._loaded && e !== t && (this.fire("zoomlevelschange"), this.getZoom() > this.options.maxZoom) ? this.setZoom(t) : this;
      },
      // @method panInsideBounds(bounds: LatLngBounds, options?: Pan options): this
      // Pans the map to the closest view that would lie inside the given bounds (if it's not already), controlling the animation using the options specific, if any.
      panInsideBounds: function(t, e) {
        this._enforcingBounds = !0;
        var i = this.getCenter(), n = this._limitCenter(i, this._zoom, D(t));
        return i.equals(n) || this.panTo(n, e), this._enforcingBounds = !1, this;
      },
      // @method panInside(latlng: LatLng, options?: padding options): this
      // Pans the map the minimum amount to make the `latlng` visible. Use
      // padding options to fit the display to more restricted bounds.
      // If `latlng` is already within the (optionally padded) display bounds,
      // the map will not be panned.
      panInside: function(t, e) {
        e = e || {};
        var i = v(e.paddingTopLeft || e.padding || [0, 0]), n = v(e.paddingBottomRight || e.padding || [0, 0]), o = this.project(this.getCenter()), s = this.project(t), a = this.getPixelBounds(), l = V([a.min.add(i), a.max.subtract(n)]), c = l.getSize();
        if (!l.contains(s)) {
          this._enforcingBounds = !0;
          var u = s.subtract(l.getCenter()), _ = l.extend(s).getSize().subtract(c);
          o.x += u.x < 0 ? -_.x : _.x, o.y += u.y < 0 ? -_.y : _.y, this.panTo(this.unproject(o), e), this._enforcingBounds = !1;
        }
        return this;
      },
      // @method invalidateSize(options: Zoom/pan options): this
      // Checks if the map container size changed and updates the map if so —
      // call it after you've changed the map size dynamically, also animating
      // pan by default. If `options.pan` is `false`, panning will not occur.
      // If `options.debounceMoveend` is `true`, it will delay `moveend` event so
      // that it doesn't happen often even if the method is called many
      // times in a row.
      // @alternative
      // @method invalidateSize(animate: Boolean): this
      // Checks if the map container size changed and updates the map if so —
      // call it after you've changed the map size dynamically, also animating
      // pan by default.
      invalidateSize: function(t) {
        if (!this._loaded)
          return this;
        t = f({
          animate: !1,
          pan: !0
        }, t === !0 ? { animate: !0 } : t);
        var e = this.getSize();
        this._sizeChanged = !0, this._lastCenter = null;
        var i = this.getSize(), n = e.divideBy(2).round(), o = i.divideBy(2).round(), s = n.subtract(o);
        return !s.x && !s.y ? this : (t.animate && t.pan ? this.panBy(s) : (t.pan && this._rawPanBy(s), this.fire("move"), t.debounceMoveend ? (clearTimeout(this._sizeTimer), this._sizeTimer = setTimeout(T(this.fire, this, "moveend"), 200)) : this.fire("moveend")), this.fire("resize", {
          oldSize: e,
          newSize: i
        }));
      },
      // @section Methods for modifying map state
      // @method stop(): this
      // Stops the currently running `panTo` or `flyTo` animation, if any.
      stop: function() {
        return this.setZoom(this._limitZoom(this._zoom)), this.options.zoomSnap || this.fire("viewreset"), this._stop();
      },
      // @section Geolocation methods
      // @method locate(options?: Locate options): this
      // Tries to locate the user using the Geolocation API, firing a [`locationfound`](#map-locationfound)
      // event with location data on success or a [`locationerror`](#map-locationerror) event on failure,
      // and optionally sets the map view to the user's location with respect to
      // detection accuracy (or to the world view if geolocation failed).
      // Note that, if your page doesn't use HTTPS, this method will fail in
      // modern browsers ([Chrome 50 and newer](https://sites.google.com/a/chromium.org/dev/Home/chromium-security/deprecating-powerful-features-on-insecure-origins))
      // See `Locate options` for more details.
      locate: function(t) {
        if (t = this._locateOptions = f({
          timeout: 1e4,
          watch: !1
          // setView: false
          // maxZoom: <Number>
          // maximumAge: 0
          // enableHighAccuracy: false
        }, t), !("geolocation" in navigator))
          return this._handleGeolocationError({
            code: 0,
            message: "Geolocation not supported."
          }), this;
        var e = T(this._handleGeolocationResponse, this), i = T(this._handleGeolocationError, this);
        return t.watch ? this._locationWatchId = navigator.geolocation.watchPosition(e, i, t) : navigator.geolocation.getCurrentPosition(e, i, t), this;
      },
      // @method stopLocate(): this
      // Stops watching location previously initiated by `map.locate({watch: true})`
      // and aborts resetting the map view if map.locate was called with
      // `{setView: true}`.
      stopLocate: function() {
        return navigator.geolocation && navigator.geolocation.clearWatch && navigator.geolocation.clearWatch(this._locationWatchId), this._locateOptions && (this._locateOptions.setView = !1), this;
      },
      _handleGeolocationError: function(t) {
        if (this._container._leaflet_id) {
          var e = t.code, i = t.message || (e === 1 ? "permission denied" : e === 2 ? "position unavailable" : "timeout");
          this._locateOptions.setView && !this._loaded && this.fitWorld(), this.fire("locationerror", {
            code: e,
            message: "Geolocation error: " + i + "."
          });
        }
      },
      _handleGeolocationResponse: function(t) {
        if (this._container._leaflet_id) {
          var e = t.coords.latitude, i = t.coords.longitude, n = new S(e, i), o = n.toBounds(t.coords.accuracy * 2), s = this._locateOptions;
          if (s.setView) {
            var a = this.getBoundsZoom(o);
            this.setView(n, s.maxZoom ? Math.min(a, s.maxZoom) : a);
          }
          var l = {
            latlng: n,
            bounds: o,
            timestamp: t.timestamp
          };
          for (var c in t.coords)
            typeof t.coords[c] == "number" && (l[c] = t.coords[c]);
          this.fire("locationfound", l);
        }
      },
      // TODO Appropriate docs section?
      // @section Other Methods
      // @method addHandler(name: String, HandlerClass: Function): this
      // Adds a new `Handler` to the map, given its name and constructor function.
      addHandler: function(t, e) {
        if (!e)
          return this;
        var i = this[t] = new e(this);
        return this._handlers.push(i), this.options[t] && i.enable(), this;
      },
      // @method remove(): this
      // Destroys the map and clears all related event listeners.
      remove: function() {
        if (this._initEvents(!0), this.options.maxBounds && this.off("moveend", this._panInsideMaxBounds), this._containerId !== this._container._leaflet_id)
          throw new Error("Map container is being reused by another instance");
        try {
          delete this._container._leaflet_id, delete this._containerId;
        } catch {
          this._container._leaflet_id = void 0, this._containerId = void 0;
        }
        this._locationWatchId !== void 0 && this.stopLocate(), this._stop(), Z(this._mapPane), this._clearControlPos && this._clearControlPos(), this._resizeRequest && (K(this._resizeRequest), this._resizeRequest = null), this._clearHandlers(), this._loaded && this.fire("unload");
        var t;
        for (t in this._layers)
          this._layers[t].remove();
        for (t in this._panes)
          Z(this._panes[t]);
        return this._layers = [], this._panes = [], delete this._mapPane, delete this._renderer, this;
      },
      // @section Other Methods
      // @method createPane(name: String, container?: HTMLElement): HTMLElement
      // Creates a new [map pane](#map-pane) with the given name if it doesn't exist already,
      // then returns it. The pane is created as a child of `container`, or
      // as a child of the main map pane if not set.
      createPane: function(t, e) {
        var i = "leaflet-pane" + (t ? " leaflet-" + t.replace("Pane", "") + "-pane" : ""), n = z("div", i, e || this._mapPane);
        return t && (this._panes[t] = n), n;
      },
      // @section Methods for Getting Map State
      // @method getCenter(): LatLng
      // Returns the geographical center of the map view
      getCenter: function() {
        return this._checkIfLoaded(), this._lastCenter && !this._moved() ? this._lastCenter.clone() : this.layerPointToLatLng(this._getCenterLayerPoint());
      },
      // @method getZoom(): Number
      // Returns the current zoom level of the map view
      getZoom: function() {
        return this._zoom;
      },
      // @method getBounds(): LatLngBounds
      // Returns the geographical bounds visible in the current map view
      getBounds: function() {
        var t = this.getPixelBounds(), e = this.unproject(t.getBottomLeft()), i = this.unproject(t.getTopRight());
        return new q(e, i);
      },
      // @method getMinZoom(): Number
      // Returns the minimum zoom level of the map (if set in the `minZoom` option of the map or of any layers), or `0` by default.
      getMinZoom: function() {
        return this.options.minZoom === void 0 ? this._layersMinZoom || 0 : this.options.minZoom;
      },
      // @method getMaxZoom(): Number
      // Returns the maximum zoom level of the map (if set in the `maxZoom` option of the map or of any layers).
      getMaxZoom: function() {
        return this.options.maxZoom === void 0 ? this._layersMaxZoom === void 0 ? 1 / 0 : this._layersMaxZoom : this.options.maxZoom;
      },
      // @method getBoundsZoom(bounds: LatLngBounds, inside?: Boolean, padding?: Point): Number
      // Returns the maximum zoom level on which the given bounds fit to the map
      // view in its entirety. If `inside` (optional) is set to `true`, the method
      // instead returns the minimum zoom level on which the map view fits into
      // the given bounds in its entirety.
      getBoundsZoom: function(t, e, i) {
        t = D(t), i = v(i || [0, 0]);
        var n = this.getZoom() || 0, o = this.getMinZoom(), s = this.getMaxZoom(), a = t.getNorthWest(), l = t.getSouthEast(), c = this.getSize().subtract(i), u = V(this.project(l, n), this.project(a, n)).getSize(), _ = p.any3d ? this.options.zoomSnap : 1, g = c.x / u.x, P = c.y / u.y, G = e ? Math.max(g, P) : Math.min(g, P);
        return n = this.getScaleZoom(G, n), _ && (n = Math.round(n / (_ / 100)) * (_ / 100), n = e ? Math.ceil(n / _) * _ : Math.floor(n / _) * _), Math.max(o, Math.min(s, n));
      },
      // @method getSize(): Point
      // Returns the current size of the map container (in pixels).
      getSize: function() {
        return (!this._size || this._sizeChanged) && (this._size = new x(
          this._container.clientWidth || 0,
          this._container.clientHeight || 0
        ), this._sizeChanged = !1), this._size.clone();
      },
      // @method getPixelBounds(): Bounds
      // Returns the bounds of the current map view in projected pixel
      // coordinates (sometimes useful in layer and overlay implementations).
      getPixelBounds: function(t, e) {
        var i = this._getTopLeftPoint(t, e);
        return new B(i, i.add(this.getSize()));
      },
      // TODO: Check semantics - isn't the pixel origin the 0,0 coord relative to
      // the map pane? "left point of the map layer" can be confusing, specially
      // since there can be negative offsets.
      // @method getPixelOrigin(): Point
      // Returns the projected pixel coordinates of the top left point of
      // the map layer (useful in custom layer and overlay implementations).
      getPixelOrigin: function() {
        return this._checkIfLoaded(), this._pixelOrigin;
      },
      // @method getPixelWorldBounds(zoom?: Number): Bounds
      // Returns the world's bounds in pixel coordinates for zoom level `zoom`.
      // If `zoom` is omitted, the map's current zoom level is used.
      getPixelWorldBounds: function(t) {
        return this.options.crs.getProjectedBounds(t === void 0 ? this.getZoom() : t);
      },
      // @section Other Methods
      // @method getPane(pane: String|HTMLElement): HTMLElement
      // Returns a [map pane](#map-pane), given its name or its HTML element (its identity).
      getPane: function(t) {
        return typeof t == "string" ? this._panes[t] : t;
      },
      // @method getPanes(): Object
      // Returns a plain object containing the names of all [panes](#map-pane) as keys and
      // the panes as values.
      getPanes: function() {
        return this._panes;
      },
      // @method getContainer: HTMLElement
      // Returns the HTML element that contains the map.
      getContainer: function() {
        return this._container;
      },
      // @section Conversion Methods
      // @method getZoomScale(toZoom: Number, fromZoom: Number): Number
      // Returns the scale factor to be applied to a map transition from zoom level
      // `fromZoom` to `toZoom`. Used internally to help with zoom animations.
      getZoomScale: function(t, e) {
        var i = this.options.crs;
        return e = e === void 0 ? this._zoom : e, i.scale(t) / i.scale(e);
      },
      // @method getScaleZoom(scale: Number, fromZoom: Number): Number
      // Returns the zoom level that the map would end up at, if it is at `fromZoom`
      // level and everything is scaled by a factor of `scale`. Inverse of
      // [`getZoomScale`](#map-getZoomScale).
      getScaleZoom: function(t, e) {
        var i = this.options.crs;
        e = e === void 0 ? this._zoom : e;
        var n = i.zoom(t * i.scale(e));
        return isNaN(n) ? 1 / 0 : n;
      },
      // @method project(latlng: LatLng, zoom: Number): Point
      // Projects a geographical coordinate `LatLng` according to the projection
      // of the map's CRS, then scales it according to `zoom` and the CRS's
      // `Transformation`. The result is pixel coordinate relative to
      // the CRS origin.
      project: function(t, e) {
        return e = e === void 0 ? this._zoom : e, this.options.crs.latLngToPoint(M(t), e);
      },
      // @method unproject(point: Point, zoom: Number): LatLng
      // Inverse of [`project`](#map-project).
      unproject: function(t, e) {
        return e = e === void 0 ? this._zoom : e, this.options.crs.pointToLatLng(v(t), e);
      },
      // @method layerPointToLatLng(point: Point): LatLng
      // Given a pixel coordinate relative to the [origin pixel](#map-getpixelorigin),
      // returns the corresponding geographical coordinate (for the current zoom level).
      layerPointToLatLng: function(t) {
        var e = v(t).add(this.getPixelOrigin());
        return this.unproject(e);
      },
      // @method latLngToLayerPoint(latlng: LatLng): Point
      // Given a geographical coordinate, returns the corresponding pixel coordinate
      // relative to the [origin pixel](#map-getpixelorigin).
      latLngToLayerPoint: function(t) {
        var e = this.project(M(t))._round();
        return e._subtract(this.getPixelOrigin());
      },
      // @method wrapLatLng(latlng: LatLng): LatLng
      // Returns a `LatLng` where `lat` and `lng` has been wrapped according to the
      // map's CRS's `wrapLat` and `wrapLng` properties, if they are outside the
      // CRS's bounds.
      // By default this means longitude is wrapped around the dateline so its
      // value is between -180 and +180 degrees.
      wrapLatLng: function(t) {
        return this.options.crs.wrapLatLng(M(t));
      },
      // @method wrapLatLngBounds(bounds: LatLngBounds): LatLngBounds
      // Returns a `LatLngBounds` with the same size as the given one, ensuring that
      // its center is within the CRS's bounds.
      // By default this means the center longitude is wrapped around the dateline so its
      // value is between -180 and +180 degrees, and the majority of the bounds
      // overlaps the CRS's bounds.
      wrapLatLngBounds: function(t) {
        return this.options.crs.wrapLatLngBounds(D(t));
      },
      // @method distance(latlng1: LatLng, latlng2: LatLng): Number
      // Returns the distance between two geographical coordinates according to
      // the map's CRS. By default this measures distance in meters.
      distance: function(t, e) {
        return this.options.crs.distance(M(t), M(e));
      },
      // @method containerPointToLayerPoint(point: Point): Point
      // Given a pixel coordinate relative to the map container, returns the corresponding
      // pixel coordinate relative to the [origin pixel](#map-getpixelorigin).
      containerPointToLayerPoint: function(t) {
        return v(t).subtract(this._getMapPanePos());
      },
      // @method layerPointToContainerPoint(point: Point): Point
      // Given a pixel coordinate relative to the [origin pixel](#map-getpixelorigin),
      // returns the corresponding pixel coordinate relative to the map container.
      layerPointToContainerPoint: function(t) {
        return v(t).add(this._getMapPanePos());
      },
      // @method containerPointToLatLng(point: Point): LatLng
      // Given a pixel coordinate relative to the map container, returns
      // the corresponding geographical coordinate (for the current zoom level).
      containerPointToLatLng: function(t) {
        var e = this.containerPointToLayerPoint(v(t));
        return this.layerPointToLatLng(e);
      },
      // @method latLngToContainerPoint(latlng: LatLng): Point
      // Given a geographical coordinate, returns the corresponding pixel coordinate
      // relative to the map container.
      latLngToContainerPoint: function(t) {
        return this.layerPointToContainerPoint(this.latLngToLayerPoint(M(t)));
      },
      // @method mouseEventToContainerPoint(ev: MouseEvent): Point
      // Given a MouseEvent object, returns the pixel coordinate relative to the
      // map container where the event took place.
      mouseEventToContainerPoint: function(t) {
        return jt(t, this._container);
      },
      // @method mouseEventToLayerPoint(ev: MouseEvent): Point
      // Given a MouseEvent object, returns the pixel coordinate relative to
      // the [origin pixel](#map-getpixelorigin) where the event took place.
      mouseEventToLayerPoint: function(t) {
        return this.containerPointToLayerPoint(this.mouseEventToContainerPoint(t));
      },
      // @method mouseEventToLatLng(ev: MouseEvent): LatLng
      // Given a MouseEvent object, returns geographical coordinate where the
      // event took place.
      mouseEventToLatLng: function(t) {
        return this.layerPointToLatLng(this.mouseEventToLayerPoint(t));
      },
      // map initialization methods
      _initContainer: function(t) {
        var e = this._container = Ut(t);
        if (e) {
          if (e._leaflet_id)
            throw new Error("Map container is already initialized.");
        } else throw new Error("Map container not found.");
        w(e, "scroll", this._onScroll, this), this._containerId = C(e);
      },
      _initLayout: function() {
        var t = this._container;
        this._fadeAnimated = this.options.fadeAnimation && p.any3d, b(t, "leaflet-container" + (p.touch ? " leaflet-touch" : "") + (p.retina ? " leaflet-retina" : "") + (p.ielt9 ? " leaflet-oldie" : "") + (p.safari ? " leaflet-safari" : "") + (this._fadeAnimated ? " leaflet-fade-anim" : ""));
        var e = H2(t, "position");
        e !== "absolute" && e !== "relative" && e !== "fixed" && e !== "sticky" && (t.style.position = "relative"), this._initPanes(), this._initControlPos && this._initControlPos();
      },
      _initPanes: function() {
        var t = this._panes = {};
        this._paneRenderers = {}, this._mapPane = this.createPane("mapPane", this._container), N(this._mapPane, new x(0, 0)), this.createPane("tilePane"), this.createPane("overlayPane"), this.createPane("shadowPane"), this.createPane("markerPane"), this.createPane("tooltipPane"), this.createPane("popupPane"), this.options.markerZoomAnimation || (b(t.markerPane, "leaflet-zoom-hide"), b(t.shadowPane, "leaflet-zoom-hide"));
      },
      // private methods that modify map state
      // @section Map state change events
      _resetView: function(t, e, i) {
        N(this._mapPane, new x(0, 0));
        var n = !this._loaded;
        this._loaded = !0, e = this._limitZoom(e), this.fire("viewprereset");
        var o = this._zoom !== e;
        this._moveStart(o, i)._move(t, e)._moveEnd(o), this.fire("viewreset"), n && this.fire("load");
      },
      _moveStart: function(t, e) {
        return t && this.fire("zoomstart"), e || this.fire("movestart"), this;
      },
      _move: function(t, e, i, n) {
        e === void 0 && (e = this._zoom);
        var o = this._zoom !== e;
        return this._zoom = e, this._lastCenter = t, this._pixelOrigin = this._getNewPixelOrigin(t), n ? i && i.pinch && this.fire("zoom", i) : ((o || i && i.pinch) && this.fire("zoom", i), this.fire("move", i)), this;
      },
      _moveEnd: function(t) {
        return t && this.fire("zoomend"), this.fire("moveend");
      },
      _stop: function() {
        return K(this._flyToFrame), this._panAnim && this._panAnim.stop(), this;
      },
      _rawPanBy: function(t) {
        N(this._mapPane, this._getMapPanePos().subtract(t));
      },
      _getZoomSpan: function() {
        return this.getMaxZoom() - this.getMinZoom();
      },
      _panInsideMaxBounds: function() {
        this._enforcingBounds || this.panInsideBounds(this.options.maxBounds);
      },
      _checkIfLoaded: function() {
        if (!this._loaded)
          throw new Error("Set map center and zoom first.");
      },
      // DOM event handling
      // @section Interaction events
      _initEvents: function(t) {
        this._targets = {}, this._targets[C(this._container)] = this;
        var e = t ? A : w;
        e(this._container, "click dblclick mousedown mouseup mouseover mouseout mousemove contextmenu keypress keydown keyup", this._handleDOMEvent, this), this.options.trackResize && e(window, "resize", this._onResize, this), p.any3d && this.options.transform3DLimit && (t ? this.off : this.on).call(this, "moveend", this._onMoveEnd);
      },
      _onResize: function() {
        K(this._resizeRequest), this._resizeRequest = U(
          function() {
            this.invalidateSize({ debounceMoveend: !0 });
          },
          this
        );
      },
      _onScroll: function() {
        this._container.scrollTop = 0, this._container.scrollLeft = 0;
      },
      _onMoveEnd: function() {
        var t = this._getMapPanePos();
        Math.max(Math.abs(t.x), Math.abs(t.y)) >= this.options.transform3DLimit && this._resetView(this.getCenter(), this.getZoom());
      },
      _findEventTargets: function(t, e) {
        for (var i = [], n, o = e === "mouseout" || e === "mouseover", s = t.target || t.srcElement, a = !1; s; ) {
          if (n = this._targets[C(s)], n && (e === "click" || e === "preclick") && this._draggableMoved(n)) {
            a = !0;
            break;
          }
          if (n && n.listens(e, !0) && (o && !K0(s, t) || (i.push(n), o)) || s === this._container)
            break;
          s = s.parentNode;
        }
        return !i.length && !a && !o && this.listens(e, !0) && (i = [this]), i;
      },
      _isClickDisabled: function(t) {
        for (; t && t !== this._container; ) {
          if (t._leaflet_disable_click)
            return !0;
          t = t.parentNode;
        }
      },
      _handleDOMEvent: function(t) {
        var e = t.target || t.srcElement;
        if (!(!this._loaded || e._leaflet_disable_events || t.type === "click" && this._isClickDisabled(e))) {
          var i = t.type;
          i === "mousedown" && U0(e), this._fireDOMEvent(t, i);
        }
      },
      _mouseEvents: ["click", "dblclick", "mouseover", "mouseout", "contextmenu"],
      _fireDOMEvent: function(t, e, i) {
        if (t.type === "click") {
          var n = f({}, t);
          n.type = "preclick", this._fireDOMEvent(n, n.type, i);
        }
        var o = this._findEventTargets(t, e);
        if (i) {
          for (var s = [], a = 0; a < i.length; a++)
            i[a].listens(e, !0) && s.push(i[a]);
          o = s.concat(o);
        }
        if (o.length) {
          e === "contextmenu" && H(t);
          var l = o[0], c = {
            originalEvent: t
          };
          if (t.type !== "keypress" && t.type !== "keydown" && t.type !== "keyup") {
            var u = l.getLatLng && (!l._radius || l._radius <= 10);
            c.containerPoint = u ? this.latLngToContainerPoint(l.getLatLng()) : this.mouseEventToContainerPoint(t), c.layerPoint = this.containerPointToLayerPoint(c.containerPoint), c.latlng = u ? l.getLatLng() : this.layerPointToLatLng(c.layerPoint);
          }
          for (a = 0; a < o.length; a++)
            if (o[a].fire(e, c, !0), c.originalEvent._stopped || o[a].options.bubblingMouseEvents === !1 && P0(this._mouseEvents, e) !== -1)
              return;
        }
      },
      _draggableMoved: function(t) {
        return t = t.dragging && t.dragging.enabled() ? t : this, t.dragging && t.dragging.moved() || this.boxZoom && this.boxZoom.moved();
      },
      _clearHandlers: function() {
        for (var t = 0, e = this._handlers.length; t < e; t++)
          this._handlers[t].disable();
      },
      // @section Other Methods
      // @method whenReady(fn: Function, context?: Object): this
      // Runs the given function `fn` when the map gets initialized with
      // a view (center and zoom) and at least one layer, or immediately
      // if it's already initialized, optionally passing a function context.
      whenReady: function(t, e) {
        return this._loaded ? t.call(e || this, { target: this }) : this.on("load", t, e), this;
      },
      // private methods for getting map state
      _getMapPanePos: function() {
        return C2(this._mapPane) || new x(0, 0);
      },
      _moved: function() {
        var t = this._getMapPanePos();
        return t && !t.equals([0, 0]);
      },
      _getTopLeftPoint: function(t, e) {
        var i = t && e !== void 0 ? this._getNewPixelOrigin(t, e) : this.getPixelOrigin();
        return i.subtract(this._getMapPanePos());
      },
      _getNewPixelOrigin: function(t, e) {
        var i = this.getSize()._divideBy(2);
        return this.project(t, e)._subtract(i)._add(this._getMapPanePos())._round();
      },
      _latLngToNewLayerPoint: function(t, e, i) {
        var n = this._getNewPixelOrigin(i, e);
        return this.project(t, e)._subtract(n);
      },
      _latLngBoundsToNewLayerBounds: function(t, e, i) {
        var n = this._getNewPixelOrigin(i, e);
        return V([
          this.project(t.getSouthWest(), e)._subtract(n),
          this.project(t.getNorthWest(), e)._subtract(n),
          this.project(t.getSouthEast(), e)._subtract(n),
          this.project(t.getNorthEast(), e)._subtract(n)
        ]);
      },
      // layer point of the current center
      _getCenterLayerPoint: function() {
        return this.containerPointToLayerPoint(this.getSize()._divideBy(2));
      },
      // offset of the specified place to the current center in pixels
      _getCenterOffset: function(t) {
        return this.latLngToLayerPoint(t).subtract(this._getCenterLayerPoint());
      },
      // adjust center for view to get inside bounds
      _limitCenter: function(t, e, i) {
        if (!i)
          return t;
        var n = this.project(t, e), o = this.getSize().divideBy(2), s = new B(n.subtract(o), n.add(o)), a = this._getBoundsOffset(s, i, e);
        return Math.abs(a.x) <= 1 && Math.abs(a.y) <= 1 ? t : this.unproject(n.add(a), e);
      },
      // adjust offset for view to get inside bounds
      _limitOffset: function(t, e) {
        if (!e)
          return t;
        var i = this.getPixelBounds(), n = new B(i.min.add(t), i.max.add(t));
        return t.add(this._getBoundsOffset(n, e));
      },
      // returns offset needed for pxBounds to get inside maxBounds at a specified zoom
      _getBoundsOffset: function(t, e, i) {
        var n = V(
          this.project(e.getNorthEast(), i),
          this.project(e.getSouthWest(), i)
        ), o = n.min.subtract(t.min), s = n.max.subtract(t.max), a = this._rebound(o.x, -s.x), l = this._rebound(o.y, -s.y);
        return new x(a, l);
      },
      _rebound: function(t, e) {
        return t + e > 0 ? Math.round(t - e) / 2 : Math.max(0, Math.ceil(t)) - Math.max(0, Math.floor(e));
      },
      _limitZoom: function(t) {
        var e = this.getMinZoom(), i = this.getMaxZoom(), n = p.any3d ? this.options.zoomSnap : 1;
        return n && (t = Math.round(t / n) * n), Math.max(e, Math.min(i, t));
      },
      _onPanTransitionStep: function() {
        this.fire("move");
      },
      _onPanTransitionEnd: function() {
        R(this._mapPane, "leaflet-pan-anim"), this.fire("moveend");
      },
      _tryAnimatedPan: function(t, e) {
        var i = this._getCenterOffset(t)._trunc();
        return (e && e.animate) !== !0 && !this.getSize().contains(i) ? !1 : (this.panBy(i, e), !0);
      },
      _createAnimProxy: function() {
        var t = this._proxy = z("div", "leaflet-proxy leaflet-zoom-animated");
        this._panes.mapPane.appendChild(t), this.on("zoomanim", function(e) {
          var i = R0, n = this._proxy.style[i];
          w2(this._proxy, this.project(e.center, e.zoom), this.getZoomScale(e.zoom, 1)), n === this._proxy.style[i] && this._animatingZoom && this._onZoomTransitionEnd();
        }, this), this.on("load moveend", this._animMoveEnd, this), this._on("unload", this._destroyAnimProxy, this);
      },
      _destroyAnimProxy: function() {
        Z(this._proxy), this.off("load moveend", this._animMoveEnd, this), delete this._proxy;
      },
      _animMoveEnd: function() {
        var t = this.getCenter(), e = this.getZoom();
        w2(this._proxy, this.project(t, e), this.getZoomScale(e, 1));
      },
      _catchTransitionEnd: function(t) {
        this._animatingZoom && t.propertyName.indexOf("transform") >= 0 && this._onZoomTransitionEnd();
      },
      _nothingToAnimate: function() {
        return !this._container.getElementsByClassName("leaflet-zoom-animated").length;
      },
      _tryAnimatedZoom: function(t, e, i) {
        if (this._animatingZoom)
          return !0;
        if (i = i || {}, !this._zoomAnimated || i.animate === !1 || this._nothingToAnimate() || Math.abs(e - this._zoom) > this.options.zoomAnimationThreshold)
          return !1;
        var n = this.getZoomScale(e), o = this._getCenterOffset(t)._divideBy(1 - 1 / n);
        return i.animate !== !0 && !this.getSize().contains(o) ? !1 : (U(function() {
          this._moveStart(!0, i.noMoveStart || !1)._animateZoom(t, e, !0);
        }, this), !0);
      },
      _animateZoom: function(t, e, i, n) {
        this._mapPane && (i && (this._animatingZoom = !0, this._animateToCenter = t, this._animateToZoom = e, b(this._mapPane, "leaflet-zoom-anim")), this.fire("zoomanim", {
          center: t,
          zoom: e,
          noUpdate: n
        }), this._tempFireZoomEvent || (this._tempFireZoomEvent = this._zoom !== this._animateToZoom), this._move(this._animateToCenter, this._animateToZoom, void 0, !0), setTimeout(T(this._onZoomTransitionEnd, this), 250));
      },
      _onZoomTransitionEnd: function() {
        this._animatingZoom && (this._mapPane && R(this._mapPane, "leaflet-zoom-anim"), this._animatingZoom = !1, this._move(this._animateToCenter, this._animateToZoom, void 0, !0), this._tempFireZoomEvent && this.fire("zoom"), delete this._tempFireZoomEvent, this.fire("move"), this._moveEnd(!0));
      }
    });
    function pi(t, e) {
      return new k(t, e);
    }
    var e2 = c2.extend({
      // @section
      // @aka Control Options
      options: {
        // @option position: String = 'topright'
        // The position of the control (one of the map corners). Possible values are `'topleft'`,
        // `'topright'`, `'bottomleft'` or `'bottomright'`
        position: "topright"
      },
      initialize: function(t) {
        O(this, t);
      },
      /* @section
       * Classes extending L.Control will inherit the following methods:
       *
       * @method getPosition: string
       * Returns the position of the control.
       */
      getPosition: function() {
        return this.options.position;
      },
      // @method setPosition(position: string): this
      // Sets the position of the control.
      setPosition: function(t) {
        var e = this._map;
        return e && e.removeControl(this), this.options.position = t, e && e.addControl(this), this;
      },
      // @method getContainer: HTMLElement
      // Returns the HTMLElement that contains the control.
      getContainer: function() {
        return this._container;
      },
      // @method addTo(map: Map): this
      // Adds the control to the given map.
      addTo: function(t) {
        this.remove(), this._map = t;
        var e = this._container = this.onAdd(t), i = this.getPosition(), n = t._controlCorners[i];
        return b(e, "leaflet-control"), i.indexOf("bottom") !== -1 ? n.insertBefore(e, n.firstChild) : n.appendChild(e), this._map.on("unload", this.remove, this), this;
      },
      // @method remove: this
      // Removes the control from the map it is currently active on.
      remove: function() {
        return this._map ? (Z(this._container), this.onRemove && this.onRemove(this._map), this._map.off("unload", this.remove, this), this._map = null, this) : this;
      },
      _refocusOnMap: function(t) {
        this._map && t && t.screenX > 0 && t.screenY > 0 && this._map.getContainer().focus();
      }
    }), Y2 = function(t) {
      return new e2(t);
    };
    k.include({
      // @method addControl(control: Control): this
      // Adds the given control to the map
      addControl: function(t) {
        return t.addTo(this), this;
      },
      // @method removeControl(control: Control): this
      // Removes the given control from the map
      removeControl: function(t) {
        return t.remove(), this;
      },
      _initControlPos: function() {
        var t = this._controlCorners = {}, e = "leaflet-", i = this._controlContainer = z("div", e + "control-container", this._container);
        function n(o, s) {
          var a = e + o + " " + e + s;
          t[o + s] = z("div", a, i);
        }
        n("top", "left"), n("top", "right"), n("bottom", "left"), n("bottom", "right");
      },
      _clearControlPos: function() {
        for (var t in this._controlCorners)
          Z(this._controlCorners[t]);
        Z(this._controlContainer), delete this._controlCorners, delete this._controlContainer;
      }
    });
    var Xt = e2.extend({
      // @section
      // @aka Control.Layers options
      options: {
        // @option collapsed: Boolean = true
        // If `true`, the control will be collapsed into an icon and expanded on mouse hover, touch, or keyboard activation.
        collapsed: !0,
        position: "topright",
        // @option autoZIndex: Boolean = true
        // If `true`, the control will assign zIndexes in increasing order to all of its layers so that the order is preserved when switching them on/off.
        autoZIndex: !0,
        // @option hideSingleBase: Boolean = false
        // If `true`, the base layers in the control will be hidden when there is only one.
        hideSingleBase: !1,
        // @option sortLayers: Boolean = false
        // Whether to sort the layers. When `false`, layers will keep the order
        // in which they were added to the control.
        sortLayers: !1,
        // @option sortFunction: Function = *
        // A [compare function](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Array/sort)
        // that will be used for sorting the layers, when `sortLayers` is `true`.
        // The function receives both the `L.Layer` instances and their names, as in
        // `sortFunction(layerA, layerB, nameA, nameB)`.
        // By default, it sorts layers alphabetically by their name.
        sortFunction: function(t, e, i, n) {
          return i < n ? -1 : n < i ? 1 : 0;
        }
      },
      initialize: function(t, e, i) {
        O(this, i), this._layerControlInputs = [], this._layers = [], this._lastZIndex = 0, this._handlingClick = !1, this._preventClick = !1;
        for (var n in t)
          this._addLayer(t[n], n);
        for (n in e)
          this._addLayer(e[n], n, !0);
      },
      onAdd: function(t) {
        this._initLayout(), this._update(), this._map = t, t.on("zoomend", this._checkDisabledLayers, this);
        for (var e = 0; e < this._layers.length; e++)
          this._layers[e].layer.on("add remove", this._onLayerChange, this);
        return this._container;
      },
      addTo: function(t) {
        return e2.prototype.addTo.call(this, t), this._expandIfNotCollapsed();
      },
      onRemove: function() {
        this._map.off("zoomend", this._checkDisabledLayers, this);
        for (var t = 0; t < this._layers.length; t++)
          this._layers[t].layer.off("add remove", this._onLayerChange, this);
      },
      // @method addBaseLayer(layer: Layer, name: String): this
      // Adds a base layer (radio button entry) with the given name to the control.
      addBaseLayer: function(t, e) {
        return this._addLayer(t, e), this._map ? this._update() : this;
      },
      // @method addOverlay(layer: Layer, name: String): this
      // Adds an overlay (checkbox entry) with the given name to the control.
      addOverlay: function(t, e) {
        return this._addLayer(t, e, !0), this._map ? this._update() : this;
      },
      // @method removeLayer(layer: Layer): this
      // Remove the given layer from the control.
      removeLayer: function(t) {
        t.off("add remove", this._onLayerChange, this);
        var e = this._getLayer(C(t));
        return e && this._layers.splice(this._layers.indexOf(e), 1), this._map ? this._update() : this;
      },
      // @method expand(): this
      // Expand the control container if collapsed.
      expand: function() {
        b(this._container, "leaflet-control-layers-expanded"), this._section.style.height = null;
        var t = this._map.getSize().y - (this._container.offsetTop + 50);
        return t < this._section.clientHeight ? (b(this._section, "leaflet-control-layers-scrollbar"), this._section.style.height = t + "px") : R(this._section, "leaflet-control-layers-scrollbar"), this._checkDisabledLayers(), this;
      },
      // @method collapse(): this
      // Collapse the control container if expanded.
      collapse: function() {
        return R(this._container, "leaflet-control-layers-expanded"), this;
      },
      _initLayout: function() {
        var t = "leaflet-control-layers", e = this._container = z("div", t), i = this.options.collapsed;
        e.setAttribute("aria-haspopup", !0), q2(e), J0(e);
        var n = this._section = z("section", t + "-list");
        i && (this._map.on("click", this.collapse, this), w(e, {
          mouseenter: this._expandSafely,
          mouseleave: this.collapse
        }, this));
        var o = this._layersLink = z("a", t + "-toggle", e);
        o.href = "#", o.title = "Layers", o.setAttribute("role", "button"), w(o, {
          keydown: function(s) {
            s.keyCode === 13 && this._expandSafely();
          },
          // Certain screen readers intercept the key event and instead send a click event
          click: function(s) {
            H(s), this._expandSafely();
          }
        }, this), i || this.expand(), this._baseLayersList = z("div", t + "-base", n), this._separator = z("div", t + "-separator", n), this._overlaysList = z("div", t + "-overlays", n), e.appendChild(n);
      },
      _getLayer: function(t) {
        for (var e = 0; e < this._layers.length; e++)
          if (this._layers[e] && C(this._layers[e].layer) === t)
            return this._layers[e];
      },
      _addLayer: function(t, e, i) {
        this._map && t.on("add remove", this._onLayerChange, this), this._layers.push({
          layer: t,
          name: e,
          overlay: i
        }), this.options.sortLayers && this._layers.sort(T(function(n, o) {
          return this.options.sortFunction(n.layer, o.layer, n.name, o.name);
        }, this)), this.options.autoZIndex && t.setZIndex && (this._lastZIndex++, t.setZIndex(this._lastZIndex)), this._expandIfNotCollapsed();
      },
      _update: function() {
        if (!this._container)
          return this;
        o0(this._baseLayersList), o0(this._overlaysList), this._layerControlInputs = [];
        var t, e, i, n, o = 0;
        for (i = 0; i < this._layers.length; i++)
          n = this._layers[i], this._addItem(n), e = e || n.overlay, t = t || !n.overlay, o += n.overlay ? 0 : 1;
        return this.options.hideSingleBase && (t = t && o > 1, this._baseLayersList.style.display = t ? "" : "none"), this._separator.style.display = e && t ? "" : "none", this;
      },
      _onLayerChange: function(t) {
        this._handlingClick || this._update();
        var e = this._getLayer(C(t.target)), i = e.overlay ? t.type === "add" ? "overlayadd" : "overlayremove" : t.type === "add" ? "baselayerchange" : null;
        i && this._map.fire(i, e);
      },
      // IE7 bugs out if you create a radio dynamically, so you have to do it this hacky way (see https://stackoverflow.com/a/119079)
      _createRadioElement: function(t, e) {
        var i = '<input type="radio" class="leaflet-control-layers-selector" name="' + t + '"' + (e ? ' checked="checked"' : "") + "/>", n = document.createElement("div");
        return n.innerHTML = i, n.firstChild;
      },
      _addItem: function(t) {
        var e = document.createElement("label"), i = this._map.hasLayer(t.layer), n;
        t.overlay ? (n = document.createElement("input"), n.type = "checkbox", n.className = "leaflet-control-layers-selector", n.defaultChecked = i) : n = this._createRadioElement("leaflet-base-layers_" + C(this), i), this._layerControlInputs.push(n), n.layerId = C(t.layer), w(n, "click", this._onInputClick, this);
        var o = document.createElement("span");
        o.innerHTML = " " + t.name;
        var s = document.createElement("span");
        e.appendChild(s), s.appendChild(n), s.appendChild(o);
        var a = t.overlay ? this._overlaysList : this._baseLayersList;
        return a.appendChild(e), this._checkDisabledLayers(), e;
      },
      _onInputClick: function() {
        if (!this._preventClick) {
          var t = this._layerControlInputs, e, i, n = [], o = [];
          this._handlingClick = !0;
          for (var s = t.length - 1; s >= 0; s--)
            e = t[s], i = this._getLayer(e.layerId).layer, e.checked ? n.push(i) : e.checked || o.push(i);
          for (s = 0; s < o.length; s++)
            this._map.hasLayer(o[s]) && this._map.removeLayer(o[s]);
          for (s = 0; s < n.length; s++)
            this._map.hasLayer(n[s]) || this._map.addLayer(n[s]);
          this._handlingClick = !1, this._refocusOnMap();
        }
      },
      _checkDisabledLayers: function() {
        for (var t = this._layerControlInputs, e, i, n = this._map.getZoom(), o = t.length - 1; o >= 0; o--)
          e = t[o], i = this._getLayer(e.layerId).layer, e.disabled = i.options.minZoom !== void 0 && n < i.options.minZoom || i.options.maxZoom !== void 0 && n > i.options.maxZoom;
      },
      _expandIfNotCollapsed: function() {
        return this._map && !this.options.collapsed && this.expand(), this;
      },
      _expandSafely: function() {
        var t = this._section;
        this._preventClick = !0, w(t, "click", H), this.expand();
        var e = this;
        setTimeout(function() {
          A(t, "click", H), e._preventClick = !1;
        });
      }
    }), gi = function(t, e, i) {
      return new Xt(t, e, i);
    }, X0 = e2.extend({
      // @section
      // @aka Control.Zoom options
      options: {
        position: "topleft",
        // @option zoomInText: String = '<span aria-hidden="true">+</span>'
        // The text set on the 'zoom in' button.
        zoomInText: '<span aria-hidden="true">+</span>',
        // @option zoomInTitle: String = 'Zoom in'
        // The title set on the 'zoom in' button.
        zoomInTitle: "Zoom in",
        // @option zoomOutText: String = '<span aria-hidden="true">&#x2212;</span>'
        // The text set on the 'zoom out' button.
        zoomOutText: '<span aria-hidden="true">&#x2212;</span>',
        // @option zoomOutTitle: String = 'Zoom out'
        // The title set on the 'zoom out' button.
        zoomOutTitle: "Zoom out"
      },
      onAdd: function(t) {
        var e = "leaflet-control-zoom", i = z("div", e + " leaflet-bar"), n = this.options;
        return this._zoomInButton = this._createButton(
          n.zoomInText,
          n.zoomInTitle,
          e + "-in",
          i,
          this._zoomIn
        ), this._zoomOutButton = this._createButton(
          n.zoomOutText,
          n.zoomOutTitle,
          e + "-out",
          i,
          this._zoomOut
        ), this._updateDisabled(), t.on("zoomend zoomlevelschange", this._updateDisabled, this), i;
      },
      onRemove: function(t) {
        t.off("zoomend zoomlevelschange", this._updateDisabled, this);
      },
      disable: function() {
        return this._disabled = !0, this._updateDisabled(), this;
      },
      enable: function() {
        return this._disabled = !1, this._updateDisabled(), this;
      },
      _zoomIn: function(t) {
        !this._disabled && this._map._zoom < this._map.getMaxZoom() && this._map.zoomIn(this._map.options.zoomDelta * (t.shiftKey ? 3 : 1));
      },
      _zoomOut: function(t) {
        !this._disabled && this._map._zoom > this._map.getMinZoom() && this._map.zoomOut(this._map.options.zoomDelta * (t.shiftKey ? 3 : 1));
      },
      _createButton: function(t, e, i, n, o) {
        var s = z("a", i, n);
        return s.innerHTML = t, s.href = "#", s.title = e, s.setAttribute("role", "button"), s.setAttribute("aria-label", e), q2(s), w(s, "click", L2), w(s, "click", o, this), w(s, "click", this._refocusOnMap, this), s;
      },
      _updateDisabled: function() {
        var t = this._map, e = "leaflet-disabled";
        R(this._zoomInButton, e), R(this._zoomOutButton, e), this._zoomInButton.setAttribute("aria-disabled", "false"), this._zoomOutButton.setAttribute("aria-disabled", "false"), (this._disabled || t._zoom === t.getMinZoom()) && (b(this._zoomOutButton, e), this._zoomOutButton.setAttribute("aria-disabled", "true")), (this._disabled || t._zoom === t.getMaxZoom()) && (b(this._zoomInButton, e), this._zoomInButton.setAttribute("aria-disabled", "true"));
      }
    });
    k.mergeOptions({
      zoomControl: !0
    }), k.addInitHook(function() {
      this.options.zoomControl && (this.zoomControl = new X0(), this.addControl(this.zoomControl));
    });
    var vi = function(t) {
      return new X0(t);
    }, Qt = e2.extend({
      // @section
      // @aka Control.Scale options
      options: {
        position: "bottomleft",
        // @option maxWidth: Number = 100
        // Maximum width of the control in pixels. The width is set dynamically to show round values (e.g. 100, 200, 500).
        maxWidth: 100,
        // @option metric: Boolean = True
        // Whether to show the metric scale line (m/km).
        metric: !0,
        // @option imperial: Boolean = True
        // Whether to show the imperial scale line (mi/ft).
        imperial: !0
        // @option updateWhenIdle: Boolean = false
        // If `true`, the control is updated on [`moveend`](#map-moveend), otherwise it's always up-to-date (updated on [`move`](#map-move)).
      },
      onAdd: function(t) {
        var e = "leaflet-control-scale", i = z("div", e), n = this.options;
        return this._addScales(n, e + "-line", i), t.on(n.updateWhenIdle ? "moveend" : "move", this._update, this), t.whenReady(this._update, this), i;
      },
      onRemove: function(t) {
        t.off(this.options.updateWhenIdle ? "moveend" : "move", this._update, this);
      },
      _addScales: function(t, e, i) {
        t.metric && (this._mScale = z("div", e, i)), t.imperial && (this._iScale = z("div", e, i));
      },
      _update: function() {
        var t = this._map, e = t.getSize().y / 2, i = t.distance(
          t.containerPointToLatLng([0, e]),
          t.containerPointToLatLng([this.options.maxWidth, e])
        );
        this._updateScales(i);
      },
      _updateScales: function(t) {
        this.options.metric && t && this._updateMetric(t), this.options.imperial && t && this._updateImperial(t);
      },
      _updateMetric: function(t) {
        var e = this._getRoundNum(t), i = e < 1e3 ? e + " m" : e / 1e3 + " km";
        this._updateScale(this._mScale, i, e / t);
      },
      _updateImperial: function(t) {
        var e = t * 3.2808399, i, n, o;
        e > 5280 ? (i = e / 5280, n = this._getRoundNum(i), this._updateScale(this._iScale, n + " mi", n / i)) : (o = this._getRoundNum(e), this._updateScale(this._iScale, o + " ft", o / e));
      },
      _updateScale: function(t, e, i) {
        t.style.width = Math.round(this.options.maxWidth * i) + "px", t.innerHTML = e;
      },
      _getRoundNum: function(t) {
        var e = Math.pow(10, (Math.floor(t) + "").length - 1), i = t / e;
        return i = i >= 10 ? 10 : i >= 5 ? 5 : i >= 3 ? 3 : i >= 2 ? 2 : 1, e * i;
      }
    }), yi = function(t) {
      return new Qt(t);
    }, xi = '<svg aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="12" height="8" viewBox="0 0 12 8" class="leaflet-attribution-flag"><path fill="#4C7BE1" d="M0 0h12v4H0z"/><path fill="#FFD500" d="M0 4h12v3H0z"/><path fill="#E0BC00" d="M0 7h12v1H0z"/></svg>', Q0 = e2.extend({
      // @section
      // @aka Control.Attribution options
      options: {
        position: "bottomright",
        // @option prefix: String|false = 'Leaflet'
        // The HTML text shown before the attributions. Pass `false` to disable.
        prefix: '<a href="https://leafletjs.com" title="A JavaScript library for interactive maps">' + (p.inlineSvg ? xi + " " : "") + "Leaflet</a>"
      },
      initialize: function(t) {
        O(this, t), this._attributions = {};
      },
      onAdd: function(t) {
        t.attributionControl = this, this._container = z("div", "leaflet-control-attribution"), q2(this._container);
        for (var e in t._layers)
          t._layers[e].getAttribution && this.addAttribution(t._layers[e].getAttribution());
        return this._update(), t.on("layeradd", this._addAttribution, this), this._container;
      },
      onRemove: function(t) {
        t.off("layeradd", this._addAttribution, this);
      },
      _addAttribution: function(t) {
        t.layer.getAttribution && (this.addAttribution(t.layer.getAttribution()), t.layer.once("remove", function() {
          this.removeAttribution(t.layer.getAttribution());
        }, this));
      },
      // @method setPrefix(prefix: String|false): this
      // The HTML text shown before the attributions. Pass `false` to disable.
      setPrefix: function(t) {
        return this.options.prefix = t, this._update(), this;
      },
      // @method addAttribution(text: String): this
      // Adds an attribution text (e.g. `'&copy; OpenStreetMap contributors'`).
      addAttribution: function(t) {
        return t ? (this._attributions[t] || (this._attributions[t] = 0), this._attributions[t]++, this._update(), this) : this;
      },
      // @method removeAttribution(text: String): this
      // Removes an attribution text.
      removeAttribution: function(t) {
        return t ? (this._attributions[t] && (this._attributions[t]--, this._update()), this) : this;
      },
      _update: function() {
        if (this._map) {
          var t = [];
          for (var e in this._attributions)
            this._attributions[e] && t.push(e);
          var i = [];
          this.options.prefix && i.push(this.options.prefix), t.length && i.push(t.join(", ")), this._container.innerHTML = i.join(' <span aria-hidden="true">|</span> ');
        }
      }
    });
    k.mergeOptions({
      attributionControl: !0
    }), k.addInitHook(function() {
      this.options.attributionControl && new Q0().addTo(this);
    });
    var wi = function(t) {
      return new Q0(t);
    };
    e2.Layers = Xt, e2.Zoom = X0, e2.Scale = Qt, e2.Attribution = Q0, Y2.layers = gi, Y2.zoom = vi, Y2.scale = yi, Y2.attribution = wi;
    var r2 = c2.extend({
      initialize: function(t) {
        this._map = t;
      },
      // @method enable(): this
      // Enables the handler
      enable: function() {
        return this._enabled ? this : (this._enabled = !0, this.addHooks(), this);
      },
      // @method disable(): this
      // Disables the handler
      disable: function() {
        return this._enabled ? (this._enabled = !1, this.removeHooks(), this) : this;
      },
      // @method enabled(): Boolean
      // Returns `true` if the handler is enabled
      enabled: function() {
        return !!this._enabled;
      }
      // @section Extension methods
      // Classes inheriting from `Handler` must implement the two following methods:
      // @method addHooks()
      // Called when the handler is enabled, should add event hooks.
      // @method removeHooks()
      // Called when the handler is disabled, should remove the event hooks added previously.
    });
    r2.addTo = function(t, e) {
      return t.addHandler(e, this), this;
    };
    var Ci = { Events: J }, $t = p.touch ? "touchstart mousedown" : "mousedown", v2 = D2.extend({
      options: {
        // @section
        // @aka Draggable options
        // @option clickTolerance: Number = 3
        // The max number of pixels a user can shift the mouse pointer during a click
        // for it to be considered a valid click (as opposed to a mouse drag).
        clickTolerance: 3
      },
      // @constructor L.Draggable(el: HTMLElement, dragHandle?: HTMLElement, preventOutline?: Boolean, options?: Draggable options)
      // Creates a `Draggable` object for moving `el` when you start dragging the `dragHandle` element (equals `el` itself by default).
      initialize: function(t, e, i, n) {
        O(this, n), this._element = t, this._dragStartTarget = e || t, this._preventOutline = i;
      },
      // @method enable()
      // Enables the dragging ability
      enable: function() {
        this._enabled || (w(this._dragStartTarget, $t, this._onDown, this), this._enabled = !0);
      },
      // @method disable()
      // Disables the dragging ability
      disable: function() {
        this._enabled && (v2._dragging === this && this.finishDrag(!0), A(this._dragStartTarget, $t, this._onDown, this), this._enabled = !1, this._moved = !1);
      },
      _onDown: function(t) {
        if (this._enabled && (this._moved = !1, !D0(this._element, "leaflet-zoom-anim"))) {
          if (t.touches && t.touches.length !== 1) {
            v2._dragging === this && this.finishDrag();
            return;
          }
          if (!(v2._dragging || t.shiftKey || t.which !== 1 && t.button !== 1 && !t.touches) && (v2._dragging = this, this._preventOutline && U0(this._element), W0(), G2(), !this._moving)) {
            this.fire("down");
            var e = t.touches ? t.touches[0] : t, i = Vt(this._element);
            this._startPoint = new x(e.clientX, e.clientY), this._startPos = C2(this._element), this._parentScale = V0(i);
            var n = t.type === "mousedown";
            w(document, n ? "mousemove" : "touchmove", this._onMove, this), w(document, n ? "mouseup" : "touchend touchcancel", this._onUp, this);
          }
        }
      },
      _onMove: function(t) {
        if (this._enabled) {
          if (t.touches && t.touches.length > 1) {
            this._moved = !0;
            return;
          }
          var e = t.touches && t.touches.length === 1 ? t.touches[0] : t, i = new x(e.clientX, e.clientY)._subtract(this._startPoint);
          !i.x && !i.y || Math.abs(i.x) + Math.abs(i.y) < this.options.clickTolerance || (i.x /= this._parentScale.x, i.y /= this._parentScale.y, H(t), this._moved || (this.fire("dragstart"), this._moved = !0, b(document.body, "leaflet-dragging"), this._lastTarget = t.target || t.srcElement, window.SVGElementInstance && this._lastTarget instanceof window.SVGElementInstance && (this._lastTarget = this._lastTarget.correspondingUseElement), b(this._lastTarget, "leaflet-drag-target")), this._newPos = this._startPos.add(i), this._moving = !0, this._lastEvent = t, this._updatePosition());
        }
      },
      _updatePosition: function() {
        var t = { originalEvent: this._lastEvent };
        this.fire("predrag", t), N(this._element, this._newPos), this.fire("drag", t);
      },
      _onUp: function() {
        this._enabled && this.finishDrag();
      },
      finishDrag: function(t) {
        R(document.body, "leaflet-dragging"), this._lastTarget && (R(this._lastTarget, "leaflet-drag-target"), this._lastTarget = null), A(document, "mousemove touchmove", this._onMove, this), A(document, "mouseup touchend touchcancel", this._onUp, this), H0(), U2();
        var e = this._moved && this._moving;
        this._moving = !1, v2._dragging = !1, e && this.fire("dragend", {
          noInertia: t,
          distance: this._newPos.distanceTo(this._startPos)
        });
      }
    });
    function te(t, e, i) {
      var n, o = [1, 4, 2, 8], s, a, l, c, u, _, g, P;
      for (s = 0, _ = t.length; s < _; s++)
        t[s]._code = P2(t[s], e);
      for (l = 0; l < 4; l++) {
        for (g = o[l], n = [], s = 0, _ = t.length, a = _ - 1; s < _; a = s++)
          c = t[s], u = t[a], c._code & g ? u._code & g || (P = l0(u, c, g, e, i), P._code = P2(P, e), n.push(P)) : (u._code & g && (P = l0(u, c, g, e, i), P._code = P2(P, e), n.push(P)), n.push(c));
        t = n;
      }
      return t;
    }
    function ee(t, e) {
      var i, n, o, s, a, l, c, u, _;
      if (!t || t.length === 0)
        throw new Error("latlngs not passed");
      Q(t) || (console.warn("latlngs are not flat! Only the first ring will be used"), t = t[0]);
      var g = M([0, 0]), P = D(t), G = P.getNorthWest().distanceTo(P.getSouthWest()) * P.getNorthEast().distanceTo(P.getNorthWest());
      G < 1700 && (g = $0(t));
      var W = t.length, $ = [];
      for (i = 0; i < W; i++) {
        var Y = M(t[i]);
        $.push(e.project(M([Y.lat - g.lat, Y.lng - g.lng])));
      }
      for (l = c = u = 0, i = 0, n = W - 1; i < W; n = i++)
        o = $[i], s = $[n], a = o.y * s.x - s.y * o.x, c += (o.x + s.x) * a, u += (o.y + s.y) * a, l += a * 3;
      l === 0 ? _ = $[0] : _ = [c / l, u / l];
      var Z2 = e.unproject(v(_));
      return M([Z2.lat + g.lat, Z2.lng + g.lng]);
    }
    function $0(t) {
      for (var e = 0, i = 0, n = 0, o = 0; o < t.length; o++) {
        var s = M(t[o]);
        e += s.lat, i += s.lng, n++;
      }
      return M([e / n, i / n]);
    }
    var bi = {
      __proto__: null,
      clipPolygon: te,
      polygonCenter: ee,
      centroid: $0
    };
    function ie(t, e) {
      if (!e || !t.length)
        return t.slice();
      var i = e * e;
      return t = Ti(t, i), t = Pi(t, i), t;
    }
    function ne(t, e, i) {
      return Math.sqrt(j2(t, e, i, !0));
    }
    function Li(t, e, i) {
      return j2(t, e, i);
    }
    function Pi(t, e) {
      var i = t.length, n = typeof Uint8Array < "u" ? Uint8Array : Array, o = new n(i);
      o[0] = o[i - 1] = 1, tt(t, o, e, 0, i - 1);
      var s, a = [];
      for (s = 0; s < i; s++)
        o[s] && a.push(t[s]);
      return a;
    }
    function tt(t, e, i, n, o) {
      var s = 0, a, l, c;
      for (l = n + 1; l <= o - 1; l++)
        c = j2(t[l], t[n], t[o], !0), c > s && (a = l, s = c);
      s > i && (e[a] = 1, tt(t, e, i, n, a), tt(t, e, i, a, o));
    }
    function Ti(t, e) {
      for (var i = [t[0]], n = 1, o = 0, s = t.length; n < s; n++)
        Mi(t[n], t[o]) > e && (i.push(t[n]), o = n);
      return o < s - 1 && i.push(t[s - 1]), i;
    }
    var oe;
    function se(t, e, i, n, o) {
      var s = n ? oe : P2(t, i), a = P2(e, i), l, c, u;
      for (oe = a; ; ) {
        if (!(s | a))
          return [t, e];
        if (s & a)
          return !1;
        l = s || a, c = l0(t, e, l, i, o), u = P2(c, i), l === s ? (t = c, s = u) : (e = c, a = u);
      }
    }
    function l0(t, e, i, n, o) {
      var s = e.x - t.x, a = e.y - t.y, l = n.min, c = n.max, u, _;
      return i & 8 ? (u = t.x + s * (c.y - t.y) / a, _ = c.y) : i & 4 ? (u = t.x + s * (l.y - t.y) / a, _ = l.y) : i & 2 ? (u = c.x, _ = t.y + a * (c.x - t.x) / s) : i & 1 && (u = l.x, _ = t.y + a * (l.x - t.x) / s), new x(u, _, o);
    }
    function P2(t, e) {
      var i = 0;
      return t.x < e.min.x ? i |= 1 : t.x > e.max.x && (i |= 2), t.y < e.min.y ? i |= 4 : t.y > e.max.y && (i |= 8), i;
    }
    function Mi(t, e) {
      var i = e.x - t.x, n = e.y - t.y;
      return i * i + n * n;
    }
    function j2(t, e, i, n) {
      var o = e.x, s = e.y, a = i.x - o, l = i.y - s, c = a * a + l * l, u;
      return c > 0 && (u = ((t.x - o) * a + (t.y - s) * l) / c, u > 1 ? (o = i.x, s = i.y) : u > 0 && (o += a * u, s += l * u)), a = t.x - o, l = t.y - s, n ? a * a + l * l : new x(o, s);
    }
    function Q(t) {
      return !t2(t[0]) || typeof t[0][0] != "object" && typeof t[0][0] < "u";
    }
    function re(t) {
      return console.warn("Deprecated use of _flat, please use L.LineUtil.isFlat instead."), Q(t);
    }
    function ae(t, e) {
      var i, n, o, s, a, l, c, u;
      if (!t || t.length === 0)
        throw new Error("latlngs not passed");
      Q(t) || (console.warn("latlngs are not flat! Only the first ring will be used"), t = t[0]);
      var _ = M([0, 0]), g = D(t), P = g.getNorthWest().distanceTo(g.getSouthWest()) * g.getNorthEast().distanceTo(g.getNorthWest());
      P < 1700 && (_ = $0(t));
      var G = t.length, W = [];
      for (i = 0; i < G; i++) {
        var $ = M(t[i]);
        W.push(e.project(M([$.lat - _.lat, $.lng - _.lng])));
      }
      for (i = 0, n = 0; i < G - 1; i++)
        n += W[i].distanceTo(W[i + 1]) / 2;
      if (n === 0)
        u = W[0];
      else
        for (i = 0, s = 0; i < G - 1; i++)
          if (a = W[i], l = W[i + 1], o = a.distanceTo(l), s += o, s > n) {
            c = (s - n) / o, u = [
              l.x - c * (l.x - a.x),
              l.y - c * (l.y - a.y)
            ];
            break;
          }
      var Y = e.unproject(v(u));
      return M([Y.lat + _.lat, Y.lng + _.lng]);
    }
    var ki = {
      __proto__: null,
      simplify: ie,
      pointToSegmentDistance: ne,
      closestPointOnSegment: Li,
      clipSegment: se,
      _getEdgeIntersection: l0,
      _getBitCode: P2,
      _sqClosestPointOnSegment: j2,
      isFlat: Q,
      _flat: re,
      polylineCenter: ae
    }, et = {
      project: function(t) {
        return new x(t.lng, t.lat);
      },
      unproject: function(t) {
        return new S(t.y, t.x);
      },
      bounds: new B([-180, -90], [180, 90])
    }, it = {
      R: 6378137,
      R_MINOR: 6356752314245179e-9,
      bounds: new B([-2003750834279e-5, -1549657073972e-5], [2003750834279e-5, 1876465623138e-5]),
      project: function(t) {
        var e = Math.PI / 180, i = this.R, n = t.lat * e, o = this.R_MINOR / i, s = Math.sqrt(1 - o * o), a = s * Math.sin(n), l = Math.tan(Math.PI / 4 - n / 2) / Math.pow((1 - a) / (1 + a), s / 2);
        return n = -i * Math.log(Math.max(l, 1e-10)), new x(t.lng * e * i, n);
      },
      unproject: function(t) {
        for (var e = 180 / Math.PI, i = this.R, n = this.R_MINOR / i, o = Math.sqrt(1 - n * n), s = Math.exp(-t.y / i), a = Math.PI / 2 - 2 * Math.atan(s), l = 0, c = 0.1, u; l < 15 && Math.abs(c) > 1e-7; l++)
          u = o * Math.sin(a), u = Math.pow((1 - u) / (1 + u), o / 2), c = Math.PI / 2 - 2 * Math.atan(s * u) - a, a += c;
        return new S(a * e, t.x * e / i);
      }
    }, zi = {
      __proto__: null,
      LonLat: et,
      Mercator: it,
      SphericalMercator: k0
    }, Si = f({}, g2, {
      code: "EPSG:3395",
      projection: it,
      transformation: function() {
        var t = 0.5 / (Math.PI * it.R);
        return N2(t, 0.5, -t, 0.5);
      }()
    }), he = f({}, g2, {
      code: "EPSG:4326",
      projection: et,
      transformation: N2(1 / 180, 1, -1 / 180, 0.5)
    }), Ei = f({}, u2, {
      projection: et,
      transformation: N2(1, 0, -1, 0),
      scale: function(t) {
        return Math.pow(2, t);
      },
      zoom: function(t) {
        return Math.log(t) / Math.LN2;
      },
      distance: function(t, e) {
        var i = e.lng - t.lng, n = e.lat - t.lat;
        return Math.sqrt(i * i + n * n);
      },
      infinite: !0
    });
    u2.Earth = g2, u2.EPSG3395 = Si, u2.EPSG3857 = S0, u2.EPSG900913 = Re, u2.EPSG4326 = he, u2.Simple = Ei;
    var i2 = D2.extend({
      // Classes extending `L.Layer` will inherit the following options:
      options: {
        // @option pane: String = 'overlayPane'
        // By default the layer will be added to the map's [overlay pane](#map-overlaypane). Overriding this option will cause the layer to be placed on another pane by default.
        pane: "overlayPane",
        // @option attribution: String = null
        // String to be shown in the attribution control, e.g. "© OpenStreetMap contributors". It describes the layer data and is often a legal obligation towards copyright holders and tile providers.
        attribution: null,
        bubblingMouseEvents: !0
      },
      /* @section
       * Classes extending `L.Layer` will inherit the following methods:
       *
       * @method addTo(map: Map|LayerGroup): this
       * Adds the layer to the given map or layer group.
       */
      addTo: function(t) {
        return t.addLayer(this), this;
      },
      // @method remove: this
      // Removes the layer from the map it is currently active on.
      remove: function() {
        return this.removeFrom(this._map || this._mapToAdd);
      },
      // @method removeFrom(map: Map): this
      // Removes the layer from the given map
      //
      // @alternative
      // @method removeFrom(group: LayerGroup): this
      // Removes the layer from the given `LayerGroup`
      removeFrom: function(t) {
        return t && t.removeLayer(this), this;
      },
      // @method getPane(name? : String): HTMLElement
      // Returns the `HTMLElement` representing the named pane on the map. If `name` is omitted, returns the pane for this layer.
      getPane: function(t) {
        return this._map.getPane(t ? this.options[t] || t : this.options.pane);
      },
      addInteractiveTarget: function(t) {
        return this._map._targets[C(t)] = this, this;
      },
      removeInteractiveTarget: function(t) {
        return delete this._map._targets[C(t)], this;
      },
      // @method getAttribution: String
      // Used by the `attribution control`, returns the [attribution option](#gridlayer-attribution).
      getAttribution: function() {
        return this.options.attribution;
      },
      _layerAdd: function(t) {
        var e = t.target;
        if (e.hasLayer(this)) {
          if (this._map = e, this._zoomAnimated = e._zoomAnimated, this.getEvents) {
            var i = this.getEvents();
            e.on(i, this), this.once("remove", function() {
              e.off(i, this);
            }, this);
          }
          this.onAdd(e), this.fire("add"), e.fire("layeradd", { layer: this });
        }
      }
    });
    k.include({
      // @method addLayer(layer: Layer): this
      // Adds the given layer to the map
      addLayer: function(t) {
        if (!t._layerAdd)
          throw new Error("The provided object is not a Layer.");
        var e = C(t);
        return this._layers[e] ? this : (this._layers[e] = t, t._mapToAdd = this, t.beforeAdd && t.beforeAdd(this), this.whenReady(t._layerAdd, t), this);
      },
      // @method removeLayer(layer: Layer): this
      // Removes the given layer from the map.
      removeLayer: function(t) {
        var e = C(t);
        return this._layers[e] ? (this._loaded && t.onRemove(this), delete this._layers[e], this._loaded && (this.fire("layerremove", { layer: t }), t.fire("remove")), t._map = t._mapToAdd = null, this) : this;
      },
      // @method hasLayer(layer: Layer): Boolean
      // Returns `true` if the given layer is currently added to the map
      hasLayer: function(t) {
        return C(t) in this._layers;
      },
      /* @method eachLayer(fn: Function, context?: Object): this
       * Iterates over the layers of the map, optionally specifying context of the iterator function.
       * ```
       * map.eachLayer(function(layer){
       *     layer.bindPopup('Hello');
       * });
       * ```
       */
      eachLayer: function(t, e) {
        for (var i in this._layers)
          t.call(e, this._layers[i]);
        return this;
      },
      _addLayers: function(t) {
        t = t ? t2(t) ? t : [t] : [];
        for (var e = 0, i = t.length; e < i; e++)
          this.addLayer(t[e]);
      },
      _addZoomLimit: function(t) {
        (!isNaN(t.options.maxZoom) || !isNaN(t.options.minZoom)) && (this._zoomBoundLayers[C(t)] = t, this._updateZoomLevels());
      },
      _removeZoomLimit: function(t) {
        var e = C(t);
        this._zoomBoundLayers[e] && (delete this._zoomBoundLayers[e], this._updateZoomLevels());
      },
      _updateZoomLevels: function() {
        var t = 1 / 0, e = -1 / 0, i = this._getZoomSpan();
        for (var n in this._zoomBoundLayers) {
          var o = this._zoomBoundLayers[n].options;
          t = o.minZoom === void 0 ? t : Math.min(t, o.minZoom), e = o.maxZoom === void 0 ? e : Math.max(e, o.maxZoom);
        }
        this._layersMaxZoom = e === -1 / 0 ? void 0 : e, this._layersMinZoom = t === 1 / 0 ? void 0 : t, i !== this._getZoomSpan() && this.fire("zoomlevelschange"), this.options.maxZoom === void 0 && this._layersMaxZoom && this.getZoom() > this._layersMaxZoom && this.setZoom(this._layersMaxZoom), this.options.minZoom === void 0 && this._layersMinZoom && this.getZoom() < this._layersMinZoom && this.setZoom(this._layersMinZoom);
      }
    });
    var S2 = i2.extend({
      initialize: function(t, e) {
        O(this, e), this._layers = {};
        var i, n;
        if (t)
          for (i = 0, n = t.length; i < n; i++)
            this.addLayer(t[i]);
      },
      // @method addLayer(layer: Layer): this
      // Adds the given layer to the group.
      addLayer: function(t) {
        var e = this.getLayerId(t);
        return this._layers[e] = t, this._map && this._map.addLayer(t), this;
      },
      // @method removeLayer(layer: Layer): this
      // Removes the given layer from the group.
      // @alternative
      // @method removeLayer(id: Number): this
      // Removes the layer with the given internal ID from the group.
      removeLayer: function(t) {
        var e = t in this._layers ? t : this.getLayerId(t);
        return this._map && this._layers[e] && this._map.removeLayer(this._layers[e]), delete this._layers[e], this;
      },
      // @method hasLayer(layer: Layer): Boolean
      // Returns `true` if the given layer is currently added to the group.
      // @alternative
      // @method hasLayer(id: Number): Boolean
      // Returns `true` if the given internal ID is currently added to the group.
      hasLayer: function(t) {
        var e = typeof t == "number" ? t : this.getLayerId(t);
        return e in this._layers;
      },
      // @method clearLayers(): this
      // Removes all the layers from the group.
      clearLayers: function() {
        return this.eachLayer(this.removeLayer, this);
      },
      // @method invoke(methodName: String, …): this
      // Calls `methodName` on every layer contained in this group, passing any
      // additional parameters. Has no effect if the layers contained do not
      // implement `methodName`.
      invoke: function(t) {
        var e = Array.prototype.slice.call(arguments, 1), i, n;
        for (i in this._layers)
          n = this._layers[i], n[t] && n[t].apply(n, e);
        return this;
      },
      onAdd: function(t) {
        this.eachLayer(t.addLayer, t);
      },
      onRemove: function(t) {
        this.eachLayer(t.removeLayer, t);
      },
      // @method eachLayer(fn: Function, context?: Object): this
      // Iterates over the layers of the group, optionally specifying context of the iterator function.
      // ```js
      // group.eachLayer(function (layer) {
      // 	layer.bindPopup('Hello');
      // });
      // ```
      eachLayer: function(t, e) {
        for (var i in this._layers)
          t.call(e, this._layers[i]);
        return this;
      },
      // @method getLayer(id: Number): Layer
      // Returns the layer with the given internal ID.
      getLayer: function(t) {
        return this._layers[t];
      },
      // @method getLayers(): Layer[]
      // Returns an array of all the layers added to the group.
      getLayers: function() {
        var t = [];
        return this.eachLayer(t.push, t), t;
      },
      // @method setZIndex(zIndex: Number): this
      // Calls `setZIndex` on every layer contained in this group, passing the z-index.
      setZIndex: function(t) {
        return this.invoke("setZIndex", t);
      },
      // @method getLayerId(layer: Layer): Number
      // Returns the internal ID for a layer
      getLayerId: function(t) {
        return C(t);
      }
    }), Ai = function(t, e) {
      return new S2(t, e);
    }, f2 = S2.extend({
      addLayer: function(t) {
        return this.hasLayer(t) ? this : (t.addEventParent(this), S2.prototype.addLayer.call(this, t), this.fire("layeradd", { layer: t }));
      },
      removeLayer: function(t) {
        return this.hasLayer(t) ? (t in this._layers && (t = this._layers[t]), t.removeEventParent(this), S2.prototype.removeLayer.call(this, t), this.fire("layerremove", { layer: t })) : this;
      },
      // @method setStyle(style: Path options): this
      // Sets the given path options to each layer of the group that has a `setStyle` method.
      setStyle: function(t) {
        return this.invoke("setStyle", t);
      },
      // @method bringToFront(): this
      // Brings the layer group to the top of all other layers
      bringToFront: function() {
        return this.invoke("bringToFront");
      },
      // @method bringToBack(): this
      // Brings the layer group to the back of all other layers
      bringToBack: function() {
        return this.invoke("bringToBack");
      },
      // @method getBounds(): LatLngBounds
      // Returns the LatLngBounds of the Feature Group (created from bounds and coordinates of its children).
      getBounds: function() {
        var t = new q();
        for (var e in this._layers) {
          var i = this._layers[e];
          t.extend(i.getBounds ? i.getBounds() : i.getLatLng());
        }
        return t;
      }
    }), Oi = function(t, e) {
      return new f2(t, e);
    }, E2 = c2.extend({
      /* @section
       * @aka Icon options
       *
       * @option iconUrl: String = null
       * **(required)** The URL to the icon image (absolute or relative to your script path).
       *
       * @option iconRetinaUrl: String = null
       * The URL to a retina sized version of the icon image (absolute or relative to your
       * script path). Used for Retina screen devices.
       *
       * @option iconSize: Point = null
       * Size of the icon image in pixels.
       *
       * @option iconAnchor: Point = null
       * The coordinates of the "tip" of the icon (relative to its top left corner). The icon
       * will be aligned so that this point is at the marker's geographical location. Centered
       * by default if size is specified, also can be set in CSS with negative margins.
       *
       * @option popupAnchor: Point = [0, 0]
       * The coordinates of the point from which popups will "open", relative to the icon anchor.
       *
       * @option tooltipAnchor: Point = [0, 0]
       * The coordinates of the point from which tooltips will "open", relative to the icon anchor.
       *
       * @option shadowUrl: String = null
       * The URL to the icon shadow image. If not specified, no shadow image will be created.
       *
       * @option shadowRetinaUrl: String = null
       *
       * @option shadowSize: Point = null
       * Size of the shadow image in pixels.
       *
       * @option shadowAnchor: Point = null
       * The coordinates of the "tip" of the shadow (relative to its top left corner) (the same
       * as iconAnchor if not specified).
       *
       * @option className: String = ''
       * A custom class name to assign to both icon and shadow images. Empty by default.
       */
      options: {
        popupAnchor: [0, 0],
        tooltipAnchor: [0, 0],
        // @option crossOrigin: Boolean|String = false
        // Whether the crossOrigin attribute will be added to the tiles.
        // If a String is provided, all tiles will have their crossOrigin attribute set to the String provided. This is needed if you want to access tile pixel data.
        // Refer to [CORS Settings](https://developer.mozilla.org/en-US/docs/Web/HTML/CORS_settings_attributes) for valid String values.
        crossOrigin: !1
      },
      initialize: function(t) {
        O(this, t);
      },
      // @method createIcon(oldIcon?: HTMLElement): HTMLElement
      // Called internally when the icon has to be shown, returns a `<img>` HTML element
      // styled according to the options.
      createIcon: function(t) {
        return this._createIcon("icon", t);
      },
      // @method createShadow(oldIcon?: HTMLElement): HTMLElement
      // As `createIcon`, but for the shadow beneath it.
      createShadow: function(t) {
        return this._createIcon("shadow", t);
      },
      _createIcon: function(t, e) {
        var i = this._getIconUrl(t);
        if (!i) {
          if (t === "icon")
            throw new Error("iconUrl not set in Icon options (see the docs).");
          return null;
        }
        var n = this._createImg(i, e && e.tagName === "IMG" ? e : null);
        return this._setIconStyles(n, t), (this.options.crossOrigin || this.options.crossOrigin === "") && (n.crossOrigin = this.options.crossOrigin === !0 ? "" : this.options.crossOrigin), n;
      },
      _setIconStyles: function(t, e) {
        var i = this.options, n = i[e + "Size"];
        typeof n == "number" && (n = [n, n]);
        var o = v(n), s = v(e === "shadow" && i.shadowAnchor || i.iconAnchor || o && o.divideBy(2, !0));
        t.className = "leaflet-marker-" + e + " " + (i.className || ""), s && (t.style.marginLeft = -s.x + "px", t.style.marginTop = -s.y + "px"), o && (t.style.width = o.x + "px", t.style.height = o.y + "px");
      },
      _createImg: function(t, e) {
        return e = e || document.createElement("img"), e.src = t, e;
      },
      _getIconUrl: function(t) {
        return p.retina && this.options[t + "RetinaUrl"] || this.options[t + "Url"];
      }
    });
    function Bi(t) {
      return new E2(t);
    }
    var J2 = E2.extend({
      options: {
        iconUrl: "marker-icon.png",
        iconRetinaUrl: "marker-icon-2x.png",
        shadowUrl: "marker-shadow.png",
        iconSize: [25, 41],
        iconAnchor: [12, 41],
        popupAnchor: [1, -34],
        tooltipAnchor: [16, -28],
        shadowSize: [41, 41]
      },
      _getIconUrl: function(t) {
        return typeof J2.imagePath != "string" && (J2.imagePath = this._detectIconPath()), (this.options.imagePath || J2.imagePath) + E2.prototype._getIconUrl.call(this, t);
      },
      _stripUrl: function(t) {
        var e = function(i, n, o) {
          var s = n.exec(i);
          return s && s[o];
        };
        return t = e(t, /^url\((['"])?(.+)\1\)$/, 2), t && e(t, /^(.*)marker-icon\.png$/, 1);
      },
      _detectIconPath: function() {
        var t = z("div", "leaflet-default-icon-path", document.body), e = H2(t, "background-image") || H2(t, "backgroundImage");
        if (document.body.removeChild(t), e = this._stripUrl(e), e)
          return e;
        var i = document.querySelector('link[href$="leaflet.css"]');
        return i ? i.href.substring(0, i.href.length - 11 - 1) : "";
      }
    }), le = r2.extend({
      initialize: function(t) {
        this._marker = t;
      },
      addHooks: function() {
        var t = this._marker._icon;
        this._draggable || (this._draggable = new v2(t, t, !0)), this._draggable.on({
          dragstart: this._onDragStart,
          predrag: this._onPreDrag,
          drag: this._onDrag,
          dragend: this._onDragEnd
        }, this).enable(), b(t, "leaflet-marker-draggable");
      },
      removeHooks: function() {
        this._draggable.off({
          dragstart: this._onDragStart,
          predrag: this._onPreDrag,
          drag: this._onDrag,
          dragend: this._onDragEnd
        }, this).disable(), this._marker._icon && R(this._marker._icon, "leaflet-marker-draggable");
      },
      moved: function() {
        return this._draggable && this._draggable._moved;
      },
      _adjustPan: function(t) {
        var e = this._marker, i = e._map, n = this._marker.options.autoPanSpeed, o = this._marker.options.autoPanPadding, s = C2(e._icon), a = i.getPixelBounds(), l = i.getPixelOrigin(), c = V(
          a.min._subtract(l).add(o),
          a.max._subtract(l).subtract(o)
        );
        if (!c.contains(s)) {
          var u = v(
            (Math.max(c.max.x, s.x) - c.max.x) / (a.max.x - c.max.x) - (Math.min(c.min.x, s.x) - c.min.x) / (a.min.x - c.min.x),
            (Math.max(c.max.y, s.y) - c.max.y) / (a.max.y - c.max.y) - (Math.min(c.min.y, s.y) - c.min.y) / (a.min.y - c.min.y)
          ).multiplyBy(n);
          i.panBy(u, { animate: !1 }), this._draggable._newPos._add(u), this._draggable._startPos._add(u), N(e._icon, this._draggable._newPos), this._onDrag(t), this._panRequest = U(this._adjustPan.bind(this, t));
        }
      },
      _onDragStart: function() {
        this._oldLatLng = this._marker.getLatLng(), this._marker.closePopup && this._marker.closePopup(), this._marker.fire("movestart").fire("dragstart");
      },
      _onPreDrag: function(t) {
        this._marker.options.autoPan && (K(this._panRequest), this._panRequest = U(this._adjustPan.bind(this, t)));
      },
      _onDrag: function(t) {
        var e = this._marker, i = e._shadow, n = C2(e._icon), o = e._map.layerPointToLatLng(n);
        i && N(i, n), e._latlng = o, t.latlng = o, t.oldLatLng = this._oldLatLng, e.fire("move", t).fire("drag", t);
      },
      _onDragEnd: function(t) {
        K(this._panRequest), delete this._oldLatLng, this._marker.fire("moveend").fire("dragend", t);
      }
    }), c0 = i2.extend({
      // @section
      // @aka Marker options
      options: {
        // @option icon: Icon = *
        // Icon instance to use for rendering the marker.
        // See [Icon documentation](#L.Icon) for details on how to customize the marker icon.
        // If not specified, a common instance of `L.Icon.Default` is used.
        icon: new J2(),
        // Option inherited from "Interactive layer" abstract class
        interactive: !0,
        // @option keyboard: Boolean = true
        // Whether the marker can be tabbed to with a keyboard and clicked by pressing enter.
        keyboard: !0,
        // @option title: String = ''
        // Text for the browser tooltip that appear on marker hover (no tooltip by default).
        // [Useful for accessibility](https://leafletjs.com/examples/accessibility/#markers-must-be-labelled).
        title: "",
        // @option alt: String = 'Marker'
        // Text for the `alt` attribute of the icon image.
        // [Useful for accessibility](https://leafletjs.com/examples/accessibility/#markers-must-be-labelled).
        alt: "Marker",
        // @option zIndexOffset: Number = 0
        // By default, marker images zIndex is set automatically based on its latitude. Use this option if you want to put the marker on top of all others (or below), specifying a high value like `1000` (or high negative value, respectively).
        zIndexOffset: 0,
        // @option opacity: Number = 1.0
        // The opacity of the marker.
        opacity: 1,
        // @option riseOnHover: Boolean = false
        // If `true`, the marker will get on top of others when you hover the mouse over it.
        riseOnHover: !1,
        // @option riseOffset: Number = 250
        // The z-index offset used for the `riseOnHover` feature.
        riseOffset: 250,
        // @option pane: String = 'markerPane'
        // `Map pane` where the markers icon will be added.
        pane: "markerPane",
        // @option shadowPane: String = 'shadowPane'
        // `Map pane` where the markers shadow will be added.
        shadowPane: "shadowPane",
        // @option bubblingMouseEvents: Boolean = false
        // When `true`, a mouse event on this marker will trigger the same event on the map
        // (unless [`L.DomEvent.stopPropagation`](#domevent-stoppropagation) is used).
        bubblingMouseEvents: !1,
        // @option autoPanOnFocus: Boolean = true
        // When `true`, the map will pan whenever the marker is focused (via
        // e.g. pressing `tab` on the keyboard) to ensure the marker is
        // visible within the map's bounds
        autoPanOnFocus: !0,
        // @section Draggable marker options
        // @option draggable: Boolean = false
        // Whether the marker is draggable with mouse/touch or not.
        draggable: !1,
        // @option autoPan: Boolean = false
        // Whether to pan the map when dragging this marker near its edge or not.
        autoPan: !1,
        // @option autoPanPadding: Point = Point(50, 50)
        // Distance (in pixels to the left/right and to the top/bottom) of the
        // map edge to start panning the map.
        autoPanPadding: [50, 50],
        // @option autoPanSpeed: Number = 10
        // Number of pixels the map should pan by.
        autoPanSpeed: 10
      },
      /* @section
       *
       * In addition to [shared layer methods](#Layer) like `addTo()` and `remove()` and [popup methods](#Popup) like bindPopup() you can also use the following methods:
       */
      initialize: function(t, e) {
        O(this, e), this._latlng = M(t);
      },
      onAdd: function(t) {
        this._zoomAnimated = this._zoomAnimated && t.options.markerZoomAnimation, this._zoomAnimated && t.on("zoomanim", this._animateZoom, this), this._initIcon(), this.update();
      },
      onRemove: function(t) {
        this.dragging && this.dragging.enabled() && (this.options.draggable = !0, this.dragging.removeHooks()), delete this.dragging, this._zoomAnimated && t.off("zoomanim", this._animateZoom, this), this._removeIcon(), this._removeShadow();
      },
      getEvents: function() {
        return {
          zoom: this.update,
          viewreset: this.update
        };
      },
      // @method getLatLng: LatLng
      // Returns the current geographical position of the marker.
      getLatLng: function() {
        return this._latlng;
      },
      // @method setLatLng(latlng: LatLng): this
      // Changes the marker position to the given point.
      setLatLng: function(t) {
        var e = this._latlng;
        return this._latlng = M(t), this.update(), this.fire("move", { oldLatLng: e, latlng: this._latlng });
      },
      // @method setZIndexOffset(offset: Number): this
      // Changes the [zIndex offset](#marker-zindexoffset) of the marker.
      setZIndexOffset: function(t) {
        return this.options.zIndexOffset = t, this.update();
      },
      // @method getIcon: Icon
      // Returns the current icon used by the marker
      getIcon: function() {
        return this.options.icon;
      },
      // @method setIcon(icon: Icon): this
      // Changes the marker icon.
      setIcon: function(t) {
        return this.options.icon = t, this._map && (this._initIcon(), this.update()), this._popup && this.bindPopup(this._popup, this._popup.options), this;
      },
      getElement: function() {
        return this._icon;
      },
      update: function() {
        if (this._icon && this._map) {
          var t = this._map.latLngToLayerPoint(this._latlng).round();
          this._setPos(t);
        }
        return this;
      },
      _initIcon: function() {
        var t = this.options, e = "leaflet-zoom-" + (this._zoomAnimated ? "animated" : "hide"), i = t.icon.createIcon(this._icon), n = !1;
        i !== this._icon && (this._icon && this._removeIcon(), n = !0, t.title && (i.title = t.title), i.tagName === "IMG" && (i.alt = t.alt || "")), b(i, e), t.keyboard && (i.tabIndex = "0", i.setAttribute("role", "button")), this._icon = i, t.riseOnHover && this.on({
          mouseover: this._bringToFront,
          mouseout: this._resetZIndex
        }), this.options.autoPanOnFocus && w(i, "focus", this._panOnFocus, this);
        var o = t.icon.createShadow(this._shadow), s = !1;
        o !== this._shadow && (this._removeShadow(), s = !0), o && (b(o, e), o.alt = ""), this._shadow = o, t.opacity < 1 && this._updateOpacity(), n && this.getPane().appendChild(this._icon), this._initInteraction(), o && s && this.getPane(t.shadowPane).appendChild(this._shadow);
      },
      _removeIcon: function() {
        this.options.riseOnHover && this.off({
          mouseover: this._bringToFront,
          mouseout: this._resetZIndex
        }), this.options.autoPanOnFocus && A(this._icon, "focus", this._panOnFocus, this), Z(this._icon), this.removeInteractiveTarget(this._icon), this._icon = null;
      },
      _removeShadow: function() {
        this._shadow && Z(this._shadow), this._shadow = null;
      },
      _setPos: function(t) {
        this._icon && N(this._icon, t), this._shadow && N(this._shadow, t), this._zIndex = t.y + this.options.zIndexOffset, this._resetZIndex();
      },
      _updateZIndex: function(t) {
        this._icon && (this._icon.style.zIndex = this._zIndex + t);
      },
      _animateZoom: function(t) {
        var e = this._map._latLngToNewLayerPoint(this._latlng, t.zoom, t.center).round();
        this._setPos(e);
      },
      _initInteraction: function() {
        if (this.options.interactive && (b(this._icon, "leaflet-interactive"), this.addInteractiveTarget(this._icon), le)) {
          var t = this.options.draggable;
          this.dragging && (t = this.dragging.enabled(), this.dragging.disable()), this.dragging = new le(this), t && this.dragging.enable();
        }
      },
      // @method setOpacity(opacity: Number): this
      // Changes the opacity of the marker.
      setOpacity: function(t) {
        return this.options.opacity = t, this._map && this._updateOpacity(), this;
      },
      _updateOpacity: function() {
        var t = this.options.opacity;
        this._icon && X(this._icon, t), this._shadow && X(this._shadow, t);
      },
      _bringToFront: function() {
        this._updateZIndex(this.options.riseOffset);
      },
      _resetZIndex: function() {
        this._updateZIndex(0);
      },
      _panOnFocus: function() {
        var t = this._map;
        if (t) {
          var e = this.options.icon.options, i = e.iconSize ? v(e.iconSize) : v(0, 0), n = e.iconAnchor ? v(e.iconAnchor) : v(0, 0);
          t.panInside(this._latlng, {
            paddingTopLeft: n,
            paddingBottomRight: i.subtract(n)
          });
        }
      },
      _getPopupAnchor: function() {
        return this.options.icon.options.popupAnchor;
      },
      _getTooltipAnchor: function() {
        return this.options.icon.options.tooltipAnchor;
      }
    });
    function Zi(t, e) {
      return new c0(t, e);
    }
    var y2 = i2.extend({
      // @section
      // @aka Path options
      options: {
        // @option stroke: Boolean = true
        // Whether to draw stroke along the path. Set it to `false` to disable borders on polygons or circles.
        stroke: !0,
        // @option color: String = '#3388ff'
        // Stroke color
        color: "#3388ff",
        // @option weight: Number = 3
        // Stroke width in pixels
        weight: 3,
        // @option opacity: Number = 1.0
        // Stroke opacity
        opacity: 1,
        // @option lineCap: String= 'round'
        // A string that defines [shape to be used at the end](https://developer.mozilla.org/docs/Web/SVG/Attribute/stroke-linecap) of the stroke.
        lineCap: "round",
        // @option lineJoin: String = 'round'
        // A string that defines [shape to be used at the corners](https://developer.mozilla.org/docs/Web/SVG/Attribute/stroke-linejoin) of the stroke.
        lineJoin: "round",
        // @option dashArray: String = null
        // A string that defines the stroke [dash pattern](https://developer.mozilla.org/docs/Web/SVG/Attribute/stroke-dasharray). Doesn't work on `Canvas`-powered layers in [some old browsers](https://developer.mozilla.org/docs/Web/API/CanvasRenderingContext2D/setLineDash#Browser_compatibility).
        dashArray: null,
        // @option dashOffset: String = null
        // A string that defines the [distance into the dash pattern to start the dash](https://developer.mozilla.org/docs/Web/SVG/Attribute/stroke-dashoffset). Doesn't work on `Canvas`-powered layers in [some old browsers](https://developer.mozilla.org/docs/Web/API/CanvasRenderingContext2D/setLineDash#Browser_compatibility).
        dashOffset: null,
        // @option fill: Boolean = depends
        // Whether to fill the path with color. Set it to `false` to disable filling on polygons or circles.
        fill: !1,
        // @option fillColor: String = *
        // Fill color. Defaults to the value of the [`color`](#path-color) option
        fillColor: null,
        // @option fillOpacity: Number = 0.2
        // Fill opacity.
        fillOpacity: 0.2,
        // @option fillRule: String = 'evenodd'
        // A string that defines [how the inside of a shape](https://developer.mozilla.org/docs/Web/SVG/Attribute/fill-rule) is determined.
        fillRule: "evenodd",
        // className: '',
        // Option inherited from "Interactive layer" abstract class
        interactive: !0,
        // @option bubblingMouseEvents: Boolean = true
        // When `true`, a mouse event on this path will trigger the same event on the map
        // (unless [`L.DomEvent.stopPropagation`](#domevent-stoppropagation) is used).
        bubblingMouseEvents: !0
      },
      beforeAdd: function(t) {
        this._renderer = t.getRenderer(this);
      },
      onAdd: function() {
        this._renderer._initPath(this), this._reset(), this._renderer._addPath(this);
      },
      onRemove: function() {
        this._renderer._removePath(this);
      },
      // @method redraw(): this
      // Redraws the layer. Sometimes useful after you changed the coordinates that the path uses.
      redraw: function() {
        return this._map && this._renderer._updatePath(this), this;
      },
      // @method setStyle(style: Path options): this
      // Changes the appearance of a Path based on the options in the `Path options` object.
      setStyle: function(t) {
        return O(this, t), this._renderer && (this._renderer._updateStyle(this), this.options.stroke && t && Object.prototype.hasOwnProperty.call(t, "weight") && this._updateBounds()), this;
      },
      // @method bringToFront(): this
      // Brings the layer to the top of all path layers.
      bringToFront: function() {
        return this._renderer && this._renderer._bringToFront(this), this;
      },
      // @method bringToBack(): this
      // Brings the layer to the bottom of all path layers.
      bringToBack: function() {
        return this._renderer && this._renderer._bringToBack(this), this;
      },
      getElement: function() {
        return this._path;
      },
      _reset: function() {
        this._project(), this._update();
      },
      _clickTolerance: function() {
        return (this.options.stroke ? this.options.weight / 2 : 0) + (this._renderer.options.tolerance || 0);
      }
    }), u0 = y2.extend({
      // @section
      // @aka CircleMarker options
      options: {
        fill: !0,
        // @option radius: Number = 10
        // Radius of the circle marker, in pixels
        radius: 10
      },
      initialize: function(t, e) {
        O(this, e), this._latlng = M(t), this._radius = this.options.radius;
      },
      // @method setLatLng(latLng: LatLng): this
      // Sets the position of a circle marker to a new location.
      setLatLng: function(t) {
        var e = this._latlng;
        return this._latlng = M(t), this.redraw(), this.fire("move", { oldLatLng: e, latlng: this._latlng });
      },
      // @method getLatLng(): LatLng
      // Returns the current geographical position of the circle marker
      getLatLng: function() {
        return this._latlng;
      },
      // @method setRadius(radius: Number): this
      // Sets the radius of a circle marker. Units are in pixels.
      setRadius: function(t) {
        return this.options.radius = this._radius = t, this.redraw();
      },
      // @method getRadius(): Number
      // Returns the current radius of the circle
      getRadius: function() {
        return this._radius;
      },
      setStyle: function(t) {
        var e = t && t.radius || this._radius;
        return y2.prototype.setStyle.call(this, t), this.setRadius(e), this;
      },
      _project: function() {
        this._point = this._map.latLngToLayerPoint(this._latlng), this._updateBounds();
      },
      _updateBounds: function() {
        var t = this._radius, e = this._radiusY || t, i = this._clickTolerance(), n = [t + i, e + i];
        this._pxBounds = new B(this._point.subtract(n), this._point.add(n));
      },
      _update: function() {
        this._map && this._updatePath();
      },
      _updatePath: function() {
        this._renderer._updateCircle(this);
      },
      _empty: function() {
        return this._radius && !this._renderer._bounds.intersects(this._pxBounds);
      },
      // Needed by the `Canvas` renderer for interactivity
      _containsPoint: function(t) {
        return t.distanceTo(this._point) <= this._radius + this._clickTolerance();
      }
    });
    function Ii(t, e) {
      return new u0(t, e);
    }
    var nt = u0.extend({
      initialize: function(t, e, i) {
        if (typeof e == "number" && (e = f({}, i, { radius: e })), O(this, e), this._latlng = M(t), isNaN(this.options.radius))
          throw new Error("Circle radius cannot be NaN");
        this._mRadius = this.options.radius;
      },
      // @method setRadius(radius: Number): this
      // Sets the radius of a circle. Units are in meters.
      setRadius: function(t) {
        return this._mRadius = t, this.redraw();
      },
      // @method getRadius(): Number
      // Returns the current radius of a circle. Units are in meters.
      getRadius: function() {
        return this._mRadius;
      },
      // @method getBounds(): LatLngBounds
      // Returns the `LatLngBounds` of the path.
      getBounds: function() {
        var t = [this._radius, this._radiusY || this._radius];
        return new q(
          this._map.layerPointToLatLng(this._point.subtract(t)),
          this._map.layerPointToLatLng(this._point.add(t))
        );
      },
      setStyle: y2.prototype.setStyle,
      _project: function() {
        var t = this._latlng.lng, e = this._latlng.lat, i = this._map, n = i.options.crs;
        if (n.distance === g2.distance) {
          var o = Math.PI / 180, s = this._mRadius / g2.R / o, a = i.project([e + s, t]), l = i.project([e - s, t]), c = a.add(l).divideBy(2), u = i.unproject(c).lat, _ = Math.acos((Math.cos(s * o) - Math.sin(e * o) * Math.sin(u * o)) / (Math.cos(e * o) * Math.cos(u * o))) / o;
          (isNaN(_) || _ === 0) && (_ = s / Math.cos(Math.PI / 180 * e)), this._point = c.subtract(i.getPixelOrigin()), this._radius = isNaN(_) ? 0 : c.x - i.project([u, t - _]).x, this._radiusY = c.y - a.y;
        } else {
          var g = n.unproject(n.project(this._latlng).subtract([this._mRadius, 0]));
          this._point = i.latLngToLayerPoint(this._latlng), this._radius = this._point.x - i.latLngToLayerPoint(g).x;
        }
        this._updateBounds();
      }
    });
    function Ri(t, e, i) {
      return new nt(t, e, i);
    }
    var d2 = y2.extend({
      // @section
      // @aka Polyline options
      options: {
        // @option smoothFactor: Number = 1.0
        // How much to simplify the polyline on each zoom level. More means
        // better performance and smoother look, and less means more accurate representation.
        smoothFactor: 1,
        // @option noClip: Boolean = false
        // Disable polyline clipping.
        noClip: !1
      },
      initialize: function(t, e) {
        O(this, e), this._setLatLngs(t);
      },
      // @method getLatLngs(): LatLng[]
      // Returns an array of the points in the path, or nested arrays of points in case of multi-polyline.
      getLatLngs: function() {
        return this._latlngs;
      },
      // @method setLatLngs(latlngs: LatLng[]): this
      // Replaces all the points in the polyline with the given array of geographical points.
      setLatLngs: function(t) {
        return this._setLatLngs(t), this.redraw();
      },
      // @method isEmpty(): Boolean
      // Returns `true` if the Polyline has no LatLngs.
      isEmpty: function() {
        return !this._latlngs.length;
      },
      // @method closestLayerPoint(p: Point): Point
      // Returns the point closest to `p` on the Polyline.
      closestLayerPoint: function(t) {
        for (var e = 1 / 0, i = null, n = j2, o, s, a = 0, l = this._parts.length; a < l; a++)
          for (var c = this._parts[a], u = 1, _ = c.length; u < _; u++) {
            o = c[u - 1], s = c[u];
            var g = n(t, o, s, !0);
            g < e && (e = g, i = n(t, o, s));
          }
        return i && (i.distance = Math.sqrt(e)), i;
      },
      // @method getCenter(): LatLng
      // Returns the center ([centroid](https://en.wikipedia.org/wiki/Centroid)) of the polyline.
      getCenter: function() {
        if (!this._map)
          throw new Error("Must add layer to map before using getCenter()");
        return ae(this._defaultShape(), this._map.options.crs);
      },
      // @method getBounds(): LatLngBounds
      // Returns the `LatLngBounds` of the path.
      getBounds: function() {
        return this._bounds;
      },
      // @method addLatLng(latlng: LatLng, latlngs?: LatLng[]): this
      // Adds a given point to the polyline. By default, adds to the first ring of
      // the polyline in case of a multi-polyline, but can be overridden by passing
      // a specific ring as a LatLng array (that you can earlier access with [`getLatLngs`](#polyline-getlatlngs)).
      addLatLng: function(t, e) {
        return e = e || this._defaultShape(), t = M(t), e.push(t), this._bounds.extend(t), this.redraw();
      },
      _setLatLngs: function(t) {
        this._bounds = new q(), this._latlngs = this._convertLatLngs(t);
      },
      _defaultShape: function() {
        return Q(this._latlngs) ? this._latlngs : this._latlngs[0];
      },
      // recursively convert latlngs input into actual LatLng instances; calculate bounds along the way
      _convertLatLngs: function(t) {
        for (var e = [], i = Q(t), n = 0, o = t.length; n < o; n++)
          i ? (e[n] = M(t[n]), this._bounds.extend(e[n])) : e[n] = this._convertLatLngs(t[n]);
        return e;
      },
      _project: function() {
        var t = new B();
        this._rings = [], this._projectLatlngs(this._latlngs, this._rings, t), this._bounds.isValid() && t.isValid() && (this._rawPxBounds = t, this._updateBounds());
      },
      _updateBounds: function() {
        var t = this._clickTolerance(), e = new x(t, t);
        this._rawPxBounds && (this._pxBounds = new B([
          this._rawPxBounds.min.subtract(e),
          this._rawPxBounds.max.add(e)
        ]));
      },
      // recursively turns latlngs into a set of rings with projected coordinates
      _projectLatlngs: function(t, e, i) {
        var n = t[0] instanceof S, o = t.length, s, a;
        if (n) {
          for (a = [], s = 0; s < o; s++)
            a[s] = this._map.latLngToLayerPoint(t[s]), i.extend(a[s]);
          e.push(a);
        } else
          for (s = 0; s < o; s++)
            this._projectLatlngs(t[s], e, i);
      },
      // clip polyline by renderer bounds so that we have less to render for performance
      _clipPoints: function() {
        var t = this._renderer._bounds;
        if (this._parts = [], !(!this._pxBounds || !this._pxBounds.intersects(t))) {
          if (this.options.noClip) {
            this._parts = this._rings;
            return;
          }
          var e = this._parts, i, n, o, s, a, l, c;
          for (i = 0, o = 0, s = this._rings.length; i < s; i++)
            for (c = this._rings[i], n = 0, a = c.length; n < a - 1; n++)
              l = se(c[n], c[n + 1], t, n, !0), l && (e[o] = e[o] || [], e[o].push(l[0]), (l[1] !== c[n + 1] || n === a - 2) && (e[o].push(l[1]), o++));
        }
      },
      // simplify each clipped part of the polyline for performance
      _simplifyPoints: function() {
        for (var t = this._parts, e = this.options.smoothFactor, i = 0, n = t.length; i < n; i++)
          t[i] = ie(t[i], e);
      },
      _update: function() {
        this._map && (this._clipPoints(), this._simplifyPoints(), this._updatePath());
      },
      _updatePath: function() {
        this._renderer._updatePoly(this);
      },
      // Needed by the `Canvas` renderer for interactivity
      _containsPoint: function(t, e) {
        var i, n, o, s, a, l, c = this._clickTolerance();
        if (!this._pxBounds || !this._pxBounds.contains(t))
          return !1;
        for (i = 0, s = this._parts.length; i < s; i++)
          for (l = this._parts[i], n = 0, a = l.length, o = a - 1; n < a; o = n++)
            if (!(!e && n === 0) && ne(t, l[o], l[n]) <= c)
              return !0;
        return !1;
      }
    });
    function Di(t, e) {
      return new d2(t, e);
    }
    d2._flat = re;
    var A2 = d2.extend({
      options: {
        fill: !0
      },
      isEmpty: function() {
        return !this._latlngs.length || !this._latlngs[0].length;
      },
      // @method getCenter(): LatLng
      // Returns the center ([centroid](http://en.wikipedia.org/wiki/Centroid)) of the Polygon.
      getCenter: function() {
        if (!this._map)
          throw new Error("Must add layer to map before using getCenter()");
        return ee(this._defaultShape(), this._map.options.crs);
      },
      _convertLatLngs: function(t) {
        var e = d2.prototype._convertLatLngs.call(this, t), i = e.length;
        return i >= 2 && e[0] instanceof S && e[0].equals(e[i - 1]) && e.pop(), e;
      },
      _setLatLngs: function(t) {
        d2.prototype._setLatLngs.call(this, t), Q(this._latlngs) && (this._latlngs = [this._latlngs]);
      },
      _defaultShape: function() {
        return Q(this._latlngs[0]) ? this._latlngs[0] : this._latlngs[0][0];
      },
      _clipPoints: function() {
        var t = this._renderer._bounds, e = this.options.weight, i = new x(e, e);
        if (t = new B(t.min.subtract(i), t.max.add(i)), this._parts = [], !(!this._pxBounds || !this._pxBounds.intersects(t))) {
          if (this.options.noClip) {
            this._parts = this._rings;
            return;
          }
          for (var n = 0, o = this._rings.length, s; n < o; n++)
            s = te(this._rings[n], t, !0), s.length && this._parts.push(s);
        }
      },
      _updatePath: function() {
        this._renderer._updatePoly(this, !0);
      },
      // Needed by the `Canvas` renderer for interactivity
      _containsPoint: function(t) {
        var e = !1, i, n, o, s, a, l, c, u;
        if (!this._pxBounds || !this._pxBounds.contains(t))
          return !1;
        for (s = 0, c = this._parts.length; s < c; s++)
          for (i = this._parts[s], a = 0, u = i.length, l = u - 1; a < u; l = a++)
            n = i[a], o = i[l], n.y > t.y != o.y > t.y && t.x < (o.x - n.x) * (t.y - n.y) / (o.y - n.y) + n.x && (e = !e);
        return e || d2.prototype._containsPoint.call(this, t, !0);
      }
    });
    function Ni(t, e) {
      return new A2(t, e);
    }
    var _2 = f2.extend({
      /* @section
       * @aka GeoJSON options
       *
       * @option pointToLayer: Function = *
       * A `Function` defining how GeoJSON points spawn Leaflet layers. It is internally
       * called when data is added, passing the GeoJSON point feature and its `LatLng`.
       * The default is to spawn a default `Marker`:
       * ```js
       * function(geoJsonPoint, latlng) {
       * 	return L.marker(latlng);
       * }
       * ```
       *
       * @option style: Function = *
       * A `Function` defining the `Path options` for styling GeoJSON lines and polygons,
       * called internally when data is added.
       * The default value is to not override any defaults:
       * ```js
       * function (geoJsonFeature) {
       * 	return {}
       * }
       * ```
       *
       * @option onEachFeature: Function = *
       * A `Function` that will be called once for each created `Feature`, after it has
       * been created and styled. Useful for attaching events and popups to features.
       * The default is to do nothing with the newly created layers:
       * ```js
       * function (feature, layer) {}
       * ```
       *
       * @option filter: Function = *
       * A `Function` that will be used to decide whether to include a feature or not.
       * The default is to include all features:
       * ```js
       * function (geoJsonFeature) {
       * 	return true;
       * }
       * ```
       * Note: dynamically changing the `filter` option will have effect only on newly
       * added data. It will _not_ re-evaluate already included features.
       *
       * @option coordsToLatLng: Function = *
       * A `Function` that will be used for converting GeoJSON coordinates to `LatLng`s.
       * The default is the `coordsToLatLng` static method.
       *
       * @option markersInheritOptions: Boolean = false
       * Whether default Markers for "Point" type Features inherit from group options.
       */
      initialize: function(t, e) {
        O(this, e), this._layers = {}, t && this.addData(t);
      },
      // @method addData( <GeoJSON> data ): this
      // Adds a GeoJSON object to the layer.
      addData: function(t) {
        var e = t2(t) ? t : t.features, i, n, o;
        if (e) {
          for (i = 0, n = e.length; i < n; i++)
            o = e[i], (o.geometries || o.geometry || o.features || o.coordinates) && this.addData(o);
          return this;
        }
        var s = this.options;
        if (s.filter && !s.filter(t))
          return this;
        var a = f0(t, s);
        return a ? (a.feature = m0(t), a.defaultOptions = a.options, this.resetStyle(a), s.onEachFeature && s.onEachFeature(t, a), this.addLayer(a)) : this;
      },
      // @method resetStyle( <Path> layer? ): this
      // Resets the given vector layer's style to the original GeoJSON style, useful for resetting style after hover events.
      // If `layer` is omitted, the style of all features in the current layer is reset.
      resetStyle: function(t) {
        return t === void 0 ? this.eachLayer(this.resetStyle, this) : (t.options = f({}, t.defaultOptions), this._setLayerStyle(t, this.options.style), this);
      },
      // @method setStyle( <Function> style ): this
      // Changes styles of GeoJSON vector layers with the given style function.
      setStyle: function(t) {
        return this.eachLayer(function(e) {
          this._setLayerStyle(e, t);
        }, this);
      },
      _setLayerStyle: function(t, e) {
        t.setStyle && (typeof e == "function" && (e = e(t.feature)), t.setStyle(e));
      }
    });
    function f0(t, e) {
      var i = t.type === "Feature" ? t.geometry : t, n = i ? i.coordinates : null, o = [], s = e && e.pointToLayer, a = e && e.coordsToLatLng || ot, l, c, u, _;
      if (!n && !i)
        return null;
      switch (i.type) {
        case "Point":
          return l = a(n), ce(s, t, l, e);
        case "MultiPoint":
          for (u = 0, _ = n.length; u < _; u++)
            l = a(n[u]), o.push(ce(s, t, l, e));
          return new f2(o);
        case "LineString":
        case "MultiLineString":
          return c = d0(n, i.type === "LineString" ? 0 : 1, a), new d2(c, e);
        case "Polygon":
        case "MultiPolygon":
          return c = d0(n, i.type === "Polygon" ? 1 : 2, a), new A2(c, e);
        case "GeometryCollection":
          for (u = 0, _ = i.geometries.length; u < _; u++) {
            var g = f0({
              geometry: i.geometries[u],
              type: "Feature",
              properties: t.properties
            }, e);
            g && o.push(g);
          }
          return new f2(o);
        case "FeatureCollection":
          for (u = 0, _ = i.features.length; u < _; u++) {
            var P = f0(i.features[u], e);
            P && o.push(P);
          }
          return new f2(o);
        default:
          throw new Error("Invalid GeoJSON object.");
      }
    }
    function ce(t, e, i, n) {
      return t ? t(e, i) : new c0(i, n && n.markersInheritOptions && n);
    }
    function ot(t) {
      return new S(t[1], t[0], t[2]);
    }
    function d0(t, e, i) {
      for (var n = [], o = 0, s = t.length, a; o < s; o++)
        a = e ? d0(t[o], e - 1, i) : (i || ot)(t[o]), n.push(a);
      return n;
    }
    function st(t, e) {
      return t = M(t), t.alt !== void 0 ? [n2(t.lng, e), n2(t.lat, e), n2(t.alt, e)] : [n2(t.lng, e), n2(t.lat, e)];
    }
    function _0(t, e, i, n) {
      for (var o = [], s = 0, a = t.length; s < a; s++)
        o.push(e ? _0(t[s], Q(t[s]) ? 0 : e - 1, i, n) : st(t[s], n));
      return !e && i && o.length > 0 && o.push(o[0].slice()), o;
    }
    function O2(t, e) {
      return t.feature ? f({}, t.feature, { geometry: e }) : m0(e);
    }
    function m0(t) {
      return t.type === "Feature" || t.type === "FeatureCollection" ? t : {
        type: "Feature",
        properties: {},
        geometry: t
      };
    }
    var rt = {
      toGeoJSON: function(t) {
        return O2(this, {
          type: "Point",
          coordinates: st(this.getLatLng(), t)
        });
      }
    };
    c0.include(rt), nt.include(rt), u0.include(rt), d2.include({
      toGeoJSON: function(t) {
        var e = !Q(this._latlngs), i = _0(this._latlngs, e ? 1 : 0, !1, t);
        return O2(this, {
          type: (e ? "Multi" : "") + "LineString",
          coordinates: i
        });
      }
    }), A2.include({
      toGeoJSON: function(t) {
        var e = !Q(this._latlngs), i = e && !Q(this._latlngs[0]), n = _0(this._latlngs, i ? 2 : e ? 1 : 0, !0, t);
        return e || (n = [n]), O2(this, {
          type: (i ? "Multi" : "") + "Polygon",
          coordinates: n
        });
      }
    }), S2.include({
      toMultiPoint: function(t) {
        var e = [];
        return this.eachLayer(function(i) {
          e.push(i.toGeoJSON(t).geometry.coordinates);
        }), O2(this, {
          type: "MultiPoint",
          coordinates: e
        });
      },
      // @method toGeoJSON(precision?: Number|false): Object
      // Coordinates values are rounded with [`formatNum`](#util-formatnum) function with given `precision`.
      // Returns a [`GeoJSON`](https://en.wikipedia.org/wiki/GeoJSON) representation of the layer group (as a GeoJSON `FeatureCollection`, `GeometryCollection`, or `MultiPoint`).
      toGeoJSON: function(t) {
        var e = this.feature && this.feature.geometry && this.feature.geometry.type;
        if (e === "MultiPoint")
          return this.toMultiPoint(t);
        var i = e === "GeometryCollection", n = [];
        return this.eachLayer(function(o) {
          if (o.toGeoJSON) {
            var s = o.toGeoJSON(t);
            if (i)
              n.push(s.geometry);
            else {
              var a = m0(s);
              a.type === "FeatureCollection" ? n.push.apply(n, a.features) : n.push(a);
            }
          }
        }), i ? O2(this, {
          geometries: n,
          type: "GeometryCollection"
        }) : {
          type: "FeatureCollection",
          features: n
        };
      }
    });
    function ue(t, e) {
      return new _2(t, e);
    }
    var Fi = ue, p0 = i2.extend({
      // @section
      // @aka ImageOverlay options
      options: {
        // @option opacity: Number = 1.0
        // The opacity of the image overlay.
        opacity: 1,
        // @option alt: String = ''
        // Text for the `alt` attribute of the image (useful for accessibility).
        alt: "",
        // @option interactive: Boolean = false
        // If `true`, the image overlay will emit [mouse events](#interactive-layer) when clicked or hovered.
        interactive: !1,
        // @option crossOrigin: Boolean|String = false
        // Whether the crossOrigin attribute will be added to the image.
        // If a String is provided, the image will have its crossOrigin attribute set to the String provided. This is needed if you want to access image pixel data.
        // Refer to [CORS Settings](https://developer.mozilla.org/en-US/docs/Web/HTML/CORS_settings_attributes) for valid String values.
        crossOrigin: !1,
        // @option errorOverlayUrl: String = ''
        // URL to the overlay image to show in place of the overlay that failed to load.
        errorOverlayUrl: "",
        // @option zIndex: Number = 1
        // The explicit [zIndex](https://developer.mozilla.org/docs/Web/CSS/CSS_Positioning/Understanding_z_index) of the overlay layer.
        zIndex: 1,
        // @option className: String = ''
        // A custom class name to assign to the image. Empty by default.
        className: ""
      },
      initialize: function(t, e, i) {
        this._url = t, this._bounds = D(e), O(this, i);
      },
      onAdd: function() {
        this._image || (this._initImage(), this.options.opacity < 1 && this._updateOpacity()), this.options.interactive && (b(this._image, "leaflet-interactive"), this.addInteractiveTarget(this._image)), this.getPane().appendChild(this._image), this._reset();
      },
      onRemove: function() {
        Z(this._image), this.options.interactive && this.removeInteractiveTarget(this._image);
      },
      // @method setOpacity(opacity: Number): this
      // Sets the opacity of the overlay.
      setOpacity: function(t) {
        return this.options.opacity = t, this._image && this._updateOpacity(), this;
      },
      setStyle: function(t) {
        return t.opacity && this.setOpacity(t.opacity), this;
      },
      // @method bringToFront(): this
      // Brings the layer to the top of all overlays.
      bringToFront: function() {
        return this._map && k2(this._image), this;
      },
      // @method bringToBack(): this
      // Brings the layer to the bottom of all overlays.
      bringToBack: function() {
        return this._map && z2(this._image), this;
      },
      // @method setUrl(url: String): this
      // Changes the URL of the image.
      setUrl: function(t) {
        return this._url = t, this._image && (this._image.src = t), this;
      },
      // @method setBounds(bounds: LatLngBounds): this
      // Update the bounds that this ImageOverlay covers
      setBounds: function(t) {
        return this._bounds = D(t), this._map && this._reset(), this;
      },
      getEvents: function() {
        var t = {
          zoom: this._reset,
          viewreset: this._reset
        };
        return this._zoomAnimated && (t.zoomanim = this._animateZoom), t;
      },
      // @method setZIndex(value: Number): this
      // Changes the [zIndex](#imageoverlay-zindex) of the image overlay.
      setZIndex: function(t) {
        return this.options.zIndex = t, this._updateZIndex(), this;
      },
      // @method getBounds(): LatLngBounds
      // Get the bounds that this ImageOverlay covers
      getBounds: function() {
        return this._bounds;
      },
      // @method getElement(): HTMLElement
      // Returns the instance of [`HTMLImageElement`](https://developer.mozilla.org/docs/Web/API/HTMLImageElement)
      // used by this overlay.
      getElement: function() {
        return this._image;
      },
      _initImage: function() {
        var t = this._url.tagName === "IMG", e = this._image = t ? this._url : z("img");
        if (b(e, "leaflet-image-layer"), this._zoomAnimated && b(e, "leaflet-zoom-animated"), this.options.className && b(e, this.options.className), e.onselectstart = I, e.onmousemove = I, e.onload = T(this.fire, this, "load"), e.onerror = T(this._overlayOnError, this, "error"), (this.options.crossOrigin || this.options.crossOrigin === "") && (e.crossOrigin = this.options.crossOrigin === !0 ? "" : this.options.crossOrigin), this.options.zIndex && this._updateZIndex(), t) {
          this._url = e.src;
          return;
        }
        e.src = this._url, e.alt = this.options.alt;
      },
      _animateZoom: function(t) {
        var e = this._map.getZoomScale(t.zoom), i = this._map._latLngBoundsToNewLayerBounds(this._bounds, t.zoom, t.center).min;
        w2(this._image, i, e);
      },
      _reset: function() {
        var t = this._image, e = new B(
          this._map.latLngToLayerPoint(this._bounds.getNorthWest()),
          this._map.latLngToLayerPoint(this._bounds.getSouthEast())
        ), i = e.getSize();
        N(t, e.min), t.style.width = i.x + "px", t.style.height = i.y + "px";
      },
      _updateOpacity: function() {
        X(this._image, this.options.opacity);
      },
      _updateZIndex: function() {
        this._image && this.options.zIndex !== void 0 && this.options.zIndex !== null && (this._image.style.zIndex = this.options.zIndex);
      },
      _overlayOnError: function() {
        this.fire("error");
        var t = this.options.errorOverlayUrl;
        t && this._url !== t && (this._url = t, this._image.src = t);
      },
      // @method getCenter(): LatLng
      // Returns the center of the ImageOverlay.
      getCenter: function() {
        return this._bounds.getCenter();
      }
    }), Wi = function(t, e, i) {
      return new p0(t, e, i);
    }, fe = p0.extend({
      // @section
      // @aka VideoOverlay options
      options: {
        // @option autoplay: Boolean = true
        // Whether the video starts playing automatically when loaded.
        // On some browsers autoplay will only work with `muted: true`
        autoplay: !0,
        // @option loop: Boolean = true
        // Whether the video will loop back to the beginning when played.
        loop: !0,
        // @option keepAspectRatio: Boolean = true
        // Whether the video will save aspect ratio after the projection.
        // Relevant for supported browsers. See [browser compatibility](https://developer.mozilla.org/en-US/docs/Web/CSS/object-fit)
        keepAspectRatio: !0,
        // @option muted: Boolean = false
        // Whether the video starts on mute when loaded.
        muted: !1,
        // @option playsInline: Boolean = true
        // Mobile browsers will play the video right where it is instead of open it up in fullscreen mode.
        playsInline: !0
      },
      _initImage: function() {
        var t = this._url.tagName === "VIDEO", e = this._image = t ? this._url : z("video");
        if (b(e, "leaflet-image-layer"), this._zoomAnimated && b(e, "leaflet-zoom-animated"), this.options.className && b(e, this.options.className), e.onselectstart = I, e.onmousemove = I, e.onloadeddata = T(this.fire, this, "load"), t) {
          for (var i = e.getElementsByTagName("source"), n = [], o = 0; o < i.length; o++)
            n.push(i[o].src);
          this._url = i.length > 0 ? n : [e.src];
          return;
        }
        t2(this._url) || (this._url = [this._url]), !this.options.keepAspectRatio && Object.prototype.hasOwnProperty.call(e.style, "objectFit") && (e.style.objectFit = "fill"), e.autoplay = !!this.options.autoplay, e.loop = !!this.options.loop, e.muted = !!this.options.muted, e.playsInline = !!this.options.playsInline;
        for (var s = 0; s < this._url.length; s++) {
          var a = z("source");
          a.src = this._url[s], e.appendChild(a);
        }
      }
      // @method getElement(): HTMLVideoElement
      // Returns the instance of [`HTMLVideoElement`](https://developer.mozilla.org/docs/Web/API/HTMLVideoElement)
      // used by this overlay.
    });
    function Hi(t, e, i) {
      return new fe(t, e, i);
    }
    var de = p0.extend({
      _initImage: function() {
        var t = this._image = this._url;
        b(t, "leaflet-image-layer"), this._zoomAnimated && b(t, "leaflet-zoom-animated"), this.options.className && b(t, this.options.className), t.onselectstart = I, t.onmousemove = I;
      }
      // @method getElement(): SVGElement
      // Returns the instance of [`SVGElement`](https://developer.mozilla.org/docs/Web/API/SVGElement)
      // used by this overlay.
    });
    function Gi(t, e, i) {
      return new de(t, e, i);
    }
    var a2 = i2.extend({
      // @section
      // @aka DivOverlay options
      options: {
        // @option interactive: Boolean = false
        // If true, the popup/tooltip will listen to the mouse events.
        interactive: !1,
        // @option offset: Point = Point(0, 0)
        // The offset of the overlay position.
        offset: [0, 0],
        // @option className: String = ''
        // A custom CSS class name to assign to the overlay.
        className: "",
        // @option pane: String = undefined
        // `Map pane` where the overlay will be added.
        pane: void 0,
        // @option content: String|HTMLElement|Function = ''
        // Sets the HTML content of the overlay while initializing. If a function is passed the source layer will be
        // passed to the function. The function should return a `String` or `HTMLElement` to be used in the overlay.
        content: ""
      },
      initialize: function(t, e) {
        t && (t instanceof S || t2(t)) ? (this._latlng = M(t), O(this, e)) : (O(this, t), this._source = e), this.options.content && (this._content = this.options.content);
      },
      // @method openOn(map: Map): this
      // Adds the overlay to the map.
      // Alternative to `map.openPopup(popup)`/`.openTooltip(tooltip)`.
      openOn: function(t) {
        return t = arguments.length ? t : this._source._map, t.hasLayer(this) || t.addLayer(this), this;
      },
      // @method close(): this
      // Closes the overlay.
      // Alternative to `map.closePopup(popup)`/`.closeTooltip(tooltip)`
      // and `layer.closePopup()`/`.closeTooltip()`.
      close: function() {
        return this._map && this._map.removeLayer(this), this;
      },
      // @method toggle(layer?: Layer): this
      // Opens or closes the overlay bound to layer depending on its current state.
      // Argument may be omitted only for overlay bound to layer.
      // Alternative to `layer.togglePopup()`/`.toggleTooltip()`.
      toggle: function(t) {
        return this._map ? this.close() : (arguments.length ? this._source = t : t = this._source, this._prepareOpen(), this.openOn(t._map)), this;
      },
      onAdd: function(t) {
        this._zoomAnimated = t._zoomAnimated, this._container || this._initLayout(), t._fadeAnimated && X(this._container, 0), clearTimeout(this._removeTimeout), this.getPane().appendChild(this._container), this.update(), t._fadeAnimated && X(this._container, 1), this.bringToFront(), this.options.interactive && (b(this._container, "leaflet-interactive"), this.addInteractiveTarget(this._container));
      },
      onRemove: function(t) {
        t._fadeAnimated ? (X(this._container, 0), this._removeTimeout = setTimeout(T(Z, void 0, this._container), 200)) : Z(this._container), this.options.interactive && (R(this._container, "leaflet-interactive"), this.removeInteractiveTarget(this._container));
      },
      // @namespace DivOverlay
      // @method getLatLng: LatLng
      // Returns the geographical point of the overlay.
      getLatLng: function() {
        return this._latlng;
      },
      // @method setLatLng(latlng: LatLng): this
      // Sets the geographical point where the overlay will open.
      setLatLng: function(t) {
        return this._latlng = M(t), this._map && (this._updatePosition(), this._adjustPan()), this;
      },
      // @method getContent: String|HTMLElement
      // Returns the content of the overlay.
      getContent: function() {
        return this._content;
      },
      // @method setContent(htmlContent: String|HTMLElement|Function): this
      // Sets the HTML content of the overlay. If a function is passed the source layer will be passed to the function.
      // The function should return a `String` or `HTMLElement` to be used in the overlay.
      setContent: function(t) {
        return this._content = t, this.update(), this;
      },
      // @method getElement: String|HTMLElement
      // Returns the HTML container of the overlay.
      getElement: function() {
        return this._container;
      },
      // @method update: null
      // Updates the overlay content, layout and position. Useful for updating the overlay after something inside changed, e.g. image loaded.
      update: function() {
        this._map && (this._container.style.visibility = "hidden", this._updateContent(), this._updateLayout(), this._updatePosition(), this._container.style.visibility = "", this._adjustPan());
      },
      getEvents: function() {
        var t = {
          zoom: this._updatePosition,
          viewreset: this._updatePosition
        };
        return this._zoomAnimated && (t.zoomanim = this._animateZoom), t;
      },
      // @method isOpen: Boolean
      // Returns `true` when the overlay is visible on the map.
      isOpen: function() {
        return !!this._map && this._map.hasLayer(this);
      },
      // @method bringToFront: this
      // Brings this overlay in front of other overlays (in the same map pane).
      bringToFront: function() {
        return this._map && k2(this._container), this;
      },
      // @method bringToBack: this
      // Brings this overlay to the back of other overlays (in the same map pane).
      bringToBack: function() {
        return this._map && z2(this._container), this;
      },
      // prepare bound overlay to open: update latlng pos / content source (for FeatureGroup)
      _prepareOpen: function(t) {
        var e = this._source;
        if (!e._map)
          return !1;
        if (e instanceof f2) {
          e = null;
          var i = this._source._layers;
          for (var n in i)
            if (i[n]._map) {
              e = i[n];
              break;
            }
          if (!e)
            return !1;
          this._source = e;
        }
        if (!t)
          if (e.getCenter)
            t = e.getCenter();
          else if (e.getLatLng)
            t = e.getLatLng();
          else if (e.getBounds)
            t = e.getBounds().getCenter();
          else
            throw new Error("Unable to get source layer LatLng.");
        return this.setLatLng(t), this._map && this.update(), !0;
      },
      _updateContent: function() {
        if (this._content) {
          var t = this._contentNode, e = typeof this._content == "function" ? this._content(this._source || this) : this._content;
          if (typeof e == "string")
            t.innerHTML = e;
          else {
            for (; t.hasChildNodes(); )
              t.removeChild(t.firstChild);
            t.appendChild(e);
          }
          this.fire("contentupdate");
        }
      },
      _updatePosition: function() {
        if (this._map) {
          var t = this._map.latLngToLayerPoint(this._latlng), e = v(this.options.offset), i = this._getAnchor();
          this._zoomAnimated ? N(this._container, t.add(i)) : e = e.add(t).add(i);
          var n = this._containerBottom = -e.y, o = this._containerLeft = -Math.round(this._containerWidth / 2) + e.x;
          this._container.style.bottom = n + "px", this._container.style.left = o + "px";
        }
      },
      _getAnchor: function() {
        return [0, 0];
      }
    });
    k.include({
      _initOverlay: function(t, e, i, n) {
        var o = e;
        return o instanceof t || (o = new t(n).setContent(e)), i && o.setLatLng(i), o;
      }
    }), i2.include({
      _initOverlay: function(t, e, i, n) {
        var o = i;
        return o instanceof t ? (O(o, n), o._source = this) : (o = e && !n ? e : new t(n, this), o.setContent(i)), o;
      }
    });
    var g0 = a2.extend({
      // @section
      // @aka Popup options
      options: {
        // @option pane: String = 'popupPane'
        // `Map pane` where the popup will be added.
        pane: "popupPane",
        // @option offset: Point = Point(0, 7)
        // The offset of the popup position.
        offset: [0, 7],
        // @option maxWidth: Number = 300
        // Max width of the popup, in pixels.
        maxWidth: 300,
        // @option minWidth: Number = 50
        // Min width of the popup, in pixels.
        minWidth: 50,
        // @option maxHeight: Number = null
        // If set, creates a scrollable container of the given height
        // inside a popup if its content exceeds it.
        // The scrollable container can be styled using the
        // `leaflet-popup-scrolled` CSS class selector.
        maxHeight: null,
        // @option autoPan: Boolean = true
        // Set it to `false` if you don't want the map to do panning animation
        // to fit the opened popup.
        autoPan: !0,
        // @option autoPanPaddingTopLeft: Point = null
        // The margin between the popup and the top left corner of the map
        // view after autopanning was performed.
        autoPanPaddingTopLeft: null,
        // @option autoPanPaddingBottomRight: Point = null
        // The margin between the popup and the bottom right corner of the map
        // view after autopanning was performed.
        autoPanPaddingBottomRight: null,
        // @option autoPanPadding: Point = Point(5, 5)
        // Equivalent of setting both top left and bottom right autopan padding to the same value.
        autoPanPadding: [5, 5],
        // @option keepInView: Boolean = false
        // Set it to `true` if you want to prevent users from panning the popup
        // off of the screen while it is open.
        keepInView: !1,
        // @option closeButton: Boolean = true
        // Controls the presence of a close button in the popup.
        closeButton: !0,
        // @option autoClose: Boolean = true
        // Set it to `false` if you want to override the default behavior of
        // the popup closing when another popup is opened.
        autoClose: !0,
        // @option closeOnEscapeKey: Boolean = true
        // Set it to `false` if you want to override the default behavior of
        // the ESC key for closing of the popup.
        closeOnEscapeKey: !0,
        // @option closeOnClick: Boolean = *
        // Set it if you want to override the default behavior of the popup closing when user clicks
        // on the map. Defaults to the map's [`closePopupOnClick`](#map-closepopuponclick) option.
        // @option className: String = ''
        // A custom CSS class name to assign to the popup.
        className: ""
      },
      // @namespace Popup
      // @method openOn(map: Map): this
      // Alternative to `map.openPopup(popup)`.
      // Adds the popup to the map and closes the previous one.
      openOn: function(t) {
        return t = arguments.length ? t : this._source._map, !t.hasLayer(this) && t._popup && t._popup.options.autoClose && t.removeLayer(t._popup), t._popup = this, a2.prototype.openOn.call(this, t);
      },
      onAdd: function(t) {
        a2.prototype.onAdd.call(this, t), t.fire("popupopen", { popup: this }), this._source && (this._source.fire("popupopen", { popup: this }, !0), this._source instanceof y2 || this._source.on("preclick", b2));
      },
      onRemove: function(t) {
        a2.prototype.onRemove.call(this, t), t.fire("popupclose", { popup: this }), this._source && (this._source.fire("popupclose", { popup: this }, !0), this._source instanceof y2 || this._source.off("preclick", b2));
      },
      getEvents: function() {
        var t = a2.prototype.getEvents.call(this);
        return (this.options.closeOnClick !== void 0 ? this.options.closeOnClick : this._map.options.closePopupOnClick) && (t.preclick = this.close), this.options.keepInView && (t.moveend = this._adjustPan), t;
      },
      _initLayout: function() {
        var t = "leaflet-popup", e = this._container = z(
          "div",
          t + " " + (this.options.className || "") + " leaflet-zoom-animated"
        ), i = this._wrapper = z("div", t + "-content-wrapper", e);
        if (this._contentNode = z("div", t + "-content", i), q2(e), J0(this._contentNode), w(e, "contextmenu", b2), this._tipContainer = z("div", t + "-tip-container", e), this._tip = z("div", t + "-tip", this._tipContainer), this.options.closeButton) {
          var n = this._closeButton = z("a", t + "-close-button", e);
          n.setAttribute("role", "button"), n.setAttribute("aria-label", "Close popup"), n.href = "#close", n.innerHTML = '<span aria-hidden="true">&#215;</span>', w(n, "click", function(o) {
            H(o), this.close();
          }, this);
        }
      },
      _updateLayout: function() {
        var t = this._contentNode, e = t.style;
        e.width = "", e.whiteSpace = "nowrap";
        var i = t.offsetWidth;
        i = Math.min(i, this.options.maxWidth), i = Math.max(i, this.options.minWidth), e.width = i + 1 + "px", e.whiteSpace = "", e.height = "";
        var n = t.offsetHeight, o = this.options.maxHeight, s = "leaflet-popup-scrolled";
        o && n > o ? (e.height = o + "px", b(t, s)) : R(t, s), this._containerWidth = this._container.offsetWidth;
      },
      _animateZoom: function(t) {
        var e = this._map._latLngToNewLayerPoint(this._latlng, t.zoom, t.center), i = this._getAnchor();
        N(this._container, e.add(i));
      },
      _adjustPan: function() {
        if (this.options.autoPan) {
          if (this._map._panAnim && this._map._panAnim.stop(), this._autopanning) {
            this._autopanning = !1;
            return;
          }
          var t = this._map, e = parseInt(H2(this._container, "marginBottom"), 10) || 0, i = this._container.offsetHeight + e, n = this._containerWidth, o = new x(this._containerLeft, -i - this._containerBottom);
          o._add(C2(this._container));
          var s = t.layerPointToContainerPoint(o), a = v(this.options.autoPanPadding), l = v(this.options.autoPanPaddingTopLeft || a), c = v(this.options.autoPanPaddingBottomRight || a), u = t.getSize(), _ = 0, g = 0;
          s.x + n + c.x > u.x && (_ = s.x + n - u.x + c.x), s.x - _ - l.x < 0 && (_ = s.x - l.x), s.y + i + c.y > u.y && (g = s.y + i - u.y + c.y), s.y - g - l.y < 0 && (g = s.y - l.y), (_ || g) && (this.options.keepInView && (this._autopanning = !0), t.fire("autopanstart").panBy([_, g]));
        }
      },
      _getAnchor: function() {
        return v(this._source && this._source._getPopupAnchor ? this._source._getPopupAnchor() : [0, 0]);
      }
    }), Ui = function(t, e) {
      return new g0(t, e);
    };
    k.mergeOptions({
      closePopupOnClick: !0
    }), k.include({
      // @method openPopup(popup: Popup): this
      // Opens the specified popup while closing the previously opened (to make sure only one is opened at one time for usability).
      // @alternative
      // @method openPopup(content: String|HTMLElement, latlng: LatLng, options?: Popup options): this
      // Creates a popup with the specified content and options and opens it in the given point on a map.
      openPopup: function(t, e, i) {
        return this._initOverlay(g0, t, e, i).openOn(this), this;
      },
      // @method closePopup(popup?: Popup): this
      // Closes the popup previously opened with [openPopup](#map-openpopup) (or the given one).
      closePopup: function(t) {
        return t = arguments.length ? t : this._popup, t && t.close(), this;
      }
    }), i2.include({
      // @method bindPopup(content: String|HTMLElement|Function|Popup, options?: Popup options): this
      // Binds a popup to the layer with the passed `content` and sets up the
      // necessary event listeners. If a `Function` is passed it will receive
      // the layer as the first argument and should return a `String` or `HTMLElement`.
      bindPopup: function(t, e) {
        return this._popup = this._initOverlay(g0, this._popup, t, e), this._popupHandlersAdded || (this.on({
          click: this._openPopup,
          keypress: this._onKeyPress,
          remove: this.closePopup,
          move: this._movePopup
        }), this._popupHandlersAdded = !0), this;
      },
      // @method unbindPopup(): this
      // Removes the popup previously bound with `bindPopup`.
      unbindPopup: function() {
        return this._popup && (this.off({
          click: this._openPopup,
          keypress: this._onKeyPress,
          remove: this.closePopup,
          move: this._movePopup
        }), this._popupHandlersAdded = !1, this._popup = null), this;
      },
      // @method openPopup(latlng?: LatLng): this
      // Opens the bound popup at the specified `latlng` or at the default popup anchor if no `latlng` is passed.
      openPopup: function(t) {
        return this._popup && (this instanceof f2 || (this._popup._source = this), this._popup._prepareOpen(t || this._latlng) && this._popup.openOn(this._map)), this;
      },
      // @method closePopup(): this
      // Closes the popup bound to this layer if it is open.
      closePopup: function() {
        return this._popup && this._popup.close(), this;
      },
      // @method togglePopup(): this
      // Opens or closes the popup bound to this layer depending on its current state.
      togglePopup: function() {
        return this._popup && this._popup.toggle(this), this;
      },
      // @method isPopupOpen(): boolean
      // Returns `true` if the popup bound to this layer is currently open.
      isPopupOpen: function() {
        return this._popup ? this._popup.isOpen() : !1;
      },
      // @method setPopupContent(content: String|HTMLElement|Popup): this
      // Sets the content of the popup bound to this layer.
      setPopupContent: function(t) {
        return this._popup && this._popup.setContent(t), this;
      },
      // @method getPopup(): Popup
      // Returns the popup bound to this layer.
      getPopup: function() {
        return this._popup;
      },
      _openPopup: function(t) {
        if (!(!this._popup || !this._map)) {
          L2(t);
          var e = t.layer || t.target;
          if (this._popup._source === e && !(e instanceof y2)) {
            this._map.hasLayer(this._popup) ? this.closePopup() : this.openPopup(t.latlng);
            return;
          }
          this._popup._source = e, this.openPopup(t.latlng);
        }
      },
      _movePopup: function(t) {
        this._popup.setLatLng(t.latlng);
      },
      _onKeyPress: function(t) {
        t.originalEvent.keyCode === 13 && this._openPopup(t);
      }
    });
    var v0 = a2.extend({
      // @section
      // @aka Tooltip options
      options: {
        // @option pane: String = 'tooltipPane'
        // `Map pane` where the tooltip will be added.
        pane: "tooltipPane",
        // @option offset: Point = Point(0, 0)
        // Optional offset of the tooltip position.
        offset: [0, 0],
        // @option direction: String = 'auto'
        // Direction where to open the tooltip. Possible values are: `right`, `left`,
        // `top`, `bottom`, `center`, `auto`.
        // `auto` will dynamically switch between `right` and `left` according to the tooltip
        // position on the map.
        direction: "auto",
        // @option permanent: Boolean = false
        // Whether to open the tooltip permanently or only on mouseover.
        permanent: !1,
        // @option sticky: Boolean = false
        // If true, the tooltip will follow the mouse instead of being fixed at the feature center.
        sticky: !1,
        // @option opacity: Number = 0.9
        // Tooltip container opacity.
        opacity: 0.9
      },
      onAdd: function(t) {
        a2.prototype.onAdd.call(this, t), this.setOpacity(this.options.opacity), t.fire("tooltipopen", { tooltip: this }), this._source && (this.addEventParent(this._source), this._source.fire("tooltipopen", { tooltip: this }, !0));
      },
      onRemove: function(t) {
        a2.prototype.onRemove.call(this, t), t.fire("tooltipclose", { tooltip: this }), this._source && (this.removeEventParent(this._source), this._source.fire("tooltipclose", { tooltip: this }, !0));
      },
      getEvents: function() {
        var t = a2.prototype.getEvents.call(this);
        return this.options.permanent || (t.preclick = this.close), t;
      },
      _initLayout: function() {
        var t = "leaflet-tooltip", e = t + " " + (this.options.className || "") + " leaflet-zoom-" + (this._zoomAnimated ? "animated" : "hide");
        this._contentNode = this._container = z("div", e), this._container.setAttribute("role", "tooltip"), this._container.setAttribute("id", "leaflet-tooltip-" + C(this));
      },
      _updateLayout: function() {
      },
      _adjustPan: function() {
      },
      _setPosition: function(t) {
        var e, i, n = this._map, o = this._container, s = n.latLngToContainerPoint(n.getCenter()), a = n.layerPointToContainerPoint(t), l = this.options.direction, c = o.offsetWidth, u = o.offsetHeight, _ = v(this.options.offset), g = this._getAnchor();
        l === "top" ? (e = c / 2, i = u) : l === "bottom" ? (e = c / 2, i = 0) : l === "center" ? (e = c / 2, i = u / 2) : l === "right" ? (e = 0, i = u / 2) : l === "left" ? (e = c, i = u / 2) : a.x < s.x ? (l = "right", e = 0, i = u / 2) : (l = "left", e = c + (_.x + g.x) * 2, i = u / 2), t = t.subtract(v(e, i, !0)).add(_).add(g), R(o, "leaflet-tooltip-right"), R(o, "leaflet-tooltip-left"), R(o, "leaflet-tooltip-top"), R(o, "leaflet-tooltip-bottom"), b(o, "leaflet-tooltip-" + l), N(o, t);
      },
      _updatePosition: function() {
        var t = this._map.latLngToLayerPoint(this._latlng);
        this._setPosition(t);
      },
      setOpacity: function(t) {
        this.options.opacity = t, this._container && X(this._container, t);
      },
      _animateZoom: function(t) {
        var e = this._map._latLngToNewLayerPoint(this._latlng, t.zoom, t.center);
        this._setPosition(e);
      },
      _getAnchor: function() {
        return v(this._source && this._source._getTooltipAnchor && !this.options.sticky ? this._source._getTooltipAnchor() : [0, 0]);
      }
    }), Vi = function(t, e) {
      return new v0(t, e);
    };
    k.include({
      // @method openTooltip(tooltip: Tooltip): this
      // Opens the specified tooltip.
      // @alternative
      // @method openTooltip(content: String|HTMLElement, latlng: LatLng, options?: Tooltip options): this
      // Creates a tooltip with the specified content and options and open it.
      openTooltip: function(t, e, i) {
        return this._initOverlay(v0, t, e, i).openOn(this), this;
      },
      // @method closeTooltip(tooltip: Tooltip): this
      // Closes the tooltip given as parameter.
      closeTooltip: function(t) {
        return t.close(), this;
      }
    }), i2.include({
      // @method bindTooltip(content: String|HTMLElement|Function|Tooltip, options?: Tooltip options): this
      // Binds a tooltip to the layer with the passed `content` and sets up the
      // necessary event listeners. If a `Function` is passed it will receive
      // the layer as the first argument and should return a `String` or `HTMLElement`.
      bindTooltip: function(t, e) {
        return this._tooltip && this.isTooltipOpen() && this.unbindTooltip(), this._tooltip = this._initOverlay(v0, this._tooltip, t, e), this._initTooltipInteractions(), this._tooltip.options.permanent && this._map && this._map.hasLayer(this) && this.openTooltip(), this;
      },
      // @method unbindTooltip(): this
      // Removes the tooltip previously bound with `bindTooltip`.
      unbindTooltip: function() {
        return this._tooltip && (this._initTooltipInteractions(!0), this.closeTooltip(), this._tooltip = null), this;
      },
      _initTooltipInteractions: function(t) {
        if (!(!t && this._tooltipHandlersAdded)) {
          var e = t ? "off" : "on", i = {
            remove: this.closeTooltip,
            move: this._moveTooltip
          };
          this._tooltip.options.permanent ? i.add = this._openTooltip : (i.mouseover = this._openTooltip, i.mouseout = this.closeTooltip, i.click = this._openTooltip, this._map ? this._addFocusListeners() : i.add = this._addFocusListeners), this._tooltip.options.sticky && (i.mousemove = this._moveTooltip), this[e](i), this._tooltipHandlersAdded = !t;
        }
      },
      // @method openTooltip(latlng?: LatLng): this
      // Opens the bound tooltip at the specified `latlng` or at the default tooltip anchor if no `latlng` is passed.
      openTooltip: function(t) {
        return this._tooltip && (this instanceof f2 || (this._tooltip._source = this), this._tooltip._prepareOpen(t) && (this._tooltip.openOn(this._map), this.getElement ? this._setAriaDescribedByOnLayer(this) : this.eachLayer && this.eachLayer(this._setAriaDescribedByOnLayer, this))), this;
      },
      // @method closeTooltip(): this
      // Closes the tooltip bound to this layer if it is open.
      closeTooltip: function() {
        if (this._tooltip)
          return this._tooltip.close();
      },
      // @method toggleTooltip(): this
      // Opens or closes the tooltip bound to this layer depending on its current state.
      toggleTooltip: function() {
        return this._tooltip && this._tooltip.toggle(this), this;
      },
      // @method isTooltipOpen(): boolean
      // Returns `true` if the tooltip bound to this layer is currently open.
      isTooltipOpen: function() {
        return this._tooltip.isOpen();
      },
      // @method setTooltipContent(content: String|HTMLElement|Tooltip): this
      // Sets the content of the tooltip bound to this layer.
      setTooltipContent: function(t) {
        return this._tooltip && this._tooltip.setContent(t), this;
      },
      // @method getTooltip(): Tooltip
      // Returns the tooltip bound to this layer.
      getTooltip: function() {
        return this._tooltip;
      },
      _addFocusListeners: function() {
        this.getElement ? this._addFocusListenersOnLayer(this) : this.eachLayer && this.eachLayer(this._addFocusListenersOnLayer, this);
      },
      _addFocusListenersOnLayer: function(t) {
        var e = typeof t.getElement == "function" && t.getElement();
        e && (w(e, "focus", function() {
          this._tooltip._source = t, this.openTooltip();
        }, this), w(e, "blur", this.closeTooltip, this));
      },
      _setAriaDescribedByOnLayer: function(t) {
        var e = typeof t.getElement == "function" && t.getElement();
        e && e.setAttribute("aria-describedby", this._tooltip._container.id);
      },
      _openTooltip: function(t) {
        if (!(!this._tooltip || !this._map)) {
          if (this._map.dragging && this._map.dragging.moving() && !this._openOnceFlag) {
            this._openOnceFlag = !0;
            var e = this;
            this._map.once("moveend", function() {
              e._openOnceFlag = !1, e._openTooltip(t);
            });
            return;
          }
          this._tooltip._source = t.layer || t.target, this.openTooltip(this._tooltip.options.sticky ? t.latlng : void 0);
        }
      },
      _moveTooltip: function(t) {
        var e = t.latlng, i, n;
        this._tooltip.options.sticky && t.originalEvent && (i = this._map.mouseEventToContainerPoint(t.originalEvent), n = this._map.containerPointToLayerPoint(i), e = this._map.layerPointToLatLng(n)), this._tooltip.setLatLng(e);
      }
    });
    var _e = E2.extend({
      options: {
        // @section
        // @aka DivIcon options
        iconSize: [12, 12],
        // also can be set through CSS
        // iconAnchor: (Point),
        // popupAnchor: (Point),
        // @option html: String|HTMLElement = ''
        // Custom HTML code to put inside the div element, empty by default. Alternatively,
        // an instance of `HTMLElement`.
        html: !1,
        // @option bgPos: Point = [0, 0]
        // Optional relative position of the background, in pixels
        bgPos: null,
        className: "leaflet-div-icon"
      },
      createIcon: function(t) {
        var e = t && t.tagName === "DIV" ? t : document.createElement("div"), i = this.options;
        if (i.html instanceof Element ? (o0(e), e.appendChild(i.html)) : e.innerHTML = i.html !== !1 ? i.html : "", i.bgPos) {
          var n = v(i.bgPos);
          e.style.backgroundPosition = -n.x + "px " + -n.y + "px";
        }
        return this._setIconStyles(e, "icon"), e;
      },
      createShadow: function() {
        return null;
      }
    });
    function qi(t) {
      return new _e(t);
    }
    E2.Default = J2;
    var K2 = i2.extend({
      // @section
      // @aka GridLayer options
      options: {
        // @option tileSize: Number|Point = 256
        // Width and height of tiles in the grid. Use a number if width and height are equal, or `L.point(width, height)` otherwise.
        tileSize: 256,
        // @option opacity: Number = 1.0
        // Opacity of the tiles. Can be used in the `createTile()` function.
        opacity: 1,
        // @option updateWhenIdle: Boolean = (depends)
        // Load new tiles only when panning ends.
        // `true` by default on mobile browsers, in order to avoid too many requests and keep smooth navigation.
        // `false` otherwise in order to display new tiles _during_ panning, since it is easy to pan outside the
        // [`keepBuffer`](#gridlayer-keepbuffer) option in desktop browsers.
        updateWhenIdle: p.mobile,
        // @option updateWhenZooming: Boolean = true
        // By default, a smooth zoom animation (during a [touch zoom](#map-touchzoom) or a [`flyTo()`](#map-flyto)) will update grid layers every integer zoom level. Setting this option to `false` will update the grid layer only when the smooth animation ends.
        updateWhenZooming: !0,
        // @option updateInterval: Number = 200
        // Tiles will not update more than once every `updateInterval` milliseconds when panning.
        updateInterval: 200,
        // @option zIndex: Number = 1
        // The explicit zIndex of the tile layer.
        zIndex: 1,
        // @option bounds: LatLngBounds = undefined
        // If set, tiles will only be loaded inside the set `LatLngBounds`.
        bounds: null,
        // @option minZoom: Number = 0
        // The minimum zoom level down to which this layer will be displayed (inclusive).
        minZoom: 0,
        // @option maxZoom: Number = undefined
        // The maximum zoom level up to which this layer will be displayed (inclusive).
        maxZoom: void 0,
        // @option maxNativeZoom: Number = undefined
        // Maximum zoom number the tile source has available. If it is specified,
        // the tiles on all zoom levels higher than `maxNativeZoom` will be loaded
        // from `maxNativeZoom` level and auto-scaled.
        maxNativeZoom: void 0,
        // @option minNativeZoom: Number = undefined
        // Minimum zoom number the tile source has available. If it is specified,
        // the tiles on all zoom levels lower than `minNativeZoom` will be loaded
        // from `minNativeZoom` level and auto-scaled.
        minNativeZoom: void 0,
        // @option noWrap: Boolean = false
        // Whether the layer is wrapped around the antimeridian. If `true`, the
        // GridLayer will only be displayed once at low zoom levels. Has no
        // effect when the [map CRS](#map-crs) doesn't wrap around. Can be used
        // in combination with [`bounds`](#gridlayer-bounds) to prevent requesting
        // tiles outside the CRS limits.
        noWrap: !1,
        // @option pane: String = 'tilePane'
        // `Map pane` where the grid layer will be added.
        pane: "tilePane",
        // @option className: String = ''
        // A custom class name to assign to the tile layer. Empty by default.
        className: "",
        // @option keepBuffer: Number = 2
        // When panning the map, keep this many rows and columns of tiles before unloading them.
        keepBuffer: 2
      },
      initialize: function(t) {
        O(this, t);
      },
      onAdd: function() {
        this._initContainer(), this._levels = {}, this._tiles = {}, this._resetView();
      },
      beforeAdd: function(t) {
        t._addZoomLimit(this);
      },
      onRemove: function(t) {
        this._removeAllTiles(), Z(this._container), t._removeZoomLimit(this), this._container = null, this._tileZoom = void 0;
      },
      // @method bringToFront: this
      // Brings the tile layer to the top of all tile layers.
      bringToFront: function() {
        return this._map && (k2(this._container), this._setAutoZIndex(Math.max)), this;
      },
      // @method bringToBack: this
      // Brings the tile layer to the bottom of all tile layers.
      bringToBack: function() {
        return this._map && (z2(this._container), this._setAutoZIndex(Math.min)), this;
      },
      // @method getContainer: HTMLElement
      // Returns the HTML element that contains the tiles for this layer.
      getContainer: function() {
        return this._container;
      },
      // @method setOpacity(opacity: Number): this
      // Changes the [opacity](#gridlayer-opacity) of the grid layer.
      setOpacity: function(t) {
        return this.options.opacity = t, this._updateOpacity(), this;
      },
      // @method setZIndex(zIndex: Number): this
      // Changes the [zIndex](#gridlayer-zindex) of the grid layer.
      setZIndex: function(t) {
        return this.options.zIndex = t, this._updateZIndex(), this;
      },
      // @method isLoading: Boolean
      // Returns `true` if any tile in the grid layer has not finished loading.
      isLoading: function() {
        return this._loading;
      },
      // @method redraw: this
      // Causes the layer to clear all the tiles and request them again.
      redraw: function() {
        if (this._map) {
          this._removeAllTiles();
          var t = this._clampZoom(this._map.getZoom());
          t !== this._tileZoom && (this._tileZoom = t, this._updateLevels()), this._update();
        }
        return this;
      },
      getEvents: function() {
        var t = {
          viewprereset: this._invalidateAll,
          viewreset: this._resetView,
          zoom: this._resetView,
          moveend: this._onMoveEnd
        };
        return this.options.updateWhenIdle || (this._onMove || (this._onMove = p2(this._onMoveEnd, this.options.updateInterval, this)), t.move = this._onMove), this._zoomAnimated && (t.zoomanim = this._animateZoom), t;
      },
      // @section Extension methods
      // Layers extending `GridLayer` shall reimplement the following method.
      // @method createTile(coords: Object, done?: Function): HTMLElement
      // Called only internally, must be overridden by classes extending `GridLayer`.
      // Returns the `HTMLElement` corresponding to the given `coords`. If the `done` callback
      // is specified, it must be called when the tile has finished loading and drawing.
      createTile: function() {
        return document.createElement("div");
      },
      // @section
      // @method getTileSize: Point
      // Normalizes the [tileSize option](#gridlayer-tilesize) into a point. Used by the `createTile()` method.
      getTileSize: function() {
        var t = this.options.tileSize;
        return t instanceof x ? t : new x(t, t);
      },
      _updateZIndex: function() {
        this._container && this.options.zIndex !== void 0 && this.options.zIndex !== null && (this._container.style.zIndex = this.options.zIndex);
      },
      _setAutoZIndex: function(t) {
        for (var e = this.getPane().children, i = -t(-1 / 0, 1 / 0), n = 0, o = e.length, s; n < o; n++)
          s = e[n].style.zIndex, e[n] !== this._container && s && (i = t(i, +s));
        isFinite(i) && (this.options.zIndex = i + t(-1, 1), this._updateZIndex());
      },
      _updateOpacity: function() {
        if (this._map && !p.ielt9) {
          X(this._container, this.options.opacity);
          var t = +/* @__PURE__ */ new Date(), e = !1, i = !1;
          for (var n in this._tiles) {
            var o = this._tiles[n];
            if (!(!o.current || !o.loaded)) {
              var s = Math.min(1, (t - o.loaded) / 200);
              X(o.el, s), s < 1 ? e = !0 : (o.active ? i = !0 : this._onOpaqueTile(o), o.active = !0);
            }
          }
          i && !this._noPrune && this._pruneTiles(), e && (K(this._fadeFrame), this._fadeFrame = U(this._updateOpacity, this));
        }
      },
      _onOpaqueTile: I,
      _initContainer: function() {
        this._container || (this._container = z("div", "leaflet-layer " + (this.options.className || "")), this._updateZIndex(), this.options.opacity < 1 && this._updateOpacity(), this.getPane().appendChild(this._container));
      },
      _updateLevels: function() {
        var t = this._tileZoom, e = this.options.maxZoom;
        if (t !== void 0) {
          for (var i in this._levels)
            i = Number(i), this._levels[i].el.children.length || i === t ? (this._levels[i].el.style.zIndex = e - Math.abs(t - i), this._onUpdateLevel(i)) : (Z(this._levels[i].el), this._removeTilesAtZoom(i), this._onRemoveLevel(i), delete this._levels[i]);
          var n = this._levels[t], o = this._map;
          return n || (n = this._levels[t] = {}, n.el = z("div", "leaflet-tile-container leaflet-zoom-animated", this._container), n.el.style.zIndex = e, n.origin = o.project(o.unproject(o.getPixelOrigin()), t).round(), n.zoom = t, this._setZoomTransform(n, o.getCenter(), o.getZoom()), I(n.el.offsetWidth), this._onCreateLevel(n)), this._level = n, n;
        }
      },
      _onUpdateLevel: I,
      _onRemoveLevel: I,
      _onCreateLevel: I,
      _pruneTiles: function() {
        if (this._map) {
          var t, e, i = this._map.getZoom();
          if (i > this.options.maxZoom || i < this.options.minZoom) {
            this._removeAllTiles();
            return;
          }
          for (t in this._tiles)
            e = this._tiles[t], e.retain = e.current;
          for (t in this._tiles)
            if (e = this._tiles[t], e.current && !e.active) {
              var n = e.coords;
              this._retainParent(n.x, n.y, n.z, n.z - 5) || this._retainChildren(n.x, n.y, n.z, n.z + 2);
            }
          for (t in this._tiles)
            this._tiles[t].retain || this._removeTile(t);
        }
      },
      _removeTilesAtZoom: function(t) {
        for (var e in this._tiles)
          this._tiles[e].coords.z === t && this._removeTile(e);
      },
      _removeAllTiles: function() {
        for (var t in this._tiles)
          this._removeTile(t);
      },
      _invalidateAll: function() {
        for (var t in this._levels)
          Z(this._levels[t].el), this._onRemoveLevel(Number(t)), delete this._levels[t];
        this._removeAllTiles(), this._tileZoom = void 0;
      },
      _retainParent: function(t, e, i, n) {
        var o = Math.floor(t / 2), s = Math.floor(e / 2), a = i - 1, l = new x(+o, +s);
        l.z = +a;
        var c = this._tileCoordsToKey(l), u = this._tiles[c];
        return u && u.active ? (u.retain = !0, !0) : (u && u.loaded && (u.retain = !0), a > n ? this._retainParent(o, s, a, n) : !1);
      },
      _retainChildren: function(t, e, i, n) {
        for (var o = 2 * t; o < 2 * t + 2; o++)
          for (var s = 2 * e; s < 2 * e + 2; s++) {
            var a = new x(o, s);
            a.z = i + 1;
            var l = this._tileCoordsToKey(a), c = this._tiles[l];
            if (c && c.active) {
              c.retain = !0;
              continue;
            } else c && c.loaded && (c.retain = !0);
            i + 1 < n && this._retainChildren(o, s, i + 1, n);
          }
      },
      _resetView: function(t) {
        var e = t && (t.pinch || t.flyTo);
        this._setView(this._map.getCenter(), this._map.getZoom(), e, e);
      },
      _animateZoom: function(t) {
        this._setView(t.center, t.zoom, !0, t.noUpdate);
      },
      _clampZoom: function(t) {
        var e = this.options;
        return e.minNativeZoom !== void 0 && t < e.minNativeZoom ? e.minNativeZoom : e.maxNativeZoom !== void 0 && e.maxNativeZoom < t ? e.maxNativeZoom : t;
      },
      _setView: function(t, e, i, n) {
        var o = Math.round(e);
        this.options.maxZoom !== void 0 && o > this.options.maxZoom || this.options.minZoom !== void 0 && o < this.options.minZoom ? o = void 0 : o = this._clampZoom(o);
        var s = this.options.updateWhenZooming && o !== this._tileZoom;
        (!n || s) && (this._tileZoom = o, this._abortLoading && this._abortLoading(), this._updateLevels(), this._resetGrid(), o !== void 0 && this._update(t), i || this._pruneTiles(), this._noPrune = !!i), this._setZoomTransforms(t, e);
      },
      _setZoomTransforms: function(t, e) {
        for (var i in this._levels)
          this._setZoomTransform(this._levels[i], t, e);
      },
      _setZoomTransform: function(t, e, i) {
        var n = this._map.getZoomScale(i, t.zoom), o = t.origin.multiplyBy(n).subtract(this._map._getNewPixelOrigin(e, i)).round();
        p.any3d ? w2(t.el, o, n) : N(t.el, o);
      },
      _resetGrid: function() {
        var t = this._map, e = t.options.crs, i = this._tileSize = this.getTileSize(), n = this._tileZoom, o = this._map.getPixelWorldBounds(this._tileZoom);
        o && (this._globalTileRange = this._pxBoundsToTileRange(o)), this._wrapX = e.wrapLng && !this.options.noWrap && [
          Math.floor(t.project([0, e.wrapLng[0]], n).x / i.x),
          Math.ceil(t.project([0, e.wrapLng[1]], n).x / i.y)
        ], this._wrapY = e.wrapLat && !this.options.noWrap && [
          Math.floor(t.project([e.wrapLat[0], 0], n).y / i.x),
          Math.ceil(t.project([e.wrapLat[1], 0], n).y / i.y)
        ];
      },
      _onMoveEnd: function() {
        !this._map || this._map._animatingZoom || this._update();
      },
      _getTiledPixelBounds: function(t) {
        var e = this._map, i = e._animatingZoom ? Math.max(e._animateToZoom, e.getZoom()) : e.getZoom(), n = e.getZoomScale(i, this._tileZoom), o = e.project(t, this._tileZoom).floor(), s = e.getSize().divideBy(n * 2);
        return new B(o.subtract(s), o.add(s));
      },
      // Private method to load tiles in the grid's active zoom level according to map bounds
      _update: function(t) {
        var e = this._map;
        if (e) {
          var i = this._clampZoom(e.getZoom());
          if (t === void 0 && (t = e.getCenter()), this._tileZoom !== void 0) {
            var n = this._getTiledPixelBounds(t), o = this._pxBoundsToTileRange(n), s = o.getCenter(), a = [], l = this.options.keepBuffer, c = new B(
              o.getBottomLeft().subtract([l, -l]),
              o.getTopRight().add([l, -l])
            );
            if (!(isFinite(o.min.x) && isFinite(o.min.y) && isFinite(o.max.x) && isFinite(o.max.y)))
              throw new Error("Attempted to load an infinite number of tiles");
            for (var u in this._tiles) {
              var _ = this._tiles[u].coords;
              (_.z !== this._tileZoom || !c.contains(new x(_.x, _.y))) && (this._tiles[u].current = !1);
            }
            if (Math.abs(i - this._tileZoom) > 1) {
              this._setView(t, i);
              return;
            }
            for (var g = o.min.y; g <= o.max.y; g++)
              for (var P = o.min.x; P <= o.max.x; P++) {
                var G = new x(P, g);
                if (G.z = this._tileZoom, !!this._isValidTile(G)) {
                  var W = this._tiles[this._tileCoordsToKey(G)];
                  W ? W.current = !0 : a.push(G);
                }
              }
            if (a.sort(function(Y, Z2) {
              return Y.distanceTo(s) - Z2.distanceTo(s);
            }), a.length !== 0) {
              this._loading || (this._loading = !0, this.fire("loading"));
              var $ = document.createDocumentFragment();
              for (P = 0; P < a.length; P++)
                this._addTile(a[P], $);
              this._level.el.appendChild($);
            }
          }
        }
      },
      _isValidTile: function(t) {
        var e = this._map.options.crs;
        if (!e.infinite) {
          var i = this._globalTileRange;
          if (!e.wrapLng && (t.x < i.min.x || t.x > i.max.x) || !e.wrapLat && (t.y < i.min.y || t.y > i.max.y))
            return !1;
        }
        if (!this.options.bounds)
          return !0;
        var n = this._tileCoordsToBounds(t);
        return D(this.options.bounds).overlaps(n);
      },
      _keyToBounds: function(t) {
        return this._tileCoordsToBounds(this._keyToTileCoords(t));
      },
      _tileCoordsToNwSe: function(t) {
        var e = this._map, i = this.getTileSize(), n = t.scaleBy(i), o = n.add(i), s = e.unproject(n, t.z), a = e.unproject(o, t.z);
        return [s, a];
      },
      // converts tile coordinates to its geographical bounds
      _tileCoordsToBounds: function(t) {
        var e = this._tileCoordsToNwSe(t), i = new q(e[0], e[1]);
        return this.options.noWrap || (i = this._map.wrapLatLngBounds(i)), i;
      },
      // converts tile coordinates to key for the tile cache
      _tileCoordsToKey: function(t) {
        return t.x + ":" + t.y + ":" + t.z;
      },
      // converts tile cache key to coordinates
      _keyToTileCoords: function(t) {
        var e = t.split(":"), i = new x(+e[0], +e[1]);
        return i.z = +e[2], i;
      },
      _removeTile: function(t) {
        var e = this._tiles[t];
        e && (Z(e.el), delete this._tiles[t], this.fire("tileunload", {
          tile: e.el,
          coords: this._keyToTileCoords(t)
        }));
      },
      _initTile: function(t) {
        b(t, "leaflet-tile");
        var e = this.getTileSize();
        t.style.width = e.x + "px", t.style.height = e.y + "px", t.onselectstart = I, t.onmousemove = I, p.ielt9 && this.options.opacity < 1 && X(t, this.options.opacity);
      },
      _addTile: function(t, e) {
        var i = this._getTilePos(t), n = this._tileCoordsToKey(t), o = this.createTile(this._wrapCoords(t), T(this._tileReady, this, t));
        this._initTile(o), this.createTile.length < 2 && U(T(this._tileReady, this, t, null, o)), N(o, i), this._tiles[n] = {
          el: o,
          coords: t,
          current: !0
        }, e.appendChild(o), this.fire("tileloadstart", {
          tile: o,
          coords: t
        });
      },
      _tileReady: function(t, e, i) {
        e && this.fire("tileerror", {
          error: e,
          tile: i,
          coords: t
        });
        var n = this._tileCoordsToKey(t);
        i = this._tiles[n], i && (i.loaded = +/* @__PURE__ */ new Date(), this._map._fadeAnimated ? (X(i.el, 0), K(this._fadeFrame), this._fadeFrame = U(this._updateOpacity, this)) : (i.active = !0, this._pruneTiles()), e || (b(i.el, "leaflet-tile-loaded"), this.fire("tileload", {
          tile: i.el,
          coords: t
        })), this._noTilesToLoad() && (this._loading = !1, this.fire("load"), p.ielt9 || !this._map._fadeAnimated ? U(this._pruneTiles, this) : setTimeout(T(this._pruneTiles, this), 250)));
      },
      _getTilePos: function(t) {
        return t.scaleBy(this.getTileSize()).subtract(this._level.origin);
      },
      _wrapCoords: function(t) {
        var e = new x(
          this._wrapX ? R2(t.x, this._wrapX) : t.x,
          this._wrapY ? R2(t.y, this._wrapY) : t.y
        );
        return e.z = t.z, e;
      },
      _pxBoundsToTileRange: function(t) {
        var e = this.getTileSize();
        return new B(
          t.min.unscaleBy(e).floor(),
          t.max.unscaleBy(e).ceil().subtract([1, 1])
        );
      },
      _noTilesToLoad: function() {
        for (var t in this._tiles)
          if (!this._tiles[t].loaded)
            return !1;
        return !0;
      }
    });
    function Yi(t) {
      return new K2(t);
    }
    var B2 = K2.extend({
      // @section
      // @aka TileLayer options
      options: {
        // @option minZoom: Number = 0
        // The minimum zoom level down to which this layer will be displayed (inclusive).
        minZoom: 0,
        // @option maxZoom: Number = 18
        // The maximum zoom level up to which this layer will be displayed (inclusive).
        maxZoom: 18,
        // @option subdomains: String|String[] = 'abc'
        // Subdomains of the tile service. Can be passed in the form of one string (where each letter is a subdomain name) or an array of strings.
        subdomains: "abc",
        // @option errorTileUrl: String = ''
        // URL to the tile image to show in place of the tile that failed to load.
        errorTileUrl: "",
        // @option zoomOffset: Number = 0
        // The zoom number used in tile URLs will be offset with this value.
        zoomOffset: 0,
        // @option tms: Boolean = false
        // If `true`, inverses Y axis numbering for tiles (turn this on for [TMS](https://en.wikipedia.org/wiki/Tile_Map_Service) services).
        tms: !1,
        // @option zoomReverse: Boolean = false
        // If set to true, the zoom number used in tile URLs will be reversed (`maxZoom - zoom` instead of `zoom`)
        zoomReverse: !1,
        // @option detectRetina: Boolean = false
        // If `true` and user is on a retina display, it will request four tiles of half the specified size and a bigger zoom level in place of one to utilize the high resolution.
        detectRetina: !1,
        // @option crossOrigin: Boolean|String = false
        // Whether the crossOrigin attribute will be added to the tiles.
        // If a String is provided, all tiles will have their crossOrigin attribute set to the String provided. This is needed if you want to access tile pixel data.
        // Refer to [CORS Settings](https://developer.mozilla.org/en-US/docs/Web/HTML/CORS_settings_attributes) for valid String values.
        crossOrigin: !1,
        // @option referrerPolicy: Boolean|String = false
        // Whether the referrerPolicy attribute will be added to the tiles.
        // If a String is provided, all tiles will have their referrerPolicy attribute set to the String provided.
        // This may be needed if your map's rendering context has a strict default but your tile provider expects a valid referrer
        // (e.g. to validate an API token).
        // Refer to [HTMLImageElement.referrerPolicy](https://developer.mozilla.org/en-US/docs/Web/API/HTMLImageElement/referrerPolicy) for valid String values.
        referrerPolicy: !1
      },
      initialize: function(t, e) {
        this._url = t, e = O(this, e), e.detectRetina && p.retina && e.maxZoom > 0 ? (e.tileSize = Math.floor(e.tileSize / 2), e.zoomReverse ? (e.zoomOffset--, e.minZoom = Math.min(e.maxZoom, e.minZoom + 1)) : (e.zoomOffset++, e.maxZoom = Math.max(e.minZoom, e.maxZoom - 1)), e.minZoom = Math.max(0, e.minZoom)) : e.zoomReverse ? e.minZoom = Math.min(e.maxZoom, e.minZoom) : e.maxZoom = Math.max(e.minZoom, e.maxZoom), typeof e.subdomains == "string" && (e.subdomains = e.subdomains.split("")), this.on("tileunload", this._onTileRemove);
      },
      // @method setUrl(url: String, noRedraw?: Boolean): this
      // Updates the layer's URL template and redraws it (unless `noRedraw` is set to `true`).
      // If the URL does not change, the layer will not be redrawn unless
      // the noRedraw parameter is set to false.
      setUrl: function(t, e) {
        return this._url === t && e === void 0 && (e = !0), this._url = t, e || this.redraw(), this;
      },
      // @method createTile(coords: Object, done?: Function): HTMLElement
      // Called only internally, overrides GridLayer's [`createTile()`](#gridlayer-createtile)
      // to return an `<img>` HTML element with the appropriate image URL given `coords`. The `done`
      // callback is called when the tile has been loaded.
      createTile: function(t, e) {
        var i = document.createElement("img");
        return w(i, "load", T(this._tileOnLoad, this, e, i)), w(i, "error", T(this._tileOnError, this, e, i)), (this.options.crossOrigin || this.options.crossOrigin === "") && (i.crossOrigin = this.options.crossOrigin === !0 ? "" : this.options.crossOrigin), typeof this.options.referrerPolicy == "string" && (i.referrerPolicy = this.options.referrerPolicy), i.alt = "", i.src = this.getTileUrl(t), i;
      },
      // @section Extension methods
      // @uninheritable
      // Layers extending `TileLayer` might reimplement the following method.
      // @method getTileUrl(coords: Object): String
      // Called only internally, returns the URL for a tile given its coordinates.
      // Classes extending `TileLayer` can override this function to provide custom tile URL naming schemes.
      getTileUrl: function(t) {
        var e = {
          r: p.retina ? "@2x" : "",
          s: this._getSubdomain(t),
          x: t.x,
          y: t.y,
          z: this._getZoomForUrl()
        };
        if (this._map && !this._map.options.crs.infinite) {
          var i = this._globalTileRange.max.y - t.y;
          this.options.tms && (e.y = i), e["-y"] = i;
        }
        return pt(this._url, f(e, this.options));
      },
      _tileOnLoad: function(t, e) {
        p.ielt9 ? setTimeout(T(t, this, null, e), 0) : t(null, e);
      },
      _tileOnError: function(t, e, i) {
        var n = this.options.errorTileUrl;
        n && e.getAttribute("src") !== n && (e.src = n), t(i, e);
      },
      _onTileRemove: function(t) {
        t.tile.onload = null;
      },
      _getZoomForUrl: function() {
        var t = this._tileZoom, e = this.options.maxZoom, i = this.options.zoomReverse, n = this.options.zoomOffset;
        return i && (t = e - t), t + n;
      },
      _getSubdomain: function(t) {
        var e = Math.abs(t.x + t.y) % this.options.subdomains.length;
        return this.options.subdomains[e];
      },
      // stops loading all tiles in the background layer
      _abortLoading: function() {
        var t, e;
        for (t in this._tiles)
          if (this._tiles[t].coords.z !== this._tileZoom && (e = this._tiles[t].el, e.onload = I, e.onerror = I, !e.complete)) {
            e.src = e0;
            var i = this._tiles[t].coords;
            Z(e), delete this._tiles[t], this.fire("tileabort", {
              tile: e,
              coords: i
            });
          }
      },
      _removeTile: function(t) {
        var e = this._tiles[t];
        if (e)
          return e.el.setAttribute("src", e0), K2.prototype._removeTile.call(this, t);
      },
      _tileReady: function(t, e, i) {
        if (!(!this._map || i && i.getAttribute("src") === e0))
          return K2.prototype._tileReady.call(this, t, e, i);
      }
    });
    function me(t, e) {
      return new B2(t, e);
    }
    var pe = B2.extend({
      // @section
      // @aka TileLayer.WMS options
      // If any custom options not documented here are used, they will be sent to the
      // WMS server as extra parameters in each request URL. This can be useful for
      // [non-standard vendor WMS parameters](https://docs.geoserver.org/stable/en/user/services/wms/vendor.html).
      defaultWmsParams: {
        service: "WMS",
        request: "GetMap",
        // @option layers: String = ''
        // **(required)** Comma-separated list of WMS layers to show.
        layers: "",
        // @option styles: String = ''
        // Comma-separated list of WMS styles.
        styles: "",
        // @option format: String = 'image/jpeg'
        // WMS image format (use `'image/png'` for layers with transparency).
        format: "image/jpeg",
        // @option transparent: Boolean = false
        // If `true`, the WMS service will return images with transparency.
        transparent: !1,
        // @option version: String = '1.1.1'
        // Version of the WMS service to use
        version: "1.1.1"
      },
      options: {
        // @option crs: CRS = null
        // Coordinate Reference System to use for the WMS requests, defaults to
        // map CRS. Don't change this if you're not sure what it means.
        crs: null,
        // @option uppercase: Boolean = false
        // If `true`, WMS request parameter keys will be uppercase.
        uppercase: !1
      },
      initialize: function(t, e) {
        this._url = t;
        var i = f({}, this.defaultWmsParams);
        for (var n in e)
          n in this.options || (i[n] = e[n]);
        e = O(this, e);
        var o = e.detectRetina && p.retina ? 2 : 1, s = this.getTileSize();
        i.width = s.x * o, i.height = s.y * o, this.wmsParams = i;
      },
      onAdd: function(t) {
        this._crs = this.options.crs || t.options.crs, this._wmsVersion = parseFloat(this.wmsParams.version);
        var e = this._wmsVersion >= 1.3 ? "crs" : "srs";
        this.wmsParams[e] = this._crs.code, B2.prototype.onAdd.call(this, t);
      },
      getTileUrl: function(t) {
        var e = this._tileCoordsToNwSe(t), i = this._crs, n = V(i.project(e[0]), i.project(e[1])), o = n.min, s = n.max, a = (this._wmsVersion >= 1.3 && this._crs === he ? [o.y, o.x, s.y, s.x] : [o.x, o.y, s.x, s.y]).join(","), l = B2.prototype.getTileUrl.call(this, t);
        return l + mt(this.wmsParams, l, this.options.uppercase) + (this.options.uppercase ? "&BBOX=" : "&bbox=") + a;
      },
      // @method setParams(params: Object, noRedraw?: Boolean): this
      // Merges an object with the new parameters and re-requests tiles on the current screen (unless `noRedraw` was set to true).
      setParams: function(t, e) {
        return f(this.wmsParams, t), e || this.redraw(), this;
      }
    });
    function ji(t, e) {
      return new pe(t, e);
    }
    B2.WMS = pe, me.wms = ji;
    var m2 = i2.extend({
      // @section
      // @aka Renderer options
      options: {
        // @option padding: Number = 0.1
        // How much to extend the clip area around the map view (relative to its size)
        // e.g. 0.1 would be 10% of map view in each direction
        padding: 0.1
      },
      initialize: function(t) {
        O(this, t), C(this), this._layers = this._layers || {};
      },
      onAdd: function() {
        this._container || (this._initContainer(), b(this._container, "leaflet-zoom-animated")), this.getPane().appendChild(this._container), this._update(), this.on("update", this._updatePaths, this);
      },
      onRemove: function() {
        this.off("update", this._updatePaths, this), this._destroyContainer();
      },
      getEvents: function() {
        var t = {
          viewreset: this._reset,
          zoom: this._onZoom,
          moveend: this._update,
          zoomend: this._onZoomEnd
        };
        return this._zoomAnimated && (t.zoomanim = this._onAnimZoom), t;
      },
      _onAnimZoom: function(t) {
        this._updateTransform(t.center, t.zoom);
      },
      _onZoom: function() {
        this._updateTransform(this._map.getCenter(), this._map.getZoom());
      },
      _updateTransform: function(t, e) {
        var i = this._map.getZoomScale(e, this._zoom), n = this._map.getSize().multiplyBy(0.5 + this.options.padding), o = this._map.project(this._center, e), s = n.multiplyBy(-i).add(o).subtract(this._map._getNewPixelOrigin(t, e));
        p.any3d ? w2(this._container, s, i) : N(this._container, s);
      },
      _reset: function() {
        this._update(), this._updateTransform(this._center, this._zoom);
        for (var t in this._layers)
          this._layers[t]._reset();
      },
      _onZoomEnd: function() {
        for (var t in this._layers)
          this._layers[t]._project();
      },
      _updatePaths: function() {
        for (var t in this._layers)
          this._layers[t]._update();
      },
      _update: function() {
        var t = this.options.padding, e = this._map.getSize(), i = this._map.containerPointToLayerPoint(e.multiplyBy(-t)).round();
        this._bounds = new B(i, i.add(e.multiplyBy(1 + t * 2)).round()), this._center = this._map.getCenter(), this._zoom = this._map.getZoom();
      }
    }), ge = m2.extend({
      // @section
      // @aka Canvas options
      options: {
        // @option tolerance: Number = 0
        // How much to extend the click tolerance around a path/object on the map.
        tolerance: 0
      },
      getEvents: function() {
        var t = m2.prototype.getEvents.call(this);
        return t.viewprereset = this._onViewPreReset, t;
      },
      _onViewPreReset: function() {
        this._postponeUpdatePaths = !0;
      },
      onAdd: function() {
        m2.prototype.onAdd.call(this), this._draw();
      },
      _initContainer: function() {
        var t = this._container = document.createElement("canvas");
        w(t, "mousemove", this._onMouseMove, this), w(t, "click dblclick mousedown mouseup contextmenu", this._onClick, this), w(t, "mouseout", this._handleMouseOut, this), t._leaflet_disable_events = !0, this._ctx = t.getContext("2d");
      },
      _destroyContainer: function() {
        K(this._redrawRequest), delete this._ctx, Z(this._container), A(this._container), delete this._container;
      },
      _updatePaths: function() {
        if (!this._postponeUpdatePaths) {
          var t;
          this._redrawBounds = null;
          for (var e in this._layers)
            t = this._layers[e], t._update();
          this._redraw();
        }
      },
      _update: function() {
        if (!(this._map._animatingZoom && this._bounds)) {
          m2.prototype._update.call(this);
          var t = this._bounds, e = this._container, i = t.getSize(), n = p.retina ? 2 : 1;
          N(e, t.min), e.width = n * i.x, e.height = n * i.y, e.style.width = i.x + "px", e.style.height = i.y + "px", p.retina && this._ctx.scale(2, 2), this._ctx.translate(-t.min.x, -t.min.y), this.fire("update");
        }
      },
      _reset: function() {
        m2.prototype._reset.call(this), this._postponeUpdatePaths && (this._postponeUpdatePaths = !1, this._updatePaths());
      },
      _initPath: function(t) {
        this._updateDashArray(t), this._layers[C(t)] = t;
        var e = t._order = {
          layer: t,
          prev: this._drawLast,
          next: null
        };
        this._drawLast && (this._drawLast.next = e), this._drawLast = e, this._drawFirst = this._drawFirst || this._drawLast;
      },
      _addPath: function(t) {
        this._requestRedraw(t);
      },
      _removePath: function(t) {
        var e = t._order, i = e.next, n = e.prev;
        i ? i.prev = n : this._drawLast = n, n ? n.next = i : this._drawFirst = i, delete t._order, delete this._layers[C(t)], this._requestRedraw(t);
      },
      _updatePath: function(t) {
        this._extendRedrawBounds(t), t._project(), t._update(), this._requestRedraw(t);
      },
      _updateStyle: function(t) {
        this._updateDashArray(t), this._requestRedraw(t);
      },
      _updateDashArray: function(t) {
        if (typeof t.options.dashArray == "string") {
          var e = t.options.dashArray.split(/[, ]+/), i = [], n, o;
          for (o = 0; o < e.length; o++) {
            if (n = Number(e[o]), isNaN(n))
              return;
            i.push(n);
          }
          t.options._dashArray = i;
        } else
          t.options._dashArray = t.options.dashArray;
      },
      _requestRedraw: function(t) {
        this._map && (this._extendRedrawBounds(t), this._redrawRequest = this._redrawRequest || U(this._redraw, this));
      },
      _extendRedrawBounds: function(t) {
        if (t._pxBounds) {
          var e = (t.options.weight || 0) + 1;
          this._redrawBounds = this._redrawBounds || new B(), this._redrawBounds.extend(t._pxBounds.min.subtract([e, e])), this._redrawBounds.extend(t._pxBounds.max.add([e, e]));
        }
      },
      _redraw: function() {
        this._redrawRequest = null, this._redrawBounds && (this._redrawBounds.min._floor(), this._redrawBounds.max._ceil()), this._clear(), this._draw(), this._redrawBounds = null;
      },
      _clear: function() {
        var t = this._redrawBounds;
        if (t) {
          var e = t.getSize();
          this._ctx.clearRect(t.min.x, t.min.y, e.x, e.y);
        } else
          this._ctx.save(), this._ctx.setTransform(1, 0, 0, 1, 0, 0), this._ctx.clearRect(0, 0, this._container.width, this._container.height), this._ctx.restore();
      },
      _draw: function() {
        var t, e = this._redrawBounds;
        if (this._ctx.save(), e) {
          var i = e.getSize();
          this._ctx.beginPath(), this._ctx.rect(e.min.x, e.min.y, i.x, i.y), this._ctx.clip();
        }
        this._drawing = !0;
        for (var n = this._drawFirst; n; n = n.next)
          t = n.layer, (!e || t._pxBounds && t._pxBounds.intersects(e)) && t._updatePath();
        this._drawing = !1, this._ctx.restore();
      },
      _updatePoly: function(t, e) {
        if (this._drawing) {
          var i, n, o, s, a = t._parts, l = a.length, c = this._ctx;
          if (l) {
            for (c.beginPath(), i = 0; i < l; i++) {
              for (n = 0, o = a[i].length; n < o; n++)
                s = a[i][n], c[n ? "lineTo" : "moveTo"](s.x, s.y);
              e && c.closePath();
            }
            this._fillStroke(c, t);
          }
        }
      },
      _updateCircle: function(t) {
        if (!(!this._drawing || t._empty())) {
          var e = t._point, i = this._ctx, n = Math.max(Math.round(t._radius), 1), o = (Math.max(Math.round(t._radiusY), 1) || n) / n;
          o !== 1 && (i.save(), i.scale(1, o)), i.beginPath(), i.arc(e.x, e.y / o, n, 0, Math.PI * 2, !1), o !== 1 && i.restore(), this._fillStroke(i, t);
        }
      },
      _fillStroke: function(t, e) {
        var i = e.options;
        i.fill && (t.globalAlpha = i.fillOpacity, t.fillStyle = i.fillColor || i.color, t.fill(i.fillRule || "evenodd")), i.stroke && i.weight !== 0 && (t.setLineDash && t.setLineDash(e.options && e.options._dashArray || []), t.globalAlpha = i.opacity, t.lineWidth = i.weight, t.strokeStyle = i.color, t.lineCap = i.lineCap, t.lineJoin = i.lineJoin, t.stroke());
      },
      // Canvas obviously doesn't have mouse events for individual drawn objects,
      // so we emulate that by calculating what's under the mouse on mousemove/click manually
      _onClick: function(t) {
        for (var e = this._map.mouseEventToLayerPoint(t), i, n, o = this._drawFirst; o; o = o.next)
          i = o.layer, i.options.interactive && i._containsPoint(e) && (!(t.type === "click" || t.type === "preclick") || !this._map._draggableMoved(i)) && (n = i);
        this._fireEvent(n ? [n] : !1, t);
      },
      _onMouseMove: function(t) {
        if (!(!this._map || this._map.dragging.moving() || this._map._animatingZoom)) {
          var e = this._map.mouseEventToLayerPoint(t);
          this._handleMouseHover(t, e);
        }
      },
      _handleMouseOut: function(t) {
        var e = this._hoveredLayer;
        e && (R(this._container, "leaflet-interactive"), this._fireEvent([e], t, "mouseout"), this._hoveredLayer = null, this._mouseHoverThrottled = !1);
      },
      _handleMouseHover: function(t, e) {
        if (!this._mouseHoverThrottled) {
          for (var i, n, o = this._drawFirst; o; o = o.next)
            i = o.layer, i.options.interactive && i._containsPoint(e) && (n = i);
          n !== this._hoveredLayer && (this._handleMouseOut(t), n && (b(this._container, "leaflet-interactive"), this._fireEvent([n], t, "mouseover"), this._hoveredLayer = n)), this._fireEvent(this._hoveredLayer ? [this._hoveredLayer] : !1, t), this._mouseHoverThrottled = !0, setTimeout(T(function() {
            this._mouseHoverThrottled = !1;
          }, this), 32);
        }
      },
      _fireEvent: function(t, e, i) {
        this._map._fireDOMEvent(e, i || e.type, t);
      },
      _bringToFront: function(t) {
        var e = t._order;
        if (e) {
          var i = e.next, n = e.prev;
          if (i)
            i.prev = n;
          else
            return;
          n ? n.next = i : i && (this._drawFirst = i), e.prev = this._drawLast, this._drawLast.next = e, e.next = null, this._drawLast = e, this._requestRedraw(t);
        }
      },
      _bringToBack: function(t) {
        var e = t._order;
        if (e) {
          var i = e.next, n = e.prev;
          if (n)
            n.next = i;
          else
            return;
          i ? i.prev = n : n && (this._drawLast = n), e.prev = null, e.next = this._drawFirst, this._drawFirst.prev = e, this._drawFirst = e, this._requestRedraw(t);
        }
      }
    });
    function ve(t) {
      return p.canvas ? new ge(t) : null;
    }
    var X2 = function() {
      try {
        return document.namespaces.add("lvml", "urn:schemas-microsoft-com:vml"), function(t) {
          return document.createElement("<lvml:" + t + ' class="lvml">');
        };
      } catch {
      }
      return function(t) {
        return document.createElement("<" + t + ' xmlns="urn:schemas-microsoft.com:vml" class="lvml">');
      };
    }(), Ji = {
      _initContainer: function() {
        this._container = z("div", "leaflet-vml-container");
      },
      _update: function() {
        this._map._animatingZoom || (m2.prototype._update.call(this), this.fire("update"));
      },
      _initPath: function(t) {
        var e = t._container = X2("shape");
        b(e, "leaflet-vml-shape " + (this.options.className || "")), e.coordsize = "1 1", t._path = X2("path"), e.appendChild(t._path), this._updateStyle(t), this._layers[C(t)] = t;
      },
      _addPath: function(t) {
        var e = t._container;
        this._container.appendChild(e), t.options.interactive && t.addInteractiveTarget(e);
      },
      _removePath: function(t) {
        var e = t._container;
        Z(e), t.removeInteractiveTarget(e), delete this._layers[C(t)];
      },
      _updateStyle: function(t) {
        var e = t._stroke, i = t._fill, n = t.options, o = t._container;
        o.stroked = !!n.stroke, o.filled = !!n.fill, n.stroke ? (e || (e = t._stroke = X2("stroke")), o.appendChild(e), e.weight = n.weight + "px", e.color = n.color, e.opacity = n.opacity, n.dashArray ? e.dashStyle = t2(n.dashArray) ? n.dashArray.join(" ") : n.dashArray.replace(/( *, *)/g, " ") : e.dashStyle = "", e.endcap = n.lineCap.replace("butt", "flat"), e.joinstyle = n.lineJoin) : e && (o.removeChild(e), t._stroke = null), n.fill ? (i || (i = t._fill = X2("fill")), o.appendChild(i), i.color = n.fillColor || n.color, i.opacity = n.fillOpacity) : i && (o.removeChild(i), t._fill = null);
      },
      _updateCircle: function(t) {
        var e = t._point.round(), i = Math.round(t._radius), n = Math.round(t._radiusY || i);
        this._setPath(t, t._empty() ? "M0 0" : "AL " + e.x + "," + e.y + " " + i + "," + n + " 0," + 65535 * 360);
      },
      _setPath: function(t, e) {
        t._path.v = e;
      },
      _bringToFront: function(t) {
        k2(t._container);
      },
      _bringToBack: function(t) {
        z2(t._container);
      }
    }, y0 = p.vml ? X2 : Ct, Q2 = m2.extend({
      _initContainer: function() {
        this._container = y0("svg"), this._container.setAttribute("pointer-events", "none"), this._rootGroup = y0("g"), this._container.appendChild(this._rootGroup);
      },
      _destroyContainer: function() {
        Z(this._container), A(this._container), delete this._container, delete this._rootGroup, delete this._svgSize;
      },
      _update: function() {
        if (!(this._map._animatingZoom && this._bounds)) {
          m2.prototype._update.call(this);
          var t = this._bounds, e = t.getSize(), i = this._container;
          (!this._svgSize || !this._svgSize.equals(e)) && (this._svgSize = e, i.setAttribute("width", e.x), i.setAttribute("height", e.y)), N(i, t.min), i.setAttribute("viewBox", [t.min.x, t.min.y, e.x, e.y].join(" ")), this.fire("update");
        }
      },
      // methods below are called by vector layers implementations
      _initPath: function(t) {
        var e = t._path = y0("path");
        t.options.className && b(e, t.options.className), t.options.interactive && b(e, "leaflet-interactive"), this._updateStyle(t), this._layers[C(t)] = t;
      },
      _addPath: function(t) {
        this._rootGroup || this._initContainer(), this._rootGroup.appendChild(t._path), t.addInteractiveTarget(t._path);
      },
      _removePath: function(t) {
        Z(t._path), t.removeInteractiveTarget(t._path), delete this._layers[C(t)];
      },
      _updatePath: function(t) {
        t._project(), t._update();
      },
      _updateStyle: function(t) {
        var e = t._path, i = t.options;
        e && (i.stroke ? (e.setAttribute("stroke", i.color), e.setAttribute("stroke-opacity", i.opacity), e.setAttribute("stroke-width", i.weight), e.setAttribute("stroke-linecap", i.lineCap), e.setAttribute("stroke-linejoin", i.lineJoin), i.dashArray ? e.setAttribute("stroke-dasharray", i.dashArray) : e.removeAttribute("stroke-dasharray"), i.dashOffset ? e.setAttribute("stroke-dashoffset", i.dashOffset) : e.removeAttribute("stroke-dashoffset")) : e.setAttribute("stroke", "none"), i.fill ? (e.setAttribute("fill", i.fillColor || i.color), e.setAttribute("fill-opacity", i.fillOpacity), e.setAttribute("fill-rule", i.fillRule || "evenodd")) : e.setAttribute("fill", "none"));
      },
      _updatePoly: function(t, e) {
        this._setPath(t, bt(t._parts, e));
      },
      _updateCircle: function(t) {
        var e = t._point, i = Math.max(Math.round(t._radius), 1), n = Math.max(Math.round(t._radiusY), 1) || i, o = "a" + i + "," + n + " 0 1,0 ", s = t._empty() ? "M0 0" : "M" + (e.x - i) + "," + e.y + o + i * 2 + ",0 " + o + -i * 2 + ",0 ";
        this._setPath(t, s);
      },
      _setPath: function(t, e) {
        t._path.setAttribute("d", e);
      },
      // SVG does not have the concept of zIndex so we resort to changing the DOM order of elements
      _bringToFront: function(t) {
        k2(t._path);
      },
      _bringToBack: function(t) {
        z2(t._path);
      }
    });
    p.vml && Q2.include(Ji);
    function ye(t) {
      return p.svg || p.vml ? new Q2(t) : null;
    }
    k.include({
      // @namespace Map; @method getRenderer(layer: Path): Renderer
      // Returns the instance of `Renderer` that should be used to render the given
      // `Path`. It will ensure that the `renderer` options of the map and paths
      // are respected, and that the renderers do exist on the map.
      getRenderer: function(t) {
        var e = t.options.renderer || this._getPaneRenderer(t.options.pane) || this.options.renderer || this._renderer;
        return e || (e = this._renderer = this._createRenderer()), this.hasLayer(e) || this.addLayer(e), e;
      },
      _getPaneRenderer: function(t) {
        if (t === "overlayPane" || t === void 0)
          return !1;
        var e = this._paneRenderers[t];
        return e === void 0 && (e = this._createRenderer({ pane: t }), this._paneRenderers[t] = e), e;
      },
      _createRenderer: function(t) {
        return this.options.preferCanvas && ve(t) || ye(t);
      }
    });
    var xe = A2.extend({
      initialize: function(t, e) {
        A2.prototype.initialize.call(this, this._boundsToLatLngs(t), e);
      },
      // @method setBounds(latLngBounds: LatLngBounds): this
      // Redraws the rectangle with the passed bounds.
      setBounds: function(t) {
        return this.setLatLngs(this._boundsToLatLngs(t));
      },
      _boundsToLatLngs: function(t) {
        return t = D(t), [
          t.getSouthWest(),
          t.getNorthWest(),
          t.getNorthEast(),
          t.getSouthEast()
        ];
      }
    });
    function Ki(t, e) {
      return new xe(t, e);
    }
    Q2.create = y0, Q2.pointsToPath = bt, _2.geometryToLayer = f0, _2.coordsToLatLng = ot, _2.coordsToLatLngs = d0, _2.latLngToCoords = st, _2.latLngsToCoords = _0, _2.getFeature = O2, _2.asFeature = m0, k.mergeOptions({
      // @option boxZoom: Boolean = true
      // Whether the map can be zoomed to a rectangular area specified by
      // dragging the mouse while pressing the shift key.
      boxZoom: !0
    });
    var we = r2.extend({
      initialize: function(t) {
        this._map = t, this._container = t._container, this._pane = t._panes.overlayPane, this._resetStateTimeout = 0, t.on("unload", this._destroy, this);
      },
      addHooks: function() {
        w(this._container, "mousedown", this._onMouseDown, this);
      },
      removeHooks: function() {
        A(this._container, "mousedown", this._onMouseDown, this);
      },
      moved: function() {
        return this._moved;
      },
      _destroy: function() {
        Z(this._pane), delete this._pane;
      },
      _resetState: function() {
        this._resetStateTimeout = 0, this._moved = !1;
      },
      _clearDeferredResetState: function() {
        this._resetStateTimeout !== 0 && (clearTimeout(this._resetStateTimeout), this._resetStateTimeout = 0);
      },
      _onMouseDown: function(t) {
        if (!t.shiftKey || t.which !== 1 && t.button !== 1)
          return !1;
        this._clearDeferredResetState(), this._resetState(), G2(), W0(), this._startPoint = this._map.mouseEventToContainerPoint(t), w(document, {
          contextmenu: L2,
          mousemove: this._onMouseMove,
          mouseup: this._onMouseUp,
          keydown: this._onKeyDown
        }, this);
      },
      _onMouseMove: function(t) {
        this._moved || (this._moved = !0, this._box = z("div", "leaflet-zoom-box", this._container), b(this._container, "leaflet-crosshair"), this._map.fire("boxzoomstart")), this._point = this._map.mouseEventToContainerPoint(t);
        var e = new B(this._point, this._startPoint), i = e.getSize();
        N(this._box, e.min), this._box.style.width = i.x + "px", this._box.style.height = i.y + "px";
      },
      _finish: function() {
        this._moved && (Z(this._box), R(this._container, "leaflet-crosshair")), U2(), H0(), A(document, {
          contextmenu: L2,
          mousemove: this._onMouseMove,
          mouseup: this._onMouseUp,
          keydown: this._onKeyDown
        }, this);
      },
      _onMouseUp: function(t) {
        if (!(t.which !== 1 && t.button !== 1) && (this._finish(), !!this._moved)) {
          this._clearDeferredResetState(), this._resetStateTimeout = setTimeout(T(this._resetState, this), 0);
          var e = new q(
            this._map.containerPointToLatLng(this._startPoint),
            this._map.containerPointToLatLng(this._point)
          );
          this._map.fitBounds(e).fire("boxzoomend", { boxZoomBounds: e });
        }
      },
      _onKeyDown: function(t) {
        t.keyCode === 27 && (this._finish(), this._clearDeferredResetState(), this._resetState());
      }
    });
    k.addInitHook("addHandler", "boxZoom", we), k.mergeOptions({
      // @option doubleClickZoom: Boolean|String = true
      // Whether the map can be zoomed in by double clicking on it and
      // zoomed out by double clicking while holding shift. If passed
      // `'center'`, double-click zoom will zoom to the center of the
      //  view regardless of where the mouse was.
      doubleClickZoom: !0
    });
    var Ce = r2.extend({
      addHooks: function() {
        this._map.on("dblclick", this._onDoubleClick, this);
      },
      removeHooks: function() {
        this._map.off("dblclick", this._onDoubleClick, this);
      },
      _onDoubleClick: function(t) {
        var e = this._map, i = e.getZoom(), n = e.options.zoomDelta, o = t.originalEvent.shiftKey ? i - n : i + n;
        e.options.doubleClickZoom === "center" ? e.setZoom(o) : e.setZoomAround(t.containerPoint, o);
      }
    });
    k.addInitHook("addHandler", "doubleClickZoom", Ce), k.mergeOptions({
      // @option dragging: Boolean = true
      // Whether the map is draggable with mouse/touch or not.
      dragging: !0,
      // @section Panning Inertia Options
      // @option inertia: Boolean = *
      // If enabled, panning of the map will have an inertia effect where
      // the map builds momentum while dragging and continues moving in
      // the same direction for some time. Feels especially nice on touch
      // devices. Enabled by default.
      inertia: !0,
      // @option inertiaDeceleration: Number = 3000
      // The rate with which the inertial movement slows down, in pixels/second².
      inertiaDeceleration: 3400,
      // px/s^2
      // @option inertiaMaxSpeed: Number = Infinity
      // Max speed of the inertial movement, in pixels/second.
      inertiaMaxSpeed: 1 / 0,
      // px/s
      // @option easeLinearity: Number = 0.2
      easeLinearity: 0.2,
      // TODO refactor, move to CRS
      // @option worldCopyJump: Boolean = false
      // With this option enabled, the map tracks when you pan to another "copy"
      // of the world and seamlessly jumps to the original one so that all overlays
      // like markers and vector layers are still visible.
      worldCopyJump: !1,
      // @option maxBoundsViscosity: Number = 0.0
      // If `maxBounds` is set, this option will control how solid the bounds
      // are when dragging the map around. The default value of `0.0` allows the
      // user to drag outside the bounds at normal speed, higher values will
      // slow down map dragging outside bounds, and `1.0` makes the bounds fully
      // solid, preventing the user from dragging outside the bounds.
      maxBoundsViscosity: 0
    });
    var be = r2.extend({
      addHooks: function() {
        if (!this._draggable) {
          var t = this._map;
          this._draggable = new v2(t._mapPane, t._container), this._draggable.on({
            dragstart: this._onDragStart,
            drag: this._onDrag,
            dragend: this._onDragEnd
          }, this), this._draggable.on("predrag", this._onPreDragLimit, this), t.options.worldCopyJump && (this._draggable.on("predrag", this._onPreDragWrap, this), t.on("zoomend", this._onZoomEnd, this), t.whenReady(this._onZoomEnd, this));
        }
        b(this._map._container, "leaflet-grab leaflet-touch-drag"), this._draggable.enable(), this._positions = [], this._times = [];
      },
      removeHooks: function() {
        R(this._map._container, "leaflet-grab"), R(this._map._container, "leaflet-touch-drag"), this._draggable.disable();
      },
      moved: function() {
        return this._draggable && this._draggable._moved;
      },
      moving: function() {
        return this._draggable && this._draggable._moving;
      },
      _onDragStart: function() {
        var t = this._map;
        if (t._stop(), this._map.options.maxBounds && this._map.options.maxBoundsViscosity) {
          var e = D(this._map.options.maxBounds);
          this._offsetLimit = V(
            this._map.latLngToContainerPoint(e.getNorthWest()).multiplyBy(-1),
            this._map.latLngToContainerPoint(e.getSouthEast()).multiplyBy(-1).add(this._map.getSize())
          ), this._viscosity = Math.min(1, Math.max(0, this._map.options.maxBoundsViscosity));
        } else
          this._offsetLimit = null;
        t.fire("movestart").fire("dragstart"), t.options.inertia && (this._positions = [], this._times = []);
      },
      _onDrag: function(t) {
        if (this._map.options.inertia) {
          var e = this._lastTime = +/* @__PURE__ */ new Date(), i = this._lastPos = this._draggable._absPos || this._draggable._newPos;
          this._positions.push(i), this._times.push(e), this._prunePositions(e);
        }
        this._map.fire("move", t).fire("drag", t);
      },
      _prunePositions: function(t) {
        for (; this._positions.length > 1 && t - this._times[0] > 50; )
          this._positions.shift(), this._times.shift();
      },
      _onZoomEnd: function() {
        var t = this._map.getSize().divideBy(2), e = this._map.latLngToLayerPoint([0, 0]);
        this._initialWorldOffset = e.subtract(t).x, this._worldWidth = this._map.getPixelWorldBounds().getSize().x;
      },
      _viscousLimit: function(t, e) {
        return t - (t - e) * this._viscosity;
      },
      _onPreDragLimit: function() {
        if (!(!this._viscosity || !this._offsetLimit)) {
          var t = this._draggable._newPos.subtract(this._draggable._startPos), e = this._offsetLimit;
          t.x < e.min.x && (t.x = this._viscousLimit(t.x, e.min.x)), t.y < e.min.y && (t.y = this._viscousLimit(t.y, e.min.y)), t.x > e.max.x && (t.x = this._viscousLimit(t.x, e.max.x)), t.y > e.max.y && (t.y = this._viscousLimit(t.y, e.max.y)), this._draggable._newPos = this._draggable._startPos.add(t);
        }
      },
      _onPreDragWrap: function() {
        var t = this._worldWidth, e = Math.round(t / 2), i = this._initialWorldOffset, n = this._draggable._newPos.x, o = (n - e + i) % t + e - i, s = (n + e + i) % t - e - i, a = Math.abs(o + i) < Math.abs(s + i) ? o : s;
        this._draggable._absPos = this._draggable._newPos.clone(), this._draggable._newPos.x = a;
      },
      _onDragEnd: function(t) {
        var e = this._map, i = e.options, n = !i.inertia || t.noInertia || this._times.length < 2;
        if (e.fire("dragend", t), n)
          e.fire("moveend");
        else {
          this._prunePositions(+/* @__PURE__ */ new Date());
          var o = this._lastPos.subtract(this._positions[0]), s = (this._lastTime - this._times[0]) / 1e3, a = i.easeLinearity, l = o.multiplyBy(a / s), c = l.distanceTo([0, 0]), u = Math.min(i.inertiaMaxSpeed, c), _ = l.multiplyBy(u / c), g = u / (i.inertiaDeceleration * a), P = _.multiplyBy(-g / 2).round();
          !P.x && !P.y ? e.fire("moveend") : (P = e._limitOffset(P, e.options.maxBounds), U(function() {
            e.panBy(P, {
              duration: g,
              easeLinearity: a,
              noMoveStart: !0,
              animate: !0
            });
          }));
        }
      }
    });
    k.addInitHook("addHandler", "dragging", be), k.mergeOptions({
      // @option keyboard: Boolean = true
      // Makes the map focusable and allows users to navigate the map with keyboard
      // arrows and `+`/`-` keys.
      keyboard: !0,
      // @option keyboardPanDelta: Number = 80
      // Amount of pixels to pan when pressing an arrow key.
      keyboardPanDelta: 80
    });
    var Le = r2.extend({
      keyCodes: {
        left: [37],
        right: [39],
        down: [40],
        up: [38],
        zoomIn: [187, 107, 61, 171],
        zoomOut: [189, 109, 54, 173]
      },
      initialize: function(t) {
        this._map = t, this._setPanDelta(t.options.keyboardPanDelta), this._setZoomDelta(t.options.zoomDelta);
      },
      addHooks: function() {
        var t = this._map._container;
        t.tabIndex <= 0 && (t.tabIndex = "0"), w(t, {
          focus: this._onFocus,
          blur: this._onBlur,
          mousedown: this._onMouseDown
        }, this), this._map.on({
          focus: this._addHooks,
          blur: this._removeHooks
        }, this);
      },
      removeHooks: function() {
        this._removeHooks(), A(this._map._container, {
          focus: this._onFocus,
          blur: this._onBlur,
          mousedown: this._onMouseDown
        }, this), this._map.off({
          focus: this._addHooks,
          blur: this._removeHooks
        }, this);
      },
      _onMouseDown: function() {
        if (!this._focused) {
          var t = document.body, e = document.documentElement, i = t.scrollTop || e.scrollTop, n = t.scrollLeft || e.scrollLeft;
          this._map._container.focus(), window.scrollTo(n, i);
        }
      },
      _onFocus: function() {
        this._focused = !0, this._map.fire("focus");
      },
      _onBlur: function() {
        this._focused = !1, this._map.fire("blur");
      },
      _setPanDelta: function(t) {
        var e = this._panKeys = {}, i = this.keyCodes, n, o;
        for (n = 0, o = i.left.length; n < o; n++)
          e[i.left[n]] = [-1 * t, 0];
        for (n = 0, o = i.right.length; n < o; n++)
          e[i.right[n]] = [t, 0];
        for (n = 0, o = i.down.length; n < o; n++)
          e[i.down[n]] = [0, t];
        for (n = 0, o = i.up.length; n < o; n++)
          e[i.up[n]] = [0, -1 * t];
      },
      _setZoomDelta: function(t) {
        var e = this._zoomKeys = {}, i = this.keyCodes, n, o;
        for (n = 0, o = i.zoomIn.length; n < o; n++)
          e[i.zoomIn[n]] = t;
        for (n = 0, o = i.zoomOut.length; n < o; n++)
          e[i.zoomOut[n]] = -t;
      },
      _addHooks: function() {
        w(document, "keydown", this._onKeyDown, this);
      },
      _removeHooks: function() {
        A(document, "keydown", this._onKeyDown, this);
      },
      _onKeyDown: function(t) {
        if (!(t.altKey || t.ctrlKey || t.metaKey)) {
          var e = t.keyCode, i = this._map, n;
          if (e in this._panKeys) {
            if (!i._panAnim || !i._panAnim._inProgress)
              if (n = this._panKeys[e], t.shiftKey && (n = v(n).multiplyBy(3)), i.options.maxBounds && (n = i._limitOffset(v(n), i.options.maxBounds)), i.options.worldCopyJump) {
                var o = i.wrapLatLng(i.unproject(i.project(i.getCenter()).add(n)));
                i.panTo(o);
              } else
                i.panBy(n);
          } else if (e in this._zoomKeys)
            i.setZoom(i.getZoom() + (t.shiftKey ? 3 : 1) * this._zoomKeys[e]);
          else if (e === 27 && i._popup && i._popup.options.closeOnEscapeKey)
            i.closePopup();
          else
            return;
          L2(t);
        }
      }
    });
    k.addInitHook("addHandler", "keyboard", Le), k.mergeOptions({
      // @section Mouse wheel options
      // @option scrollWheelZoom: Boolean|String = true
      // Whether the map can be zoomed by using the mouse wheel. If passed `'center'`,
      // it will zoom to the center of the view regardless of where the mouse was.
      scrollWheelZoom: !0,
      // @option wheelDebounceTime: Number = 40
      // Limits the rate at which a wheel can fire (in milliseconds). By default
      // user can't zoom via wheel more often than once per 40 ms.
      wheelDebounceTime: 40,
      // @option wheelPxPerZoomLevel: Number = 60
      // How many scroll pixels (as reported by [L.DomEvent.getWheelDelta](#domevent-getwheeldelta))
      // mean a change of one full zoom level. Smaller values will make wheel-zooming
      // faster (and vice versa).
      wheelPxPerZoomLevel: 60
    });
    var Pe = r2.extend({
      addHooks: function() {
        w(this._map._container, "wheel", this._onWheelScroll, this), this._delta = 0;
      },
      removeHooks: function() {
        A(this._map._container, "wheel", this._onWheelScroll, this);
      },
      _onWheelScroll: function(t) {
        var e = Jt(t), i = this._map.options.wheelDebounceTime;
        this._delta += e, this._lastMousePos = this._map.mouseEventToContainerPoint(t), this._startTime || (this._startTime = +/* @__PURE__ */ new Date());
        var n = Math.max(i - (+/* @__PURE__ */ new Date() - this._startTime), 0);
        clearTimeout(this._timer), this._timer = setTimeout(T(this._performZoom, this), n), L2(t);
      },
      _performZoom: function() {
        var t = this._map, e = t.getZoom(), i = this._map.options.zoomSnap || 0;
        t._stop();
        var n = this._delta / (this._map.options.wheelPxPerZoomLevel * 4), o = 4 * Math.log(2 / (1 + Math.exp(-Math.abs(n)))) / Math.LN2, s = i ? Math.ceil(o / i) * i : o, a = t._limitZoom(e + (this._delta > 0 ? s : -s)) - e;
        this._delta = 0, this._startTime = null, a && (t.options.scrollWheelZoom === "center" ? t.setZoom(e + a) : t.setZoomAround(this._lastMousePos, e + a));
      }
    });
    k.addInitHook("addHandler", "scrollWheelZoom", Pe);
    var Xi = 600;
    k.mergeOptions({
      // @section Touch interaction options
      // @option tapHold: Boolean
      // Enables simulation of `contextmenu` event, default is `true` for mobile Safari.
      tapHold: p.touchNative && p.safari && p.mobile,
      // @option tapTolerance: Number = 15
      // The max number of pixels a user can shift his finger during touch
      // for it to be considered a valid tap.
      tapTolerance: 15
    });
    var Te = r2.extend({
      addHooks: function() {
        w(this._map._container, "touchstart", this._onDown, this);
      },
      removeHooks: function() {
        A(this._map._container, "touchstart", this._onDown, this);
      },
      _onDown: function(t) {
        if (clearTimeout(this._holdTimeout), t.touches.length === 1) {
          var e = t.touches[0];
          this._startPos = this._newPos = new x(e.clientX, e.clientY), this._holdTimeout = setTimeout(T(function() {
            this._cancel(), this._isTapValid() && (w(document, "touchend", H), w(document, "touchend touchcancel", this._cancelClickPrevent), this._simulateEvent("contextmenu", e));
          }, this), Xi), w(document, "touchend touchcancel contextmenu", this._cancel, this), w(document, "touchmove", this._onMove, this);
        }
      },
      _cancelClickPrevent: function t() {
        A(document, "touchend", H), A(document, "touchend touchcancel", t);
      },
      _cancel: function() {
        clearTimeout(this._holdTimeout), A(document, "touchend touchcancel contextmenu", this._cancel, this), A(document, "touchmove", this._onMove, this);
      },
      _onMove: function(t) {
        var e = t.touches[0];
        this._newPos = new x(e.clientX, e.clientY);
      },
      _isTapValid: function() {
        return this._newPos.distanceTo(this._startPos) <= this._map.options.tapTolerance;
      },
      _simulateEvent: function(t, e) {
        var i = new MouseEvent(t, {
          bubbles: !0,
          cancelable: !0,
          view: window,
          // detail: 1,
          screenX: e.screenX,
          screenY: e.screenY,
          clientX: e.clientX,
          clientY: e.clientY
          // button: 2,
          // buttons: 2
        });
        i._simulated = !0, e.target.dispatchEvent(i);
      }
    });
    k.addInitHook("addHandler", "tapHold", Te), k.mergeOptions({
      // @section Touch interaction options
      // @option touchZoom: Boolean|String = *
      // Whether the map can be zoomed by touch-dragging with two fingers. If
      // passed `'center'`, it will zoom to the center of the view regardless of
      // where the touch events (fingers) were. Enabled for touch-capable web
      // browsers.
      touchZoom: p.touch,
      // @option bounceAtZoomLimits: Boolean = true
      // Set it to false if you don't want the map to zoom beyond min/max zoom
      // and then bounce back when pinch-zooming.
      bounceAtZoomLimits: !0
    });
    var Me = r2.extend({
      addHooks: function() {
        b(this._map._container, "leaflet-touch-zoom"), w(this._map._container, "touchstart", this._onTouchStart, this);
      },
      removeHooks: function() {
        R(this._map._container, "leaflet-touch-zoom"), A(this._map._container, "touchstart", this._onTouchStart, this);
      },
      _onTouchStart: function(t) {
        var e = this._map;
        if (!(!t.touches || t.touches.length !== 2 || e._animatingZoom || this._zooming)) {
          var i = e.mouseEventToContainerPoint(t.touches[0]), n = e.mouseEventToContainerPoint(t.touches[1]);
          this._centerPoint = e.getSize()._divideBy(2), this._startLatLng = e.containerPointToLatLng(this._centerPoint), e.options.touchZoom !== "center" && (this._pinchStartLatLng = e.containerPointToLatLng(i.add(n)._divideBy(2))), this._startDist = i.distanceTo(n), this._startZoom = e.getZoom(), this._moved = !1, this._zooming = !0, e._stop(), w(document, "touchmove", this._onTouchMove, this), w(document, "touchend touchcancel", this._onTouchEnd, this), H(t);
        }
      },
      _onTouchMove: function(t) {
        if (!(!t.touches || t.touches.length !== 2 || !this._zooming)) {
          var e = this._map, i = e.mouseEventToContainerPoint(t.touches[0]), n = e.mouseEventToContainerPoint(t.touches[1]), o = i.distanceTo(n) / this._startDist;
          if (this._zoom = e.getScaleZoom(o, this._startZoom), !e.options.bounceAtZoomLimits && (this._zoom < e.getMinZoom() && o < 1 || this._zoom > e.getMaxZoom() && o > 1) && (this._zoom = e._limitZoom(this._zoom)), e.options.touchZoom === "center") {
            if (this._center = this._startLatLng, o === 1)
              return;
          } else {
            var s = i._add(n)._divideBy(2)._subtract(this._centerPoint);
            if (o === 1 && s.x === 0 && s.y === 0)
              return;
            this._center = e.unproject(e.project(this._pinchStartLatLng, this._zoom).subtract(s), this._zoom);
          }
          this._moved || (e._moveStart(!0, !1), this._moved = !0), K(this._animRequest);
          var a = T(e._move, e, this._center, this._zoom, { pinch: !0, round: !1 }, void 0);
          this._animRequest = U(a, this, !0), H(t);
        }
      },
      _onTouchEnd: function() {
        if (!this._moved || !this._zooming) {
          this._zooming = !1;
          return;
        }
        this._zooming = !1, K(this._animRequest), A(document, "touchmove", this._onTouchMove, this), A(document, "touchend touchcancel", this._onTouchEnd, this), this._map.options.zoomAnimation ? this._map._animateZoom(this._center, this._map._limitZoom(this._zoom), !0, this._map.options.zoomSnap) : this._map._resetView(this._center, this._map._limitZoom(this._zoom));
      }
    });
    k.addInitHook("addHandler", "touchZoom", Me), k.BoxZoom = we, k.DoubleClickZoom = Ce, k.Drag = be, k.Keyboard = Le, k.ScrollWheelZoom = Pe, k.TapHold = Te, k.TouchZoom = Me, h.Bounds = B, h.Browser = p, h.CRS = u2, h.Canvas = ge, h.Circle = nt, h.CircleMarker = u0, h.Class = c2, h.Control = e2, h.DivIcon = _e, h.DivOverlay = a2, h.DomEvent = mi, h.DomUtil = di, h.Draggable = v2, h.Evented = D2, h.FeatureGroup = f2, h.GeoJSON = _2, h.GridLayer = K2, h.Handler = r2, h.Icon = E2, h.ImageOverlay = p0, h.LatLng = S, h.LatLngBounds = q, h.Layer = i2, h.LayerGroup = S2, h.LineUtil = ki, h.Map = k, h.Marker = c0, h.Mixin = Ci, h.Path = y2, h.Point = x, h.PolyUtil = bi, h.Polygon = A2, h.Polyline = d2, h.Popup = g0, h.PosAnimation = Kt, h.Projection = zi, h.Rectangle = xe, h.Renderer = m2, h.SVG = Q2, h.SVGOverlay = de, h.TileLayer = B2, h.Tooltip = v0, h.Transformation = z0, h.Util = Ze, h.VideoOverlay = fe, h.bind = T, h.bounds = V, h.canvas = ve, h.circle = Ri, h.circleMarker = Ii, h.control = Y2, h.divIcon = qi, h.extend = f, h.featureGroup = Oi, h.geoJSON = ue, h.geoJson = Fi, h.gridLayer = Yi, h.icon = Bi, h.imageOverlay = Wi, h.latLng = M, h.latLngBounds = D, h.layerGroup = Ai, h.map = pi, h.marker = Zi, h.point = v, h.polygon = Ni, h.polyline = Di, h.popup = Ui, h.rectangle = Ki, h.setOptions = O, h.stamp = C, h.svg = ye, h.svgOverlay = Gi, h.tileLayer = me, h.tooltip = Vi, h.transformation = N2, h.version = m, h.videoOverlay = Hi;
    var Qi = window.L;
    h.noConflict = function() {
      return window.L = Qi, this;
    }, window.L = h;
  });
})(_t, _t.exports);
var Oe = _t.exports;
const uo = /* @__PURE__ */ co(Oe), fo = /* @__PURE__ */ hn({
  __proto__: null,
  default: uo
}, [Oe]);
//# sourceMappingURL=flight-card.js.map
