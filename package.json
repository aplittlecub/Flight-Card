{
  "name": "adsb-nearby-aircraft",
  "version": "0.3.2",
  "description": "Home Assistant integration and Lovelace card for ADS-B nearby aircraft mapping.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "vite build",
    "build:watch": "vite build --watch",
    "check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.17",
    "typescript": "^5.7.3",
    "vite": "^5.4.10"
  }
}
