{
  "name": "adsb-skyvista",
  "version": "0.3.2",
  "description": "Display aircraft detected by your local dump1090 receiver directly in Home Assistant.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "vite build",
    "build:watch": "vite build --watch",
    "check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.17",
    "typescript": "^5.7.3",
    "vite": "^5.4.10"
  }
}
